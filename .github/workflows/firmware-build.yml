name: OpenWrt 智能固件构建工作流（完整功能版 - 编译器修复版）

on:
  workflow_dispatch:
    inputs:
      device_name:
        description: "📱 设备名称 (如: ac42u)"
        required: true
        default: "ac42u"
        type: string
      version_selection:
        description: "🔄 版本选择"
        required: true
        type: choice
        default: "21.02"
        options: ["23.05", "21.02"]
      config_mode:
        description: "⚙️ 配置模式选择
          
          🟣 基础模式 - 最小化配置，用于测试编译
          🟠 正常模式 - 完整功能配置：✅ TurboACC 网络加速 ✅ UPnP 自动端口转发 ✅ Samba 文件共享 ✅ 磁盘管理 ✅ KMS 激活服务 ✅ SmartDNS 智能DNS ✅ 家长控制 ✅ 微信推送 ✅ 流量控制 (SQM) ✅ FTP 服务器 ✅ ARP 绑定 ✅ CPU 限制 ✅ 硬盘休眠
          
          🔧 USB 3.0加强：所有平台的关键USB驱动都已强制启用！"
        required: true
        type: choice
        default: "normal"
        options: ["base", "normal"]
      extra_packages:
        description: "额外安装插件 - 格式：用分号;分隔。启用插件：+插件名。禁用插件：-插件名。"
        required: false
        type: string
        default: ""
      use_git_archive:
        description: "📦 使用Git Archive API下载源代码"
        required: false
        default: true
        type: boolean

env:
  BUILD_DIR: "/mnt/openwrt-build"
  COMPILER_DIR: "${{ github.workspace }}/firmware-config/build-Compiler-file"

jobs:
  build-firmware:
    runs-on: ubuntu-22.04
    
    steps:
      # 步骤 1: 使用Git Archive API下载源代码（不在界面显示）
      - name: "1. 使用Git Archive API下载源代码"
        if: github.event.inputs.use_git_archive == 'true'
        run: |
          echo "正在下载源代码..."
          
          mkdir -p /tmp/openwrt-source
          cd /tmp/openwrt-source
          
          DEFAULT_BRANCH=$(curl -s -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" "https://api.github.com/repos/${{ github.repository }}" | jq -r '.default_branch')
          
          echo "下载源代码压缩包..."
          curl -s -L -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" "https://api.github.com/repos/${{ github.repository }}/tarball/$DEFAULT_BRANCH" -o source.tar.gz
          
          if [ ! -f "source.tar.gz" ]; then
            echo "❌ 下载源代码压缩包失败"
            exit 1
          fi
          
          echo "解压源代码..."
          tar -xzf source.tar.gz --strip-components=1
          
          if [ -f "firmware-config/scripts/build_firmware_main.sh" ]; then
            mkdir -p ${{ github.workspace }}
            
            if [ -d "firmware-config/build-Compiler-file" ]; then
              rm -rf firmware-config/build-Compiler-file
            fi
            
            rsync -av --exclude='.git' . ${{ github.workspace }}/
            
            chmod +x ${{ github.workspace }}/firmware-config/scripts/*.sh 2>/dev/null || true
            
            mkdir -p /tmp/build-artifacts/source-archive
            cp source.tar.gz /tmp/build-artifacts/source-archive/
          else
            echo "❌ 源代码解压失败，重要文件缺失"
            exit 1
          fi
          
          cd /tmp
          rm -rf openwrt-source
          echo "✅ Git Archive API下载完成"
      
      # 步骤 2: 立即上传Git Archive源代码
      - name: "2. 立即上传Git Archive源代码"
        if: github.event.inputs.use_git_archive == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: git-archive-source-${{ github.event.inputs.device_name }}
          path: /tmp/build-artifacts/source-archive/
          retention-days: 7
      
      # 步骤 3: 检出配置仓库
      - name: "3. 检出配置仓库"
        if: github.event.inputs.use_git_archive == 'false'
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive
      
      # 步骤 4: 设置脚本执行权限
      - name: "4. 设置脚本执行权限"
        run: |
          echo "=== 步骤 4: 设置脚本执行权限 ==="
          find firmware-config/scripts -name "*.sh" -exec chmod +x {} \;
          echo "✅ 脚本执行权限设置完成"
      
      # 步骤 5: 检查编译器文件目录状态
      - name: "5. 检查编译器文件目录状态"
        run: |
          echo "=== 步骤 5: 检查编译器文件目录状态 ==="
          COMPILER_DIR="${{ env.COMPILER_DIR }}"
          
          if [ -d "$COMPILER_DIR" ]; then
            echo "✅ 编译器文件目录存在"
            echo "路径: $COMPILER_DIR"
            echo "目录大小: $(du -sh "$COMPILER_DIR" 2>/dev/null | cut -f1 || echo '未知')"
            echo "目录内容:"
            ls -la "$COMPILER_DIR/" 2>/dev/null | head -10 || echo "无法列出"
            
            required_compilers=("gcc" "g++" "as" "ld" "ar" "strip" "objcopy" "objdump" "nm" "ranlib")
            for compiler in "${required_compilers[@]}"; do
              if find "$COMPILER_DIR" -name "*$compiler*" -type f 2>/dev/null | grep -q .; then
                echo "  ✅ 找到: $compiler"
              else
                echo "  ❌ 未找到: $compiler"
              fi
            done
          else
            echo "ℹ️ 编译器文件目录不存在，将创建新目录"
            mkdir -p "$COMPILER_DIR"
            echo "✅ 已创建编译器文件目录: $COMPILER_DIR"
          fi
      
      # 步骤 6: 下载编译器源代码
      - name: "6. 下载编译器源代码"
        run: |
          echo "=== 步骤 6: 下载编译器源代码 ==="
          
          COMPILER_DIR="${{ env.COMPILER_DIR }}"
          mkdir -p "$COMPILER_DIR"
          
          echo "🔍 编译器源代码清单:"
          echo "1. gcc - GNU C编译器源代码"
          echo "2. binutils - GNU二进制工具集源代码"
          echo "3. make - GNU make工具源代码"
          echo "4. gmp - GNU多精度算术库源代码"
          echo "5. mpfr - GNU多精度浮点库源代码"
          echo "6. mpc - GNU多精度复数库源代码"
          echo "7. isl - 整数集库源代码"
          
          declare -A compiler_urls
          compiler_urls["gcc-11.3.0.tar.xz"]="https://ftp.gnu.org/gnu/gcc/gcc-11.3.0/gcc-11.3.0.tar.xz"
          compiler_urls["binutils-2.38.tar.xz"]="https://ftp.gnu.org/gnu/binutils/binutils-2.38.tar.xz"
          compiler_urls["make-4.3.tar.gz"]="https://ftp.gnu.org/gnu/make/make-4.3.tar.gz"
          compiler_urls["gmp-6.2.1.tar.xz"]="https://ftp.gnu.org/gnu/gmp/gmp-6.2.1.tar.xz"
          compiler_urls["mpfr-4.1.0.tar.xz"]="https://ftp.gnu.org/gnu/mpfr/mpfr-4.1.0.tar.xz"
          compiler_urls["mpc-1.2.1.tar.gz"]="https://ftp.gnu.org/gnu/mpc/mpc-1.2.1.tar.gz"
          compiler_urls["isl-0.24.tar.xz"]="https://gcc.gnu.org/pub/gcc/infrastructure/isl-0.24.tar.xz"
          
          cd "$COMPILER_DIR"
          
          echo "📥 下载编译器源代码..."
          for file in "${!compiler_urls[@]}"; do
            url="${compiler_urls[$file]}"
            if [ ! -f "$file" ]; then
              echo "下载: $file"
              echo "URL: $url"
              wget --no-check-certificate -q --show-progress "$url" || {
                echo "下载失败: $file，尝试备用方案..."
                curl -L "$url" -o "$file" || echo "备用下载也失败: $file"
              }
            else
              echo "已存在: $file"
            fi
          done
          
          echo "✅ 编译器源代码下载完成"
          echo "📊 当前编译器源代码文件:"
          ls -lh "$COMPILER_DIR" 2>/dev/null || echo "无文件"
          
          file_count=$(find "$COMPILER_DIR" -type f -name "*.tar.*" -o -name "*.gz" 2>/dev/null | wc -l)
          
          if [ $file_count -gt 0 ]; then
            echo "📊 编译器源代码统计:"
            echo "  文件数量: $file_count"
            echo "  目录大小: $(du -sh "$COMPILER_DIR" 2>/dev/null | cut -f1 || echo '未知')"
          else
            echo "⚠️ 编译器目录为空"
          fi
          
          echo "✅ 编译器源代码下载流程完成"
      
      # 步骤 7: 初始空间检查
      - name: "7. 初始空间检查"
        run: |
          echo "=== 步骤 7: 初始磁盘空间检查 ==="
          df -h
          AVAILABLE_SPACE=$(df /mnt --output=avail | tail -1)
          AVAILABLE_GB=$((AVAILABLE_SPACE / 1024 / 1024))
          echo "/mnt 可用空间: ${AVAILABLE_GB}G"
          if [ $AVAILABLE_GB -lt 50 ]; then
            echo "❌ 错误: /mnt 空间不足50G"
            exit 1
          fi
          echo "✅ 初始空间检查通过"
      
      # 步骤 8: 设置编译环境和构建目录
      - name: "8. 设置编译环境和构建目录"
        run: |
          echo "=== 步骤 8: 设置编译环境和构建目录 ==="
          
          echo "🔄 步骤 8.1: 设置编译环境"
          firmware-config/scripts/build_firmware_main.sh setup_environment
          
          echo "🔄 步骤 8.2: 创建构建目录"
          firmware-config/scripts/build_firmware_main.sh create_build_dir
          
          echo "🔄 步骤 8.3: 初始化构建环境"
          echo "设备: ${{ github.event.inputs.device_name }}"
          echo "版本: ${{ github.event.inputs.version_selection }}"
          echo "配置模式: ${{ github.event.inputs.config_mode }}"
          firmware-config/scripts/build_firmware_main.sh initialize_build_env "${{ github.event.inputs.device_name }}" "${{ github.event.inputs.version_selection }}" "${{ github.event.inputs.config_mode }}"
          
          echo "✅ 编译环境和构建目录设置完成"
      
      # 步骤 9: 添加 TurboACC 支持
      - name: "9. 添加 TurboACC 支持"
        run: |
          echo "=== 步骤 9: 添加 TurboACC 支持 ==="
          firmware-config/scripts/build_firmware_main.sh add_turboacc_support
      
      # 步骤 10: 配置Feeds
      - name: "10. 配置Feeds"
        run: |
          echo "=== 步骤 10: 配置Feeds ==="
          firmware-config/scripts/build_firmware_main.sh configure_feeds
      
      # 步骤 11: 安装 TurboACC 包
      - name: "11. 安装 TurboACC 包"
        if: env.SELECTED_BRANCH == 'openwrt-23.05' && github.event.inputs.config_mode == 'normal'
        run: |
          echo "=== 步骤 11: 安装 TurboACC 包 ==="
          firmware-config/scripts/build_firmware_main.sh install_turboacc_packages
      
      # 步骤 12: 编译前空间检查
      - name: "12. 编译前空间检查"
        run: |
          echo "=== 步骤 12: 编译前空间检查 ==="
          firmware-config/scripts/build_firmware_main.sh pre_build_space_check
      
      # 步骤 13: 智能配置生成（USB完全修复加强版）
      - name: "13. 智能配置生成（USB完全修复加强版）"
        run: |
          echo "=== 步骤 13: 智能配置生成 ==="
          echo "🚨 USB 3.0加强：所有关键USB驱动强制启用"
          echo "🚫 GDB禁用：避免internal_error Assertion编译错误"
          firmware-config/scripts/build_firmware_main.sh generate_config "${{ github.event.inputs.extra_packages }}"
      
      # 步骤 14: 验证USB配置
      - name: "14. 验证USB配置"
        run: |
          echo "=== 步骤 14: 验证USB配置 ==="
          firmware-config/scripts/build_firmware_main.sh verify_usb_config
      
      # 步骤 15: USB驱动完整性检查
      - name: "15. USB驱动完整性检查"
        run: |
          echo "=== 步骤 15: USB驱动完整性检查 ==="
          firmware-config/scripts/build_firmware_main.sh check_usb_drivers_integrity
      
      # 步骤 16: 应用配置并显示详情
      - name: "16. 应用配置并显示详情"
        run: |
          echo "=== 步骤 16: 应用配置并显示详情 ==="
          
          if [ -f "${{ env.BUILD_DIR }}/.config" ]; then
            cp "${{ env.BUILD_DIR }}/.config" "${{ env.BUILD_DIR }}/.config.original"
            echo "✅ 已备份原始配置文件"
          fi
          
          firmware-config/scripts/build_firmware_main.sh apply_config
          
          echo "=== 插件配置状态检查 ==="
          cd "${{ env.BUILD_DIR }}"
          
          declare -A normal_plugins
          normal_plugins["TurboACC 网络加速"]="luci-app-turboacc"
          normal_plugins["UPnP 自动端口转发"]="luci-app-upnp"
          normal_plugins["Samba 文件共享"]="luci-app-samba4"
          normal_plugins["磁盘管理"]="luci-app-diskman"
          normal_plugins["KMS 激活服务"]="luci-app-vlmcsd"
          normal_plugins["SmartDNS 智能DNS"]="luci-app-smartdns"
          normal_plugins["家长控制"]="luci-app-accesscontrol"
          normal_plugins["微信推送"]="luci-app-wechatpush"
          normal_plugins["流量控制 (SQM)"]="luci-app-sqm"
          normal_plugins["FTP 服务器"]="luci-app-vsftpd"
          normal_plugins["ARP 绑定"]="luci-app-arpbind"
          normal_plugins["CPU 限制"]="luci-app-cpulimit"
          normal_plugins["硬盘休眠"]="luci-app-hd-idle"
          
          echo "📋 正常模式插件状态检查:"
          
          total_plugins=0
          enabled_plugins=0
          disabled_plugins=0
          
          for plugin_name in "${!normal_plugins[@]}"; do
            plugin_config="${normal_plugins[$plugin_name]}"
            total_plugins=$((total_plugins + 1))
            
            if grep -q "^CONFIG_PACKAGE_${plugin_config}=y" .config; then
              echo "  ✅ $plugin_name: 已启用"
              enabled_plugins=$((enabled_plugins + 1))
            elif grep -q "^# CONFIG_PACKAGE_${plugin_config} is not set$" .config; then
              echo "  ❌ $plugin_name: 已禁用"
              disabled_plugins=$((disabled_plugins + 1))
            else
              echo "  ⚠️  $plugin_name: 未配置"
            fi
          done
          
          echo ""
          echo "📊 插件状态统计:"
          echo "  总计: $total_plugins 个插件"
          echo "  已启用: $enabled_plugins 个"
          echo "  已禁用: $disabled_plugins 个"
          
          if [ "${{ github.event.inputs.config_mode }}" = "base" ]; then
            echo ""
            echo "🔧 基础模式配置确认:"
            echo "  🟣 基础模式 - 最小化配置，用于测试编译"
            echo "  📋 特性: 仅包含基本系统功能，USB 3.0驱动已完全启用"
            echo "  ⚡ 优点: 编译速度快，固件体积小，适合测试和验证"
            if [ $enabled_plugins -eq 0 ]; then
              echo "  ✅ 基础模式: 所有额外插件已正确禁用"
            else
              echo "  ⚠️  基础模式: 有 $enabled_plugins 个插件未禁用，需要检查"
            fi
          else
            echo ""
            echo "🔧 正常模式配置确认:"
            echo "  🟠 正常模式 - 完整功能配置"
            echo "  📋 特性: 包含以下完整功能插件:"
            echo "    ✅ TurboACC 网络加速"
            echo "    ✅ UPnP 自动端口转发"
            echo "    ✅ Samba 文件共享"
            echo "    ✅ 磁盘管理"
            echo "    ✅ KMS 激活服务"
            echo "    ✅ SmartDNS 智能DNS"
            echo "    ✅ 家长控制"
            echo "    ✅ 微信推送"
            echo "    ✅ 流量控制 (SQM)"
            echo "    ✅ FTP 服务器"
            echo "    ✅ ARP 绑定"
            echo "    ✅ CPU 限制"
            echo "    ✅ 硬盘休眠"
            echo "  🚀 优点: 功能完整，适合日常使用"
            echo "  🔧 USB 3.0加强：所有平台的关键USB驱动都已强制启用！"
          fi
          
          echo ""
          echo "🔌 USB驱动状态确认:"
          critical_usb_drivers=("kmod-usb-xhci-hcd" "kmod-usb3" "kmod-usb-dwc3" "kmod-usb-storage" "kmod-scsi-core")
          for driver in "${critical_usb_drivers[@]}"; do
            if grep -q "^CONFIG_PACKAGE_${driver}=y" .config; then
              echo "  ✅ $driver: 已启用"
            else
              echo "  ❌ $driver: 未启用 (需要修复)"
            fi
          done
          
          echo ""
          echo "🔧 调试工具状态确认:"
          if grep -q "^# CONFIG_PACKAGE_gdb is not set$" .config; then
            echo "  ✅ GDB: 已禁用（避免编译错误）"
          else
            echo "  ⚠️  GDB: 未禁用（可能会遇到编译错误）"
          fi
          
          echo "✅ 配置详情检查完成"
      
      # 步骤 17: 检查并备份配置文件
      - name: "17. 检查并备份配置文件"
        run: |
          echo "=== 步骤 17: 检查并备份配置文件 ==="
          
          if [ -f "${{ env.BUILD_DIR }}/.config" ]; then
            echo "✅ .config 文件存在"
            
            mkdir -p firmware-config/config-backup
            
            backup_file="firmware-config/config-backup/config_${{ github.event.inputs.device_name }}_${{ env.SELECTED_BRANCH }}_${{ github.event.inputs.config_mode }}_$(date +%Y%m%d_%H%M%S).config"
            
            cp "${{ env.BUILD_DIR }}/.config" "$backup_file"
            echo "✅ 配置文件备份到仓库目录: $backup_file"
            
            echo "备份文件大小: $(ls -lh $backup_file | awk '{print $5}')"
          else
            echo "❌ .config 文件不存在"
            exit 1
          fi
      
      # 步骤 18: 修复网络环境
      - name: "18. 修复网络环境"
        run: |
          echo "=== 步骤 18: 修复网络环境 ==="
          firmware-config/scripts/build_firmware_main.sh fix_network
      
      # 步骤 19: 修复libtool和头文件问题（增强版）
      - name: "19. 修复libtool和头文件问题（增强版）"
        run: |
          echo "=== 步骤 19: 修复libtool和头文件问题（增强版）==="
          
          cd "${{ env.BUILD_DIR }}"
          
          echo "🔧 安装必要的开发包..."
          sudo apt-get update && sudo apt-get install -y libtool autoconf automake libltdl-dev m4 libtool-bin gperf autoconf-archive
          
          echo "📁 创建缺失的目录..."
          mkdir -p staging_dir/host/include
          mkdir -p staging_dir/host/share/aclocal
          mkdir -p staging_dir/host/share/aclocal-1.16
          mkdir -p staging_dir/host/lib/pkgconfig
          
          echo "📋 复制关键头文件..."
          HEADERS="stdc-predef.h stdio.h stdlib.h string.h features.h"
          for header in $HEADERS; do
            if [ -f "/usr/include/$header" ]; then
              cp "/usr/include/$header" staging_dir/host/include/ 2>/dev/null || true
              echo "  ✅ 复制: $header"
            else
              echo "  ⚠️  未找到: $header"
            fi
          done
          
          echo "📋 复制libtool.m4..."
          if [ -f "/usr/share/aclocal/libtool.m4" ]; then
            cp "/usr/share/aclocal/libtool.m4" staging_dir/host/share/aclocal/ 2>/dev/null || true
            echo "  ✅ 复制: libtool.m4"
          else
            echo "  ⚠️  未找到系统libtool.m4"
            find /usr -name "libtool.m4" 2>/dev/null | head -1 | while read m4file; do
              cp "$m4file" staging_dir/host/share/aclocal/ 2>/dev/null && echo "  ✅ 从其他地方复制: libtool.m4"
            done
          fi
          
          echo "📋 复制aclocal-1.16文件..."
          if [ -d "/usr/share/aclocal-1.16" ]; then
            cp /usr/share/aclocal-1.16/*.m4 staging_dir/host/share/aclocal-1.16/ 2>/dev/null || true
            echo "  ✅ 复制aclocal-1.16文件"
          fi
          
          echo "🌍 设置环境变量..."
          export CFLAGS="-I${{ env.BUILD_DIR }}/staging_dir/host/include -O2 -pipe"
          export LDFLAGS="-L${{ env.BUILD_DIR }}/staging_dir/host/lib -Wl,-O1"
          export CPPFLAGS="-I${{ env.BUILD_DIR }}/staging_dir/host/include"
          export ACLOCAL_PATH="${{ env.BUILD_DIR }}/staging_dir/host/share/aclocal:\${ACLOCAL_PATH}"
          export PKG_CONFIG_PATH="${{ env.BUILD_DIR }}/staging_dir/host/lib/pkgconfig:\${PKG_CONFIG_PATH}"
          
          echo "✅ libtool和头文件修复完成"
      
      # 步骤 20: 修复编译器头文件冲突（新增关键步骤）
      - name: "20. 修复编译器头文件冲突"
        run: |
          echo "=== 步骤 20: 修复编译器头文件冲突 ==="
          
          cd "${{ env.BUILD_DIR }}"
          
          echo "🔍 检查GCC构建目录..."
          GCC_DIR=$(find build_dir -name "gcc-8.4.0" -type d 2>/dev/null | head -1)
          
          if [ -n "$GCC_DIR" ]; then
            echo "✅ 找到GCC目录: $GCC_DIR"
            
            echo "📋 备份原始头文件..."
            if [ -f "$GCC_DIR/gcc/system.h" ]; then
              cp "$GCC_DIR/gcc/system.h" "$GCC_DIR/gcc/system.h.backup"
              echo "  ✅ 备份system.h"
            fi
            
            if [ -f "$GCC_DIR/gcc/auto-host.h" ]; then
              cp "$GCC_DIR/gcc/auto-host.h" "$GCC_DIR/gcc/auto-host.h.backup"
              echo "  ✅ 备份auto-host.h"
            fi
            
            echo "🔧 修复头文件声明冲突..."
            
            # 修复system.h中的冲突声明
            if [ -f "$GCC_DIR/gcc/system.h" ]; then
              sed -i 's/^void\* sbrk(int);$//' "$GCC_DIR/gcc/system.h"
              sed -i 's/^const char\* strsignal(int);$//' "$GCC_DIR/gcc/system.h"
              sed -i 's/^char\* basename(const char\*);$//' "$GCC_DIR/gcc/system.h"
              echo "  ✅ 修复system.h完成"
            fi
            
            # 修复auto-host.h中的声明配置
            if [ -f "$GCC_DIR/gcc/auto-host.h" ]; then
              sed -i 's/^#define HAVE_DECL_SBRK.*$/#undef HAVE_DECL_SBRK/' "$GCC_DIR/gcc/auto-host.h"
              sed -i 's/^#define HAVE_DECL_STRSIGNAL.*$/#undef HAVE_DECL_STRSIGNAL/' "$GCC_DIR/gcc/auto-host.h"
              sed -i 's/^#define HAVE_DECL_BASENAME.*$/#undef HAVE_DECL_BASENAME/' "$GCC_DIR/gcc/auto-host.h"
              echo "  ✅ 修复auto-host.h完成"
            fi
            
            echo "🚨 添加-fpermissive编译标志..."
            export CFLAGS="-I${{ env.BUILD_DIR }}/staging_dir/host/include -O2 -pipe -fpermissive"
            export CXXFLAGS="$CFLAGS"
            export LDFLAGS="-L${{ env.BUILD_DIR }}/staging_dir/host/lib -Wl,-O1"
            
            echo "✅ 编译器头文件修复完成"
          else
            echo "ℹ️ 未找到GCC 8.4.0目录，可能尚未编译或版本不同"
            echo "🚨 设置-fpermissive编译标志预防..."
            export CFLAGS="-I${{ env.BUILD_DIR }}/staging_dir/host/include -O2 -pipe -fpermissive"
            export CXXFLAGS="$CFLAGS"
          fi
      
      # 步骤 21: 修复GDB编译错误（新增关键步骤）
      - name: "21. 修复GDB编译错误"
        run: |
          echo "=== 步骤 21: 修复GDB编译错误 ==="
          
          cd "${{ env.BUILD_DIR }}"
          
          echo "🔧 修复GDB编译错误问题..."
          firmware-config/scripts/build_firmware_main.sh fix_gdb_issues
          
          echo "✅ GDB编译错误修复完成"
      
      # 步骤 22: 下载依赖包
      - name: "22. 下载依赖包"
        run: |
          echo "=== 步骤 22: 下载依赖包 ==="
          firmware-config/scripts/build_firmware_main.sh download_dependencies
      
      # 步骤 23: 集成自定义文件
      - name: "23. 集成自定义文件"
        run: |
          echo "=== 步骤 23: 集成自定义文件 ==="
          firmware-config/scripts/build_firmware_main.sh integrate_custom_files
      
      # 步骤 24: 前置错误检查
      - name: "24. 前置错误检查"
        run: |
          echo "=== 步骤 24: 前置错误检查 ==="
          firmware-config/scripts/build_firmware_main.sh pre_build_error_check
      
      # 步骤 25: 编译固件前的空间检查
      - name: "25. 编译固件前的空间检查"
        run: |
          echo "=== 步骤 25: 编译固件前的空间检查 ==="
          df -h
          AVAILABLE_SPACE=$(df /mnt --output=avail | tail -1)
          AVAILABLE_GB=$((AVAILABLE_SPACE / 1024 / 1024))
          echo "/mnt 可用空间: ${AVAILABLE_GB}G"
          
          if [ $AVAILABLE_GB -lt 10 ]; then
            echo "❌ 错误: 编译前空间不足 (需要至少10G，当前${AVAILABLE_GB}G)"
            exit 1
          elif [ $AVAILABLE_GB -lt 20 ]; then
            echo "⚠️ 警告: 编译前空间较低 (建议至少20G，当前${AVAILABLE_GB}G)"
          else
            echo "✅ 编译前空间充足"
          fi
      
      # 步骤 26: 编译固件（使用修复版）
      - name: "26. 编译固件（使用修复版）"
        run: |
          echo "=== 步骤 26: 编译固件（使用修复版）==="
          
          echo "🚨 设置编译器修复环境变量..."
          export CFLAGS="-I${{ env.BUILD_DIR }}/staging_dir/host/include -O2 -pipe -fpermissive"
          export CXXFLAGS="$CFLAGS"
          export LDFLAGS="-L${{ env.BUILD_DIR }}/staging_dir/host/lib -Wl,-O1"
          export CPPFLAGS="-I${{ env.BUILD_DIR }}/staging_dir/host/include"
          export ACLOCAL_PATH="${{ env.BUILD_DIR }}/staging_dir/host/share/aclocal:\${ACLOCAL_PATH}"
          export PKG_CONFIG_PATH="${{ env.BUILD_DIR }}/staging_dir/host/lib/pkgconfig:\${PKG_CONFIG_PATH}"
          
          echo "🔧 开始编译固件..."
          firmware-config/scripts/build_firmware_main.sh build_firmware "true"
      
      # 步骤 27: 搜索并打包构建产物
      - name: "27. 搜索并打包构建产物"
        if: success()
        run: |
          echo "=== 步骤 27: 搜索并打包构建产物 ==="
          cd ${{ env.BUILD_DIR }}
          
          mkdir -p /tmp/build-artifacts
          
          echo "🔍 检查是否已有相同构建产物..."
          need_upload=false
          
          if [ -d "bin/targets" ]; then
            echo "打包固件文件..."
            tar -czf /tmp/build-artifacts/firmware.tar.gz -C bin targets/
            
            firmware_size=$(stat -c%s /tmp/build-artifacts/firmware.tar.gz 2>/dev/null || echo "0")
            if [ $firmware_size -gt 0 ]; then
              echo "✅ 固件打包完成，大小: $(du -h /tmp/build-artifacts/firmware.tar.gz | cut -f1)"
              need_upload=true
            else
              echo "⚠️ 固件打包失败或文件为空"
            fi
          else
            echo "⚠️ bin/targets目录不存在"
          fi
          
          if [ -f "build.log" ]; then
            echo "打包编译日志..."
            cp build.log /tmp/build-artifacts/build.log
            echo "✅ 日志打包完成"
            need_upload=true
          fi
          
          echo "构建时间: $(date)" > /tmp/build-artifacts/build-info.txt
          echo "设备: ${{ env.DEVICE }}" >> /tmp/build-artifacts/build-info.txt
          echo "版本: ${{ env.SELECTED_BRANCH }}" >> /tmp/build-artifacts/build-info.txt
          echo "目标平台: ${{ env.TARGET }}/${{ env.SUBTARGET }}" >> /tmp/build-artifacts/build-info.txt
          echo "配置模式: ${{ github.event.inputs.config_mode }}" >> /tmp/build-artifacts/build-info.txt
          echo "编译器修复: 已应用-fpermissive标志和头文件修复" >> /tmp/build-artifacts/build-info.txt
          echo "GDB修复: 已禁用GDB以避免编译错误" >> /tmp/build-artifacts/build-info.txt
          
          if [ "${{ github.event.inputs.config_mode }}" = "base" ]; then
            echo "模式说明: 🟣 基础模式 - 最小化配置，用于测试编译" >> /tmp/build-artifacts/build-info.txt
          else
            echo "模式说明: 🟠 正常模式 - 完整功能配置" >> /tmp/build-artifacts/build-info.txt
          fi
          
          if [ "$need_upload" = true ]; then
            echo "✅ 构建产物准备完成"
            echo "📤 构建产物已保存在: /tmp/build-artifacts/"
            ls -lh /tmp/build-artifacts/
          else
            echo "ℹ️ 没有需要上传的构建产物"
          fi
      
      # 步骤 28: 上传固件原始目录
      - name: "28. 上传固件原始目录"
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: firmware-${{ github.event.inputs.device_name }}-${{ env.SELECTED_BRANCH }}-${{ github.event.inputs.config_mode }}
          path: ${{ env.BUILD_DIR }}/bin/targets/
          retention-days: 7
      
      # 步骤 29: 汇总已编译的编译器文件并提交到仓库目录
      - name: "29. 汇总已编译的编译器文件并提交到仓库目录"
        if: success()
        run: |
          echo "=== 步骤 29: 汇总已编译的编译器文件并提交到仓库目录 ==="
          echo "🔍 开始收集已编译的编译器文件..."
          
          COMPILER_SAVE_DIR="${{ github.workspace }}/firmware-config/build-Compiler-file/compiled"
          mkdir -p "$COMPILER_SAVE_DIR"
          
          echo "编译器文件保存目录: $COMPILER_SAVE_DIR"
          
          BUILD_DIR="${{ env.BUILD_DIR }}"
          
          echo "📊 搜索已编译的编译器文件..."
          
          find "$BUILD_DIR/staging_dir" -type f \( -name "*gcc*" -o -name "*g++*" -o -name "*as*" -o -name "*ld*" -o -name "*ar*" -o -name "*strip*" -o -name "*objcopy*" -o -name "*objdump*" -o -name "*nm*" -o -name "*ranlib*" \) 2>/dev/null | head -100 > /tmp/compiler_files.txt
          
          total_files=$(wc -l < /tmp/compiler_files.txt)
          echo "📈 找到 $total_files 个编译器文件"
          
          if [ $total_files -eq 0 ]; then
            echo "⚠️ 未找到编译器文件，跳过保存"
            exit 0
          fi
          
          echo "📁 创建分类目录..."
          mkdir -p "$COMPILER_SAVE_DIR/arm"
          mkdir -p "$COMPILER_SAVE_DIR/mips"
          mkdir -p "$COMPILER_SAVE_DIR/mipsel"
          mkdir -p "$COMPILER_SAVE_DIR/x86"
          mkdir -p "$COMPILER_SAVE_DIR/x86_64"
          mkdir -p "$COMPILER_SAVE_DIR/generic"
          
          echo "📋 分类复制编译器文件..."
          
          arm_count=0
          mips_count=0
          mipsel_count=0
          x86_count=0
          x86_64_count=0
          generic_count=0
          
          while IFS= read -r file; do
            if [ -f "$file" ] && [ -x "$file" ]; then
              filename=$(basename "$file")
              
              if [[ "$filename" == *"arm"* ]] || [[ "$file" == *"arm"* ]]; then
                cp "$file" "$COMPILER_SAVE_DIR/arm/" 2>/dev/null && arm_count=$((arm_count + 1))
              elif [[ "$filename" == *"mips"* ]] && [[ "$filename" != *"mipsel"* ]]; then
                cp "$file" "$COMPILER_SAVE_DIR/mips/" 2>/dev/null && mips_count=$((mips_count + 1))
              elif [[ "$filename" == *"mipsel"* ]] || [[ "$file" == *"mipsel"* ]]; then
                cp "$file" "$COMPILER_SAVE_DIR/mipsel/" 2>/dev/null && mipsel_count=$((mipsel_count + 1))
              elif [[ "$filename" == *"i386"* ]] || [[ "$filename" == *"i686"* ]] || [[ "$file" == *"x86"* ]] && [[ "$file" != *"x86_64"* ]]; then
                cp "$file" "$COMPILER_SAVE_DIR/x86/" 2>/dev/null && x86_count=$((x86_count + 1))
              elif [[ "$filename" == *"x86_64"* ]] || [[ "$file" == *"x86_64"* ]]; then
                cp "$file" "$COMPILER_SAVE_DIR/x86_64/" 2>/dev/null && x86_64_count=$((x86_64_count + 1))
              else
                cp "$file" "$COMPILER_SAVE_DIR/generic/" 2>/dev/null && generic_count=$((generic_count + 1))
              fi
            fi
          done < /tmp/compiler_files.txt
          
          echo ""
          echo "📊 编译器文件分类统计:"
          echo "  ARM架构: $arm_count 个文件"
          echo "  MIPS架构: $mips_count 个文件"
          echo "  MIPS小端: $mipsel_count 个文件"
          echo "  x86架构: $x86_count 个文件"
          echo "  x86_64架构: $x86_64_count 个文件"
          echo "  通用编译器: $generic_count 个文件"
          echo "  总计: $((arm_count + mips_count + mipsel_count + x86_count + x86_64_count + generic_count)) 个文件"
          
          echo ""
          echo "📁 各目录详细内容:"
          
          for arch_dir in arm mips mipsel x86 x86_64 generic; do
            dir_path="$COMPILER_SAVE_DIR/$arch_dir"
            if [ -d "$dir_path" ] && [ "$(ls -A "$dir_path" 2>/dev/null)" ]; then
              file_count=$(find "$dir_path" -type f | wc -l)
              echo "  $arch_dir 目录 ($file_count 个文件):"
              ls "$dir_path" | head -5 | while read file; do
                size=$(stat -c%s "$dir_path/$file" 2>/dev/null || echo "0")
                size_kb=$((size / 1024))
                echo "    - $file (${size_kb}KB)"
              done
              if [ $file_count -gt 5 ]; then
                echo "    ... 还有 $((file_count - 5)) 个文件"
              fi
            else
              echo "  $arch_dir 目录: 空"
            fi
          done
          
          echo ""
          echo "📝 创建编译器信息文件..."
          echo "编译器文件汇总信息" > "$COMPILER_SAVE_DIR/compiler_info.txt"
          echo "==================" >> "$COMPILER_SAVE_DIR/compiler_info.txt"
          echo "" >> "$COMPILER_SAVE_DIR/compiler_info.txt"
          echo "生成时间: $(date)" >> "$COMPILER_SAVE_DIR/compiler_info.txt"
          echo "构建设备: ${{ env.DEVICE }}" >> "$COMPILER_SAVE_DIR/compiler_info.txt"
          echo "目标平台: ${{ env.TARGET }}/${{ env.SUBTARGET }}" >> "$COMPILER_SAVE_DIR/compiler_info.txt"
          echo "OpenWrt版本: ${{ env.SELECTED_BRANCH }}" >> "$COMPILER_SAVE_DIR/compiler_info.txt"
          echo "编译器修复: 已应用头文件冲突修复" >> "$COMPILER_SAVE_DIR/compiler_info.txt"
          echo "GDB修复: 已禁用GDB以避免编译错误" >> "$COMPILER_SAVE_DIR/compiler_info.txt"
          echo "配置模式: ${{ github.event.inputs.config_mode }}" >> "$COMPILER_SAVE_DIR/compiler_info.txt"
          echo "" >> "$COMPILER_SAVE_DIR/compiler_info.txt"
          echo "文件分类统计:" >> "$COMPILER_SAVE_DIR/compiler_info.txt"
          echo "------------" >> "$COMPILER_SAVE_DIR/compiler_info.txt"
          echo "ARM架构: $arm_count 个文件" >> "$COMPILER_SAVE_DIR/compiler_info.txt"
          echo "MIPS架构: $mips_count 个文件" >> "$COMPILER_SAVE_DIR/compiler_info.txt"
          echo "MIPS小端: $mipsel_count 个文件" >> "$COMPILER_SAVE_DIR/compiler_info.txt"
          echo "x86架构: $x86_count 个文件" >> "$COMPILER_SAVE_DIR/compiler_info.txt"
          echo "x86_64架构: $x86_64_count 个文件" >> "$COMPILER_SAVE_DIR/compiler_info.txt"
          echo "通用编译器: $generic_count 个文件" >> "$COMPILER_SAVE_DIR/compiler_info.txt"
          echo "总计: $((arm_count + mips_count + mipsel_count + x86_count + x86_64_count + generic_count)) 个文件" >> "$COMPILER_SAVE_DIR/compiler_info.txt"
          
          total_size=$(du -sh "$COMPILER_SAVE_DIR" 2>/dev/null | cut -f1)
          echo ""
          echo "📦 编译器文件总目录大小: $total_size"
          
          echo "📦 创建编译器文件压缩包..."
          cd "$COMPILER_SAVE_DIR"
          tar -czf "../compiled-compilers.tar.gz" ./*
          cd - > /dev/null
          
          echo "✅ 编译器文件收集完成"
          echo "📁 保存目录: $COMPILER_SAVE_DIR"
          echo "📦 压缩包: ${{ github.workspace }}/firmware-config/build-Compiler-file/compiled-compilers.tar.gz"
          
          echo "🔄 提交编译器文件到仓库目录..."
          
          cd "${{ github.workspace }}"
          
          git config --local user.email "github-actions@github.com"
          git config --local user.name "GitHub Actions"
          
          git add firmware-config/build-Compiler-file/
          
          git status --porcelain firmware-config/build-Compiler-file/
          
          if git diff --cached --quiet -- firmware-config/build-Compiler-file/; then
            echo "ℹ️ 没有新的编译器文件需要提交"
          else
            echo "📝 提交编译器文件到仓库..."
            git commit -m "feat: 添加已编译的编译器文件" -m "设备: ${{ env.DEVICE }}" -m "平台: ${{ env.TARGET }}/${{ env.SUBTARGET }}" -m "版本: ${{ env.SELECTED_BRANCH }}" -m "编译器修复: 已应用头文件冲突修复" -m "GDB修复: 已禁用GDB以避免编译错误"
            git commit --amend -m "feat: 添加已编译的编译器文件" -m "设备: ${{ env.DEVICE }}" -m "平台: ${{ env.TARGET }}/${{ env.SUBTARGET }}" -m "版本: ${{ env.SELECTED_BRANCH }}" -m "编译器修复: 已应用头文件冲突修复" -m "GDB修复: 已禁用GDB以避免编译错误" -m "配置模式: ${{ github.event.inputs.config_mode }}" -m "ARM: $arm_count 文件" -m "MIPS: $mips_count 文件" -m "MIPS小端: $mipsel_count 文件" -m "x86: $x86_count 文件" -m "x86_64: $x86_64_count 文件" -m "通用: $generic_count 文件"
            
            echo "🔄 推送到仓库..."
            git push origin HEAD:${{ github.ref_name }}
            
            echo "✅ 编译器文件已提交到仓库目录"
          fi
      
      # 步骤 30: 上传配置文件
      - name: "30. 上传配置文件"
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: config-${{ github.event.inputs.device_name }}-${{ env.SELECTED_BRANCH }}-${{ github.event.inputs.config_mode }}
          path: ${{ github.workspace }}/firmware-config/config-backup/
          retention-days: 7
      
      # 步骤 31: 上传编译器文件压缩包
      - name: "31. 上传编译器文件压缩包"
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: compiler-files-${{ github.event.inputs.device_name }}-${{ env.SELECTED_BRANCH }}
          path: ${{ github.workspace }}/firmware-config/build-Compiler-file/compiled-compilers.tar.gz
          retention-days: 7
      
      # 步骤 32: 错误分析（无论成功失败都运行）
      - name: "32. 错误分析"
        if: always()
        run: |
          echo "=== 步骤 32: 错误分析 ==="
          firmware-config/scripts/error_analysis.sh
      
      # 步骤 33: 编译后空间检查
      - name: "33. 编译后空间检查"
        if: always()
        run: |
          echo "=== 步骤 33: 编译后空间检查 ==="
          firmware-config/scripts/build_firmware_main.sh post_build_space_check
