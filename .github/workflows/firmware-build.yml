#【firmware-build.yml-00】
# OpenWrt 智能固件构建工作流 - 系统主入口
# 版本: 3.0.0
# 最后更新: 2026-02-12
#【firmware-build.yml-00】

name: OpenWrt 智能固件构建工作流

on:
  workflow_dispatch:
    inputs:
      device_name:
        description: "📱 设备名称"
        required: true
        default: "ac42u"
        type: choice
        options: ["ac42u", "cmcc_rax3000m", "netgear_3800"]
      version_selection:
        description: "🔄 版本选择"
        required: true
        type: choice
        default: "21.02"
        options: ["23.05", "21.02"]
      config_mode:
        description: "⚙️ 配置模式选择"
        required: true
        type: choice
        default: "normal"
        options: ["base", "normal"]
      extra_packages:
        description: "额外安装插件 - 格式：用分号;分隔"
        required: false
        type: string
        default: ""
      enable_parallel:
        description: "⚡ 启用智能并行优化"
        required: false
        type: boolean
        default: true

env:
  BUILD_DIR: "/mnt/openwrt-build"
  REPO_ROOT: "${{ github.workspace }}"
  ENABLE_PARALLEL: "${{ github.event.inputs.enable_parallel }}"

jobs:
  build-firmware:
    runs-on: ubuntu-22.04
#【firmware-build.yml-00】

    steps:
      #【firmware-build.yml-01】
      - name: "01. 生成时间戳（UTC+8时区）"
        run: |
          echo "=== 步骤01: 生成时间戳（UTC+8时区） ==="
          
          sudo timedatectl set-timezone Asia/Shanghai 2>/dev/null || true
          
          START_TIME=$(date +'%Y-%m-%d %H:%M:%S')
          echo "🕐 开始时间: $START_TIME"
          
          TIMESTAMP_LOCAL=$(date +'%Y%m%d_%H%M%S')
          TIMESTAMP_SEC="${{ github.event.inputs.device_name }}-${{ github.event.inputs.version_selection }}-${{ github.event.inputs.config_mode }}-$TIMESTAMP_LOCAL"
          
          echo "TIMESTAMP_SEC=$TIMESTAMP_SEC" >> $GITHUB_ENV
          echo "START_TIME=$START_TIME" >> $GITHUB_ENV
          
          echo "✅ 生成时间戳: $TIMESTAMP_SEC"
          echo "📋 构建配置:"
          echo "  设备: ${{ github.event.inputs.device_name }}"
          echo "  版本: ${{ github.event.inputs.version_selection }}"
          echo "  模式: ${{ github.event.inputs.config_mode }}"
          echo "  并行优化: ${{ github.event.inputs.enable_parallel }}"
      #【firmware-build.yml-01】

      #【firmware-build.yml-02】
      - name: "02. 使用Git Archive API下载源代码"
        run: |
          echo "=== 步骤02: 使用Git Archive API下载源代码 ==="
          
          set -e
          trap 'echo "❌ 步骤02 失败，退出代码: $?"; exit 1' ERR
          
          mkdir -p /tmp/openwrt-source
          cd /tmp/openwrt-source
          
          echo "🔍 获取仓库默认分支..."
          DEFAULT_BRANCH=$(curl -s -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            "https://api.github.com/repos/${{ github.repository }}" | jq -r '.default_branch')
          echo "✅ 默认分支: $DEFAULT_BRANCH"
          
          echo "📥 下载源代码压缩包..."
          curl -s -L -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            "https://api.github.com/repos/${{ github.repository }}/tarball/$DEFAULT_BRANCH" \
            -o source.tar.gz
          
          if [ ! -f "source.tar.gz" ]; then
            echo "❌ 错误: 下载源代码压缩包失败"
            exit 1
          fi
          
          echo "✅ 下载完成，文件大小: $(ls -lh source.tar.gz | awk '{print $5}')"
          
          echo "📦 解压源代码..."
          tar -xzf source.tar.gz --strip-components=1
          
          echo "🔍 检查解压结果..."
          if [ -f "firmware-config/scripts/build_firmware_main.sh" ]; then
            echo "✅ 源代码解压成功"
            echo "✅ 主脚本存在: firmware-config/scripts/build_firmware_main.sh"
            
            echo "📁 复制文件到工作区..."
            mkdir -p ${{ github.workspace }}
            
            if [ -d "firmware-config/build-Compiler-file" ]; then
              echo "⚠️ 发现编译器目录，排除不复制"
              rm -rf firmware-config/build-Compiler-file
            fi
            
            rsync -av --exclude='.git' . ${{ github.workspace }}/
            
            echo "✅ 文件复制完成"
            
            mkdir -p /tmp/build-artifacts/source-archive
            cp source.tar.gz /tmp/build-artifacts/source-archive/
            echo "📦 源代码已保存到: /tmp/build-artifacts/source-archive/"
          else
            echo "❌ 错误: 源代码解压失败，重要文件缺失"
            ls -la
            exit 1
          fi
          
          cd /tmp
          rm -rf openwrt-source
          echo "✅ 步骤02 完成"
      #【firmware-build.yml-02】

      #【firmware-build.yml-03】
      - name: "03. 上传源代码Artifacts"
        uses: actions/upload-artifact@v4
        with:
          name: source-${{ env.TIMESTAMP_SEC }}
          path: /tmp/build-artifacts/source-archive/
          retention-days: 7
          if-no-files-found: error
      #【firmware-build.yml-03】

      #【firmware-build.yml-04】
      - name: "04. 修复权限问题"
        run: |
          echo "=== 步骤04: 修复权限问题 ==="
          
          set -e
          trap 'echo "❌ 步骤04 失败，退出代码: $?"; exit 1' ERR
          
          echo "🔧 修复所有脚本的执行权限..."
          
          if [ -f "${{ github.workspace }}/firmware-config/scripts/build_firmware_main.sh" ]; then
            chmod +x "${{ github.workspace }}/firmware-config/scripts/build_firmware_main.sh"
            echo "✅ 修复主脚本权限"
          else
            echo "❌ 错误: 主脚本不存在"
            exit 1
          fi
          
          if [ -f "${{ github.workspace }}/support.sh" ]; then
            chmod +x "${{ github.workspace }}/support.sh"
            echo "✅ 修复support.sh权限"
          else
            echo "❌ 错误: support.sh不存在"
            exit 1
          fi
          
          find "${{ github.workspace }}/firmware-config/scripts" -name "*.sh" -exec chmod +x {} \;
          echo "✅ 修复所有脚本权限完成"
      #【firmware-build.yml-04】

      #【firmware-build.yml-05】
      - name: "05. 安装基础工具（优化版）"
        run: |
          echo "=== 步骤05: 安装基础工具（优化版） ==="
          
          set -e
          trap 'echo "❌ 步骤05 失败，退出代码: $?"; exit 1' ERR
          
          echo "🔄 更新软件包列表..."
          sudo apt-get update -q
          
          echo "📦 安装核心编译工具..."
          sudo apt-get install -y -q \
            build-essential clang flex bison g++ gawk gcc-multilib g++-multilib \
            gettext git libncurses5-dev libssl-dev python3-distutils rsync unzip \
            zlib1g-dev file wget libelf-dev cmake ninja-build
          
          echo "🐍 安装Python和脚本工具..."
          sudo apt-get install -y -q \
            python3 python3-dev python3-pip python3-setuptools python3-yaml \
            xsltproc zip subversion automake autoconf libtool pkg-config \
            help2man texinfo groff texlive texinfo
          
          echo "🌐 安装网络和下载工具..."
          sudo apt-get install -y -q \
            curl net-tools iputils-ping dnsutils openssh-client ca-certificates \
            gnupg lsb-release aria2 libcurl4-openssl-dev
          
          echo "💿 安装文件系统工具..."
          sudo apt-get install -y -q \
            squashfs-tools dosfstools e2fsprogs mtools parted fdisk gdisk hdparm smartmontools
          
          echo "🔍 安装调试和开发工具..."
          sudo apt-get install -y -q \
            gdb strace ltrace valgrind binutils-dev libdw-dev libiberty-dev \
            ecj fastjar java-propose-classpath libpython3-dev liblz4-dev zstd
          
          echo "✅ 基础工具安装完成"
          
          echo "🔍 验证重要工具安装..."
          tools_to_check=("gcc" "g++" "make" "git" "python3" "cmake" "flex" "bison")
          missing_tools=()
          
          for tool in "${tools_to_check[@]}"; do
            if command -v $tool >/dev/null 2>&1; then
              echo "✅ $tool 已安装: $(which $tool)"
            else
              echo "❌ $tool 未安装"
              missing_tools+=("$tool")
            fi
          done
          
          if [ ${#missing_tools[@]} -gt 0 ]; then
            echo "❌ 错误: 以下重要工具缺失: ${missing_tools[*]}"
            exit 1
          fi
          
          GCC_VERSION=$(gcc --version | head -1)
          echo "🔧 系统GCC版本: $GCC_VERSION"
          echo "✅ 工具验证完成"
      #【firmware-build.yml-05】

      #【firmware-build.yml-06】
      - name: "06. 初始空间检查"
        run: |
          echo "=== 步骤06: 初始空间检查 ==="
          
          set -e
          trap 'echo "❌ 步骤06 失败，退出代码: $?"; exit 1' ERR
          
          echo "📊 /mnt 分区详细信息:"
          df -h /mnt
          
          AVAILABLE_SPACE=$(df /mnt --output=avail | tail -1 | awk '{print $1}')
          AVAILABLE_GB=$((AVAILABLE_SPACE / 1024 / 1024))
          echo "/mnt 可用空间: ${AVAILABLE_GB}G"
          
          if [ $AVAILABLE_GB -lt 50 ]; then
            echo "❌ 错误: /mnt 空间不足50G，当前只有${AVAILABLE_GB}G"
            exit 1
          else
            echo "✅ 初始空间检查通过 - ${AVAILABLE_GB}G 可用"
          fi
      #【firmware-build.yml-06】

      #【firmware-build.yml-07】
      - name: "07. 创建构建目录"
        run: |
          echo "=== 步骤07: 创建构建目录 ==="
          
          set -e
          trap 'echo "❌ 步骤07 失败，退出代码: $?"; exit 1' ERR
          
          ${{ github.workspace }}/firmware-config/scripts/build_firmware_main.sh \
            step07_create_build_dir
      #【firmware-build.yml-07】

      #【firmware-build.yml-08】
      - name: "08. 初始化构建环境"
        run: |
          echo "=== 步骤08: 初始化构建环境 ==="
          
          set -e
          trap 'echo "❌ 步骤08 失败，退出代码: $?"; exit 1' ERR
          
          ${{ github.workspace }}/firmware-config/scripts/build_firmware_main.sh \
            step08_initialize_build_env \
            "${{ github.event.inputs.device_name }}" \
            "${{ github.event.inputs.version_selection }}" \
            "${{ github.event.inputs.config_mode }}"
      #【firmware-build.yml-08】

      #【firmware-build.yml-09】
      - name: "09. 下载OpenWrt官方SDK"
        run: |
          echo "=== 步骤09: 下载OpenWrt官方SDK ==="
          
          set -e
          trap 'echo "❌ 步骤09 失败，退出代码: $?"; exit 1' ERR
          
          ${{ github.workspace }}/firmware-config/scripts/build_firmware_main.sh \
            step09_download_sdk \
            "${{ github.event.inputs.device_name }}"
      #【firmware-build.yml-09】

      #【firmware-build.yml-10】
      - name: "10. 验证SDK下载结果"
        run: |
          echo "=== 步骤10: 验证SDK下载结果 ==="
          
          trap 'echo "⚠️ 步骤10 验证过程中出现错误，继续执行..."' ERR
          
          ${{ github.workspace }}/firmware-config/scripts/build_firmware_main.sh \
            step10_verify_sdk
      #【firmware-build.yml-10】

      #【firmware-build.yml-11】
      - name: "11. 添加TurboACC支持"
        run: |
          echo "=== 步骤11: 添加TurboACC支持 ==="
          
          set -e
          trap 'echo "❌ 步骤11 失败，退出代码: $?"; exit 1' ERR
          
          ${{ github.workspace }}/firmware-config/scripts/build_firmware_main.sh \
            step11_add_turboacc
      #【firmware-build.yml-11】

      #【firmware-build.yml-12】
      - name: "12. 配置Feeds"
        run: |
          echo "=== 步骤12: 配置Feeds ==="
          
          set -e
          trap 'echo "❌ 步骤12 失败，退出代码: $?"; exit 1' ERR
          
          ${{ github.workspace }}/firmware-config/scripts/build_firmware_main.sh \
            step12_configure_feeds
      #【firmware-build.yml-12】

      #【firmware-build.yml-13】
      - name: "13. 安装TurboACC包"
        if: github.event.inputs.config_mode == 'normal'
        run: |
          echo "=== 步骤13: 安装TurboACC包 ==="
          
          set -e
          trap 'echo "❌ 步骤13 失败，退出代码: $?"; exit 1' ERR
          
          ${{ github.workspace }}/firmware-config/scripts/build_firmware_main.sh \
            step13_install_turboacc
      #【firmware-build.yml-13】

      #【firmware-build.yml-14】
      - name: "14. 编译前空间检查"
        run: |
          echo "=== 步骤14: 编译前空间检查 ==="
          
          set -e
          trap 'echo "❌ 步骤14 失败，退出代码: $?"; exit 1' ERR
          
          ${{ github.workspace }}/firmware-config/scripts/build_firmware_main.sh \
            step14_pre_build_space_check
      #【firmware-build.yml-14】

      #【firmware-build.yml-15】
      - name: "15. 智能配置生成"
        run: |
          echo "=== 步骤15: 智能配置生成 ==="
          
          set -e
          trap 'echo "❌ 步骤15 失败，退出代码: $?"; exit 1' ERR
          
          ${{ github.workspace }}/firmware-config/scripts/build_firmware_main.sh \
            step15_generate_config \
            "${{ github.event.inputs.extra_packages }}"
      #【firmware-build.yml-15】

      #【firmware-build.yml-16】
      - name: "16. 验证USB配置"
        run: |
          echo "=== 步骤16: 验证USB配置 ==="
          
          trap 'echo "⚠️ 步骤16 验证过程中出现错误，继续执行..."' ERR
          
          ${{ github.workspace }}/firmware-config/scripts/build_firmware_main.sh \
            step16_verify_usb
      #【firmware-build.yml-16】

      #【firmware-build.yml-17】
      - name: "17. USB驱动完整性检查"
        run: |
          echo "=== 步骤17: USB驱动完整性检查 ==="
          
          trap 'echo "⚠️ 步骤17 检查过程中出现错误，继续执行..."' ERR
          
          ${{ github.workspace }}/firmware-config/scripts/build_firmware_main.sh \
            step17_check_usb_drivers
      #【firmware-build.yml-17】

      #【firmware-build.yml-18】
      - name: "18. 应用配置"
        run: |
          echo "=== 步骤18: 应用配置 ==="
          
          set -e
          trap 'echo "❌ 步骤18 失败，退出代码: $?"; exit 1' ERR
          
          cd /mnt/openwrt-build
          
          echo "🔄 运行 make defconfig..."
          make defconfig || {
            echo "❌ make defconfig 失败"
            exit 1
          }
          
          echo "✅ 配置应用完成"
      #【firmware-build.yml-18】

      #【firmware-build.yml-19】
      - name: "19. 备份配置文件"
        run: |
          echo "=== 步骤19: 备份配置文件 ==="
          
          set -e
          trap 'echo "❌ 步骤19 失败，退出代码: $?"; exit 1' ERR
          
          cd /mnt/openwrt-build
          
          if [ ! -f ".config" ]; then
            echo "❌ 错误: .config 文件不存在"
            exit 1
          fi
          
          echo "✅ .config 文件存在"
          echo "📊 文件大小: $(ls -lh .config | awk '{print $5}')"
          echo "📝 文件行数: $(wc -l < .config)"
          
          mkdir -p ${{ github.workspace }}/firmware-config/config-backup
          BACKUP_FILE="${{ github.workspace }}/firmware-config/config-backup/config_${{ env.TIMESTAMP_SEC }}.config"
          
          cp .config "$BACKUP_FILE"
          echo "✅ 配置文件已备份到: $BACKUP_FILE"
      #【firmware-build.yml-19】

      #【firmware-build.yml-20】
      - name: "20. 修复网络环境"
        run: |
          echo "=== 步骤20: 修复网络环境 ==="
          
          trap 'echo "⚠️ 步骤20 修复过程中出现错误，继续执行..."' ERR
          
          ${{ github.workspace }}/firmware-config/scripts/build_firmware_main.sh \
            step20_fix_network
      #【firmware-build.yml-20】

      #【firmware-build.yml-21】
      - name: "21. 下载依赖包"
        run: |
          echo "=== 步骤21: 下载依赖包 ==="
          
          set -e
          trap 'echo "❌ 步骤21 失败，退出代码: $?"; exit 1' ERR
          
          ${{ github.workspace }}/firmware-config/scripts/build_firmware_main.sh \
            step21_download_deps
      #【firmware-build.yml-21】

      #【firmware-build.yml-22】
      - name: "22. 集成自定义文件"
        run: |
          echo "=== 步骤22: 集成自定义文件 ==="
          
          trap 'echo "⚠️ 步骤22 集成过程中出现错误，继续执行..."' ERR
          
          ${{ github.workspace }}/firmware-config/scripts/build_firmware_main.sh \
            step22_integrate_custom_files
      #【firmware-build.yml-22】

      #【firmware-build.yml-23】
      - name: "23. 前置错误检查"
        run: |
          echo "=== 步骤23: 前置错误检查 ==="
          
          set -e
          trap 'echo "❌ 步骤23 失败，退出代码: $?"; exit 1' ERR
          
          ${{ github.workspace }}/firmware-config/scripts/build_firmware_main.sh \
            step23_pre_build_check
      #【firmware-build.yml-23】

      #【firmware-build.yml-24】
      - name: "24. 编译前空间确认"
        run: |
          echo "=== 步骤24: 编译前空间确认 ==="
          
          set -e
          trap 'echo "❌ 步骤24 失败，退出代码: $?"; exit 1' ERR
          
          df -h /mnt
          AVAILABLE_SPACE=$(df /mnt --output=avail | tail -1 | awk '{print $1}')
          AVAILABLE_GB=$((AVAILABLE_SPACE / 1024 / 1024))
          echo "/mnt 可用空间: ${AVAILABLE_GB}G"
          
          if [ $AVAILABLE_GB -lt 10 ]; then
            echo "❌ 错误: 编译前空间不足 (需要至少10G，当前${AVAILABLE_GB}G)"
            exit 1
          else
            echo "✅ 编译前空间充足"
          fi
      #【firmware-build.yml-24】

      #【firmware-build.yml-25】
      - name: "25. 编译固件"
        run: |
          echo "=== 步骤25: 编译固件 ==="
          
          set -e
          trap 'echo "❌ 步骤25 失败，退出代码: $?"; exit 1' ERR
          
          ${{ github.workspace }}/firmware-config/scripts/build_firmware_main.sh \
            step25_build_firmware \
            "${{ env.ENABLE_PARALLEL }}"
      #【firmware-build.yml-25】

      #【firmware-build.yml-26】
      - name: "26. 检查构建产物"
        if: success()
        run: |
          echo "=== 步骤26: 检查构建产物 ==="
          
          set -e
          trap 'echo "❌ 步骤26 失败，退出代码: $?"; exit 1' ERR
          
          ${{ github.workspace }}/firmware-config/scripts/build_firmware_main.sh \
            step26_check_artifacts
      #【firmware-build.yml-26】

      #【firmware-build.yml-27】
      - name: "27. 上传固件"
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: firmware-${{ env.TIMESTAMP_SEC }}
          path: /mnt/openwrt-build/bin/targets/
          retention-days: 7
          if-no-files-found: error
      #【firmware-build.yml-27】

      #【firmware-build.yml-28】
      - name: "28. 上传配置文件"
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: config-${{ env.TIMESTAMP_SEC }}
          path: ${{ github.workspace }}/firmware-config/config-backup/
          retention-days: 7
          if-no-files-found: error
      #【firmware-build.yml-28】

      #【firmware-build.yml-29】
      - name: "29. 编译后空间检查"
        if: always()
        run: |
          echo "=== 步骤29: 编译后空间检查 ==="
          
          trap 'echo "⚠️ 步骤29 检查过程中出现错误，继续执行..."' ERR
          
          ${{ github.workspace }}/firmware-config/scripts/build_firmware_main.sh \
            step29_post_build_space_check
      #【firmware-build.yml-29】

      #【firmware-build.yml-30】
      - name: "30. 编译总结"
        if: always()
        run: |
          echo "=== 步骤30: 编译总结 ==="
          
          trap 'echo "⚠️ 步骤30 总结过程中出现错误，继续执行..."' ERR
          
          ${{ github.workspace }}/firmware-config/scripts/build_firmware_main.sh \
            step30_build_summary \
            "${{ github.event.inputs.device_name }}" \
            "${{ github.event.inputs.version_selection }}" \
            "${{ github.event.inputs.config_mode }}" \
            "${{ env.TIMESTAMP_SEC }}" \
            "${{ github.event.inputs.enable_parallel }}"
      #【firmware-build.yml-30】
