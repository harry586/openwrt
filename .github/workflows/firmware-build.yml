# .github/workflows/firmware-build.yml
name: OpenWrt 智能固件构建工作流（完整功能版 - 编译器预构建版）

on:
  workflow_dispatch:
    inputs:
      device_name:
        description: "📱 设备名称 (如: ac42u)"
        required: true
        default: "ac42u"
        type: string
      version_selection:
        description: "🔄 版本选择"
        required: true
        type: choice
        default: "21.02"
        options: ["23.05", "21.02"]
      config_mode:
        description: "⚙️ 配置模式选择 - 🟣 基础模式 - 最小化配置，用于测试编译。🟠 正常模式 - 完整功能配置"
        required: true
        type: choice
        default: "normal"
        options: ["base", "normal"]
      extra_packages:
        description: "额外安装插件 - 格式：用分号;分隔。启用插件：+插件名。禁用插件：-插件名。"
        required: false
        type: string
        default: ""
      use_git_archive:
        description: "📦 使用Git Archive API下载源代码"
        required: false
        default: true
        type: boolean

env:
  BUILD_DIR: "/mnt/openwrt-build"
  REPO_ROOT: "${{ github.workspace }}"
  COMPILER_ROOT: "${{ github.workspace }}/firmware-config/build-Compiler-file"

jobs:
  build-firmware:
    runs-on: ubuntu-22.04
    
    steps:
      # 步骤 1: 使用Git Archive API下载源代码（排除编译器大目录）
      - name: "1. 使用Git Archive API下载源代码（排除编译器大目录）"
        if: github.event.inputs.use_git_archive == 'true'
        run: |
          echo "=== 步骤 1: 使用Git Archive API下载源代码（排除编译器大目录）==="
          echo "🔄 从GitHub API获取源代码压缩包，排除超过30MB的编译器大目录..."
          
          # 创建临时目录
          mkdir -p /tmp/openwrt-source
          cd /tmp/openwrt-source
          
          # 获取默认分支
          DEFAULT_BRANCH=$(curl -s -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" "https://api.github.com/repos/${{ github.repository }}" | jq -r '.default_branch')
          echo "默认分支: $DEFAULT_BRANCH"
          
          # 使用Git Archive API下载压缩包
          echo "下载源代码压缩包..."
          curl -s -L -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" "https://api.github.com/repos/${{ github.repository }}/tarball/$DEFAULT_BRANCH" -o source.tar.gz
          
          if [ ! -f "source.tar.gz" ]; then
            echo "❌ 下载源代码压缩包失败"
            exit 1
          fi
          
          echo "✅ 下载完成，文件大小: $(ls -lh source.tar.gz | awk '{print $5}')"
          
          # 解压压缩包
          echo "解压源代码..."
          tar -xzf source.tar.gz --strip-components=1
          
          # 检查解压结果并验证多个重要文件
          echo "🔍 验证源代码完整性..."
          IMPORTANT_FILES=(
            "firmware-config/scripts/build_firmware_main.sh"
            ".github/workflows/firmware-build.yml"
            "firmware-config/scripts/error_analysis.sh"
            "firmware-config/custom-files/README.md"
            "README.md"
          )
          
          file_check_pass=true
          for file in "${IMPORTANT_FILES[@]}"; do
            if [ -f "$file" ]; then
              echo "✅ 重要文件存在: $file ($(ls -lh "$file" | awk '{print $5}'))"
            else
              echo "❌ 重要文件缺失: $file"
              file_check_pass=false
            fi
          done
          
          if [ "$file_check_pass" = false ]; then
            echo "❌ 源代码完整性检查失败"
            echo "当前目录内容:"
            find . -maxdepth 3 -type f | head -30
            exit 1
          fi
          
          echo "✅ 源代码完整性检查通过"
          
          # 检查编译器目录大小（30MB限制）
          if [ -d "firmware-config/build-Compiler-file" ]; then
            COMPILER_SIZE=$(du -sm firmware-config/build-Compiler-file 2>/dev/null | cut -f1 || echo "0")
            echo "🔍 检查编译器目录大小: ${COMPILER_SIZE}MB"
            
            if [ "$COMPILER_SIZE" -gt 30 ]; then
              echo "⚠️ 编译器目录超过30MB限制 (${COMPILER_SIZE}MB > 30MB)，将被排除"
              echo "⚠️ 排除不复制以避免30MB限制"
              
              # 保存编译器目录信息文件
              mkdir -p firmware-config/compiler-info
              echo "编译器目录已因大小限制被排除" > firmware-config/compiler-info/README.md
              echo "原始大小: ${COMPILER_SIZE}MB" >> firmware-config/compiler-info/README.md
              echo "排除时间: $(date)" >> firmware-config/compiler-info/README.md
              echo "限制: 30MB" >> firmware-config/compiler-info/README.md
              
              # 删除编译器目录
              rm -rf firmware-config/build-Compiler-file
              echo "✅ 已删除超过30MB限制的编译器目录"
            else
              echo "✅ 编译器目录未超过30MB限制，保留"
            fi
          else
            echo "ℹ️ 未发现编译器目录"
          fi
          
          # 复制到工作区
          echo "复制文件到工作区..."
          
          # 确保工作区目录存在
          mkdir -p ${{ github.workspace }}
          
          # 使用rsync复制所有文件，排除.git目录
          rsync -av --exclude='.git' . ${{ github.workspace }}/
          
          # 确保脚本文件有执行权限
          chmod +x ${{ github.workspace }}/firmware-config/scripts/*.sh 2>/dev/null || true
          
          echo "✅ 文件复制完成"
          
          # 验证工作区文件
          echo "🔍 验证工作区文件..."
          for file in "${IMPORTANT_FILES[@]}"; do
            if [ -f "${{ github.workspace }}/$file" ]; then
              echo "✅ 工作区文件存在: $file"
            else
              echo "❌ 工作区文件缺失: $file"
            fi
          done
          
          # 保存源代码压缩包作为构建产物
          mkdir -p /tmp/build-artifacts/source-archive
          cp source.tar.gz /tmp/build-artifacts/source-archive/
          
          # 计算最终工作区大小
          WORKSPACE_SIZE=$(du -sm ${{ github.workspace }} 2>/dev/null | cut -f1 || echo "0")
          echo "📊 工作区最终大小: ${WORKSPACE_SIZE}MB"
          
          # 清理临时文件
          cd /tmp
          rm -rf openwrt-source
          echo "✅ Git Archive API下载完成"
      
      # 步骤 2: 立即上传Git Archive源代码
      - name: "2. 立即上传Git Archive源代码"
        if: github.event.inputs.use_git_archive == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: git-archive-source-${{ github.event.inputs.device_name }}
          path: /tmp/build-artifacts/source-archive/
          retention-days: 7
      
      # 步骤 3: 使用标准检出（当不使用Git Archive时）
      - name: "3. 标准检出源代码"
        if: github.event.inputs.use_git_archive == 'false'
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
      
      # 步骤 4: 全面清理工作区，重新检出完整仓库
      - name: "4. 全面清理工作区并重新检出完整仓库"
        if: github.event.inputs.use_git_archive == 'true'
        run: |
          echo "=== 步骤 4: 全面清理工作区并重新检出完整仓库 ==="
          echo "🔄 清理工作区并重新使用Git Archive API下载完整仓库..."
          
          # 清理工作区
          echo "清理工作区..."
          rm -rf ${{ github.workspace }}/*
          
          # 重新使用Git Archive API下载
          echo "重新使用Git Archive API下载完整仓库..."
          
          # 创建临时目录
          mkdir -p /tmp/openwrt-source-full
          cd /tmp/openwrt-source-full
          
          # 获取默认分支
          DEFAULT_BRANCH=$(curl -s -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" "https://api.github.com/repos/${{ github.repository }}" | jq -r '.default_branch')
          
          # 下载完整源代码压缩包
          curl -s -L -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" "https://api.github.com/repos/${{ github.repository }}/tarball/$DEFAULT_BRANCH" -o source-full.tar.gz
          
          echo "✅ 下载完成，文件大小: $(ls -lh source-full.tar.gz | awk '{print $5}')"
          
          # 解压压缩包
          echo "解压源代码..."
          tar -xzf source-full.tar.gz --strip-components=1
          
          # 检查解压结果
          IMPORTANT_FILES=(
            "firmware-config/scripts/build_firmware_main.sh"
            ".github/workflows/firmware-build.yml"
            "firmware-config/scripts/error_analysis.sh"
          )
          
          file_check_pass=true
          for file in "${IMPORTANT_FILES[@]}"; do
            if [ -f "$file" ]; then
              echo "✅ 文件存在: $file"
            else
              echo "❌ 文件缺失: $file"
              file_check_pass=false
            fi
          done
          
          if [ "$file_check_pass" = false ]; then
            echo "❌ 源代码完整性检查失败"
            exit 1
          fi
          
          echo "✅ 源代码解压成功"
          
          # 检查编译器目录（应用30MB限制）
          if [ -d "firmware-config/build-Compiler-file" ]; then
            COMPILER_SIZE=$(du -sm firmware-config/build-Compiler-file 2>/dev/null | cut -f1 || echo "0")
            echo "🔍 检查编译器目录大小: ${COMPILER_SIZE}MB"
            
            if [ "$COMPILER_SIZE" -gt 30 ]; then
              echo "⚠️ 编译器目录超过30MB限制 (${COMPILER_SIZE}MB > 30MB)，将被排除"
              echo "⚠️ 排除不复制以避免30MB限制"
              
              # 保存编译器信息
              mkdir -p firmware-config/compiler-info
              echo "编译器目录已因大小限制被排除" > firmware-config/compiler-info/README.md
              echo "原始大小: ${COMPILER_SIZE}MB" >> firmware-config/compiler-info/README.md
              echo "排除时间: $(date)" >> firmware-config/compiler-info/README.md
              
              # 删除编译器目录
              rm -rf firmware-config/build-Compiler-file
              echo "✅ 已删除超过30MB限制的编译器目录"
            else
              echo "✅ 编译器目录未超过30MB限制，保留"
            fi
          else
            echo "ℹ️ 未发现编译器目录"
          fi
          
          # 复制所有文件到工作区
          echo "复制所有文件到工作区..."
          rsync -av --exclude='.git' . ${{ github.workspace }}/
          
          # 确保脚本文件有执行权限
          chmod +x ${{ github.workspace }}/firmware-config/scripts/*.sh 2>/dev/null || true
          
          echo "✅ 完整仓库重新检出完成"
          
          # 验证工作区最终大小
          WORKSPACE_SIZE=$(du -sm ${{ github.workspace }} 2>/dev/null | cut -f1 || echo "0")
          echo "📊 工作区最终大小: ${WORKSPACE_SIZE}MB"
          
          # 清理临时文件
          cd /tmp
          rm -rf openwrt-source-full
      
      # 步骤 5: 修复权限问题（关键修复）
      - name: "5. 修复权限问题（关键修复）"
        run: |
          echo "=== 步骤 5: 修复权限问题 ==="
          echo "⚠️ 修复所有脚本的执行权限..."
          
          # 查找并修复所有脚本文件的权限
          find firmware-config -type f -name "*.sh" -exec chmod +x {} \;
          
          # 修复主脚本权限
          if [ -f "firmware-config/scripts/build_firmware_main.sh" ]; then
            chmod +x firmware-config/scripts/build_firmware_main.sh
            echo "✅ 修复主脚本权限: firmware-config/scripts/build_firmware_main.sh"
          else
            echo "❌ 主脚本不存在: firmware-config/scripts/build_firmware_main.sh"
            echo "列出firmware-config目录内容:"
            find firmware-config -type f 2>/dev/null || echo "firmware-config目录不存在"
            exit 1
          fi
          
          # 验证权限
          echo "验证脚本权限:"
          ls -la firmware-config/scripts/*.sh 2>/dev/null || echo "scripts目录不存在"
          
          echo "✅ 权限修复完成"
      
      # 步骤 6: 深度检查编译器目录并智能查找正确的编译器文件（合并版）
      - name: "6. 深度检查编译器目录并智能查找（合并版）"
        run: |
          echo "=== 步骤 6: 深度检查编译器目录并智能查找（合并版）==="
          
          REPO_ROOT="${{ github.workspace }}"
          COMPILER_ROOT="$REPO_ROOT/firmware-config/build-Compiler-file"
          
          echo "📁 仓库根目录: $REPO_ROOT"
          echo "🔧 编译器根目录: $COMPILER_ROOT"
          
          # 检查编译器根目录是否存在
          if [ ! -d "$COMPILER_ROOT" ]; then
            echo "⚠️ 编译器根目录不存在于工作空间: $COMPILER_ROOT"
            echo "ℹ️ 这是正常的，编译器大文件可能已被删除以避免30MB限制"
            echo "✅ 将使用OpenWrt自动构建的编译器"
            echo "COMPILER_DIR=" >> $GITHUB_ENV
            exit 0
          fi
          
          echo "✅ 编译器根目录存在于工作空间"
          
          # 检查目录大小（30MB限制）
          COMPILER_SIZE=$(du -sm "$COMPILER_ROOT" 2>/dev/null | cut -f1 || echo "0")
          echo "🔍 编译器目录大小检查: ${COMPILER_SIZE}MB"
          
          if [ "$COMPILER_SIZE" -gt 30 ]; then
            echo "⚠️ 编译器目录超过30MB限制 (${COMPILER_SIZE}MB > 30MB)"
            echo "⚠️ 目录将被清理以避免30MB限制"
            
            # 保存信息文件
            mkdir -p "$REPO_ROOT/firmware-config/compiler-info"
            echo "编译器目录已因大小限制被清理" > "$REPO_ROOT/firmware-config/compiler-info/README.md"
            echo "原始大小: ${COMPILER_SIZE}MB" >> "$REPO_ROOT/firmware-config/compiler-info/README.md"
            echo "清理时间: $(date)" >> "$REPO_ROOT/firmware-config/compiler-info/README.md"
            
            # 清理编译器目录
            rm -rf "$COMPILER_ROOT"
            mkdir -p "$COMPILER_ROOT"
            echo "✅ 已清理超过30MB限制的编译器目录"
            echo "COMPILER_DIR=" >> $GITHUB_ENV
            exit 0
          fi
          
          echo "✅ 编译器目录大小符合30MB限制"
          
          # 深度搜索所有子目录和文件
          echo "🔍 深度搜索编译器目录结构..."
          TOTAL_DIRS=$(find "$COMPILER_ROOT" -type d 2>/dev/null | wc -l)
          TOTAL_FILES=$(find "$COMPILER_ROOT" -type f 2>/dev/null | wc -l)
          
          echo "总目录数: $TOTAL_DIRS"
          echo "总文件数: $TOTAL_FILES"
          
          if [ $TOTAL_FILES -gt 0 ]; then
            echo "📋 目录结构（显示前100个目录）:"
            find "$COMPILER_ROOT" -type d 2>/dev/null | sort | head -100 | while read dir; do
              depth=$(echo "$dir" | tr -cd '/' | wc -c)
              if [ $depth -le 8 ]; then
                indent=""
                for ((i=1; i<=depth; i++)); do indent="$indent  "; done
                dir_name=$(basename "$dir")
                file_count=$(find "$dir" -type f 2>/dev/null | wc -l)
                relative_path=$(echo "$dir" | sed "s|$COMPILER_ROOT/||")
                echo "${indent}📁 $dir_name/ ($file_count 个文件) [$relative_path]"
              fi
            done
            
            # 查找gcc文件
            echo "🔍 查找GCC文件..."
            GCC_FILES=$(find "$COMPILER_ROOT" -type f -name "*gcc*" 2>/dev/null | head -20)
            GCC_COUNT=$(echo "$GCC_FILES" | wc -l)
            
            if [ $GCC_COUNT -gt 0 ]; then
              echo "✅ 找到 $GCC_COUNT 个GCC文件"
              echo "$GCC_FILES" | head -5 | while read file; do
                if [ -f "$file" ]; then
                  size=$(du -h "$file" 2>/dev/null | cut -f1 || echo "未知")
                  relative_path=$(echo "$file" | sed "s|$REPO_ROOT/||")
                  echo "  📄 $size - $relative_path"
                fi
              done
            else
              echo "❌ 未找到GCC文件"
            fi
          else
            echo "⚠️ 编译器目录为空（没有文件）"
            echo "COMPILER_DIR=" >> $GITHUB_ENV
            exit 0
          fi
          
          # 智能查找编译器目录
          echo ""
          echo "🔍 智能查找正确的编译器文件..."
          
          DEVICE_NAME="${{ github.event.inputs.device_name }}"
          echo "🎯 设备名称: $DEVICE_NAME"
          
          # 根据设备确定平台
          if [[ "$DEVICE_NAME" == "ac42u" || "$DEVICE_NAME" == "acrh17" ]]; then
            TARGET_PLATFORM="arm"
            SEARCH_KEYWORDS=("arm" "aarch64" "cortex-a7" "cortex-a53" "ipq40xx")
            echo "目标平台: ARM (IPQ40xx)"
          elif [[ "$DEVICE_NAME" == "mi_router_4a_gigabit" || "$DEVICE_NAME" == "r4ag" ]]; then
            TARGET_PLATFORM="mips"
            SEARCH_KEYWORDS=("mips" "mipsel" "mt76x8" "24kc")
            echo "目标平台: MIPS (MT76x8)"
          elif [[ "$DEVICE_NAME" == "mi_router_3g" || "$DEVICE_NAME" == "r3g" ]]; then
            TARGET_PLATFORM="mips"
            SEARCH_KEYWORDS=("mips" "mipsel" "mt7621" "1004kc")
            echo "目标平台: MIPS (MT7621)"
          else
            TARGET_PLATFORM="generic"
            SEARCH_KEYWORDS=("gcc" "compiler" "toolchain")
            echo "目标平台: 通用"
          fi
          
          echo "搜索关键词: ${SEARCH_KEYWORDS[*]}"
          
          FOUND_COMPILER_DIR=""
          
          # 方法1: 搜索包含平台关键词的目录
          echo "1. 搜索包含平台关键词的目录..."
          for keyword in "${SEARCH_KEYWORDS[@]}"; do
            echo "  搜索关键词: '$keyword'"
            DIRS=$(find "$COMPILER_ROOT" -type d -iname "*$keyword*" 2>/dev/null | head -10)
            if [ -n "$DIRS" ]; then
              while read dir; do
                # 检查该目录是否有gcc文件
                if find "$dir" -type f -iname "*gcc*" 2>/dev/null | head -1 >/dev/null; then
                  FOUND_COMPILER_DIR="$dir"
                  echo "  ✅ 找到候选目录: $(echo "$dir" | sed "s|$COMPILER_ROOT/||")"
                  break 2
                fi
              done <<< "$DIRS"
            fi
          done
          
          # 方法2: 搜索bin目录结构
          if [ -z "$FOUND_COMPILER_DIR" ]; then
            echo "2. 搜索bin目录结构..."
            BIN_DIRS=$(find "$COMPILER_ROOT" -type d -name "bin" 2>/dev/null | head -10)
            if [ -n "$BIN_DIRS" ]; then
              for bin_dir in $BIN_DIRS; do
                parent_dir=$(dirname "$bin_dir")
                # 检查bin目录中是否有编译器
                if find "$bin_dir" -type f \( -iname "*gcc*" -o -iname "*g++*" \) 2>/dev/null | head -1 >/dev/null; then
                  FOUND_COMPILER_DIR="$parent_dir"
                  echo "  ✅ 找到bin目录结构: $(echo "$parent_dir" | sed "s|$COMPILER_ROOT/||")"
                  break
                fi
              done
            fi
          fi
          
          # 方法3: 使用第一个包含gcc文件的目录
          if [ -z "$FOUND_COMPILER_DIR" ]; then
            echo "3. 使用第一个包含gcc文件的目录..."
            FIRST_GCC_DIR=$(find "$COMPILER_ROOT" -type f -iname "*gcc*" 2>/dev/null | head -1 | xargs dirname)
            if [ -n "$FIRST_GCC_DIR" ]; then
              # 如果是bin目录中的gcc，向上找一级
              if [[ "$FIRST_GCC_DIR" == */bin ]]; then
                FIRST_GCC_DIR=$(dirname "$FIRST_GCC_DIR")
              fi
              FOUND_COMPILER_DIR="$FIRST_GCC_DIR"
              echo "  ✅ 使用GCC文件所在目录: $(echo "$FIRST_GCC_DIR" | sed "s|$COMPILER_ROOT/||")"
            fi
          fi
          
          # 最终决策
          echo ""
          echo "=== 搜索结果 ==="
          if [ -n "$FOUND_COMPILER_DIR" ]; then
            echo "✅ 找到编译器目录: $(echo "$FOUND_COMPILER_DIR" | sed "s|$COMPILER_ROOT/||")"
            
            # 验证目录
            echo "📊 目录验证:"
            echo "  大小: $(du -sh "$FOUND_COMPILER_DIR" 2>/dev/null | cut -f1 || echo '未知')"
            echo "  文件数: $(find "$FOUND_COMPILER_DIR" -type f 2>/dev/null | wc -l)"
            
            # 设置环境变量
            echo "COMPILER_DIR=$FOUND_COMPILER_DIR" >> $GITHUB_ENV
            echo "✅ 已设置编译器目录环境变量"
            
          else
            echo "❌ 未找到合适的编译器目录"
            echo "ℹ️ 将使用OpenWrt自动构建的编译器"
            echo "COMPILER_DIR=" >> $GITHUB_ENV
          fi
          
          echo "✅ 编译器搜索完成"
      
      # 步骤 7: 初始空间检查
      - name: "7. 初始空间检查"
        run: |
          echo "=== 步骤 7: 初始磁盘空间检查 ==="
          df -h
          AVAILABLE_SPACE=$(df /mnt --output=avail | tail -1)
          AVAILABLE_GB=$((AVAILABLE_SPACE / 1024 / 1024))
          echo "/mnt 可用空间: ${AVAILABLE_GB}G"
          if [ $AVAILABLE_GB -lt 50 ]; then
            echo "❌ 错误: /mnt 空间不足50G"
            exit 1
          fi
          echo "✅ 初始空间检查通过"
      
      # 步骤 8: 设置编译环境和构建目录
      - name: "8. 设置编译环境和构建目录"
        run: |
          echo "=== 步骤 8: 设置编译环境和构建目录 ==="
          
          echo "🔄 步骤 8.1: 设置编译环境"
          chmod +x firmware-config/scripts/build_firmware_main.sh
          firmware-config/scripts/build_firmware_main.sh setup_environment
          
          echo "🔄 步骤 8.2: 创建构建目录"
          firmware-config/scripts/build_firmware_main.sh create_build_dir
          
          echo "🔄 步骤 8.3: 初始化构建环境"
          echo "设备: ${{ github.event.inputs.device_name }}"
          echo "版本: ${{ github.event.inputs.version_selection }}"
          echo "配置模式: ${{ github.event.inputs.config_mode }}"
          firmware-config/scripts/build_firmware_main.sh initialize_build_env "${{ github.event.inputs.device_name }}" "${{ github.event.inputs.version_selection }}" "${{ github.event.inputs.config_mode }}"
          
          echo "✅ 编译环境和构建目录设置完成"
      
      # 步骤 9: 添加 TurboACC 支持
      - name: "9. 添加 TurboACC 支持"
        run: firmware-config/scripts/build_firmware_main.sh add_turboacc_support
      
      # 步骤 10: 配置Feeds
      - name: "10. 配置Feeds"
        run: firmware-config/scripts/build_firmware_main.sh configure_feeds
      
      # 步骤 11: 安装 TurboACC 包
      - name: "11. 安装 TurboACC 包"
        if: env.SELECTED_BRANCH == 'openwrt-23.05' && github.event.inputs.config_mode == 'normal'
        run: firmware-config/scripts/build_firmware_main.sh install_turboacc_packages
      
      # 步骤 12: 编译前空间检查
      - name: "12. 编译前空间检查"
        run: firmware-config/scripts/build_firmware_main.sh pre_build_space_check
      
      # 步骤 13: 智能配置生成（USB完全修复加强版）
      - name: "13. 智能配置生成（USB完全修复加强版）"
        run: |
          echo "=== 步骤 13: 智能配置生成 ==="
          echo "🚨 USB 3.0加强：所有关键USB驱动强制启用"
          
          # 添加完整正常模式插件
          echo "🔧 正常模式插件配置:"
          if [ "${{ github.event.inputs.config_mode }}" = "normal" ]; then
            echo "  🟠 启用完整功能配置"
            echo "  📦 包含: TurboACC, UPnP, Samba, DiskMan, KMS, SmartDNS等"
          else
            echo "  🟣 基础模式配置"
          fi
          
          firmware-config/scripts/build_firmware_main.sh generate_config "${{ github.event.inputs.extra_packages }}"
      
      # 步骤 14: 验证USB配置
      - name: "14. 验证USB配置"
        run: firmware-config/scripts/build_firmware_main.sh verify_usb_config
      
      # 步骤 15: USB驱动完整性检查
      - name: "15. USB驱动完整性检查"
        run: firmware-config/scripts/build_firmware_main.sh check_usb_drivers_integrity
      
      # 步骤 16: 应用配置并显示详情
      - name: "16. 应用配置并显示详情"
        run: |
          echo "=== 步骤 16: 应用配置并显示详情 ==="
          
          # 先保存原始配置文件
          if [ -f "${{ env.BUILD_DIR }}/.config" ]; then
            cp "${{ env.BUILD_DIR }}/.config" "${{ env.BUILD_DIR }}/.config.original"
            echo "✅ 已备份原始配置文件"
          fi
          
          # 应用配置
          firmware-config/scripts/build_firmware_main.sh apply_config
          
          # 检查应用后的配置状态
          echo "=== 插件配置状态检查 ==="
          cd "${{ env.BUILD_DIR }}"
          
          # 完整正常模式插件列表
          declare -A normal_plugins
          normal_plugins["TurboACC 网络加速"]="luci-app-turboacc"
          normal_plugins["UPnP 自动端口转发"]="luci-app-upnp"
          normal_plugins["Samba 文件共享"]="luci-app-samba4"
          normal_plugins["磁盘管理"]="luci-app-diskman"
          normal_plugins["KMS 激活服务"]="luci-app-vlmcsd"
          normal_plugins["SmartDNS 智能DNS"]="luci-app-smartdns"
          normal_plugins["家长控制"]="luci-app-accesscontrol"
          normal_plugins["微信推送"]="luci-app-wechatpush"
          normal_plugins["流量控制 (SQM)"]="luci-app-sqm"
          normal_plugins["FTP 服务器"]="luci-app-vsftpd"
          normal_plugins["ARP 绑定"]="luci-app-arpbind"
          normal_plugins["CPU 限制"]="luci-app-cpulimit"
          normal_plugins["硬盘休眠"]="luci-app-hd-idle"
          normal_plugins["网络唤醒"]="luci-app-wol"
          normal_plugins["DDNS 动态域名"]="luci-app-ddns"
          normal_plugins["Frp 内网穿透"]="luci-app-frpc"
          normal_plugins["文件传输"]="luci-app-filetransfer"
          normal_plugins["ZeroTier 内网穿透"]="luci-app-zerotier"
          normal_plugins["网络带宽监控"]="luci-app-nlbwmon"
          normal_plugins["广告屏蔽"]="luci-app-adbyby-plus"
          
          echo "📋 正常模式插件状态检查:"
          
          total_plugins=0
          enabled_plugins=0
          disabled_plugins=0
          
          for plugin_name in "${!normal_plugins[@]}"; do
            plugin_config="${normal_plugins[$plugin_name]}"
            total_plugins=$((total_plugins + 1))
            
            if grep -q "^CONFIG_PACKAGE_${plugin_config}=y" .config; then
              echo "  ✅ $plugin_name: 已启用"
              enabled_plugins=$((enabled_plugins + 1))
            elif grep -q "^# CONFIG_PACKAGE_${plugin_config} is not set$" .config; then
              echo "  ❌ $plugin_name: 已禁用"
              disabled_plugins=$((disabled_plugins + 1))
            else
              echo "  ⚠️  $plugin_name: 未配置"
            fi
          done
          
          echo ""
          echo "📊 插件状态统计:"
          echo "  总计: $total_plugins 个插件"
          echo "  已启用: $enabled_plugins 个"
          echo "  已禁用: $disabled_plugins 个"
          
          # 检查基础模式
          if [ "${{ github.event.inputs.config_mode }}" = "base" ]; then
            echo ""
            echo "🔧 基础模式配置确认:"
            if [ $enabled_plugins -eq 0 ]; then
              echo "  ✅ 基础模式: 所有额外插件已正确禁用"
            else
              echo "  ⚠️  基础模式: 有 $enabled_plugins 个插件未禁用，需要检查"
            fi
          fi
          
          # USB驱动状态
          echo ""
          echo "🔌 USB驱动状态确认:"
          critical_usb_drivers=("kmod-usb-xhci-hcd" "kmod-usb3" "kmod-usb-dwc3" "kmod-usb-storage" "kmod-scsi-core")
          for driver in "${critical_usb_drivers[@]}"; do
            if grep -q "^CONFIG_PACKAGE_${driver}=y" .config; then
              echo "  ✅ $driver: 已启用"
            else
              echo "  ❌ $driver: 未启用"
            fi
          done
          
          echo "✅ 配置详情检查完成"
      
      # 步骤 17: 检查并备份配置文件
      - name: "17. 检查并备份配置文件"
        run: |
          echo "=== 步骤 17: 检查并备份配置文件 ==="
          
          # 检查配置文件
          if [ -f "${{ env.BUILD_DIR }}/.config" ]; then
            echo "✅ .config 文件存在"
            
            # 确保备份目录存在
            mkdir -p firmware-config/config-backup
            
            # 备份到仓库目录
            timestamp=$(date +%Y%m%d_%H%M%S)
            backup_file="firmware-config/config-backup/config_${{ github.event.inputs.device_name }}_${{ env.SELECTED_BRANCH }}_${{ github.event.inputs.config_mode }}_$timestamp.config"
            
            cp "${{ env.BUILD_DIR }}/.config" "$backup_file"
            echo "✅ 配置文件备份到仓库目录: $backup_file"
            
            # 显示备份文件信息
            echo "备份文件大小: $(ls -lh $backup_file | awk '{print $5}')"
          else
            echo "❌ .config 文件不存在"
            exit 1
          fi
      
      # 步骤 18: 修复网络环境
      - name: "18. 修复网络环境"
        run: firmware-config/scripts/build_firmware_main.sh fix_network
      
      # 步骤 19: 下载依赖包
      - name: "19. 下载依赖包"
        run: firmware-config/scripts/build_firmware_main.sh download_dependencies
      
      # 步骤 20: 集成自定义文件
      - name: "20. 集成自定义文件"
        run: firmware-config/scripts/build_firmware_main.sh integrate_custom_files
      
      # 步骤 21: 前置错误检查
      - name: "21. 前置错误检查"
        run: firmware-config/scripts/build_firmware_main.sh pre_build_error_check
      
      # 步骤 22: 编译固件前的空间检查
      - name: "22. 编译固件前的空间检查"
        run: |
          echo "=== 步骤 22: 编译固件前的空间检查 ==="
          df -h
          AVAILABLE_SPACE=$(df /mnt --output=avail | tail -1)
          AVAILABLE_GB=$((AVAILABLE_SPACE / 1024 / 1024))
          echo "/mnt 可用空间: ${AVAILABLE_GB}G"
          
          if [ $AVAILABLE_GB -lt 10 ]; then
            echo "❌ 错误: 编译前空间不足 (需要至少10G，当前${AVAILABLE_GB}G)"
            exit 1
          elif [ $AVAILABLE_GB -lt 20 ]; then
            echo "⚠️ 警告: 编译前空间较低 (建议至少20G，当前${AVAILABLE_GB}G)"
          else
            echo "✅ 编译前空间充足"
          fi
      
      # 步骤 23: 编译固件
      - name: "23. 编译固件"
        run: |
          echo "=== 步骤 23: 编译固件 ==="
          echo "🔧 编译器信息:"
          echo "  编译器目录: ${{ env.COMPILER_DIR }}"
          
          echo "🚀 开始编译..."
          firmware-config/scripts/build_firmware_main.sh build_firmware "true"
      
      # 步骤 24: 搜索并打包构建产物
      - name: "24. 搜索并打包构建产物"
        if: success()
        run: |
          echo "=== 步骤 24: 搜索并打包构建产物 ==="
          cd ${{ env.BUILD_DIR }}
          
          # 创建构建产物目录
          mkdir -p /tmp/build-artifacts
          
          # 打包固件文件
          if [ -d "bin/targets" ]; then
            echo "打包固件文件..."
            tar -czf /tmp/build-artifacts/firmware.tar.gz -C bin targets/
            echo "✅ 固件打包完成，大小: $(du -h /tmp/build-artifacts/firmware.tar.gz | cut -f1)"
          else
            echo "⚠️ bin/targets目录不存在"
          fi
          
          # 打包编译日志
          if [ -f "build.log" ]; then
            echo "打包编译日志..."
            cp build.log /tmp/build-artifacts/build.log
            echo "✅ 日志打包完成"
          fi
          
          # 创建构建信息文件
          echo "构建时间: $(date)" > /tmp/build-artifacts/build-info.txt
          echo "设备: ${{ env.DEVICE }}" >> /tmp/build-artifacts/build-info.txt
          echo "版本: ${{ env.SELECTED_BRANCH }}" >> /tmp/build-artifacts/build-info.txt
          echo "目标平台: ${{ env.TARGET }}/${{ env.SUBTARGET }}" >> /tmp/build-artifacts/build-info.txt
          echo "配置模式: ${{ github.event.inputs.config_mode }}" >> /tmp/build-artifacts/build-info.txt
          echo "编译器目录: ${{ env.COMPILER_DIR }}" >> /tmp/build-artifacts/build-info.txt
          
          echo "✅ 构建产物准备完成"
          ls -lh /tmp/build-artifacts/
      
      # 步骤 25: 上传固件原始目录
      - name: "25. 上传固件原始目录"
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: firmware-${{ github.event.inputs.device_name }}-${{ env.SELECTED_BRANCH }}-${{ github.event.inputs.config_mode }}
          path: ${{ env.BUILD_DIR }}/bin/targets/
          retention-days: 7
      
      # 步骤 26: 上传配置文件
      - name: "26. 上传配置文件"
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: config-${{ github.event.inputs.device_name }}-${{ env.SELECTED_BRANCH }}-${{ github.event.inputs.config_mode }}
          path: ${{ github.workspace }}/firmware-config/config-backup/
          retention-days: 7
      
      # 步骤 27: 执行错误分析
      - name: "27. 执行错误分析"
        if: always()
        run: |
          echo "=== 步骤 27: 执行错误分析 ==="
          
          # 检查错误分析脚本是否存在
          if [ -f "firmware-config/scripts/error_analysis.sh" ]; then
            echo "✅ 找到错误分析脚本"
            chmod +x firmware-config/scripts/error_analysis.sh
            firmware-config/scripts/error_analysis.sh
          else
            echo "⚠️ 错误分析脚本不存在，创建简易分析..."
            
            echo "简易错误分析报告:" > /tmp/error-analysis.txt
            echo "========================" >> /tmp/error-analysis.txt
            
            if [ -f "${{ env.BUILD_DIR }}/build.log" ]; then
              echo "📊 编译日志分析:" >> /tmp/error-analysis.txt
              echo "文件大小: $(ls -lh ${{ env.BUILD_DIR }}/build.log | awk '{print $5}')" >> /tmp/error-analysis.txt
              
              ERROR_COUNT=$(grep -c -i "error\|failed" ${{ env.BUILD_DIR }}/build.log || echo "0")
              WARNING_COUNT=$(grep -c -i "warning" ${{ env.BUILD_DIR }}/build.log || echo "0")
              
              echo "错误数量: $ERROR_COUNT" >> /tmp/error-analysis.txt
              echo "警告数量: $WARNING_COUNT" >> /tmp/error-analysis.txt
              
              if [ $ERROR_COUNT -gt 0 ]; then
                echo "前10个错误:" >> /tmp/error-analysis.txt
                grep -i "error\|failed" ${{ env.BUILD_DIR }}/build.log | head -10 >> /tmp/error-analysis.txt
              fi
              
              cat /tmp/error-analysis.txt
            else
              echo "ℹ️ 未找到编译日志"
            fi
          fi
      
      # 步骤 28: 编译后空间检查
      - name: "28. 编译后空间检查"
        if: always()
        run: firmware-config/scripts/build_firmware_main.sh post_build_space_check
