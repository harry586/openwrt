# .github/workflows/firmware-build.yml
name: OpenWrt 智能固件构建工作流（完整功能版 - 官方SDK版）

on:
  workflow_dispatch:
    inputs:
      device_name:
        description: "📱 设备名称 (如: ac42u)"
        required: true
        default: "ac42u"
        type: string
      version_selection:
        description: "🔄 版本选择"
        required: true
        type: choice
        default: "21.02"
        options: ["23.05", "21.02"]
      config_mode:
        description: "⚙️ 配置模式选择 - 🟣 基础模式 - 最小化配置，用于测试编译。🟠 正常模式 - 完整功能配置：✅ TurboACC 网络加速 ✅ UPnP 自动端口转发 ✅ Samba 文件共享 ✅ 磁盘管理 ✅ KMS 激活服务 ✅ SmartDNS 智能DNS ✅ 家长控制 ✅ 微信推送 ✅ 流量控制 (SQM) ✅ FTP 服务器 ✅ ARP 绑定 ✅ CPU 限制 ✅ 硬盘休眠"
        required: true
        type: choice
        default: "normal"
        options: ["base", "normal"]
      extra_packages:
        description: "额外安装插件 - 格式：用分号;分隔。启用插件：+插件名。禁用插件：-插件名。"
        required: false
        type: string
        default: ""

env:
  BUILD_DIR: "/mnt/openwrt-build"
  REPO_ROOT: "${{ github.workspace }}"

jobs:
  build-firmware:
    runs-on: ubuntu-22.04
    
    steps:
      # 步骤 1: 使用Git Archive API下载源代码（增强命名版）
      - name: "1. 使用Git Archive API下载源代码（增强命名版）"
        run: |
          echo "=== 步骤 1: 使用Git Archive API下载源代码（增强命名版）==="
          echo "🔄 从GitHub API获取源代码压缩包..."
          
          # 获取时间戳
          TIMESTAMP=$(date +%Y%m%d_%H%M%S)
          echo "时间戳: $TIMESTAMP"
          
          # 清空工作区
          echo "清空工作区..."
          rm -rf ${{ github.workspace }}/*
          
          # 进入工作区目录
          cd ${{ github.workspace }}
          
          # 获取默认分支
          DEFAULT_BRANCH=$(curl -s -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" "https://api.github.com/repos/${{ github.repository }}" | jq -r '.default_branch')
          echo "默认分支: $DEFAULT_BRANCH"
          
          # 使用Git Archive API下载压缩包到工作区
          echo "下载源代码压缩包到工作区..."
          curl -s -L -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" "https://api.github.com/repos/${{ github.repository }}/tarball/$DEFAULT_BRANCH" -o source.tar.gz
          
          if [ ! -f "source.tar.gz" ]; then
            echo "❌ 下载源代码压缩包失败"
            exit 1
          fi
          
          echo "✅ 下载完成，文件大小: $(ls -lh source.tar.gz | awk '{print $5}')"
          
          # 解压压缩包到工作区
          echo "解压源代码到工作区..."
          tar -xzf source.tar.gz --strip-components=1
          
          # 删除压缩包
          rm -f source.tar.gz
          
          # 检查解压结果
          echo "检查源代码结构..."
          if [ -d "firmware-config" ]; then
            echo "✅ 源代码解压成功"
            echo "重要目录存在: firmware-config"
            
            # 创建必要的目录（如果不存在）
            echo "🔧 创建必要的目录结构..."
            mkdir -p firmware-config/config-backup
            mkdir -p firmware-config/custom-files
            
            # 创建带有日期时间的Artifacts目录
            echo "📦 建立带时间戳的Artifacts目录..."
            mkdir -p /tmp/build-artifacts/source-archive-$TIMESTAMP
            
            # 创建源代码压缩包（带名称和日期）
            echo "📦 创建源代码压缩包（带名称和日期）..."
            echo "源目录: ${{ github.workspace }}"
            echo "Artifacts目录: /tmp/build-artifacts/source-archive-$TIMESTAMP/"
            
            # 主压缩包（带日期时间）- 仅打包实际存在的目录
            echo "📦 打包主压缩包..."
            tar -czf /tmp/build-artifacts/source-archive-$TIMESTAMP/source-$TIMESTAMP.tar.gz \
              --exclude='.git' \
              -C ${{ github.workspace }} .
            
            # 次级压缩包（按类型分类）- 仅打包存在的目录
            echo "📦 打包脚本文件..."
            if [ -d "firmware-config/scripts" ]; then
              tar -czf /tmp/build-artifacts/source-archive-$TIMESTAMP/scripts-$TIMESTAMP.tar.gz \
                -C ${{ github.workspace }} firmware-config/scripts/
              echo "✅ 脚本文件打包完成"
            else
              echo "⚠️ firmware-config/scripts 目录不存在，创建空脚本包"
              tar -czf /tmp/build-artifacts/source-archive-$TIMESTAMP/scripts-$TIMESTAMP.tar.gz -T /dev/null
            fi
            
            echo "📦 打包配置文件..."
            # 检查哪些目录存在
            local tar_files=""
            if [ -d "firmware-config/custom-files" ]; then
              tar_files="firmware-config/custom-files/"
            fi
            if [ -d "firmware-config/config-backup" ]; then
              if [ -n "$tar_files" ]; then
                tar_files="$tar_files firmware-config/config-backup/"
              else
                tar_files="firmware-config/config-backup/"
              fi
            fi
            
            if [ -n "$tar_files" ]; then
              tar -czf /tmp/build-artifacts/source-archive-$TIMESTAMP/configs-$TIMESTAMP.tar.gz \
                -C ${{ github.workspace }} $tar_files
              echo "✅ 配置文件打包完成"
            else
              echo "⚠️ 配置目录为空，创建空配置文件包"
              tar -czf /tmp/build-artifacts/source-archive-$TIMESTAMP/configs-$TIMESTAMP.tar.gz -T /dev/null
            fi
            
            echo "✅ 带日期时间的源代码压缩包创建完成"
            
            # 显示详细信息
            echo "📋 Artifacts详细信息:"
            echo "主目录: /tmp/build-artifacts/source-archive-$TIMESTAMP/"
            echo "文件列表:"
            ls -lh /tmp/build-artifacts/source-archive-$TIMESTAMP/
            echo ""
            echo "每个文件详情:"
            for file in /tmp/build-artifacts/source-archive-$TIMESTAMP/*; do
                echo "  $(basename "$file") - $(ls -lh "$file" | awk '{print $5}')"
            done
            
            # 确保脚本文件有执行权限
            echo "🔧 设置脚本执行权限..."
            chmod +x firmware-config/scripts/*.sh 2>/dev/null || true
            
            if [ -f "firmware-config/scripts/build_firmware_main.sh" ]; then
              echo "✅ 主构建脚本权限设置完成"
            else
              echo "❌ 主构建脚本不存在"
              echo "列出firmware-config目录内容:"
              find firmware-config -type f 2>/dev/null
              exit 1
            fi
            
            # 显示工作区大小
            echo ""
            echo "📊 工作区大小:"
            echo "工作区总大小: $(du -sh ${{ github.workspace }} | cut -f1 || echo '未知')"
            
            echo "✅ 步骤1完成"
            
          else
            echo "❌ 源代码解压失败，重要目录缺失"
            echo "当前工作区内容:"
            ls -la
            exit 1
          fi
      
      # 步骤 2: 上传精简源代码Artifacts（带日期时间）
      - name: "2. 上传精简源代码Artifacts（带日期时间）"
        uses: actions/upload-artifact@v4
        with:
          name: filtered-source-${{ github.event.inputs.device_name }}-${{ github.run_id }}-${{ github.run_attempt }}
          path: /tmp/build-artifacts/source-archive-*/
          retention-days: 7
          if-no-files-found: error
      
      # 步骤 3: 修复权限问题
      - name: "3. 修复权限问题"
        run: |
          echo "=== 步骤 3: 修复权限问题 ==="
          echo "🔧 修复所有脚本的执行权限..."
          
          # 查找并修复所有脚本文件的权限
          find firmware-config -type f -name "*.sh" -exec chmod +x {} \;
          
          # 修复主脚本权限
          if [ -f "firmware-config/scripts/build_firmware_main.sh" ]; then
            chmod +x firmware-config/scripts/build_firmware_main.sh
            echo "✅ 修复主脚本权限: firmware-config/scripts/build_firmware_main.sh"
          else
            echo "❌ 主脚本不存在: firmware-config/scripts/build_firmware_main.sh"
            echo "列出firmware-config目录内容:"
            find firmware-config -type f 2>/dev/null || echo "firmware-config目录不存在"
            exit 1
          fi
          
          echo "✅ 权限修复完成"
      
      # 步骤 4: 初始空间检查
      - name: "4. 初始空间检查"
        run: |
          echo "=== 步骤 4: 初始磁盘空间检查 ==="
          df -h
          AVAILABLE_SPACE=$(df /mnt --output=avail | tail -1)
          AVAILABLE_GB=$((AVAILABLE_SPACE / 1024 / 1024))
          echo "/mnt 可用空间: ${AVAILABLE_GB}G"
          if [ $AVAILABLE_GB -lt 50 ]; then
            echo "❌ 错误: /mnt 空间不足50G"
            exit 1
          fi
          echo "✅ 初始空间检查通过"
      
      # 步骤 5: 设置编译环境和构建目录
      - name: "5. 设置编译环境和构建目录"
        run: |
          echo "=== 步骤 5: 设置编译环境和构建目录 ==="
          
          echo "🔄 步骤 5.1: 设置编译环境"
          chmod +x firmware-config/scripts/build_firmware_main.sh
          firmware-config/scripts/build_firmware_main.sh setup_environment
          
          echo "🔄 步骤 5.2: 创建构建目录"
          firmware-config/scripts/build_firmware_main.sh create_build_dir
          
          echo "🔄 步骤 5.3: 初始化构建环境"
          echo "设备: ${{ github.event.inputs.device_name }}"
          echo "版本: ${{ github.event.inputs.version_selection }}"
          echo "配置模式: ${{ github.event.inputs.config_mode }}"
          firmware-config/scripts/build_firmware_main.sh initialize_build_env "${{ github.event.inputs.device_name }}" "${{ github.event.inputs.version_selection }}" "${{ github.event.inputs.config_mode }}"
          
          echo "🔄 步骤 5.4: 初始化编译器环境（下载OpenWrt官方SDK）"
          firmware-config/scripts/build_firmware_main.sh initialize_compiler_env "${{ github.event.inputs.device_name }}"
          
          echo "✅ 编译环境和构建目录设置完成"
      
      # 步骤 6: 添加 TurboACC 支持
      - name: "6. 添加 TurboACC 支持"
        run: |
          echo "=== 步骤 6: 添加 TurboACC 支持 ==="
          firmware-config/scripts/build_firmware_main.sh add_turboacc_support
      
      # 步骤 7: 配置Feeds
      - name: "7. 配置Feeds"
        run: |
          echo "=== 步骤 7: 配置Feeds ==="
          firmware-config/scripts/build_firmware_main.sh configure_feeds
      
      # 步骤 8: 安装 TurboACC 包
      - name: "8. 安装 TurboACC 包"
        if: env.SELECTED_BRANCH == 'openwrt-23.05' && github.event.inputs.config_mode == 'normal'
        run: |
          echo "=== 步骤 8: 安装 TurboACC 包 ==="
          firmware-config/scripts/build_firmware_main.sh install_turboacc_packages
      
      # 步骤 9: 编译前空间检查
      - name: "9. 编译前空间检查"
        run: |
          echo "=== 步骤 9: 编译前空间检查 ==="
          firmware-config/scripts/build_firmware_main.sh pre_build_space_check
      
      # 步骤 10: 智能配置生成（USB完全修复加强版）
      - name: "10. 智能配置生成（USB完全修复加强版）"
        run: |
          echo "=== 步骤 10: 智能配置生成 ==="
          echo "🚨 USB 3.0加强：所有平台的关键USB驱动都已强制启用！"
          firmware-config/scripts/build_firmware_main.sh generate_config "${{ github.event.inputs.extra_packages }}"
      
      # 步骤 11: 验证USB配置
      - name: "11. 验证USB配置"
        run: |
          echo "=== 步骤 11: 验证USB配置 ==="
          firmware-config/scripts/build_firmware_main.sh verify_usb_config
      
      # 步骤 12: USB驱动完整性检查
      - name: "12. USB驱动完整性检查"
        run: |
          echo "=== 步骤 12: USB驱动完整性检查 ==="
          firmware-config/scripts/build_firmware_main.sh check_usb_drivers_integrity
      
      # 步骤 13: 应用配置并显示详情
      - name: "13. 应用配置并显示详情"
        run: |
          echo "=== 步骤 13: 应用配置并显示详情 ==="
          
          # 先保存原始配置文件
          if [ -f "${{ env.BUILD_DIR }}/.config" ]; then
            cp "${{ env.BUILD_DIR }}/.config" "${{ env.BUILD_DIR }}/.config.original"
            echo "✅ 已备份原始配置文件"
          fi
          
          # 应用配置
          firmware-config/scripts/build_firmware_main.sh apply_config
          
          echo "✅ 配置详情检查完成"
      
      # 步骤 14: 检查并备份配置文件
      - name: "14. 检查并备份配置文件"
        run: |
          echo "=== 步骤 14: 检查并备份配置文件 ==="
          
          # 检查配置文件
          if [ -f "${{ env.BUILD_DIR }}/.config" ]; then
            echo "✅ .config 文件存在"
            
            # 确保备份目录存在
            mkdir -p firmware-config/config-backup
            
            # 备份到仓库目录
            timestamp=$(date +%Y%m%d_%H%M%S)
            backup_file="firmware-config/config-backup/config_${{ github.event.inputs.device_name }}_${{ env.SELECTED_BRANCH }}_${{ github.event.inputs.config_mode }}_$timestamp.config"
            
            cp "${{ env.BUILD_DIR }}/.config" "$backup_file"
            echo "✅ 配置文件备份到仓库目录: $backup_file"
          else
            echo "❌ .config 文件不存在"
            exit 1
          fi
      
      # 步骤 15: 修复网络环境
      - name: "15. 修复网络环境"
        run: |
          echo "=== 步骤 15: 修复网络环境 ==="
          firmware-config/scripts/build_firmware_main.sh fix_network
      
      # 步骤 16: 下载依赖包（带重试）
      - name: "16. 下载依赖包（带重试）"
        run: |
          echo "=== 步骤 16: 下载依赖包（带重试）==="
          
          # 最多重试3次
          for i in {1..3}; do
            echo "🔁 下载依赖包 - 第 $i 次尝试"
            
            firmware-config/scripts/build_firmware_main.sh download_dependencies
            
            # 检查下载结果
            if [ $? -eq 0 ] && [ $(find ${{ env.BUILD_DIR }}/dl -type f 2>/dev/null | wc -l) -gt 100 ]; then
              echo "✅ 依赖包下载成功"
              break
            else
              echo "⚠️ 第 $i 次下载不完整，等待10秒后重试..."
              sleep 10
            fi
          done
          
          # 最终检查
          dl_count=$(find ${{ env.BUILD_DIR }}/dl -type f 2>/dev/null | wc -l)
          echo "📊 最终依赖包数量: $dl_count 个"
          
          if [ $dl_count -lt 50 ]; then
            echo "❌ 依赖包数量不足，编译可能失败"
            echo "💡 建议手动下载或检查网络连接"
          fi
      
      # 步骤 17: 集成自定义文件
      - name: "17. 集成自定义文件"
        run: |
          echo "=== 步骤 17: 集成自定义文件 ==="
          firmware-config/scripts/build_firmware_main.sh integrate_custom_files
      
      # 步骤 18: 前置错误检查
      - name: "18. 前置错误检查"
        run: |
          echo "=== 步骤 18: 前置错误检查 ==="
          firmware-config/scripts/build_firmware_main.sh pre_build_error_check
      
      # 步骤 19: 编译固件前的空间检查
      - name: "19. 编译固件前的空间检查"
        run: |
          echo "=== 步骤 19: 编译固件前的空间检查 ==="
          df -h
          AVAILABLE_SPACE=$(df /mnt --output=avail | tail -1)
          AVAILABLE_GB=$((AVAILABLE_SPACE / 1024 / 1024))
          echo "/mnt 可用空间: ${AVAILABLE_GB}G"
          
          if [ $AVAILABLE_GB -lt 10 ]; then
            echo "❌ 错误: 编译前空间不足 (需要至少10G，当前${AVAILABLE_GB}G)"
            exit 1
          elif [ $AVAILABLE_GB -lt 20 ]; then
            echo "⚠️ 警告: 编译前空间较低 (建议至少20G，当前${AVAILABLE_GB}G)"
          else
            echo "✅ 编译前空间充足"
          fi
      
      # 步骤 20: 编译固件前显示SDK信息
      - name: "20. 编译固件前显示SDK信息"
        run: |
          echo "=== 步骤 20: 编译固件前显示SDK信息 ==="
          echo "🔧 使用OpenWrt官方SDK工具链..."
          
          # 显示编译器信息
          echo "📌 编译器信息:"
          echo "  设备: ${{ github.event.inputs.device_name }}"
          echo "  版本: ${{ env.SELECTED_BRANCH }}"
          echo "  目标平台: ${{ env.TARGET }}/${{ env.SUBTARGET }}"
          echo "  编译器目录: ${{ env.COMPILER_DIR }}"
          
          if [ -n "${{ env.COMPILER_DIR }}" ] && [ -d "${{ env.COMPILER_DIR }}" ]; then
            echo "  ✅ SDK工具链已下载"
            echo "  编译器版本:"
            find "${{ env.COMPILER_DIR }}" -type f -executable -name "*gcc" ! -name "*gcc-ar" ! -name "*gcc-ranlib" ! -name "*gcc-nm" 2>/dev/null | head -1 | xargs --version 2>&1 | head -1 || echo "无法获取版本"
          else
            echo "  ℹ️ 未使用预下载SDK，将使用OpenWrt自动构建的编译器"
          fi
      
      # 步骤 21: 编译固件
      - name: "21. 编译固件"
        run: |
          echo "=== 步骤 21: 编译固件 ==="
          echo "🚀 开始编译..."
          firmware-config/scripts/build_firmware_main.sh build_firmware "true"
      
      # 步骤 22: 搜索并打包构建产物（增强版）
      - name: "22. 搜索并打包构建产物（增强版）"
        if: success()
        run: |
          echo "=== 步骤 22: 搜索并打包构建产物（增强版）==="
          cd ${{ env.BUILD_DIR }}
          
          # 获取时间戳
          TIMESTAMP=$(date +%Y%m%d_%H%M%S)
          
          # 创建构建产物目录
          mkdir -p /tmp/build-artifacts
          mkdir -p /tmp/build-artifacts/firmware-details
          
          # 生成固件详情报告
          echo "=== 固件详情报告 ===" > /tmp/build-artifacts/firmware-details/report.txt
          echo "生成时间: $(date)" >> /tmp/build-artifacts/firmware-details/report.txt
          echo "设备: ${{ github.event.inputs.device_name }}" >> /tmp/build-artifacts/firmware-details/report.txt
          echo "版本: ${{ env.SELECTED_BRANCH }}" >> /tmp/build-artifacts/firmware-details/report.txt
          echo "配置模式: ${{ github.event.inputs.config_mode }}" >> /tmp/build-artifacts/firmware-details/report.txt
          echo "" >> /tmp/build-artifacts/firmware-details/report.txt
          
          # 检查并显示固件文件详情
          if [ -d "bin/targets" ]; then
            echo "✅ 找到固件目录" >> /tmp/build-artifacts/firmware-details/report.txt
            
            # 统计固件文件
            local firmware_count=0
            local total_size=0
            
            echo "📊 固件文件详情:" >> /tmp/build-artifacts/firmware-details/report.txt
            echo "==========================================" >> /tmp/build-artifacts/firmware-details/report.txt
            
            find bin/targets -type f \( -name "*.bin" -o -name "*.img" -o -name "*.gz" \) 2>/dev/null | while read file; do
                firmware_count=$((firmware_count + 1))
                local size=$(ls -lh "$file" 2>/dev/null | awk '{print $5}' || echo "未知")
                local bytes=$(stat -c%s "$file" 2>/dev/null || echo "0")
                total_size=$((total_size + bytes))
                
                echo "📁 文件 #$firmware_count:" >> /tmp/build-artifacts/firmware-details/report.txt
                echo "  名称: $(basename "$file")" >> /tmp/build-artifacts/firmware-details/report.txt
                echo "  大小: $size ($bytes 字节)" >> /tmp/build-artifacts/firmware-details/report.txt
                echo "  路径: $(echo "$file" | sed "s|$BUILD_DIR/||")" >> /tmp/build-artifacts/firmware-details/report.txt
                echo "  类型: $(file -b "$file" 2>/dev/null | cut -c1-50 || echo "未知")" >> /tmp/build-artifacts/firmware-details/report.txt
                echo "" >> /tmp/build-artifacts/firmware-details/report.txt
            done
            
            echo "==========================================" >> /tmp/build-artifacts/firmware-details/report.txt
            echo "📈 统计信息:" >> /tmp/build-artifacts/firmware-details/report.txt
            echo "  固件总数: $firmware_count 个" >> /tmp/build-artifacts/firmware-details/report.txt
            echo "  总大小: $(numfmt --to=iec $total_size)" >> /tmp/build-artifacts/firmware-details/report.txt
            
            # 按类型分类
            echo "" >> /tmp/build-artifacts/firmware-details/report.txt
            echo "🔍 按类型分类:" >> /tmp/build-artifacts/firmware-details/report.txt
            
            local initramfs_count=$(find bin/targets -name '*initramfs*' -type f 2>/dev/null | wc -l)
            local squashfs_count=$(find bin/targets -name '*squashfs*' -type f 2>/dev/null | wc -l)
            local sysupgrade_count=$(find bin/targets -name '*sysupgrade*' -type f 2>/dev/null | wc -l)
            
            echo "  initramfs: $initramfs_count 个" >> /tmp/build-artifacts/firmware-details/report.txt
            echo "  squashfs: $squashfs_count 个" >> /tmp/build-artifacts/firmware-details/report.txt
            echo "  sysupgrade: $sysupgrade_count 个" >> /tmp/build-artifacts/firmware-details/report.txt
            
            # 打包固件文件（带时间戳）
            echo ""
            echo "📦 打包固件文件（带时间戳）..."
            tar -czf /tmp/build-artifacts/firmware-${{ github.event.inputs.device_name }}-${{ env.SELECTED_BRANCH }}-${{ github.event.inputs.config_mode }}-$TIMESTAMP.tar.gz -C bin targets/
            echo "✅ 固件打包完成: firmware-${{ github.event.inputs.device_name }}-${{ env.SELECTED_BRANCH }}-${{ github.event.inputs.config_mode }}-$TIMESTAMP.tar.gz"
            
          else
            echo "❌ 未找到固件目录" >> /tmp/build-artifacts/firmware-details/report.txt
            echo "⚠️ bin/targets目录不存在"
          fi
          
          # 打包编译日志
          if [ -f "build.log" ]; then
            echo "📄 打包编译日志..."
            cp build.log /tmp/build-artifacts/build.log
            echo "✅ 日志打包完成"
            
            # 生成编译摘要
            echo "=== 编译摘要 ===" > /tmp/build-artifacts/compile-summary.txt
            echo "编译时间: $(date)" >> /tmp/build-artifacts/compile-summary.txt
            echo "设备: ${{ github.event.inputs.device_name }}" >> /tmp/build-artifacts/compile-summary.txt
            echo "版本: ${{ env.SELECTED_BRANCH }}" >> /tmp/build-artifacts/compile-summary.txt
            echo "配置模式: ${{ github.event.inputs.config_mode }}" >> /tmp/build-artifacts/compile-summary.txt
            echo "错误数: $(grep -c -i "error" build.log 2>/dev/null || echo "0")" >> /tmp/build-artifacts/compile-summary.txt
            echo "警告数: $(grep -c -i "warning" build.log 2>/dev/null || echo "0")" >> /tmp/build-artifacts/compile-summary.txt
            echo "编译结果: $([ -d "bin/targets" ] && echo "成功" || echo "失败")" >> /tmp/build-artifacts/compile-summary.txt
            echo "生成时间: $TIMESTAMP" >> /tmp/build-artifacts/compile-summary.txt
          fi
          
          echo "✅ 构建产物准备完成"
      
      # 步骤 23: 上传固件原始目录
      - name: "23. 上传固件原始目录"
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: firmware-${{ github.event.inputs.device_name }}-${{ env.SELECTED_BRANCH }}-${{ github.event.inputs.config_mode }}
          path: ${{ env.BUILD_DIR }}/bin/targets/
          retention-days: 7
          if-no-files-found: error
      
      # 步骤 24: 上传配置文件
      - name: "24. 上传配置文件"
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: config-${{ github.event.inputs.device_name }}-${{ env.SELECTED_BRANCH }}-${{ github.event.inputs.config_mode }}
          path: ${{ github.workspace }}/firmware-config/config-backup/
          retention-days: 7
          if-no-files-found: error
      
      # 步骤 25: 上传固件详情报告
      - name: "25. 上传固件详情报告"
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: firmware-details-${{ github.event.inputs.device_name }}-${{ env.SELECTED_BRANCH }}
          path: /tmp/build-artifacts/firmware-details/
          retention-days: 7
          if-no-files-found: warn
      
      # 步骤 26: 上传编译摘要
      - name: "26. 上传编译摘要"
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: compile-summary-${{ github.event.inputs.device_name }}-${{ env.SELECTED_BRANCH }}
          path: /tmp/build-artifacts/compile-summary.txt
          retention-days: 7
          if-no-files-found: warn
      
      # 步骤 27: 执行错误分析
      - name: "27. 执行错误分析"
        if: always()
        run: |
          echo "=== 步骤 27: 执行错误分析 ==="
          
          # 检查错误分析脚本是否存在
          if [ -f "firmware-config/scripts/error_analysis.sh" ]; then
            echo "✅ 找到错误分析脚本"
            chmod +x firmware-config/scripts/error_analysis.sh
            firmware-config/scripts/error_analysis.sh
          else
            echo "⚠️ 错误分析脚本不存在"
          fi
      
      # 步骤 28: 编译后空间检查
      - name: "28. 编译后空间检查"
        if: always()
        run: firmware-config/scripts/build_firmware_main.sh post_build_space_check
