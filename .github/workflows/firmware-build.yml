# .github/workflows/firmware-build.yml
name: OpenWrt 智能固件构建工作流（完整功能版 - 官方SDK版）

on:
  workflow_dispatch:
    inputs:
      device_name:
        description: "📱 设备名称 (如: ac42u)"
        required: true
        default: "ac42u"
        type: string
      version_selection:
        description: "🔄 版本选择"
        required: true
        type: choice
        default: "21.02"
        options: ["23.05", "21.02"]
      config_mode:
        description: "⚙️ 配置模式选择 - 🟣 基础模式 - 最小化配置，用于测试编译。🟠 正常模式 - 完整功能配置：✅ TurboACC 网络加速 ✅ UPnP 自动端口转发 ✅ Samba 文件共享 ✅ 磁盘管理 ✅ KMS 激活服务 ✅ SmartDNS 智能DNS ✅ 家长控制 ✅ 微信推送 ✅ 流量控制 (SQM) ✅ FTP 服务器 ✅ ARP 绑定 ✅ CPU 限制 ✅ 硬盘休眠"
        required: true
        type: choice
        default: "normal"
        options: ["base", "normal"]
      extra_packages:
        description: "额外安装插件 - 格式：用分号;分隔。启用插件：+插件名。禁用插件：-插件名。"
        required: false
        type: string
        default: ""

env:
  BUILD_DIR: "/mnt/openwrt-build"
  REPO_ROOT: "${{ github.workspace }}"
  ENABLE_STEP_LOGS: "true"  # 启用步骤日志记录

jobs:
  build-firmware:
    runs-on: ubuntu-22.04
    
    steps:
      # 步骤 1: 完整检出代码（修复版）
      - name: "1. 完整检出代码（修复版）"
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive
          token: ${{ secrets.GITHUB_TOKEN }}
      
      # 步骤 2: 验证源代码完整性
      - name: "2. 验证源代码完整性"
        run: |
          echo "=== 步骤 2: 验证源代码完整性 ==="
          echo "🕐 开始时间: $(date '+%Y-%m-%d %H:%M:%S')"
          echo "工作流运行ID: ${{ github.run_id }}"
          echo "工作流运行号: ${{ github.run_number }}"
          echo "触发事件: ${{ github.event_name }}"
          echo "构建时间戳: ${{ github.run_id }}"
          echo ""
          
          # 显示工作区信息
          echo "📁 工作区目录: ${{ github.workspace }}"
          echo "📊 工作区大小: $(du -sh ${{ github.workspace }} | cut -f1)"
          echo ""
          
          # 检查firmware-config目录
          echo "🔍 检查 firmware-config 目录..."
          if [ -d "firmware-config" ]; then
            echo "✅ firmware-config 目录存在"
            echo "📊 目录大小: $(du -sh firmware-config | cut -f1)"
            
            # 检查关键脚本文件
            echo ""
            echo "🔍 检查关键脚本文件..."
            local key_scripts=(
              "firmware-config/scripts/build_firmware_main.sh"
              "firmware-config/scripts/error_analysis.sh"
            )
            
            for script in "${key_scripts[@]}"; do
              if [ -f "$script" ]; then
                echo "✅ 脚本文件存在: $script ($(ls -lh "$script" | awk '{print $5}'))"
                # 设置执行权限
                chmod +x "$script"
              else
                echo "❌ 脚本文件缺失: $script"
                exit 1
              fi
            done
            
            # 显示scripts目录内容
            echo ""
            echo "📁 scripts目录内容:"
            find firmware-config/scripts -type f -name "*.sh" | sort
            
          else
            echo "❌ firmware-config 目录不存在！"
            echo "当前工作区内容:"
            ls -la
            exit 1
          fi
          
          # 检查工作流文件
          echo ""
          echo "🔍 检查工作流文件..."
          if [ -d ".github/workflows" ]; then
            echo "✅ 工作流目录存在"
            echo "📄 工作流文件:"
            ls -la .github/workflows/
          else
            echo "⚠️ 工作流目录不存在，但可能不需要"
          fi
          
          # 设置所有脚本的执行权限
          echo ""
          echo "🔧 设置所有脚本执行权限..."
          find . -name "*.sh" -exec chmod +x {} \;
          
          echo "✅ 步骤2完成，时间: $(date '+%Y-%m-%d %H:%M:%S')"
      
      # 步骤 3: 上传源代码Artifacts
      - name: "3. 上传源代码Artifacts"
        uses: actions/upload-artifact@v4
        with:
          name: source-${{ github.event.inputs.device_name }}-${{ github.run_id }}
          path: ${{ github.workspace }}
          retention-days: 7
          if-no-files-found: error
      
      # 步骤 4: 修复权限问题
      - name: "4. 修复权限问题"
        run: |
          echo "=== 步骤 4: 修复权限问题 ==="
          echo "🔧 修复所有脚本的执行权限..."
          
          # 查找并修复所有脚本文件的权限
          find firmware-config -type f -name "*.sh" -exec chmod +x {} \;
          
          # 修复主脚本权限
          if [ -f "firmware-config/scripts/build_firmware_main.sh" ]; then
            chmod +x firmware-config/scripts/build_firmware_main.sh
            echo "✅ 修复主脚本权限: firmware-config/scripts/build_firmware_main.sh"
          else
            echo "❌ 主脚本不存在: firmware-config/scripts/build_firmware_main.sh"
            echo "列出firmware-config目录内容:"
            find firmware-config -type f 2>/dev/null || echo "firmware-config目录不存在"
            exit 1
          fi
          
          echo "✅ 权限修复完成"
      
      # 步骤 5: 初始空间检查
      - name: "5. 初始空间检查"
        run: |
          echo "=== 步骤 5: 初始磁盘空间检查 ==="
          echo "检查时间: $(date '+%Y-%m-%d %H:%M:%S')"
          
          echo "📊 磁盘使用情况:"
          df -h
          
          echo ""
          echo "📊 /mnt 分区详细信息:"
          df -h /mnt
          
          AVAILABLE_SPACE=$(df /mnt --output=avail | tail -1)
          AVAILABLE_GB=$((AVAILABLE_SPACE / 1024 / 1024))
          echo "/mnt 可用空间: ${AVAILABLE_GB}G"
          
          if [ $AVAILABLE_GB -lt 50 ]; then
            echo "❌ 错误: /mnt 空间不足50G，当前只有${AVAILABLE_GB}G"
            exit 1
          fi
          echo "✅ 初始空间检查通过"
      
      # 步骤 6: 设置编译环境和构建目录
      - name: "6. 设置编译环境和构建目录"
        run: |
          echo "=== 步骤 6: 设置编译环境和构建目录 ==="
          echo "开始时间: $(date '+%Y-%m-%d %H:%M:%S')"
          
          echo "🔄 步骤 6.1: 设置编译环境"
          chmod +x firmware-config/scripts/build_firmware_main.sh
          firmware-config/scripts/build_firmware_main.sh setup_environment
          
          echo "🔄 步骤 6.2: 创建构建目录"
          firmware-config/scripts/build_firmware_main.sh create_build_dir
          
          echo "🔄 步骤 6.3: 初始化构建环境"
          echo "设备: ${{ github.event.inputs.device_name }}"
          echo "版本: ${{ github.event.inputs.version_selection }}"
          echo "配置模式: ${{ github.event.inputs.config_mode }}"
          firmware-config/scripts/build_firmware_main.sh initialize_build_env "${{ github.event.inputs.device_name }}" "${{ github.event.inputs.version_selection }}" "${{ github.event.inputs.config_mode }}"
          
          echo "✅ 编译环境和构建目录设置完成，时间: $(date '+%Y-%m-%d %H:%M:%S')"
      
      # 步骤 7: 下载OpenWrt官方SDK（独立步骤）
      - name: "7. 下载OpenWrt官方SDK（独立步骤）"
        run: |
          echo "=== 步骤 7: 下载OpenWrt官方SDK（独立步骤）==="
          echo "开始时间: $(date '+%Y-%m-%d %H:%M:%S')"
          echo "🚀 开始下载OpenWrt官方SDK工具链..."
          
          # 确保脚本有执行权限
          chmod +x firmware-config/scripts/build_firmware_main.sh
          
          # 执行SDK下载
          firmware-config/scripts/build_firmware_main.sh initialize_compiler_env "${{ github.event.inputs.device_name }}"
          
          echo "✅ SDK下载步骤完成，时间: $(date '+%Y-%m-%d %H:%M:%S')"
      
      # 步骤 8: 验证SDK下载结果（修复版）
      - name: "8. 验证SDK下载结果（修复版）"
        run: |
          echo "=== 步骤 8: 验证SDK下载结果（修复版）==="
          echo "验证时间: $(date '+%Y-%m-%d %H:%M:%S')"
          
          # 首先加载环境变量
          if [ -f "/mnt/openwrt-build/build_env.sh" ]; then
            source /mnt/openwrt-build/build_env.sh
            echo "✅ 从环境文件加载变量: COMPILER_DIR=$COMPILER_DIR"
          else
            echo "⚠️ 环境文件不存在: /mnt/openwrt-build/build_env.sh"
          fi
          
          echo "🔍 检查SDK下载结果..."
          
          # 检查SDK目录
          if [ -d "/mnt/openwrt-build/sdk" ]; then
            echo "✅ SDK目录存在: /mnt/openwrt-build/sdk"
            echo "📊 SDK目录大小: $(du -sh /mnt/openwrt-build/sdk 2>/dev/null | cut -f1 || echo '未知')"
            echo "📁 SDK目录内容:"
            ls -la /mnt/openwrt-build/sdk/ 2>/dev/null || echo "无法列出SDK目录内容"
          else
            echo "❌ SDK目录不存在"
          fi
          
          # 检查编译器目录
          if [ -n "$COMPILER_DIR" ] && [ -d "$COMPILER_DIR" ]; then
            echo "✅ 编译器目录存在: $COMPILER_DIR"
            echo "📊 编译器目录大小: $(du -sh "$COMPILER_DIR" 2>/dev/null | cut -f1 || echo '未知')"
            
            # 查找GCC编译器（不使用local关键字）
            echo "🔍 查找GCC编译器..."
            GCC_FILE=$(find "$COMPILER_DIR" -type f -executable \
              -name "*gcc" \
              ! -name "*gcc-ar" \
              ! -name "*gcc-ranlib" \
              ! -name "*gcc-nm" \
              2>/dev/null | head -1)
            
            if [ -n "$GCC_FILE" ]; then
              echo "✅ 找到GCC编译器: $(basename "$GCC_FILE")"
              echo "🔧 GCC版本: $("$GCC_FILE" --version 2>&1 | head -1)"
            else
              echo "⚠️ 未找到真正的GCC编译器"
              echo "🔍 查找所有gcc相关文件:"
              find "$COMPILER_DIR" -type f -name "*gcc*" 2>/dev/null | head -10
            fi
          else
            echo "❌ 编译器目录不存在或无效"
            echo "COMPILER_DIR: $COMPILER_DIR"
          fi
          
          # 检查是否在PATH中
          echo "🔍 检查编译器PATH:"
          echo "$PATH" | tr ':' '\n' | grep -i "gcc\|compiler\|sdk" || echo "未找到编译器路径"
          
          echo "✅ SDK验证完成，时间: $(date '+%Y-%m-%d %H:%M:%S')"
      
      # 步骤 9: 添加 TurboACC 支持
      - name: "9. 添加 TurboACC 支持"
        run: |
          echo "=== 步骤 9: 添加 TurboACC 支持 ==="
          echo "开始时间: $(date '+%Y-%m-%d %H:%M:%S')"
          firmware-config/scripts/build_firmware_main.sh add_turboacc_support
          echo "✅ 完成时间: $(date '+%Y-%m-%d %H:%M:%S')"
      
      # 步骤 10: 配置Feeds
      - name: "10. 配置Feeds"
        run: |
          echo "=== 步骤 10: 配置Feeds ==="
          echo "开始时间: $(date '+%Y-%m-%d %H:%M:%S')"
          firmware-config/scripts/build_firmware_main.sh configure_feeds
          echo "✅ 完成时间: $(date '+%Y-%m-%d %H:%M:%S')"
      
      # 步骤 11: 安装 TurboACC 包
      - name: "11. 安装 TurboACC 包"
        if: env.SELECTED_BRANCH == 'openwrt-23.05' && github.event.inputs.config_mode == 'normal'
        run: |
          echo "=== 步骤 11: 安装 TurboACC 包 ==="
          echo "开始时间: $(date '+%Y-%m-%d %H:%M:%S')"
          firmware-config/scripts/build_firmware_main.sh install_turboacc_packages
          echo "✅ 完成时间: $(date '+%Y-%m-%d %H:%M:%S')"
      
      # 步骤 12: 编译前空间检查
      - name: "12. 编译前空间检查"
        run: |
          echo "=== 步骤 12: 编译前空间检查 ==="
          echo "检查时间: $(date '+%Y-%m-%d %H:%M:%S')"
          firmware-config/scripts/build_firmware_main.sh pre_build_space_check
          echo "✅ 完成时间: $(date '+%Y-%m-%d %H:%M:%S')"
      
      # 步骤 13: 智能配置生成（USB完全修复加强版）
      - name: "13. 智能配置生成（USB完全修复加强版）"
        run: |
          echo "=== 步骤 13: 智能配置生成 ==="
          echo "开始时间: $(date '+%Y-%m-%d %H:%M:%S')"
          echo "🚨 USB 3.0加强：所有平台的关键USB驱动都已强制启用！"
          firmware-config/scripts/build_firmware_main.sh generate_config "${{ github.event.inputs.extra_packages }}"
          echo "✅ 完成时间: $(date '+%Y-%m-%d %H:%M:%S')"
      
      # 步骤 14: 验证USB配置
      - name: "14. 验证USB配置"
        run: |
          echo "=== 步骤 14: 验证USB配置 ==="
          echo "开始时间: $(date '+%Y-%m-%d %H:%M:%S')"
          firmware-config/scripts/build_firmware_main.sh verify_usb_config
          echo "✅ 完成时间: $(date '+%Y-%m-%d %H:%M:%S')"
      
      # 步骤 15: USB驱动完整性检查
      - name: "15. USB驱动完整性检查"
        run: |
          echo "=== 步骤 15: USB驱动完整性检查 ==="
          echo "开始时间: $(date '+%Y-%m-%d %H:%M:%S')"
          firmware-config/scripts/build_firmware_main.sh check_usb_drivers_integrity
          echo "✅ 完成时间: $(date '+%Y-%m-d %H:%M:%S')"
      
      # 步骤 16: 应用配置并显示详情
      - name: "16. 应用配置并显示详情"
        run: |
          echo "=== 步骤 16: 应用配置并显示详情 ==="
          echo "开始时间: $(date '+%Y-%m-%d %H:%M:%S')"
          
          # 先保存原始配置文件
          if [ -f "${{ env.BUILD_DIR }}/.config" ]; then
            cp "${{ env.BUILD_DIR }}/.config" "${{ env.BUILD_DIR }}/.config.original"
            echo "✅ 已备份原始配置文件"
          fi
          
          # 应用配置
          firmware-config/scripts/build_firmware_main.sh apply_config
          
          echo "✅ 配置详情检查完成，时间: $(date '+%Y-%m-%d %H:%M:%S')"
      
      # 步骤 17: 检查并备份配置文件
      - name: "17. 检查并备份配置文件"
        run: |
          echo "=== 步骤 17: 检查并备份配置文件 ==="
          echo "开始时间: $(date '+%Y-%m-%d %H:%M:%S')"
          
          # 检查配置文件
          if [ -f "${{ env.BUILD_DIR }}/.config" ]; then
            echo "✅ .config 文件存在"
            echo "📊 文件大小: $(ls -lh "${{ env.BUILD_DIR }}/.config" | awk '{print $5}')"
            echo "📝 文件行数: $(wc -l < "${{ env.BUILD_DIR }}/.config")"
            
            # 确保备份目录存在
            mkdir -p firmware-config/config-backup
            
            # 备份到仓库目录
            TIMESTAMP=$(date +%Y%m%d_%H%M%S)
            BACKUP_FILE="firmware-config/config-backup/config_${{ github.event.inputs.device_name }}_${TIMESTAMP}.config"
            
            cp "${{ env.BUILD_DIR }}/.config" "$BACKUP_FILE"
            echo "✅ 配置文件备份到仓库目录: $BACKUP_FILE"
            echo "📊 备份文件大小: $(ls -lh "$BACKUP_FILE" | awk '{print $5}')"
          else
            echo "❌ .config 文件不存在"
            exit 1
          fi
      
      # 步骤 18: 修复网络环境
      - name: "18. 修复网络环境"
        run: |
          echo "=== 步骤 18: 修复网络环境 ==="
          echo "开始时间: $(date '+%Y-%m-%d %H:%M:%S')"
          firmware-config/scripts/build_firmware_main.sh fix_network
          echo "✅ 完成时间: $(date '+%Y-%m-%d %H:%M:%S')"
      
      # 步骤 19: 下载依赖包（带重试）
      - name: "19. 下载依赖包（带重试）"
        run: |
          echo "=== 步骤 19: 下载依赖包（带重试）==="
          echo "开始时间: $(date '+%Y-%m-%d %H:%M:%S')"
          
          # 最多重试3次
          for i in {1..3}; do
            echo "🔁 下载依赖包 - 第 $i 次尝试"
            
            firmware-config/scripts/build_firmware_main.sh download_dependencies
            
            # 检查下载结果
            if [ $? -eq 0 ] && [ $(find ${{ env.BUILD_DIR }}/dl -type f 2>/dev/null | wc -l) -gt 100 ]; then
              echo "✅ 依赖包下载成功"
              break
            else
              echo "⚠️ 第 $i 次下载不完整，等待10秒后重试..."
              sleep 10
            fi
          done
          
          # 最终检查
          dl_count=$(find ${{ env.BUILD_DIR }}/dl -type f 2>/dev/null | wc -l)
          echo "📊 最终依赖包数量: $dl_count 个"
          
          if [ $dl_count -lt 50 ]; then
            echo "❌ 依赖包数量不足，编译可能失败"
            echo "💡 建议手动下载或检查网络连接"
          fi
          
          echo "✅ 完成时间: $(date '+%Y-%m-%d %H:%M:%S')"
      
      # 步骤 20: 集成自定义文件
      - name: "20. 集成自定义文件"
        run: |
          echo "=== 步骤 20: 集成自定义文件 ==="
          echo "开始时间: $(date '+%Y-%m-%d %H:%M:%S')"
          firmware-config/scripts/build_firmware_main.sh integrate_custom_files
          echo "✅ 完成时间: $(date '+%Y-%m-%d %H:%M:%S')"
      
      # 步骤 21: 前置错误检查
      - name: "21. 前置错误检查"
        run: |
          echo "=== 步骤 21: 前置错误检查 ==="
          echo "开始时间: $(date '+%Y-%m-%d %H:%M:%S')"
          firmware-config/scripts/build_firmware_main.sh pre_build_error_check
          echo "✅ 完成时间: $(date '+%Y-%m-%d %H:%M:%S')"
      
      # 步骤 22: 编译固件前的空间检查
      - name: "22. 编译固件前的空间检查"
        run: |
          echo "=== 步骤 22: 编译固件前的空间检查 ==="
          echo "检查时间: $(date '+%Y-%m-%d %H:%M:%S')"
          df -h
          AVAILABLE_SPACE=$(df /mnt --output=avail | tail -1)
          AVAILABLE_GB=$((AVAILABLE_SPACE / 1024 / 1024))
          echo "/mnt 可用空间: ${AVAILABLE_GB}G"
          
          if [ $AVAILABLE_GB -lt 10 ]; then
            echo "❌ 错误: 编译前空间不足 (需要至少10G，当前${AVAILABLE_GB}G)"
            exit 1
          elif [ $AVAILABLE_GB -lt 20 ]; then
            echo "⚠️ 警告: 编译前空间较低 (建议至少20G，当前${AVAILABLE_GB}G)"
          else
            echo "✅ 编译前空间充足"
          fi
      
      # 步骤 23: 编译固件前显示SDK信息
      - name: "23. 编译固件前显示SDK信息"
        run: |
          echo "=== 步骤 23: 编译固件前显示SDK信息 ==="
          echo "开始时间: $(date '+%Y-%m-%d %H:%M:%S')"
          echo "🔧 使用OpenWrt官方SDK工具链..."
          
          # 显示编译器信息
          echo "📌 编译器信息:"
          echo "  设备: ${{ github.event.inputs.device_name }}"
          echo "  版本: ${{ env.SELECTED_BRANCH }}"
          echo "  目标平台: ${{ env.TARGET }}/${{ env.SUBTARGET }}"
          echo "  编译器目录: ${{ env.COMPILER_DIR }}"
          
          if [ -n "${{ env.COMPILER_DIR }}" ] && [ -d "${{ env.COMPILER_DIR }}" ]; then
            echo "  ✅ SDK工具链已下载"
            echo "  编译器版本:"
            find "${{ env.COMPILER_DIR }}" -type f -executable -name "*gcc" ! -name "*gcc-ar" ! -name "*gcc-ranlib" ! -name "*gcc-nm" 2>/dev/null | head -1 | xargs --version 2>&1 | head -1 || echo "无法获取版本"
          else
            echo "  ℹ️ 未使用预下载SDK，将使用OpenWrt自动构建的编译器"
          fi
          
          echo "✅ 完成时间: $(date '+%Y-%m-%d %H:%M:%S')"
      
      # 步骤 24: 编译固件（启用详细日志输出）
      - name: "24. 编译固件（启用详细日志输出）"
        run: |
          echo "=== 步骤 24: 编译固件（启用详细日志输出）==="
          echo "开始时间: $(date '+%Y-%m-%d %H:%M:%S')"
          echo "🚀 开始编译..."
          
          # 创建日志目录
          mkdir -p /tmp/build-logs
          
          # 记录开始时间
          echo "编译开始时间: $(date '+%Y-%m-%d %H:%M:%S')" > /tmp/build-logs/compile_start.log
          echo "工作流运行ID: ${{ github.run_id }}" >> /tmp/build-logs/compile_start.log
          echo "设备: ${{ github.event.inputs.device_name }}" >> /tmp/build-logs/compile_start.log
          echo "版本: ${{ env.SELECTED_BRANCH }}" >> /tmp/build-logs/compile_start.log
          
          # 执行编译，同时输出到控制台和日志文件
          echo "执行编译命令..."
          firmware-config/scripts/build_firmware_main.sh build_firmware "true" 2>&1 | tee /tmp/build-logs/compile_full.log
          
          # 记录结束时间
          echo "编译结束时间: $(date '+%Y-%m-%d %H:%M:%S')" >> /tmp/build-logs/compile_start.log
          
          echo "✅ 编译完成，日志已保存，时间: $(date '+%Y-%m-%d %H:%M:%S')"
      
      # 步骤 25: 搜索并打包构建产物
      - name: "25. 搜索并打包构建产物"
        if: success()
        run: |
          echo "=== 步骤 25: 搜索并打包构建产物 ==="
          echo "开始时间: $(date '+%Y-%m-%d %H:%M:%S')"
          cd ${{ env.BUILD_DIR }}
          
          # 检查并显示固件文件详情
          if [ -d "bin/targets" ]; then
            echo "✅ 找到固件目录"
            
            # 统计固件文件
            FIRMWARE_COUNT=0
            TOTAL_SIZE=0
            
            echo "📊 固件文件详情:"
            echo "=========================================="
            
            find bin/targets -type f \( -name "*.bin" -o -name "*.img" -o -name "*.gz" \) 2>/dev/null | while read file; do
                FIRMWARE_COUNT=$((FIRMWARE_COUNT + 1))
                SIZE=$(ls -lh "$file" 2>/dev/null | awk '{print $5}' || echo "未知")
                BYTES=$(stat -c%s "$file" 2>/dev/null || echo "0")
                TOTAL_SIZE=$((TOTAL_SIZE + BYTES))
                
                echo "📁 文件 #$FIRMWARE_COUNT:"
                echo "  名称: $(basename "$file")"
                echo "  大小: $SIZE ($BYTES 字节)"
                echo "  路径: $(echo "$file" | sed "s|${{ env.BUILD_DIR }}/||")"
                echo ""
            done
            
            echo "=========================================="
            echo "📈 统计信息:"
            echo "  固件总数: $FIRMWARE_COUNT 个"
            echo "  总大小: $(numfmt --to=iec $TOTAL_SIZE)"
            
          else
            echo "❌ 未找到固件目录"
            echo "⚠️ bin/targets目录不存在"
          fi
          
          echo "✅ 完成时间: $(date '+%Y-%m-%d %H:%M:%S')"
      
      # 步骤 26: 上传固件原始目录
      - name: "26. 上传固件原始目录"
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: firmware-${{ github.event.inputs.device_name }}-${{ env.SELECTED_BRANCH }}-${{ github.event.inputs.config_mode }}
          path: ${{ env.BUILD_DIR }}/bin/targets/
          retention-days: 7
          if-no-files-found: error
      
      # 步骤 27: 上传配置文件
      - name: "27. 上传配置文件"
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: config-${{ github.event.inputs.device_name }}-${{ env.SELECTED_BRANCH }}-${{ github.event.inputs.config_mode }}
          path: ${{ github.workspace }}/firmware-config/config-backup/
          retention-days: 7
          if-no-files-found: error
      
      # 步骤 28: 上传编译日志
      - name: "28. 上传编译日志"
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-logs-${{ github.event.inputs.device_name }}-${{ env.SELECTED_BRANCH }}
          path: |
            ${{ env.BUILD_DIR }}/build.log
            ${{ env.BUILD_DIR }}/download.log
            /tmp/build-logs/
          retention-days: 7
          if-no-files-found: warn
      
      # 步骤 29: 执行错误分析
      - name: "29. 执行错误分析"
        if: always()
        run: |
          echo "=== 步骤 29: 执行错误分析 ==="
          echo "开始时间: $(date '+%Y-%m-%d %H:%M:%S')"
          
          # 检查错误分析脚本是否存在
          if [ -f "firmware-config/scripts/error_analysis.sh" ]; then
            echo "✅ 找到错误分析脚本"
            chmod +x firmware-config/scripts/error_analysis.sh
            firmware-config/scripts/error_analysis.sh
          else
            echo "⚠️ 错误分析脚本不存在"
          fi
          
          echo "✅ 完成时间: $(date '+%Y-%m-%d %H:%M:%S')"
      
      # 步骤 30: 编译后空间检查
      - name: "30. 编译后空间检查"
        if: always()
        run: |
          echo "=== 步骤 30: 编译后空间检查 ==="
          echo "检查时间: $(date '+%Y-%m-%d %H:%M:%S')"
          firmware-config/scripts/build_firmware_main.sh post_build_space_check
          echo "✅ 完成时间: $(date '+%Y-%m-%d %H:%M:%S')"
