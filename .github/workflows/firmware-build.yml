name: OpenWrt 智能固件构建工作流

on:
  workflow_dispatch:
    inputs:
      device_name:
        description: '设备名称 (如: ac42u等)'
        required: true
        default: 'ac42u'
        type: string
      version_selection:
        description: '版本选择'
        required: true
        type: choice
        default: '21.02'
        options:
        - '23.05'
        - '21.02'
      config_mode:
        description: |
          🛠️ 配置模式选择:
          
          🔹 [基础模式] - 最小化配置，用于测试编译
          • 文件传输
          
          🔸 [正常模式] - 完整功能配置
          • TurboACC 网络加速
          • UPnP 自动端口转发  
          • Samba 文件共享
          • 磁盘管理
          • KMS 激活服务
          • SmartDNS 智能DNS
          • 家长控制
          • 微信推送
          • 流量控制 (SQM)
          • FTP 服务器
          • ARP 绑定
          • CPU 限制
          • 硬盘休眠
        required: true
        type: choice
        default: 'normal'
        options:
        - 'base'
        - 'normal'
      extra_packages:
        description: |
          📦 额外安装插件 (用分号;分隔)
          
          ✅ 启用插件：+插件名
          ❌ 禁用插件：-插件名
          
          📝 示例：
          +luci-app-wol;+luci-app-ddns;-luci-app-upnp
        required: false
        type: string
        default: ''
      enable_cache:
        description: '🚀 启用编译缓存'
        required: false
        default: true
        type: boolean

# ... 其余部分保持不变 ...
