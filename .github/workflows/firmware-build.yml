name: OpenWrt 智能固件构建工作流（USB完全修复通用版）

on:
  workflow_dispatch:
    inputs:
      device_name:
        description: '📱 设备名称 (如: ac42u)'
        required: true
        default: 'ac42u'
        type: string
      version_selection:
        description: '🔄 版本选择'
        required: true
        type: choice
        default: '21.02'
        options:
        - '23.05'
        - '21.02'
      config_mode:
        description: |
          ⚙️ 配置模式选择
          
          🟣 基础模式 - 最小化配置，用于测试编译：✅ 文件传输。
          
          🟠 正常模式 - 完整功能配置：✅ TurboACC 网络加速 ✅ UPnP 自动端口转发 ✅ Samba 文件共享 ✅ 磁盘管理 ✅ KMS 激活服务 ✅ SmartDNS 智能DNS ✅ 家长控制 ✅ 微信推送 ✅ 流量控制 (SQM) ✅ FTP 服务器 ✅ ARP 绑定 ✅ CPU 限制 ✅ 硬盘休眠。
        required: true
        type: choice
        default: 'normal'
        options:
        - 'base'
        - 'normal'
      extra_packages:
        description: |
          额外安装插件
          格式：🔴用顿号、分隔。🔴 启用插件：+插件名。🔴 禁用插件：-插件名。🔴 示例：+luci-app-wol、+luci-app-ddns
        required: false
        type: string
        default: ''
      enable_cache:
        description: '⚡ 启用编译缓存 (加速编译过程)'
        required: false
        default: true
        type: boolean

env:
  BUILD_DIR: /mnt/openwrt-build

jobs:
  build-firmware:
    runs-on: ubuntu-22.04
    
    steps:
    - name: 检出配置仓库
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: 设置脚本执行权限
      run: |
        echo "=== 设置脚本执行权限 ==="
        find . -name "*.sh" -exec chmod +x {} \; 2>/dev/null || true
        echo "✅ 脚本执行权限设置完成"

    - name: 初始空间检查
      run: |
        echo "=== 初始磁盘空间检查 ==="
        df -h
        AVAILABLE_SPACE=$(df /mnt --output=avail | tail -1)
        AVAILABLE_GB=$((AVAILABLE_SPACE / 1024 / 1024))
        echo "/mnt 可用空间: ${AVAILABLE_GB}G"
        if [ $AVAILABLE_GB -lt 50 ]; then
          echo "错误: /mnt 空间不足50G"
          exit 1
        fi

    - name: 设置编译环境
      run: |
        firmware-config/scripts/build_firmware_main.sh setup_environment

    - name: 创建构建目录
      run: |
        firmware-config/scripts/build_firmware_main.sh create_build_dir

    - name: 初始化构建环境
      run: |
        firmware-config/scripts/build_firmware_main.sh initialize_build_env \
          "${{ github.event.inputs.device_name }}" \
          "${{ github.event.inputs.version_selection }}" \
          "${{ github.event.inputs.config_mode }}"

    - name: 添加 TurboACC 支持
      run: |
        firmware-config/scripts/build_firmware_main.sh add_turboacc_support

    - name: 添加文件传输插件支持
      run: |
        firmware-config/scripts/build_firmware_main.sh add_filetransfer_support

    - name: 配置Feeds
      run: |
        firmware-config/scripts/build_firmware_main.sh configure_feeds

    - name: 安装 TurboACC 包
      if: env.SELECTED_BRANCH == 'openwrt-23.05' && github.event.inputs.config_mode == 'normal'
      run: |
        firmware-config/scripts/build_firmware_main.sh install_turboacc_packages

    - name: 安装文件传输插件包
      run: |
        firmware-config/scripts/build_firmware_main.sh install_filetransfer_packages

    - name: 编译前空间检查
      run: |
        firmware-config/scripts/build_firmware_main.sh pre_build_space_check

    - name: 智能配置生成（USB完全修复通用版）
      run: |
        firmware-config/scripts/build_firmware_main.sh generate_config \
          "${{ github.event.inputs.extra_packages }}"

    - name: 验证USB配置
      run: |
        firmware-config/scripts/build_firmware_main.sh verify_usb_config

    - name: 应用配置
      run: |
        firmware-config/scripts/build_firmware_main.sh apply_config

    - name: 修复网络环境
      run: |
        firmware-config/scripts/build_firmware_main.sh fix_network

    - name: 下载依赖包
      run: |
        firmware-config/scripts/build_firmware_main.sh download_dependencies

    - name: 处理自定义文件
      run: |
        firmware-config/scripts/build_firmware_main.sh process_custom_files

    - name: 编译固件
      run: |
        firmware-config/scripts/build_firmware_main.sh build_firmware "${{ github.event.inputs.enable_cache }}"

    - name: 错误分析
      if: always()
      run: |
        firmware-config/scripts/error_analysis.sh

    - name: 编译后空间检查
      run: |
        firmware-config/scripts/build_firmware_main.sh post_build_space_check

    - name: 固件文件检查
      if: success()
      run: |
        firmware-config/scripts/build_firmware_main.sh check_firmware_files

    - name: 备份配置文件
      if: always()
      run: |
        firmware-config/scripts/build_firmware_main.sh backup_config

    - name: 上传固件
      if: success()
      uses: actions/upload-artifact@v4
      with:
        name: firmware-${{ github.event.inputs.device_name }}-${{ env.SELECTED_BRANCH }}-${{ github.event.inputs.config_mode }}
        path: ${{ env.BUILD_DIR }}/bin/targets/
        retention-days: 7

    - name: 上传编译日志
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: build-log-${{ github.event.inputs.device_name }}-${{ env.SELECTED_BRANCH }}-${{ github.event.inputs.config_mode }}
        path: |
          ${{ env.BUILD_DIR }}/build.log
          ${{ env.BUILD_DIR }}/error_analysis.log
        retention-days: 7

    - name: 上传配置文件
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: config-${{ github.event.inputs.device_name }}-${{ env.SELECTED_BRANCH }}-${{ github.event.inputs.config_mode }}
        path: ${{ env.BUILD_DIR }}/config_backup/
        retention-days: 7

    - name: 上传自定义文件
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: custom-files-${{ github.event.inputs.device_name }}-${{ env.SELECTED_BRANCH }}-${{ github.event.inputs.config_mode }}
        path: ${{ env.BUILD_DIR }}/custom_files_log/
        retention-days: 7

    - name: 清理目录
      if: always()
      run: |
        firmware-config/scripts/build_firmware_main.sh cleanup
