name: OpenWrt 智能固件构建工作流（完整修复版）

on:
  workflow_dispatch:
    inputs:
      device_name:
        description: '设备名称 (如: ac42u, ac58u, mi4a 等)'
        required: true
        default: 'ac42u'
        type: string
      old_device:
        description: '是否为老旧设备 (性能较差的设备建议选择true)'
        required: true
        default: false
        type: boolean
      version_spec:
        description: '版本规格 (可选，如: openwrt-23.05 或 immortalwrt:master)'
        required: false
        type: string
        default: ''
      config_type:
        description: '配置类型 (minimal:基础功能, normal:完整功能, custom:自定义)'
        required: true
        default: 'normal'
        type: choice
        options:
          - minimal
          - normal
          - custom
      extra_packages:
        description: '额外安装插件 (空格分隔的包名)'
        required: false
        type: string
      disabled_plugins:
        description: '禁用插件 (空格分隔的包名，如: luci-app-accesscontrol luci-app-ddns)'
        required: false
        type: string
        default: ''
      save_config:
        description: '保存配置文件到仓库'
        required: false
        default: true
        type: boolean
      custom_install:
        description: '启用自定义安装 (IPK和脚本)'
        required: false
        default: true
        type: boolean
      enable_cache:
        description: '启用编译缓存 (显著加速后续编译)'
        required: false
        default: true
        type: boolean

env:
  BUILD_DIR: /mnt/openwrt-build
  MIRROR_SOURCE: "tuna"
  CACHE_ENABLED: true

jobs:
  build-firmware:
    runs-on: ubuntu-latest
    
    steps:
    - name: 初始空间检查
      run: |
        echo "=== 初始磁盘空间检查 ==="
        df -h
        AVAILABLE_SPACE=$(df /mnt --output=avail | tail -1)
        AVAILABLE_GB=$((AVAILABLE_SPACE / 1024 / 1024))
        echo "/mnt 可用空间: ${AVAILABLE_GB}G"
        if [ $AVAILABLE_GB -lt 50 ]; then
          echo "错误: /mnt 空间不足50G"
          exit 1
        fi

    - name: 检出配置仓库
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: 设置编译环境
      run: |
        echo "=== 安装编译依赖包 ==="
        sudo apt-get update
        sudo DEBIAN_FRONTEND=noninteractive apt-get install -y build-essential libncurses5-dev libssl-dev python3 unzip zlib1g-dev subversion gcc-multilib git curl file ninja-build automake autoconf libtool pkg-config gettext help2man texinfo gawk flex bison rsync wget aria2 python3-pip libelf-dev liblz4-dev zstd libssl-dev libcurl4-openssl-dev groff texlive texinfo
        pip3 install requests urllib3
        echo "✅ 编译环境设置完成"

    - name: 创建构建目录并设置权限
      run: |
        echo "=== 创建构建目录 ==="
        sudo mkdir -p $BUILD_DIR
        sudo chown -R $USER:$USER $BUILD_DIR
        sudo chmod -R 755 $BUILD_DIR
        sudo chmod u+w $BUILD_DIR
        echo "✅ 构建目录创建完成"
        touch $BUILD_DIR/test_write.txt && echo "✅ 写入测试成功" && rm $BUILD_DIR/test_write.txt

    - name: 复制脚本到构建目录
      run: |
        echo "=== 复制脚本到构建目录 ==="
        cd $GITHUB_WORKSPACE
        SCRIPTS=("complete_version_detector.sh" "device_detection.sh" "error_analysis.sh" "pre_download.sh" "check-plugins.sh" "diagnose-packages.sh")
        echo "要复制的脚本列表: ${SCRIPTS[*]}"
        echo "当前工作目录: $(pwd)"
        echo "目录内容:"
        find . -name "*.sh" -type f | head -20
        COPIED_COUNT=0
        for script in "${SCRIPTS[@]}"; do
          echo "正在查找: $script"
          FOUND_SCRIPT=$(find . -name "$script" -type f | head -1)
          if [ -n "$FOUND_SCRIPT" ]; then
            cp "$FOUND_SCRIPT" $BUILD_DIR/
            echo "✅ 找到并复制: $script (从 $FOUND_SCRIPT)"
            COPIED_COUNT=$((COPIED_COUNT + 1))
          else
            echo "⚠️ 未找到: $script"
          fi
        done
        echo "总共复制了 $COPIED_COUNT 个脚本文件"
        cd $BUILD_DIR
        echo "构建目录中的脚本:"
        ls -la *.sh 2>/dev/null || echo "没有找到.sh文件"
        chmod +x *.sh 2>/dev/null || true
        echo "✅ 脚本复制完成"

    - name: 验证脚本存在性
      run: |
        cd $BUILD_DIR
        echo "=== 验证脚本存在性 ==="
        echo "构建目录内容:"
        ls -la
        REQUIRED_SCRIPTS=("complete_version_detector.sh" "device_detection.sh")
        ALL_SCRIPTS_EXIST=true
        for script in "${REQUIRED_SCRIPTS[@]}"; do
          if [ -f "$script" ]; then
            echo "✅ $script 存在"
            chmod +x "$script"
          else
            echo "❌ 错误: $script 不存在"
            ALL_SCRIPTS_EXIST=false
          fi
        done
        if [ "$ALL_SCRIPTS_EXIST" = false ]; then
          echo "当前目录中的文件:"
          ls -la
          echo "❌ 关键脚本缺失，构建终止"
          exit 1
        fi

    - name: 智能版本检测
      id: version-detection
      run: |
        cd $BUILD_DIR
        echo "=== 智能版本检测 ==="
        sudo chown -R $USER:$USER . 2>/dev/null || true
        chmod u+w . 2>/dev/null || true
        chmod +x *.sh 2>/dev/null || true
        echo "设备名称: ${{ github.event.inputs.device_name }}"
        echo "版本规格: ${{ github.event.inputs.version_spec }}"
        echo "老旧设备: ${{ github.event.inputs.old_device }}"
        if [ ! -f "complete_version_detector.sh" ]; then
          echo "❌ 错误: complete_version_detector.sh 不存在"
          ls -la
          exit 1
        fi
        chmod +x complete_version_detector.sh
        set +e
        OUTPUT=$(./complete_version_detector.sh "${{ github.event.inputs.device_name }}" "${{ github.event.inputs.version_spec }}" "${{ github.event.inputs.old_device }}" 2>&1)
        SCRIPT_EXIT_CODE=$?
        set -e
        echo "脚本输出:"
        echo "$OUTPUT"
        echo "脚本退出代码: $SCRIPT_EXIT_CODE"
        SELECTED_REPO=$(echo "$OUTPUT" | grep "^SELECTED_REPO=" | head -1 | cut -d'=' -f2)
        SELECTED_BRANCH=$(echo "$OUTPUT" | grep "^SELECTED_BRANCH=" | head -1 | cut -d'=' -f2)
        SELECTED_REPO_URL=$(echo "$OUTPUT" | grep "^SELECTED_REPO_URL=" | head -1 | cut -d'=' -f2)
        if [ -n "$SELECTED_REPO" ] && [ -n "$SELECTED_BRANCH" ] && [ -n "$SELECTED_REPO_URL" ]; then
            echo "✅ 从脚本输出中成功提取版本信息"
            echo "SELECTED_REPO=$SELECTED_REPO" >> $GITHUB_ENV
            echo "SELECTED_BRANCH=$SELECTED_BRANCH" >> $GITHUB_ENV
            echo "SELECTED_REPO_URL=$SELECTED_REPO_URL" >> $GITHUB_ENV
            echo "✅ 环境变量已设置:"
            echo "  仓库: $SELECTED_REPO"
            echo "  分支: $SELECTED_BRANCH"
            echo "  URL: $SELECTED_REPO_URL"
        else
            echo "❌ 无法从脚本输出中提取版本信息"
            if [ $SCRIPT_EXIT_CODE -ne 0 ]; then
                echo "❌ 版本检测失败"
                exit 1
            else
                echo "⚠️ 脚本成功但无法提取版本信息，使用默认值"
                echo "SELECTED_REPO=immortalwrt" >> $GITHUB_ENV
                echo "SELECTED_BRANCH=master" >> $GITHUB_ENV
                echo "SELECTED_REPO_URL=https://github.com/immortalwrt/immortalwrt.git" >> $GITHUB_ENV
                echo "✅ 使用默认版本信息"
            fi
        fi

    - name: 备份检测脚本
      run: |
        echo "=== 备份检测脚本 ==="
        mkdir -p /tmp/build-scripts-backup
        cp $BUILD_DIR/*.sh /tmp/build-scripts-backup/ 2>/dev/null || true
        echo "✅ 脚本备份完成"

    - name: 清理构建目录
      run: |
        cd $BUILD_DIR
        echo "=== 清理构建目录准备克隆 ==="
        ls -la
        sudo rm -rf ./* ./.git* 2>/dev/null || true
        sudo rm -rf .[!.]* 2>/dev/null || true
        ls -la
        echo "✅ 目录清理完成"

    - name: 克隆源码
      run: |
        cd $BUILD_DIR
        echo "=== 克隆选定版本的源码 ==="
        echo "仓库: $SELECTED_REPO_URL"
        echo "分支: $SELECTED_BRANCH"
        if [ -z "$SELECTED_REPO_URL" ] || [ -z "$SELECTED_BRANCH" ]; then
          echo "❌ 错误: 环境变量未正确设置"
          exit 1
        fi
        echo "检查目录状态..."
        FILE_COUNT=$(ls -A | wc -l)
        if [ $FILE_COUNT -ne 0 ]; then
          echo "❌ 错误: 目录非空，无法克隆"
          exit 1
        fi
        MAX_RETRIES=3
        RETRY_COUNT=0
        CLONE_SUCCESS=false
        while [ $RETRY_COUNT -lt $MAX_RETRIES ] && [ "$CLONE_SUCCESS" = false ]; do
          RETRY_COUNT=$((RETRY_COUNT + 1))
          echo "克隆尝试: $RETRY_COUNT/$MAX_RETRIES"
          if git clone --depth 1 --branch "$SELECTED_BRANCH" "$SELECTED_REPO_URL" .; then
            echo "✅ 源码克隆完成"
            CLONE_SUCCESS=true
          else
            echo "❌ 克隆失败，尝试 $RETRY_COUNT/$MAX_RETRIES"
            if echo "$SELECTED_REPO_URL" | grep -q "github.com" && [ $RETRY_COUNT -eq 2 ]; then
              echo "🔄 尝试使用镜像源..."
              MIRROR_URL=$(echo "$SELECTED_REPO_URL" | sed 's/github.com/github.com.cnpmjs.org/')
              rm -rf ./* ./.git* 2>/dev/null || true
              if git clone --depth 1 --branch "$SELECTED_BRANCH" "$MIRROR_URL" .; then
                echo "✅ 使用镜像源克隆成功"
                CLONE_SUCCESS=true
                break
              fi
            fi
            if [ $RETRY_COUNT -eq $MAX_RETRIES ]; then
              echo "❌ 源码克隆失败，达到最大重试次数"
              exit 1
            fi
            sleep 10
            rm -rf ./* ./.git* 2>/dev/null || true
          fi
        done

    - name: 恢复检测脚本
      run: |
        cd $BUILD_DIR
        echo "=== 恢复检测脚本 ==="
        cp /tmp/build-scripts-backup/*.sh ./ 2>/dev/null || true
        chmod +x *.sh
        echo "✅ 脚本恢复完成"

    - name: 修复feeds配置问题
      run: |
        cd $BUILD_DIR
        echo "=== 修复feeds配置问题 ==="
        
        # 检查当前feeds配置
        echo "当前feeds.conf.default内容:"
        cat feeds.conf.default
        
        # 使用更稳定的feeds配置
        echo "使用更稳定的feeds配置..."
        cat > feeds.conf.default << 'EOF'
src-git packages https://git.openwrt.org/feed/packages.git^3e7f3b2c
src-git luci https://git.openwrt.org/project/luci.git^3e7f3b2c
src-git routing https://git.openwrt.org/feed/routing.git^3e7f3b2c
src-git telephony https://git.openwrt.org/feed/telephony.git^3e7f3b2c
# src-git video https://github.com/openwrt/video.git
# src-git targets https://github.com/openwrt/targets.git
# src-git oldpackages http://git.openwrt.org/packages.git
# src-link custom /usr/src/openwrt/custom-feed
EOF
        
        echo "修复后的feeds.conf.default内容:"
        cat feeds.conf.default
        echo "✅ feeds配置修复完成"

    - name: 更新安装feeds
      run: |
        cd $BUILD_DIR
        echo "=== 更新和安装feeds ==="
        
        echo "第一次更新feeds..."
        set +e
        ./scripts/feeds update -a
        UPDATE_RESULT=$?
        set -e
        
        if [ $UPDATE_RESULT -ne 0 ]; then
            echo "⚠️ 第一次更新失败，尝试修复..."
            # 清理失败的feeds目录
            rm -rf ./feeds/* 2>/dev/null || true
            
            echo "第二次更新feeds..."
            set +e
            ./scripts/feeds update -a
            UPDATE_RESULT=$?
            set -e
            
            if [ $UPDATE_RESULT -ne 0 ]; then
                echo "❌ 更新feeds失败，但继续尝试安装..."
            fi
        fi
        
        echo "安装feeds..."
        set +e
        ./scripts/feeds install -a
        INSTALL_RESULT=$?
        set -e
        
        if [ $INSTALL_RESULT -ne 0 ]; then
            echo "⚠️ 部分feeds安装失败，但继续构建流程..."
        else
            echo "✅ feeds安装完成"
        fi

    - name: 验证feeds状态
      run: |
        cd $BUILD_DIR
        echo "=== 验证feeds状态 ==="
        
        echo "Feeds目录内容:"
        ls -la feeds/ 2>/dev/null || echo "feeds目录不存在或为空"
        
        echo "关键feeds检查:"
        CRITICAL_FEEDS=("packages" "luci")
        FEEDS_EXIST=true
        
        for feed in "${CRITICAL_FEEDS[@]}"; do
            if [ -d "feeds/$feed" ]; then
                echo "✅ 关键feed存在: $feed"
                echo "  - 内容示例:"
                ls "feeds/$feed" | head -5
            else
                echo "❌ 关键feed缺失: $feed"
                FEEDS_EXIST=false
            fi
        done
        
        if [ "$FEEDS_EXIST" = false ]; then
            echo "❌ 关键feeds缺失，无法继续构建"
            echo "当前所有feeds:"
            ./scripts/feeds list | head -20
            exit 1
        fi
        
        echo "✅ 所有关键feeds都存在"

    - name: 添加第三方feeds
      run: |
        cd $BUILD_DIR
        echo "=== 添加第三方feeds ==="
        
        # 在基本feeds工作后添加第三方feeds
        echo "添加第三方feeds到feeds.conf.default..."
        echo "" >> feeds.conf.default
        echo "# 第三方插件源" >> feeds.conf.default
        echo "src-git kenzo_packages https://github.com/kenzok8/openwrt-packages;master" >> feeds.conf.default
        echo "src-git small_packages https://github.com/kenzok8/small;master" >> feeds.conf.default
        echo "src-git smartdns https://github.com/pymumu/openwrt-smartdns;master" >> feeds.conf.default
        echo "src-git openclash https://github.com/vernesong/OpenClash;master" >> feeds.conf.default
        
        echo "更新第三方feeds..."
        set +e
        ./scripts/feeds update kenzo_packages small_packages smartdns openclash
        UPDATE_RESULT=$?
        set -e
        
        if [ $UPDATE_RESULT -eq 0 ]; then
            echo "安装第三方feeds..."
            ./scripts/feeds install -p kenzo_packages -a
            ./scripts/feeds install -p small_packages -a
            ./scripts/feeds install -p smartdns -a
            ./scripts/feeds install -p openclash -a
            echo "✅ 第三方feeds添加完成"
        else
            echo "⚠️ 第三方feeds更新失败，但继续构建"
        fi

    - name: 复制配置文件和脚本
      run: |
        cd $BUILD_DIR
        echo "=== 重新复制配置文件和脚本 ==="
        cd $GITHUB_WORKSPACE
        echo "=== 查找配置文件 ==="
        mkdir -p $BUILD_DIR/config-templates
        CONFIG_FILES=("minimal.config" "normal-new.config" "normal-old.config")
        for config_file in "${CONFIG_FILES[@]}"; do
          echo "正在查找配置文件: $config_file"
          FOUND_CONFIG=$(find . -name "$config_file" -type f | head -1)
          if [ -n "$FOUND_CONFIG" ]; then
            cp "$FOUND_CONFIG" $BUILD_DIR/config-templates/
            echo "✅ 复制: $config_file (从 $FOUND_CONFIG)"
          else
            echo "⚠️ 未找到: $config_file"
          fi
        done
        echo "✅ 配置文件复制完成"
        
        SCRIPTS=("error_analysis.sh" "pre_download.sh" "check-plugins.sh" "diagnose-packages.sh")
        for script in "${SCRIPTS[@]}"; do
          FOUND_SCRIPT=$(find . -name "$script" -type f | head -1)
          if [ -n "$FOUND_SCRIPT" ]; then
            cp "$FOUND_SCRIPT" $BUILD_DIR/
            chmod +x $BUILD_DIR/"$script"
            echo "✅ 更新脚本: $script"
          fi
        done
        echo "✅ 配置文件和脚本重新复制完成"

    - name: 设备检测
      id: device-detection
      run: |
        cd $BUILD_DIR
        echo "=== 智能设备检测与别名转换 ==="
        INPUT_DEVICE="${{ github.event.inputs.device_name }}"
        echo "输入设备名称: $INPUT_DEVICE"
        case "$INPUT_DEVICE" in
            "ac42u"|"acrh17"|"rt-acrh17") ACTUAL_DEVICE="asus_rt-ac42u" ;;
            "ac58u"|"acrh13"|"rt-ac58u"|"rt-acrh13") ACTUAL_DEVICE="asus_rt-ac58u" ;;
            "mi4a"|"r4a") ACTUAL_DEVICE="xiaomi_mi-router-4a-gigabit" ;;
            "mi3g"|"r3g") ACTUAL_DEVICE="xiaomi_mi-router-3g" ;;
            *) ACTUAL_DEVICE="$INPUT_DEVICE" ;;
        esac
        if [ "$ACTUAL_DEVICE" != "$INPUT_DEVICE" ]; then
          echo "✅ 检测到设备通用名称 '$INPUT_DEVICE'，转换为实际设备标识 '$ACTUAL_DEVICE'"
        else
          echo "ℹ️ 使用原始设备名称: $ACTUAL_DEVICE"
        fi
        if [ ! -f "device_detection.sh" ]; then
          echo "❌ 错误: 设备检测脚本不存在"
          exit 1
        fi
        chmod +x device_detection.sh
        echo "=== 开始设备检测 ==="
        TEMP_LOG=$(mktemp)
        set +e
        ./device_detection.sh "$ACTUAL_DEVICE" 2>&1 | tee "$TEMP_LOG"
        SCRIPT_EXIT_CODE=$?
        set -e
        echo "=== 脚本退出代码: $SCRIPT_EXIT_CODE ==="
        PLATFORM=$(grep "^PLATFORM=" "$TEMP_LOG" | tail -1 | cut -d'=' -f2)
        DEVICE_SHORT_NAME=$(grep "^DEVICE_SHORT_NAME=" "$TEMP_LOG" | tail -1 | cut -d'=' -f2)
        DEVICE_FULL_NAME=$(grep "^DEVICE_FULL_NAME=" "$TEMP_LOG" | tail -1 | cut -d'=' -f2)
        echo "=== 提取的设备信息 ==="
        echo "平台(PLATFORM): '$PLATFORM'"
        echo "设备短名称(DEVICE_SHORT_NAME): '$DEVICE_SHORT_NAME'"
        echo "设备全名称(DEVICE_FULL_NAME): '$DEVICE_FULL_NAME'"
        rm -f "$TEMP_LOG"
        if [ -n "$PLATFORM" ] && [ -n "$DEVICE_SHORT_NAME" ] && [ -n "$DEVICE_FULL_NAME" ]; then
            echo "✅ 从脚本输出中成功提取设备信息"
            echo "PLATFORM=$PLATFORM" >> $GITHUB_ENV
            echo "DEVICE_SHORT_NAME=$DEVICE_SHORT_NAME" >> $GITHUB_ENV
            echo "DEVICE_FULL_NAME=$DEVICE_FULL_NAME" >> $GITHUB_ENV
        else
            echo "❌ 无法从脚本输出中提取设备信息"
            if [ $SCRIPT_EXIT_CODE -ne 0 ]; then
                echo "❌ 设备检测失败"
                exit 1
            else
                echo "⚠️ 脚本成功但无法提取设备信息，使用备用设备映射"
                case "$ACTUAL_DEVICE" in
                    "asus_rt-ac42u"|"ac42u"|"acrh17"|"rt-acrh17")
                        echo "PLATFORM=ipq40xx" >> $GITHUB_ENV
                        echo "DEVICE_SHORT_NAME=asus_rt-ac42u" >> $GITHUB_ENV
                        echo "DEVICE_FULL_NAME=$ACTUAL_DEVICE" >> $GITHUB_ENV
                        ;;
                    "asus_rt-ac58u"|"ac58u"|"acrh13"|"rt-acrh13")
                        echo "PLATFORM=ipq40xx" >> $GITHUB_ENV
                        echo "DEVICE_SHORT_NAME=asus_rt-ac58u" >> $GITHUB_ENV
                        echo "DEVICE_FULL_NAME=$ACTUAL_DEVICE" >> $GITHUB_ENV
                        ;;
                    "xiaomi_mi-router-4a-gigabit"|"mi4a"|"r4a")
                        echo "PLATFORM=ramips" >> $GITHUB_ENV
                        echo "DEVICE_SHORT_NAME=xiaomi_mi-router-4a-gigabit" >> $GITHUB_ENV
                        echo "DEVICE_FULL_NAME=$ACTUAL_DEVICE" >> $GITHUB_ENV
                        ;;
                    *)
                        echo "❌ 未知设备，无法设置默认值"
                        exit 1
                        ;;
                esac
                echo "✅ 已使用备用设备信息"
            fi
        fi

    - name: 简化插件检查
      run: |
        cd $BUILD_DIR
        echo "=== 简化插件检查 ==="
        
        echo "检查关键包可用性..."
        set +e
        
        # 检查基本包
        echo "1. 检查基本包:"
        for pkg in base-files procd; do
            if ./scripts/feeds list | grep -q "$pkg"; then
                echo "✅ $pkg 可用"
            else
                echo "⚠️ $pkg 不可用"
            fi
        done
        
        # 检查luci包
        echo "2. 检查Luci包:"
        for pkg in luci-base luci-theme-bootstrap; do
            if ./scripts/feeds list | grep -q "$pkg"; then
                echo "✅ $pkg 可用"
            else
                echo "⚠️ $pkg 不可用"
            fi
        done
        
        # 列出所有可用的包
        echo "3. 可用包列表 (前20个):"
        ./scripts/feeds list | head -20
        
        set -e
        echo "✅ 简化插件检查完成"

    - name: 应用配置模板
      run: |
        cd $BUILD_DIR
        echo "=== 应用配置模板 ==="
        CONFIG_TYPE="${{ github.event.inputs.config_type }}"
        IS_OLD_DEVICE="${{ github.event.inputs.old_device }}"
        echo "配置类型: $CONFIG_TYPE"
        echo "是否为老旧设备: $IS_OLD_DEVICE"
        case "$CONFIG_TYPE" in
            "minimal")
                CONFIG_FILE="config-templates/minimal.config"
                ;;
            "normal")
                if [ "$IS_OLD_DEVICE" = "true" ]; then
                    CONFIG_FILE="config-templates/normal-old.config"
                else
                    CONFIG_FILE="config-templates/normal-new.config"
                fi
                ;;
            "custom")
                if [ "$IS_OLD_DEVICE" = "true" ]; then
                    CONFIG_FILE="config-templates/normal-old.config"
                else
                    CONFIG_FILE="config-templates/normal-new.config"
                fi
                ;;
            *)
                echo "❌ 未知的配置类型: $CONFIG_TYPE"
                exit 1
                ;;
        esac
        if [ -f "$CONFIG_FILE" ]; then
            echo "✅ 使用配置文件: $CONFIG_FILE"
            cp "$CONFIG_FILE" .config
        else
            echo "❌ 配置文件不存在: $CONFIG_FILE"
            echo "可用的配置文件:"
            ls -la config-templates/ 2>/dev/null || echo "config-templates目录不存在"
            exit 1
        fi
        echo "应用配置..."
        make defconfig
        echo "✅ 配置应用完成"

    - name: 自定义配置调整
      if: inputs.extra_packages != '' || inputs.disabled_plugins != ''
      run: |
        cd $BUILD_DIR
        echo "=== 自定义配置调整 ==="
        EXTRA_PACKAGES="${{ github.event.inputs.extra_packages }}"
        DISABLED_PLUGINS="${{ github.event.inputs.disabled_plugins }}"
        echo "额外包: $EXTRA_PACKAGES"
        echo "禁用插件: $DISABLED_PLUGINS"
        if [ -n "$EXTRA_PACKAGES" ]; then
            for package in $EXTRA_PACKAGES; do
                echo "启用包: $package"
                if grep -q "CONFIG_PACKAGE_${package}=m" .config || grep -q "CONFIG_PACKAGE_${package}=y" .config; then
                    echo "✅ 包 $package 已存在配置中"
                else
                    echo "CONFIG_PACKAGE_${package}=y" >> .config
                    echo "✅ 添加包 $package 到配置"
                fi
            done
        fi
        if [ -n "$DISABLED_PLUGINS" ]; then
            for plugin in $DISABLED_PLUGINS; do
                echo "禁用插件: $plugin"
                sed -i "s/CONFIG_PACKAGE_${plugin}=y/CONFIG_PACKAGE_${plugin}=n/" .config
                sed -i "s/CONFIG_PACKAGE_${plugin}=m/CONFIG_PACKAGE_${plugin}=n/" .config
                echo "✅ 禁用插件 $plugin"
            done
        fi
        make defconfig
        echo "✅ 自定义配置调整完成"

    - name: 预下载源文件
      run: |
        cd $BUILD_DIR
        echo "=== 预下载源文件 ==="
        echo '#!/bin/bash' > pre-download-sources.sh
        echo 'set -e' >> pre-download-sources.sh
        echo 'echo "=== 开始预下载源文件 ==="' >> pre-download-sources.sh
        echo 'mkdir -p dl' >> pre-download-sources.sh
        echo 'cd dl' >> pre-download-sources.sh
        echo 'COMMON_SOURCES=(' >> pre-download-sources.sh
        echo '  "https://github.com/protocolbuffers/protobuf/releases/download/v3.20.1/protobuf-cpp-3.20.1.tar.gz"' >> pre-download-sources.sh
        echo '  "https://www.openssl.org/source/openssl-1.1.1q.tar.gz"' >> pre-download-sources.sh
        echo '  "https://www.zlib.net/zlib-1.2.12.tar.gz"' >> pre-download-sources.sh
        echo '  "https://sqlite.org/2022/sqlite-autoconf-3390400.tar.gz"' >> pre-download-sources.sh
        echo '  "https://github.com/libevent/libevent/releases/download/release-2.1.12-stable/libevent-2.1.12-stable.tar.gz"' >> pre-download-sources.sh
        echo '  "https://github.com/curl/curl/releases/download/curl-7_86_0/curl-7.86.0.tar.xz"' >> pre-download-sources.sh
        echo ')' >> pre-download-sources.sh
        echo 'for source in "${COMMON_SOURCES[@]}"; do' >> pre-download-sources.sh
        echo '    filename=$(basename "$source")' >> pre-download-sources.sh
        echo '    if [ ! -f "$filename" ]; then' >> pre-download-sources.sh
        echo '        echo "下载: $filename"' >> pre-download-sources.sh
        echo '        wget --timeout=60 --tries=3 "$source" || echo "下载失败: $filename"' >> pre-download-sources.sh
        echo '    else' >> pre-download-sources.sh
        echo '        echo "已存在: $filename"' >> pre-download-sources.sh
        echo '    fi' >> pre-download-sources.sh
        echo 'done' >> pre-download-sources.sh
        echo 'echo "✅ 预下载完成"' >> pre-download-sources.sh
        chmod +x pre-download-sources.sh
        ./pre-download-sources.sh

    - name: 编译固件
      timeout-minutes: 180
      run: |
        cd $BUILD_DIR
        echo "=== 开始编译固件 ==="
        CPU_CORES=$(nproc)
        if [ $CPU_CORES -gt 8 ]; then
            JOBS=$((CPU_CORES - 2))
        else
            JOBS=$CPU_CORES
        fi
        echo "使用编译线程数: $JOBS"
        set +e
        make -j$JOBS V=s
        BUILD_RESULT=$?
        set -e
        if [ $BUILD_RESULT -eq 0 ]; then
            echo "✅ 固件编译成功"
        else
            echo "❌ 固件编译失败"
            if [ -f "error_analysis.sh" ]; then
                chmod +x error_analysis.sh
                ./error_analysis.sh
            fi
            exit 1
        fi

    - name: 收集构建产物
      if: success()
      run: |
        cd $BUILD_DIR
        echo "=== 收集构建产物 ==="
        mkdir -p $GITHUB_WORKSPACE/firmware-output
        FIRMWARE_FILES=$(find bin/targets -name "*.bin" -o -name "*.img" -o -name "*.gz" 2>/dev/null | head -10)
        if [ -n "$FIRMWARE_FILES" ]; then
            echo "找到固件文件:"
            echo "$FIRMWARE_FILES"
            for firmware in $FIRMWARE_FILES; do
                if [ -f "$firmware" ]; then
                    cp "$firmware" $GITHUB_WORKSPACE/firmware-output/
                    echo "✅ 复制: $(basename "$firmware")"
                fi
            done
        else
            echo "⚠️ 未找到固件文件"
            echo "构建目录内容:"
            find bin/targets -type f 2>/dev/null | head -20
        fi
        cp .config $GITHUB_WORKSPACE/firmware-output/build-config.txt 2>/dev/null || true
        echo "✅ 构建产物收集完成"

    - name: 上传构建产物
      if: success()
      uses: actions/upload-artifact@v4
      with:
        name: firmware-${{ github.event.inputs.device_name }}-${{ github.run_id }}
        path: firmware-output/
        retention-days: 7

    - name: 保存配置文件到仓库
      if: success() && inputs.save_config == 'true'
      run: |
        cd $BUILD_DIR
        echo "=== 保存配置文件到仓库 ==="
        cd $GITHUB_WORKSPACE
        mkdir -p config-backups
        TIMESTAMP=$(date +%Y%m%d_%H%M%S)
        DEVICE_NAME="${{ github.event.inputs.device_name }}"
        CONFIG_BACKUP_FILE="config-backups/${DEVICE_NAME}_${TIMESTAMP}.config"
        if [ -f "$BUILD_DIR/.config" ]; then
            cp "$BUILD_DIR/.config" "$CONFIG_BACKUP_FILE"
            echo "✅ 配置文件已保存: $CONFIG_BACKUP_FILE"
        else
            echo "⚠️ 未找到配置文件"
        fi
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add config-backups/
        git commit -m "保存构建配置: $DEVICE_NAME - $TIMESTAMP" || echo "没有变更需要提交"
        git push

    - name: 构建完成通知
      if: always()
      run: |
        echo "=== 构建完成状态 ==="
        if [ ${{ job.status }} == 'success' ]; then
          echo "✅ 固件构建成功完成"
          echo "设备: ${{ github.event.inputs.device_name }}"
          echo "版本: $SELECTED_REPO/$SELECTED_BRANCH"
        else
          echo "❌ 固件构建失败"
          echo "请检查构建日志了解详细错误信息"
        fi
