# .github/workflows/compiler-matrix-build.yml
name: 🔧 交叉编译器矩阵构建

on:
  workflow_dispatch:
    inputs:
      target_arch:
        description: '目标架构'
        required: true
        default: 'aarch64'
        type: choice
        options:
          - aarch64
          - x86_64
          - mips
          - mipsel
          - arm_cortex-a7
          - arm_cortex-a53
          - riscv64
      libc:
        description: 'C库类型'
        required: true
        default: 'musl'
        type: choice
        options:
          - musl
          - glibc
      gcc_version:
        description: 'GCC版本'
        required: true
        default: '12.3.0'
        type: string
      clean_build:
        description: '清理之前的构建缓存'
        required: true
        default: false
        type: boolean

  schedule:
    - cron: '0 2 * * 0'

env:
  OPENWRT_VERSION: "23.05.2"
  COMPILER_BASE_DIR: "${{ github.workspace }}/compilers"

jobs:
  setup:
    runs-on: ubuntu-22.04
    outputs:
      gcc_version: ${{ steps.version-detection.outputs.gcc_version }}
      binutils_version: ${{ steps.version-detection.outputs.binutils_version }}
      linux_version: ${{ steps.version-detection.outputs.linux_version }}
      musl_version: ${{ steps.version-detection.outputs.musl_version }}
      glibc_version: ${{ steps.version-detection.outputs.glibc_version }}
    steps:
      - name: 🏗️ 初始化
        run: |
          echo "=== 交叉编译器构建系统 ==="
          echo "触发方式: ${{ github.event_name }}"
          echo "运行ID: ${{ github.run_id }}"
          echo "时间: $(date)"
          
          mkdir -p "${{ env.COMPILER_BASE_DIR }}"
          echo "RUN_ID=${{ github.run_id }}" >> $GITHUB_ENV
          echo "RUN_NUMBER=${{ github.run_number }}" >> $GITHUB_ENV

      - name: 🔍 版本检测
        id: version-detection
        run: |
          echo "=== 检测工具链版本 ==="
          GCC_VERSION="${{ github.event.inputs.gcc_version }}"
          BINUTILS_VERSION="2.40"
          LINUX_VERSION="5.15.133"
          MUSL_VERSION="1.2.4"
          GLIBC_VERSION="2.37"
          
          echo "GCC版本: $GCC_VERSION"
          echo "Binutils版本: $BINUTILS_VERSION"
          echo "Linux内核版本: $LINUX_VERSION"
          
          echo "gcc_version=$GCC_VERSION" >> $GITHUB_OUTPUT
          echo "binutils_version=$BINUTILS_VERSION" >> $GITHUB_OUTPUT
          echo "linux_version=$LINUX_VERSION" >> $GITHUB_OUTPUT
          echo "musl_version=$MUSL_VERSION" >> $GITHUB_OUTPUT
          echo "glibc_version=$GLIBC_VERSION" >> $GITHUB_OUTPUT

  build-compiler:
    needs: setup
    runs-on: ubuntu-22.04
    timeout-minutes: 180
    
    steps:
      - name: 📥 检出仓库
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: 🏗️ 设置环境
        run: |
          echo "=== 设置构建环境 ==="
          
          ARCH="${{ github.event.inputs.target_arch }}"
          LIBC="${{ github.event.inputs.libc }}"
          
          case "$ARCH" in
            aarch64)
              TARGET="armvirt"
              SUBTARGET="64"
              ARCH_NAME="aarch64"
              PREFIX="aarch64-openwrt-linux"
              CPU_TYPE="cortex-a53"
              ;;
            x86_64)
              TARGET="x86"
              SUBTARGET="64"
              ARCH_NAME="x86_64"
              PREFIX="x86_64-openwrt-linux"
              CPU_TYPE="x86_64"
              ;;
            mips)
              TARGET="ramips"
              SUBTARGET="mt7621"
              ARCH_NAME="mips"
              PREFIX="mips-openwrt-linux"
              CPU_TYPE="24kc"
              ;;
            mipsel)
              TARGET="ramips"
              SUBTARGET="mt7621"
              ARCH_NAME="mipsel"
              PREFIX="mipsel-openwrt-linux"
              CPU_TYPE="24kc"
              ;;
            arm_cortex-a7)
              TARGET="sunxi"
              SUBTARGET="cortexa7"
              ARCH_NAME="arm"
              PREFIX="arm-openwrt-linux"
              CPU_TYPE="cortex-a7"
              ;;
            arm_cortex-a53)
              TARGET="rockchip"
              SUBTARGET="armv8"
              ARCH_NAME="aarch64"
              PREFIX="aarch64-openwrt-linux"
              CPU_TYPE="cortex-a53"
              ;;
            riscv64)
              TARGET="virt"
              SUBTARGET="rv64"
              ARCH_NAME="riscv64"
              PREFIX="riscv64-openwrt-linux"
              CPU_TYPE="generic"
              ;;
            *)
              echo "不支持的架构: $ARCH"
              exit 1
              ;;
          esac
          
          if [ "$LIBC" = "musl" ]; then
            PREFIX="${PREFIX}-musl"
            LIBC_SUFFIX="musl"
            USE_MUSL="y"
            USE_GLIBC="n"
          else
            PREFIX="${PREFIX}-gnu"
            LIBC_SUFFIX="glibc"
            USE_MUSL="n"
            USE_GLIBC="y"
          fi
          
          GCC_VERSION='${{ needs.setup.outputs.gcc_version }}'
          BINUTILS_VERSION='${{ needs.setup.outputs.binutils_version }}'
          LINUX_VERSION='${{ needs.setup.outputs.linux_version }}'
          
          BUILD_ID="${ARCH_NAME}_${LIBC_SUFFIX}_gcc-${GCC_VERSION}_$(date +%Y%m%d_%H%M%S)"
          BUILD_DIR="/tmp/compiler-build-${BUILD_ID}"
          OUTPUT_DIR="${{ env.COMPILER_BASE_DIR }}/${ARCH_NAME}/${LIBC_SUFFIX}/gcc-${GCC_VERSION}"
          
          echo "目标架构: $ARCH_NAME"
          echo "C库类型: $LIBC_SUFFIX"
          echo "编译器前缀: $PREFIX-"
          echo "GCC版本: $GCC_VERSION"
          echo "构建目录: $BUILD_DIR"
          echo "输出目录: $OUTPUT_DIR"
          
          echo "BUILD_DIR=$BUILD_DIR" >> $GITHUB_ENV
          echo "OUTPUT_DIR=$OUTPUT_DIR" >> $GITHUB_ENV
          echo "TARGET=$TARGET" >> $GITHUB_ENV
          echo "SUBTARGET=$SUBTARGET" >> $GITHUB_ENV
          echo "ARCH_NAME=$ARCH_NAME" >> $GITHUB_ENV
          echo "PREFIX=$PREFIX" >> $GITHUB_ENV
          echo "CPU_TYPE=$CPU_TYPE" >> $GITHUB_ENV
          echo "USE_MUSL=$USE_MUSL" >> $GITHUB_ENV
          echo "USE_GLIBC=$USE_GLIBC" >> $GITHUB_ENV
          echo "GCC_VERSION=$GCC_VERSION" >> $GITHUB_ENV
          echo "BINUTILS_VERSION=$BINUTILS_VERSION" >> $GITHUB_ENV
          echo "LINUX_VERSION=$LINUX_VERSION" >> $GITHUB_ENV
          echo "LIBC_SUFFIX=$LIBC_SUFFIX" >> $GITHUB_ENV
          echo "BUILD_ID=$BUILD_ID" >> $GITHUB_ENV

      - name: 🔧 安装依赖
        run: |
          echo "=== 安装构建依赖 ==="
          sudo apt-get update
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y build-essential gcc g++ make flex bison libgmp-dev libmpfr-dev libmpc-dev libisl-dev python3 python3-dev git wget curl zlib1g-dev liblzma-dev file gawk gettext libncurses5-dev libssl-dev python3-distutils rsync unzip ncurses-term ncurses-base libncursesw5 libtinfo5 terminfo gnutls-bin libgnutls28-dev libcurl4-gnutls-dev autoconf automake autopoint libtool pkg-config texinfo gperf cmake ninja-build patchutils bc bzip2 xz-utils help2man libexpat-dev libgdbm-dev libreadline-dev libsqlite3-dev libffi-dev lzma-dev zstd libzstd-dev libelf-dev libdw-dev libcap-dev libattr1-dev libaudit-dev libiberty-dev jq

      - name: 🗑️ 清理构建缓存
        if: ${{ github.event.inputs.clean_build == 'true' }}
        run: |
          echo "=== 清理构建缓存 ==="
          sudo rm -rf /tmp/compiler-build-* 2>/dev/null || true
          echo "清理完成"

      - name: 📥 下载OpenWrt源码
        run: |
          echo "=== 获取OpenWrt源码 ==="
          mkdir -p "${{ env.BUILD_DIR }}"
          cd "${{ env.BUILD_DIR }}"
          
          echo "克隆OpenWrt ${{ env.OPENWRT_VERSION }} 分支..."
          git clone --depth 1 --branch v${{ env.OPENWRT_VERSION }} https://github.com/openwrt/openwrt.git . 2>&1 | tee /tmp/clone.log
          
          if [ ${PIPESTATUS[0]} -ne 0 ]; then
            echo "Git clone失败，尝试使用tar包..."
            wget -q --show-progress -O openwrt.tar.xz https://github.com/openwrt/openwrt/archive/refs/tags/v${{ env.OPENWRT_VERSION }}.tar.gz
            tar -xzf openwrt.tar.xz --strip-components=1
            rm -f openwrt.tar.xz
            echo "使用tar包下载成功"
          fi
          
          echo "源码目录: $(pwd)"
          echo "源码大小: $(du -sh . | cut -f1)"

      - name: 📝 生成配置文件
        run: |
          echo "=== 生成OpenWrt配置 ==="
          cd "${{ env.BUILD_DIR }}"
          
          mkdir -p include/site
          echo "ac_cv_sys_file_offset_bits=64" > include/site/${{ env.ARCH_NAME }}
          echo "ac_cv_sys_large_files=yes" >> include/site/${{ env.ARCH_NAME }}
          echo "ac_cv_c_bigendian=no" >> include/site/${{ env.ARCH_NAME }}
          
          CONFIG_FILE=".config"
          echo "# 目标配置" > $CONFIG_FILE
          echo "CONFIG_TARGET_${{ env.TARGET }}=y" >> $CONFIG_FILE
          echo "CONFIG_TARGET_${{ env.TARGET }}_${{ env.SUBTARGET }}=y" >> $CONFIG_FILE
          echo "CONFIG_TARGET_${{ env.TARGET }}_${{ env.SUBTARGET }}_Default=y" >> $CONFIG_FILE
          echo "CONFIG_TARGET_SUBTARGET=\"${{ env.SUBTARGET }}\"" >> $CONFIG_FILE
          echo "CONFIG_TARGET_ARCH=\"${{ env.ARCH_NAME }}\"" >> $CONFIG_FILE
          echo "CONFIG_ARCH=\"${{ env.ARCH_NAME }}\"" >> $CONFIG_FILE
          echo "CONFIG_TARGET_PREFIX=\"${{ env.PREFIX }}-\"" >> $CONFIG_FILE
          echo "CONFIG_TARGET_OPTIMIZATION=\"-Os -pipe\"" >> $CONFIG_FILE
          echo "CONFIG_CPU_TYPE=\"${{ env.CPU_TYPE }}\"" >> $CONFIG_FILE
          echo "" >> $CONFIG_FILE
          
          echo "# 工具链配置" >> $CONFIG_FILE
          echo "CONFIG_EXTERNAL_TOOLCHAIN=n" >> $CONFIG_FILE
          echo "CONFIG_USE_MUSL=${{ env.USE_MUSL }}" >> $CONFIG_FILE
          echo "CONFIG_USE_GLIBC=${{ env.USE_GLIBC }}" >> $CONFIG_FILE
          echo "CONFIG_BUILD_TOOLCHAIN=y" >> $CONFIG_FILE
          echo "CONFIG_TOOLCHAIN_BUILD=y" >> $CONFIG_FILE
          echo "CONFIG_TOOLCHAINOPTS=y" >> $CONFIG_FILE
          echo "" >> $CONFIG_FILE
          
          echo "# 版本配置" >> $CONFIG_FILE
          echo "CONFIG_GCC_VERSION=\"${{ env.GCC_VERSION }}\"" >> $CONFIG_FILE
          echo "CONFIG_BINUTILS_VERSION=\"${{ env.BINUTILS_VERSION }}\"" >> $CONFIG_FILE
          echo "CONFIG_LINUX_VERSION=\"${{ env.LINUX_VERSION }}\"" >> $CONFIG_FILE
          
          if [ "${{ env.USE_MUSL }}" = "y" ]; then
            echo "CONFIG_MUSL_VERSION=\"${{ needs.setup.outputs.musl_version }}\"" >> $CONFIG_FILE
          else
            echo "CONFIG_GLIBC_VERSION=\"${{ needs.setup.outputs.glibc_version }}\"" >> $CONFIG_FILE
          fi
          
          echo "" >> $CONFIG_FILE
          echo "# 优化配置" >> $CONFIG_FILE
          echo "CONFIG_ALL=n" >> $CONFIG_FILE
          echo "CONFIG_ALL_KMODS=n" >> $CONFIG_FILE
          echo "CONFIG_ALL_NONSHARED=n" >> $CONFIG_FILE
          echo "CONFIG_BUILD_PACKAGES=n" >> $CONFIG_FILE
          echo "CONFIG_PACKAGE_busybox=n" >> $CONFIG_FILE
          echo "CONFIG_PACKAGE_dnsmasq=n" >> $CONFIG_FILE
          echo "CONFIG_PACKAGE_firewall=n" >> $CONFIG_FILE
          echo "CONFIG_PACKAGE_dropbear=n" >> $CONFIG_FILE
          echo "CONFIG_DEVEL=y" >> $CONFIG_FILE
          echo "CONFIG_CCACHE=n" >> $CONFIG_FILE
          echo "CONFIG_TARGET_ROOTFS_INITRAMFS=y" >> $CONFIG_FILE
          echo "CONFIG_TARGET_ROOTFS_CPIOGZ=y" >> $CONFIG_FILE
          echo "CONFIG_GCC_DEFAULT_PIE=n" >> $CONFIG_FILE
          echo "CONFIG_GCC_DEFAULT_SSP=n" >> $CONFIG_FILE
          
          echo "配置文件生成完成"
          echo "配置行数: $(wc -l < $CONFIG_FILE)"

      - name: 🔨 编译工具链
        timeout-minutes: 150
        run: |
          echo "=== 开始编译工具链 ==="
          cd "${{ env.BUILD_DIR }}"
          
          export FORCE_UNSAFE_CONFIGURE=1
          export TERM=xterm-256color
          
          echo "开始时间: $(date)"
          echo "CPU核心数: $(nproc)"
          
          echo "步骤1: 应用配置..."
          make defconfig 2>&1 | tee /tmp/defconfig.log
          
          echo "步骤2: 下载源码包..."
          mkdir -p dl
          make -j1 download V=s 2>&1 | tee /tmp/download.log
          
          echo "步骤3: 编译binutils..."
          make toolchain/binutils/compile -j$(nproc) V=s 2>&1 | tee /tmp/binutils.log
          if [ ${PIPESTATUS[0]} -ne 0 ]; then
            echo "binutils编译失败"
            grep -E "Error:|error:|failed:" /tmp/binutils.log | tail -10
            exit 1
          fi
          
          echo "步骤4: 编译gcc初始阶段..."
          make toolchain/gcc/initial/compile -j$(nproc) V=s 2>&1 | tee /tmp/gcc-initial.log
          if [ ${PIPESTATUS[0]} -ne 0 ]; then
            echo "gcc初始阶段编译失败"
            grep -E "Error:|error:|failed:" /tmp/gcc-initial.log | tail -10
            exit 1
          fi
          
          if [ "${{ env.USE_MUSL }}" = "y" ]; then
            echo "步骤5: 编译musl C库..."
            make toolchain/musl/compile -j$(nproc) V=s 2>&1 | tee /tmp/musl.log
          else
            echo "步骤5: 编译glibc C库..."
            make toolchain/glibc/compile -j$(nproc) V=s 2>&1 | tee /tmp/glibc.log
          fi
          
          echo "步骤6: 编译完整gcc..."
          make toolchain/gcc/final/compile -j$(nproc) V=s 2>&1 | tee /tmp/gcc-final.log
          if [ ${PIPESTATUS[0]} -ne 0 ]; then
            echo "gcc最终编译失败"
            grep -E "Error:|error:|failed:" /tmp/gcc-final.log | tail -10
            exit 1
          fi
          
          echo "步骤7: 安装工具链..."
          make toolchain/install -j$(nproc) V=s 2>&1 | tee /tmp/install.log
          
          echo "工具链编译完成"
          echo "结束时间: $(date)"
          echo "构建目录大小: $(du -sh . | cut -f1)"

      - name: ✅ 验证编译结果
        run: |
          echo "=== 验证编译结果 ==="
          cd "${{ env.BUILD_DIR }}"
          
          TOOLCHAIN_DIR=$(find staging_dir -maxdepth 1 -type d -name "toolchain-*" | head -1)
          
          if [ -z "$TOOLCHAIN_DIR" ] || [ ! -d "$TOOLCHAIN_DIR" ]; then
            echo "未找到工具链目录"
            exit 1
          fi
          
          echo "工具链目录: $TOOLCHAIN_DIR"
          echo "目录大小: $(du -sh "$TOOLCHAIN_DIR" | cut -f1)"
          
          COMPILER_PATH=$(find "$TOOLCHAIN_DIR/bin" -name "*gcc" | head -1)
          
          if [ -z "$COMPILER_PATH" ] || [ ! -x "$COMPILER_PATH" ]; then
            echo "未找到可执行的编译器"
            exit 1
          fi
          
          COMPILER_NAME=$(basename "$COMPILER_PATH")
          echo "找到编译器: $COMPILER_NAME"
          
          echo "编译器信息:"
          "$COMPILER_PATH" --version 2>&1 | head -2
          
          echo "测试交叉编译..."
          echo '#include <stdio.h>' > test.c
          echo 'int main() { printf("Test OK\\n"); return 0; }' >> test.c
          
          "$COMPILER_PATH" test.c -o test_program 2>&1
          
          if [ $? -eq 0 ] && [ -f "test_program" ]; then
            echo "交叉编译测试通过"
            file test_program
            rm -f test.c test_program
          else
            echo "交叉编译测试失败"
            exit 1
          fi
          
          echo "VALIDATION_PASSED=true" >> $GITHUB_ENV
          echo "TOOLCHAIN_DIR=$TOOLCHAIN_DIR" >> $GITHUB_ENV
          echo "COMPILER_PATH=$COMPILER_PATH" >> $GITHUB_ENV

      - name: 💾 保存编译器
        run: |
          echo "=== 保存编译器 ==="
          cd "${{ env.BUILD_DIR }}"
          
          mkdir -p "${{ env.OUTPUT_DIR }}"
          
          echo "复制工具链文件..."
          cp -r "$TOOLCHAIN_DIR"/* "${{ env.OUTPUT_DIR }}/" 2>/dev/null || true
          
          echo "创建元数据文件..."
          echo "{" > "${{ env.OUTPUT_DIR }}/metadata.json"
          echo "  \"architecture\": \"${{ env.ARCH_NAME }}\"," >> "${{ env.OUTPUT_DIR }}/metadata.json"
          echo "  \"libc\": \"${{ env.LIBC_SUFFIX }}\"," >> "${{ env.OUTPUT_DIR }}/metadata.json"
          echo "  \"target\": \"${{ env.TARGET }}\"," >> "${{ env.OUTPUT_DIR }}/metadata.json"
          echo "  \"subtarget\": \"${{ env.SUBTARGET }}\"," >> "${{ env.OUTPUT_DIR }}/metadata.json"
          echo "  \"compiler_prefix\": \"${{ env.PREFIX }}-\"," >> "${{ env.OUTPUT_DIR }}/metadata.json"
          echo "  \"gcc_version\": \"${{ env.GCC_VERSION }}\"," >> "${{ env.OUTPUT_DIR }}/metadata.json"
          echo "  \"binutils_version\": \"${{ env.BINUTILS_VERSION }}\"," >> "${{ env.OUTPUT_DIR }}/metadata.json"
          echo "  \"linux_version\": \"${{ env.LINUX_VERSION }}\"," >> "${{ env.OUTPUT_DIR }}/metadata.json"
          echo "  \"openwrt_version\": \"${{ env.OPENWRT_VERSION }}\"," >> "${{ env.OUTPUT_DIR }}/metadata.json"
          echo "  \"build_date\": \"$(date -Iseconds)\"," >> "${{ env.OUTPUT_DIR }}/metadata.json"
          echo "  \"workflow_run\": \"${{ github.run_id }}\"" >> "${{ env.OUTPUT_DIR }}/metadata.json"
          echo "}" >> "${{ env.OUTPUT_DIR }}/metadata.json"
          
          echo "创建使用说明..."
          echo "# ${{ env.ARCH_NAME }} ${{ env.LIBC_SUFFIX }} 交叉编译器" > "${{ env.OUTPUT_DIR }}/README.md"
          echo "" >> "${{ env.OUTPUT_DIR }}/README.md"
          echo "## 基本信息" >> "${{ env.OUTPUT_DIR }}/README.md"
          echo "- **架构**: ${{ env.ARCH_NAME }}" >> "${{ env.OUTPUT_DIR }}/README.md"
          echo "- **C库**: ${{ env.LIBC_SUFFIX }}" >> "${{ env.OUTPUT_DIR }}/README.md"
          echo "- **GCC版本**: ${{ env.GCC_VERSION }}" >> "${{ env.OUTPUT_DIR }}/README.md"
          echo "- **构建日期**: $(date)" >> "${{ env.OUTPUT_DIR }}/README.md"
          echo "" >> "${{ env.OUTPUT_DIR }}/README.md"
          echo "## 使用方法" >> "${{ env.OUTPUT_DIR }}/README.md"
          echo '```bash' >> "${{ env.OUTPUT_DIR }}/README.md"
          echo "export STAGING_DIR=\"$(pwd)\"" >> "${{ env.OUTPUT_DIR }}/README.md"
          echo 'export PATH="$STAGING_DIR/bin:$PATH"' >> "${{ env.OUTPUT_DIR }}/README.md"
          echo "${{ env.PREFIX }}-gcc --version" >> "${{ env.OUTPUT_DIR }}/README.md"
          echo '```' >> "${{ env.OUTPUT_DIR }}/README.md"
          
          echo "编译器保存完成"
          echo "输出目录: ${{ env.OUTPUT_DIR }}"
          echo "文件数量: $(find "${{ env.OUTPUT_DIR }}" -type f 2>/dev/null | wc -l)"

      - name: 📊 更新索引
        run: |
          echo "=== 更新编译器索引 ==="
          INDEX_FILE="${{ env.COMPILER_BASE_DIR }}/INDEX.md"
          
          echo "# 交叉编译器库" > "$INDEX_FILE"
          echo "" >> "$INDEX_FILE"
          echo "## 可用编译器" >> "$INDEX_FILE"
          echo "" >> "$INDEX_FILE"
          echo "| 架构 | C库 | GCC版本 | 构建时间 |" >> "$INDEX_FILE"
          echo "|------|-----|---------|----------|" >> "$INDEX_FILE"
          
          find "${{ env.COMPILER_BASE_DIR }}" -name "metadata.json" | while read meta; do
            if [ -f "$meta" ]; then
              ARCH=$(grep -o '"architecture": "[^"]*' "$meta" | cut -d'"' -f4)
              LIBC=$(grep -o '"libc": "[^"]*' "$meta" | cut -d'"' -f4)
              GCC_VER=$(grep -o '"gcc_version": "[^"]*' "$meta" | cut -d'"' -f4)
              BUILD_DATE=$(grep -o '"build_date": "[^"]*' "$meta" | cut -d'"' -f4 | cut -d'T' -f1)
              
              if [ -n "$ARCH" ] && [ -n "$LIBC" ] && [ -n "$GCC_VER" ]; then
                echo "| $ARCH | $LIBC | $GCC_VER | $BUILD_DATE |" >> "$INDEX_FILE"
              fi
            fi
          done
          
          echo "" >> "$INDEX_FILE"
          echo "## 目录结构" >> "$INDEX_FILE"
          echo '```' >> "$INDEX_FILE"
          echo "compilers/" >> "$INDEX_FILE"
          echo "├── aarch64/" >> "$INDEX_FILE"
          echo "│   ├── musl/" >> "$INDEX_FILE"
          echo "│   └── glibc/" >> "$INDEX_FILE"
          echo "├── x86_64/" >> "$INDEX_FILE"
          echo "├── mips/" >> "$INDEX_FILE"
          echo "├── mipsel/" >> "$INDEX_FILE"
          echo "├── arm_cortex-a7/" >> "$INDEX_FILE"
          echo "├── arm_cortex-a53/" >> "$INDEX_FILE"
          echo "└── riscv64/" >> "$INDEX_FILE"
          echo '```' >> "$INDEX_FILE"
          
          echo "索引更新完成"

      - name: 📤 提交到Git仓库
        run: |
          echo "=== 提交编译器到Git仓库 ==="
          cd "${{ github.workspace }}"
          
          git config user.email "github-actions@github.com"
          git config user.name "GitHub Actions"
          
          git add -f "${{ env.COMPILER_BASE_DIR }}"/*
          
          if ! git diff --cached --quiet; then
            echo "检测到变更，创建提交..."
            
            git commit -m "build: 添加 ${{ env.ARCH_NAME }}-${{ env.LIBC_SUFFIX }} 编译器" \
              -m "GCC: ${{ env.GCC_VERSION }}" \
              -m "目标: ${{ env.TARGET }}/${{ env.SUBTARGET }}" \
              -m "工作流: #${{ github.run_id }}"
            
            if git push; then
              echo "编译器已成功提交到仓库"
            else
              echo "推送失败，尝试拉取后重试"
              git pull origin main
              git push
            fi
          else
            echo "没有变更需要提交"
          fi

      - name: 📋 生成构建报告
        if: always()
        run: |
          echo "=== 构建报告 ==="
          echo "构建ID: ${{ env.BUILD_ID }}"
          echo "架构: ${{ env.ARCH_NAME }}"
          echo "C库: ${{ env.LIBC_SUFFIX }}"
          echo "GCC版本: ${{ env.GCC_VERSION }}"
          echo "目标平台: ${{ env.TARGET }}/${{ env.SUBTARGET }}"
          echo "构建状态: ${{ job.status }}"
          echo "报告时间: $(date)"

      - name: 📄 上传构建日志
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-logs-${{ env.BUILD_ID }}
          path: /tmp/*.log
          retention-days: 30
