# .github/workflows/compiler-matrix-build.yml
name: 🔧 交叉编译器构建

on:
  workflow_dispatch:
    inputs:
      target_arch:
        description: '目标架构（IPQ40xx选择arm_cortex-a7）'
        required: true
        default: 'arm_cortex-a7'
        type: choice
        options:
          - aarch64
          - x86_64
          - mips
          - mipsel
          - arm_cortex-a7
          - arm_cortex-a53
          - riscv64
      libc:
        description: 'C库类型（嵌入式推荐musl）'
        required: true
        default: 'musl'
        type: choice
        options:
          - musl
          - glibc
      gcc_version:
        description: 'GCC版本（推荐12.3.0）'
        required: true
        default: '12.3.0'
        type: string
      clean_build:
        description: '清理之前的构建缓存'
        required: true
        default: false
        type: boolean

  schedule:
    - cron: '0 2 * * 0'

env:
  OPENWRT_VERSION: "23.05.2"
  COMPILER_BASE_DIR: "${{ github.workspace }}/firmware-config/build-Compiler-file/compilers"

jobs:
  setup:
    runs-on: ubuntu-22.04
    outputs:
      gcc_version: ${{ steps.version-detection.outputs.gcc_version }}
      binutils_version: ${{ steps.version-detection.outputs.binutils_version }}
      linux_version: ${{ steps.version-detection.outputs.linux_version }}
      musl_version: ${{ steps.version-detection.outputs.musl_version }}
      glibc_version: ${{ steps.version-detection.outputs.glibc_version }}
    steps:
      - name: 🏗️ 初始化
        run: |
          echo "=== 交叉编译器构建系统 ==="
          mkdir -p "${{ env.COMPILER_BASE_DIR }}"

      - name: 🔍 版本检测
        id: version-detection
        run: |
          echo "=== 检测工具链版本 ==="
          USER_VERSION="${{ github.event.inputs.gcc_version }}"
          if [ "$USER_VERSION" = "12.3.0" ] || [ "$USER_VERSION" = "" ]; then
            GCC_VERSION="12.3.0"
          else
            GCC_VERSION="$USER_VERSION"
          fi
          BINUTILS_VERSION="2.40"
          LINUX_VERSION="5.15.133"
          MUSL_VERSION="1.2.4"
          GLIBC_VERSION="2.37"
          echo "gcc_version=$GCC_VERSION" >> $GITHUB_OUTPUT
          echo "binutils_version=$BINUTILS_VERSION" >> $GITHUB_OUTPUT
          echo "linux_version=$LINUX_VERSION" >> $GITHUB_OUTPUT
          echo "musl_version=$MUSL_VERSION" >> $GITHUB_OUTPUT
          echo "glibc_version=$GLIBC_VERSION" >> $GITHUB_OUTPUT

  build-compiler:
    needs: setup
    runs-on: ubuntu-22.04
    timeout-minutes: 180
    
    steps:
      - name: 📥 检出仓库
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: 🏗️ 设置环境
        run: |
          echo "=== 设置构建环境 ==="
          ARCH="${{ github.event.inputs.target_arch }}"
          LIBC="${{ github.event.inputs.libc }}"
          case "$ARCH" in
            arm_cortex-a7)
              TARGET="armvirt"
              SUBTARGET="armv7"
              ARCH_NAME="arm"
              ARCH_DIR="arm_cortex-a7"
              PREFIX="arm-openwrt-linux"
              CPU_TYPE="cortex-a7"
              TUNE="cortex-a7"
              ABI="32"
              FLOAT_ABI="hard"
              ;;
            aarch64)
              TARGET="armvirt"
              SUBTARGET="64"
              ARCH_NAME="aarch64"
              ARCH_DIR="aarch64"
              PREFIX="aarch64-openwrt-linux"
              CPU_TYPE="cortex-a53"
              TUNE="cortex-a53"
              ABI="64"
              FLOAT_ABI=""
              ;;
            x86_64)
              TARGET="x86"
              SUBTARGET="64"
              ARCH_NAME="x86_64"
              ARCH_DIR="x86_64"
              PREFIX="x86_64-openwrt-linux"
              CPU_TYPE="x86_64"
              TUNE="generic"
              ABI="64"
              FLOAT_ABI=""
              ;;
            mips)
              TARGET="ramips"
              SUBTARGET="mt7621"
              ARCH_NAME="mips"
              ARCH_DIR="mips"
              PREFIX="mips-openwrt-linux"
              CPU_TYPE="24kc"
              TUNE="24kc"
              ABI="32"
              FLOAT_ABI="softfloat"
              ;;
            mipsel)
              TARGET="ramips"
              SUBTARGET="mt7621"
              ARCH_NAME="mipsel"
              ARCH_DIR="mipsel"
              PREFIX="mipsel-openwrt-linux"
              CPU_TYPE="24kc"
              TUNE="24kc"
              ABI="32"
              FLOAT_ABI="softfloat"
              ;;
            arm_cortex-a53)
              TARGET="rockchip"
              SUBTARGET="armv8"
              ARCH_NAME="aarch64"
              ARCH_DIR="arm_cortex-a53"
              PREFIX="aarch64-openwrt-linux"
              CPU_TYPE="cortex-a53"
              TUNE="cortex-a53"
              ABI="64"
              FLOAT_ABI=""
              ;;
            riscv64)
              TARGET="virt"
              SUBTARGET="rv64"
              ARCH_NAME="riscv64"
              ARCH_DIR="riscv64"
              PREFIX="riscv64-openwrt-linux"
              CPU_TYPE="generic"
              TUNE="generic"
              ABI="lp64d"
              FLOAT_ABI="hard"
              ;;
            *)
              echo "不支持的架构: $ARCH"
              exit 1
              ;;
          esac
          if [ "$LIBC" = "musl" ]; then
            PREFIX="${PREFIX}-musl"
            LIBC_SUFFIX="musl"
            LIBC_DIR="musl"
            USE_MUSL="y"
            USE_GLIBC="n"
          else
            PREFIX="${PREFIX}-gnu"
            LIBC_SUFFIX="glibc"
            LIBC_DIR="glibc"
            USE_MUSL="n"
            USE_GLIBC="y"
          fi
          GCC_VERSION='${{ needs.setup.outputs.gcc_version }}'
          BINUTILS_VERSION='${{ needs.setup.outputs.binutils_version }}'
          LINUX_VERSION='${{ needs.setup.outputs.linux_version }}'
          BUILD_ID="${ARCH_DIR}_${LIBC_DIR}_gcc-${GCC_VERSION}_$(date +%Y%m%d_%H%M%S)"
          BUILD_DIR="/tmp/compiler-build-${BUILD_ID}"
          OUTPUT_DIR="${{ env.COMPILER_BASE_DIR }}/${ARCH_DIR}/${LIBC_DIR}/gcc-${GCC_VERSION}"
          echo "BUILD_DIR=$BUILD_DIR" >> $GITHUB_ENV
          echo "OUTPUT_DIR=$OUTPUT_DIR" >> $GITHUB_ENV
          echo "TARGET=$TARGET" >> $GITHUB_ENV
          echo "SUBTARGET=$SUBTARGET" >> $GITHUB_ENV
          echo "ARCH_NAME=$ARCH_NAME" >> $GITHUB_ENV
          echo "ARCH_DIR=$ARCH_DIR" >> $GITHUB_ENV
          echo "LIBC_DIR=$LIBC_DIR" >> $GITHUB_ENV
          echo "PREFIX=$PREFIX" >> $GITHUB_ENV
          echo "CPU_TYPE=$CPU_TYPE" >> $GITHUB_ENV
          echo "TUNE=$TUNE" >> $GITHUB_ENV
          echo "ABI=$ABI" >> $GITHUB_ENV
          echo "FLOAT_ABI=$FLOAT_ABI" >> $GITHUB_ENV
          echo "USE_MUSL=$USE_MUSL" >> $GITHUB_ENV
          echo "USE_GLIBC=$USE_GLIBC" >> $GITHUB_ENV
          echo "GCC_VERSION=$GCC_VERSION" >> $GITHUB_ENV
          echo "BINUTILS_VERSION=$BINUTILS_VERSION" >> $GITHUB_ENV
          echo "LINUX_VERSION=$LINUX_VERSION" >> $GITHUB_ENV
          echo "LIBC_SUFFIX=$LIBC_SUFFIX" >> $GITHUB_ENV
          echo "BUILD_ID=$BUILD_ID" >> $GITHUB_ENV

      - name: 🔧 安装依赖
        run: |
          echo "=== 安装构建依赖 ==="
          sudo apt-get update
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y build-essential gcc g++ make flex bison libgmp-dev libmpfr-dev libmpc-dev libisl-dev python3 python3-dev git wget curl zlib1g-dev liblzma-dev file gawk gettext libncurses5-dev libssl-dev python3-distutils rsync unzip ncurses-term ncurses-base libncursesw5 libtinfo5 terminfo gnutls-bin libgnutls28-dev libcurl4-gnutls-dev autoconf automake autopoint libtool pkg-config texinfo gperf cmake ninja-build patchutils bc bzip2 xz-utils help2man libexpat-dev libgdbm-dev libreadline-dev libsqlite3-dev libffi-dev lzma-dev zstd libzstd-dev libelf-dev libdw-dev libcap-dev libattr1-dev libaudit-dev libiberty-dev jq info install-info

      - name: 🗑️ 清理构建缓存
        if: ${{ github.event.inputs.clean_build == 'true' }}
        run: |
          echo "=== 清理构建缓存 ==="
          sudo rm -rf /tmp/compiler-build-* 2>/dev/null || true
          echo "清理完成"

      - name: 📥 下载OpenWrt源码
        run: |
          echo "=== 获取OpenWrt源码 ==="
          mkdir -p "${{ env.BUILD_DIR }}"
          cd "${{ env.BUILD_DIR }}"
          echo "克隆OpenWrt ${{ env.OPENWRT_VERSION }} 分支..."
          git clone --depth 1 --branch v${{ env.OPENWRT_VERSION }} https://github.com/openwrt/openwrt.git .
          if [ $? -ne 0 ]; then
            echo "Git clone失败，尝试使用tar包..."
            wget -q --show-progress -O openwrt.tar.xz https://github.com/openwrt/openwrt/archive/refs/tags/v${{ env.OPENWRT_VERSION }}.tar.gz
            tar -xzf openwrt.tar.xz --strip-components=1
            rm -f openwrt.tar.xz
            echo "使用tar包下载成功"
          fi

      - name: 📝 生成配置文件
        run: |
          echo "=== 生成OpenWrt配置 ==="
          cd "${{ env.BUILD_DIR }}"
          mkdir -p include/site
          echo "ac_cv_sys_file_offset_bits=64" > include/site/${{ env.ARCH_NAME }}
          echo "ac_cv_sys_large_files=yes" >> include/site/${{ env.ARCH_NAME }}
          echo "ac_cv_c_bigendian=no" >> include/site/${{ env.ARCH_NAME }}
          CONFIG_FILE=".config"
          echo "# 目标配置" > $CONFIG_FILE
          echo "CONFIG_TARGET_${{ env.TARGET }}=y" >> $CONFIG_FILE
          echo "CONFIG_TARGET_${{ env.TARGET }}_${{ env.SUBTARGET }}=y" >> $CONFIG_FILE
          echo "CONFIG_TARGET_ARCH=\"${{ env.ARCH_NAME }}\"" >> $CONFIG_FILE
          echo "CONFIG_TARGET_PREFIX=\"${{ env.PREFIX }}-\"" >> $CONFIG_FILE
          echo "CONFIG_EXTERNAL_TOOLCHAIN=n" >> $CONFIG_FILE
          echo "CONFIG_USE_MUSL=${{ env.USE_MUSL }}" >> $CONFIG_FILE
          echo "CONFIG_BUILD_TOOLCHAIN=y" >> $CONFIG_FILE
          echo "CONFIG_GCC_VERSION=\"${{ env.GCC_VERSION }}\"" >> $CONFIG_FILE
          echo "CONFIG_BINUTILS_VERSION=\"${{ env.BINUTILS_VERSION }}\"" >> $CONFIG_FILE
          echo "CONFIG_LINUX_VERSION=\"${{ env.LINUX_VERSION }}\"" >> $CONFIG_FILE
          echo "CONFIG_ALL=n" >> $CONFIG_FILE
          echo "CONFIG_ALL_KMODS=n" >> $CONFIG_FILE
          echo "CONFIG_ALL_NONSHARED=n" >> $CONFIG_FILE
          echo "CONFIG_DEVEL=y" >> $CONFIG_FILE
          echo "CONFIG_CCACHE=n" >> $CONFIG_FILE
          echo "CONFIG_TARGET_ROOTFS_INITRAMFS=y" >> $CONFIG_FILE
          if [ "${{ env.USE_MUSL }}" = "y" ]; then
            echo "CONFIG_MUSL_VERSION=\"${{ needs.setup.outputs.musl_version }}\"" >> $CONFIG_FILE
          else
            echo "CONFIG_GLIBC_VERSION=\"${{ needs.setup.outputs.glibc_version }}\"" >> $CONFIG_FILE
          fi
          echo "配置文件生成完成"

      - name: 🔨 编译工具链（修复版）
        timeout-minutes: 150
        run: |
          echo "=== 开始编译工具链 ==="
          cd "${{ env.BUILD_DIR }}"
          export FORCE_UNSAFE_CONFIGURE=1
          export TERM=xterm-256color
          echo "开始时间: $(date)"
          echo "CPU核心数: $(nproc)"
          echo "步骤1: 修复环境问题..."
          mkdir -p staging_dir/host/bin
          mkdir -p staging_dir/host/lib
          mkdir -p staging_dir/host/include
          echo "步骤2: 应用配置..."
          make defconfig
          echo "步骤3: 修复GMP编译问题..."
          export ABI=64
          export CC="gcc"
          export CXX="g++"
          echo "步骤4: 下载源码包..."
          mkdir -p dl
          make -j1 download V=s 2>&1 | tee /tmp/download.log
          echo "步骤5: 直接编译交叉工具链..."
          make toolchain/compile V=s 2>&1 | tee /tmp/compile.log
          echo "步骤6: 安装工具链..."
          make toolchain/install V=s 2>&1 | tee /tmp/install.log
          echo "✅ 工具链构建完成"
          echo "结束时间: $(date)"

      - name: ✅ 检查构建结果
        run: |
          echo "=== 检查构建结果 ==="
          cd "${{ env.BUILD_DIR }}"
          echo "查找工具链..."
          TOOLCHAIN_DIR=$(find staging_dir -name "toolchain-*" -type d 2>/dev/null | head -1)
          if [ -n "$TOOLCHAIN_DIR" ] && [ -d "$TOOLCHAIN_DIR" ]; then
            echo "找到工具链目录: $TOOLCHAIN_DIR"
            COMPILER_PATH=""
            for search_path in "$TOOLCHAIN_DIR/bin" "$TOOLCHAIN_DIR"; do
              if [ -d "$search_path" ]; then
                found=$(find "$search_path" -name "*gcc" -type f 2>/dev/null | head -1)
                if [ -n "$found" ]; then
                  COMPILER_PATH="$found"
                  break
                fi
              fi
            done
            if [ -n "$COMPILER_PATH" ] && [ -x "$COMPILER_PATH" ]; then
              echo "✅ 找到编译器: $(basename $COMPILER_PATH)"
              echo "路径: $COMPILER_PATH"
              echo "版本:"
              "$COMPILER_PATH" --version 2>&1 | head -2
              echo "TOOLCHAIN_DIR=$TOOLCHAIN_DIR" >> $GITHUB_ENV
              echo "COMPILER_PATH=$COMPILER_PATH" >> $GITHUB_ENV
              echo "BUILD_SUCCESS=true" >> $GITHUB_ENV
            else
              echo "⚠️ 工具链目录存在，但未找到编译器"
              echo "BUILD_SUCCESS=false" >> $GITHUB_ENV
            fi
          else
            echo "❌ 未找到工具链目录"
            echo "BUILD_SUCCESS=false" >> $GITHUB_ENV
          fi

      - name: 💾 保存编译器
        if: env.BUILD_SUCCESS == 'true'
        run: |
          echo "=== 保存编译器 ==="
          cd "${{ env.BUILD_DIR }}"
          mkdir -p "${{ env.OUTPUT_DIR }}"
          echo "复制工具链文件..."
          if [ -d "$TOOLCHAIN_DIR" ]; then
            mkdir -p "${{ env.OUTPUT_DIR }}/bin"
            mkdir -p "${{ env.OUTPUT_DIR }}/lib"
            mkdir -p "${{ env.OUTPUT_DIR }}/include"
            if [ -d "$TOOLCHAIN_DIR/bin" ]; then
              cp -r "$TOOLCHAIN_DIR/bin"/* "${{ env.OUTPUT_DIR }}/bin/" 2>/dev/null || true
            fi
            if [ -d "$TOOLCHAIN_DIR/lib" ]; then
              cp -r "$TOOLCHAIN_DIR/lib"/* "${{ env.OUTPUT_DIR }}/lib/" 2>/dev/null || true
            fi
            if [ -d "$TOOLCHAIN_DIR/include" ]; then
              cp -r "$TOOLCHAIN_DIR/include"/* "${{ env.OUTPUT_DIR }}/include/" 2>/dev/null || true
            fi
            for dir in libexec share; do
              if [ -d "$TOOLCHAIN_DIR/$dir" ]; then
                mkdir -p "${{ env.OUTPUT_DIR }}/$dir"
                cp -r "$TOOLCHAIN_DIR/$dir"/* "${{ env.OUTPUT_DIR }}/$dir/" 2>/dev/null || true
              fi
            done
          fi
          echo "创建说明文件..."
          echo "# ${{ env.ARCH_DIR }} ${{ env.LIBC_DIR }} 交叉编译器" > "${{ env.OUTPUT_DIR }}/README.md"
          echo "" >> "${{ env.OUTPUT_DIR }}/README.md"
          echo "## 基本信息" >> "${{ env.OUTPUT_DIR }}/README.md"
          echo "- 架构: ${{ env.ARCH_DIR }}" >> "${{ env.OUTPUT_DIR }}/README.md"
          echo "- C库: ${{ env.LIBC_DIR }}" >> "${{ env.OUTPUT_DIR }}/README.md"
          echo "- GCC版本: ${{ env.GCC_VERSION }}" >> "${{ env.OUTPUT_DIR }}/README.md"
          echo "- 编译器前缀: ${{ env.PREFIX }}-" >> "${{ env.OUTPUT_DIR }}/README.md"
          echo "- 构建时间: $(date)" >> "${{ env.OUTPUT_DIR }}/README.md"
          echo "" >> "${{ env.OUTPUT_DIR }}/README.md"
          echo "## 使用方法" >> "${{ env.OUTPUT_DIR }}/README.md"
          echo '```bash' >> "${{ env.OUTPUT_DIR }}/README.md"
          echo "# 设置环境变量" >> "${{ env.OUTPUT_DIR }}/README.md"
          echo "export STAGING_DIR=\"$(pwd)\"" >> "${{ env.OUTPUT_DIR }}/README.md"
          echo 'export PATH="$STAGING_DIR/bin:$PATH"' >> "${{ env.OUTPUT_DIR }}/README.md"
          echo "" >> "${{ env.OUTPUT_DIR }}/README.md"
          echo "# 验证编译器" >> "${{ env.OUTPUT_DIR }}/README.md"
          echo "${{ env.PREFIX }}-gcc --version" >> "${{ env.OUTPUT_DIR }}/README.md"
          echo '```' >> "${{ env.OUTPUT_DIR }}/README.md"
          echo "编译器保存完成"
          echo "输出目录: ${{ env.OUTPUT_DIR }}"

      - name: ⚠️ 构建失败处理
        if: env.BUILD_SUCCESS != 'true'
        run: |
          echo "=== 构建失败 ==="
          echo "工具链构建失败，创建错误报告..."
          ERROR_DIR="${{ env.COMPILER_BASE_DIR }}/errors/${{ env.BUILD_ID }}"
          mkdir -p "$ERROR_DIR"
          echo "构建ID: ${{ env.BUILD_ID }}" > "$ERROR_DIR/error.log"
          echo "架构: ${{ env.ARCH_DIR }}" >> "$ERROR_DIR/error.log"
          echo "C库: ${{ env.LIBC_DIR }}" >> "$ERROR_DIR/error.log"
          echo "时间: $(date)" >> "$ERROR_DIR/error.log"
          echo "错误: 工具链构建失败" >> "$ERROR_DIR/error.log"
          echo "构建失败，请检查依赖或尝试其他架构"

      - name: 📤 提交到Git仓库（修复Git冲突）
        if: env.BUILD_SUCCESS == 'true'
        run: |
          echo "=== 提交编译器到Git仓库 ==="
          cd "${{ github.workspace }}"
          
          # 修复：配置Git避免分支冲突
          git config user.email "github-actions@github.com"
          git config user.name "GitHub Actions"
          git config pull.rebase false
          
          # 先拉取最新代码
          echo "拉取最新代码..."
          git pull origin main --no-edit
          
          # 添加编译器文件
          echo "添加编译器文件..."
          git add -f "firmware-config/build-Compiler-file/compilers/*"
          
          if ! git diff --cached --quiet; then
            echo "检测到变更，创建提交..."
            
            # 创建提交
            git commit -m "build(compiler): 添加 ${{ env.ARCH_DIR }}-${{ env.LIBC_DIR }} 交叉编译器" -m "GCC: ${{ env.GCC_VERSION }}" -m "目标: ${{ env.TARGET }}/${{ env.SUBTARGET }}" -m "工作流: #${{ github.run_id }}"
            
            # 推送
            echo "推送到仓库..."
            if git push origin main; then
              echo "✅ 编译器已成功提交到仓库"
            else
              echo "⚠️ 推送失败，重新尝试..."
              git pull origin main --rebase
              git push origin main
            fi
          else
            echo "没有变更需要提交"
          fi

      - name: 📄 上传构建日志
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-logs-${{ env.BUILD_ID }}
          path: /tmp/*.log
          retention-days: 30

  cleanup:
    needs: build-compiler
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: 📋 生成构建报告
        run: |
          echo "=== 构建报告 ==="
          echo "构建ID: ${{ needs.build-compiler.outputs.BUILD_ID || 'N/A' }}"
          echo "架构: ${{ needs.build-compiler.outputs.ARCH_DIR || 'N/A' }}"
          echo "C库: ${{ needs.build-compiler.outputs.LIBC_DIR || 'N/A' }}"
          echo "GCC版本: ${{ needs.build-compiler.outputs.GCC_VERSION || 'N/A' }}"
          echo "构建状态: ${{ needs.build-compiler.result }}"
          echo "报告时间: $(date)"
