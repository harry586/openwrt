# .github/workflows/compiler-build.yml
name: 🔍 编译器构建（ARM64版本）

on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * 0'

env:
  COMPILER_DIR: "${{ github.workspace }}/firmware-config/build-Compiler-file/compiled"

jobs:
  build-compiler:
    runs-on: ubuntu-22.04
    timeout-minutes: 180
    
    steps:
      # 1. 检出仓库
      - name: 📥 检出仓库
        uses: actions/checkout@v4
      
      # 2. 设置版本信息和构建目录
      - name: 🔍 设置版本和目录
        run: |
          echo "=== 编译器版本配置 ==="
          echo "GCC_VERSION=11.3.0" >> $GITHUB_ENV
          echo "BINUTILS_VERSION=2.38" >> $GITHUB_ENV
          echo "LINUX_VERSION=5.15.133" >> $GITHUB_ENV
          echo "MUSL_VERSION=1.2.4" >> $GITHUB_ENV
          BUILD_TIMESTAMP=$(date +%s)
          BUILD_DIR="/mnt/compiler-build-${BUILD_TIMESTAMP}"
          echo "BUILD_DIR=${BUILD_DIR}" >> $GITHUB_ENV
          echo "📦 版本信息:"
          echo "├─ GCC: 11.3.0"
          echo "├─ Binutils: 2.38"
          echo "├─ Linux Kernel: 5.15.133"
          echo "└─ Musl: 1.2.4"
          echo "📁 构建目录: ${BUILD_DIR}"
          echo "🎯 目标架构: ARM64 (aarch64)"
      
      # 3. 显示构建信息
      - name: 📋 显示信息
        run: |
          echo "=== 构建配置信息 ==="
          echo "构建目录: ${{ env.BUILD_DIR }}"
          echo "编译器输出: ${{ env.COMPILER_DIR }}"
          echo ""
          echo "🖥️ 系统信息:"
          echo "当前用户: $(whoami)"
          echo "用户ID: $(id -u)"
          echo "组ID: $(id -g)"
          echo "工作目录: $(pwd)"
          echo "CPU: $(nproc) 核心"
          echo "内存: $(free -h | grep Mem | awk '{print $2}')"
          echo "/mnt 磁盘信息:"
          df -h /mnt || echo "无法访问 /mnt"
          echo ""
          echo "📅 开始时间: $(date)"
      
      # 4. 准备环境（修复网络和终端问题）
      - name: 🏗️ 准备环境
        run: |
          echo "=== 准备构建环境 ==="
          echo "[1/10] 检查 /mnt 状态..."
          if [ ! -d "/mnt" ]; then
            echo "创建 /mnt 目录..."
            sudo mkdir -p /mnt
            sudo chmod 755 /mnt
          fi
          echo "[2/10] 创建构建目录..."
          echo "创建目录: ${{ env.BUILD_DIR }}"
          sudo mkdir -p "${{ env.BUILD_DIR }}"
          sudo chown -R $USER:$USER "${{ env.BUILD_DIR }}"
          sudo chmod 755 "${{ env.BUILD_DIR }}"
          echo "✅ 目录创建完成"
          ls -ld "${{ env.BUILD_DIR }}"
          
          echo "[3/10] 安装编译依赖..."
          sudo apt-get update
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y build-essential gcc g++ make flex bison libgmp-dev libmpfr-dev libmpc-dev libisl-dev python3 python3-dev git wget curl zlib1g-dev liblzma-dev file gawk gettext libncurses5-dev libssl-dev python3-distutils rsync unzip ncurses-term ncurses-base libncursesw5 libtinfo5 terminfo gnutls-bin libgnutls28-dev
          
          echo "[4/10] 修复网络和Git配置..."
          git config --global http.postBuffer 524288000
          git config --global http.lowSpeedLimit 0
          git config --global http.lowSpeedTime 999999
          git config --global core.compression 0
          git config --global core.looseCompression 0
          git config --global http.sslVerify false
          git config --global http.sslBackend "openssl"
          
          export GIT_SSL_NO_VERIFY=1
          export CURL_SSL_NO_VERIFY=1
          export PYTHONHTTPSVERIFY=0
          echo "GIT_SSL_NO_VERIFY=1" >> $GITHUB_ENV
          echo "CURL_SSL_NO_VERIFY=1" >> $GITHUB_ENV
          echo "PYTHONHTTPSVERIFY=0" >> $GITHUB_ENV
          
          echo "[5/10] 修复终端环境..."
          sudo apt-get install -y ncurses-term ncurses-base libncursesw5 libtinfo5 terminfo
          export TERM=xterm-256color
          export TERMINFO=/usr/share/terminfo
          sudo mkdir -p /usr/share/terminfo/x
          if [ -f "/lib/terminfo/x/xterm" ]; then
            sudo ln -sf /lib/terminfo/x/xterm /usr/share/terminfo/x/xterm-256color 2>/dev/null || true
          fi
          echo "TERM=$TERM" >> $GITHUB_ENV
          echo "TERMINFO=$TERMINFO" >> $GITHUB_ENV
          
          echo "[6/10] 设置安全配置..."
          export FORCE_UNSAFE_CONFIGURE=1
          echo "FORCE_UNSAFE_CONFIGURE=1" >> $GITHUB_ENV
          
          echo "[7/10] 验证工具..."
          echo "关键工具检查:"
          for tool in gcc g++ make flex bison python3 git wget curl; do
            if command -v $tool >/dev/null; then
              echo "  ✅ $tool"
            else
              echo "  ❌ $tool: 未找到"
            fi
          done
          
          echo "[8/10] 使用GitHub加速镜像..."
          
          echo "[9/10] 清理可能的缓存..."
          sudo rm -rf /tmp/*openwrt* /tmp/*compiler* 2>/dev/null || true
          
          echo "[10/10] 环境检查完成..."
          echo "✅ 环境准备完成"
      
      # 5. 获取源码和配置（修复架构问题）
      - name: 📥 获取源码
        run: |
          echo "=== 获取OpenWrt源码 ==="
          if [ ! -d "${{ env.BUILD_DIR }}" ]; then
            echo "❌ 错误: 构建目录不存在"
            exit 1
          fi
          cd "${{ env.BUILD_DIR }}"
          echo "当前目录: $(pwd)"
          
          echo "[1/5] 克隆OpenWrt源码（使用浅克隆避免网络问题）..."
          git init
          git remote add origin https://github.com/openwrt/openwrt.git
          git fetch --depth 1 origin v23.05.2
          git checkout FETCH_HEAD
          
          echo "[2/5] 简化feeds配置..."
          echo "src-git packages https://github.com/openwrt/packages.git;v23.05.2" > feeds.conf.default
          echo "# 注释掉可能出问题的feeds" >> feeds.conf.default
          echo "# src-git luci https://github.com/openwrt/luci.git;v23.05.2" >> feeds.conf.default
          
          echo "[3/5] 创建ARM64架构配置..."
          echo "# OpenWrt ARM64 交叉编译器构建配置" > .config
          echo "# 生成时间: $(date)" >> .config
          echo "" >> .config
          echo "# 目标平台: ARM64 (aarch64)" >> .config
          echo "CONFIG_TARGET_armvirt=y" >> .config
          echo "CONFIG_TARGET_armvirt_64=y" >> .config
          echo "CONFIG_TARGET_armvirt_64_Default=y" >> .config
          echo "" >> .config
          echo "# 文件系统" >> .config
          echo "CONFIG_TARGET_ROOTFS_INITRAMFS=y" >> .config
          echo "CONFIG_TARGET_ROOTFS_SQUASHFS=n" >> .config
          echo "" >> .config
          echo "# 工具链配置" >> .config
          echo "CONFIG_EXTERNAL_TOOLCHAIN=n" >> .config
          echo "CONFIG_USE_MUSL=y" >> .config
          echo "CONFIG_BUILD_TOOLCHAIN=y" >> .config
          echo "" >> .config
          echo "# 版本配置" >> .config
          echo 'CONFIG_GCC_VERSION="11.3.0"' >> .config
          echo 'CONFIG_BINUTILS_VERSION="2.38"' >> .config
          echo 'CONFIG_LINUX_VERSION="5.15.133"' >> .config
          echo 'CONFIG_MUSL_VERSION="1.2.4"' >> .config
          echo "" >> .config
          echo "# 简化配置，只构建工具链" >> .config
          echo "CONFIG_ALL_NONSHARED=n" >> .config
          echo "CONFIG_ALL_KMODS=n" >> .config
          echo "CONFIG_ALL=n" >> .config
          echo "" >> .config
          echo "# 跳过某些检查" >> .config
          echo "CONFIG_DEVEL=y" >> .config
          echo "CONFIG_CCACHE=n" >> .config
          echo "CONFIG_SIGNED_PACKAGES=n" >> .config
          echo "" >> .config
          echo "# 确保工具链构建" >> .config
          echo "CONFIG_TOOLCHAIN=y" >> .config
          echo "CONFIG_TOOLCHAIN_BUILD=y" >> .config
          echo "" >> .config
          echo "# 禁用所有包" >> .config
          echo "CONFIG_PACKAGE_busybox=n" >> .config
          echo "CONFIG_PACKAGE_dnsmasq=n" >> .config
          echo "CONFIG_PACKAGE_firewall=n" >> .config
          echo "CONFIG_PACKAGE_dropbear=n" >> .config
          echo "CONFIG_PACKAGE_iptables=n" >> .config
          
          echo "[4/5] 配置验证..."
          echo "配置摘要:"
          echo "目标架构: ARM64 (aarch64)"
          echo "C库: musl"
          echo "GCC版本: 11.3.0"
          echo "配置文件大小: $(wc -l < .config) 行"
          
          echo "[5/5] 显示关键配置..."
          grep -E "CONFIG_TARGET|CONFIG_GCC|CONFIG_USE_MUSL" .config
          echo "✅ 源码配置完成"
      
      # 6. 编译工具链（简化流程，避免feeds问题）
      - name: 🔨 编译工具链
        timeout-minutes: 180
        run: |
          echo "=== 开始编译工具链 ==="
          if [ ! -d "${{ env.BUILD_DIR }}" ]; then
            echo "❌ 错误: 构建目录不存在"
            exit 1
          fi
          cd "${{ env.BUILD_DIR }}"
          echo "工作目录: $(pwd)"
          echo "开始时间: $(date)"
          
          export FORCE_UNSAFE_CONFIGURE=1
          export TERM=xterm-256color
          export TERMINFO=/usr/share/terminfo
          
          echo "=== 步骤1: 跳过feeds更新 ==="
          echo "时间: $(date)"
          mkdir -p feeds/luci feeds/packages
          echo "✅ 跳过feeds更新"
          
          echo "=== 步骤2: 应用配置 ==="
          echo "时间: $(date)"
          make defconfig 2>&1 | tee /tmp/defconfig.log
          
          if [ ${PIPESTATUS[0]} -ne 0 ]; then
            echo "⚠️ defconfig有警告，继续..."
          fi
          
          echo "CONFIG_TARGET_armvirt=y" >> .config
          echo "CONFIG_TARGET_armvirt_64=y" >> .config
          echo "CONFIG_TARGET_armvirt_64_Default=y" >> .config
          echo "CONFIG_ARCH=\"arm64\"" >> .config
          
          echo "=== 步骤3: 下载源码包 ==="
          echo "时间: $(date)"
          mkdir -p dl
          
          echo "下载关键源码包..."
          make -j1 download V=s 2>&1 | tee /tmp/download.log
          DOWNLOAD_STATUS=${PIPESTATUS[0]}
          
          if [ $DOWNLOAD_STATUS -ne 0 ]; then
            echo "⚠️ 下载有警告，检查关键包..."
            if [ ! -f "dl/gcc-11.3.0.tar.xz" ]; then
              echo "手动下载gcc..."
              wget --no-check-certificate -O dl/gcc-11.3.0.tar.xz https://ftp.gnu.org/gnu/gcc/gcc-11.3.0/gcc-11.3.0.tar.xz || true
            fi
            if [ ! -f "dl/binutils-2.38.tar.xz" ]; then
              echo "手动下载binutils..."
              wget --no-check-certificate -O dl/binutils-2.38.tar.xz https://ftp.gnu.org/gnu/binutils/binutils-2.38.tar.xz || true
            fi
          fi
          
          echo "=== 步骤4: 构建工具链（简化步骤） ==="
          echo "时间: $(date)"
          
          echo "构建工具链..."
          make toolchain/compile -j$(nproc) V=s 2>&1 | tee /tmp/toolchain-compile.log
          TOOLCHAIN_STATUS=${PIPESTATUS[0]}
          
          if [ $TOOLCHAIN_STATUS -ne 0 ]; then
            echo "❌ 工具链构建失败"
            echo "错误摘要:"
            grep -E "Error|ERROR|Failed|undefined|No such file" /tmp/toolchain-compile.log | head -30
            echo "尝试分步骤构建..."
            
            echo "步骤4.1: 构建binutils..."
            make toolchain/binutils/compile -j$(nproc) V=s 2>&1 | tee /tmp/binutils.log
            
            echo "步骤4.2: 构建gcc初始阶段..."
            make toolchain/gcc/initial/compile -j$(nproc) V=s 2>&1 | tee /tmp/gcc-initial.log
            
            echo "步骤4.3: 构建musl..."
            make toolchain/musl/compile -j$(nproc) V=s 2>&1 | tee /tmp/musl.log
            
            echo "步骤4.4: 构建完整gcc..."
            make toolchain/gcc/final/compile -j$(nproc) V=s 2>&1 | tee /tmp/gcc-final.log
          fi
          
          echo "=== 步骤5: 安装工具链 ==="
          echo "时间: $(date)"
          make toolchain/install -j$(nproc) V=s 2>&1 | tee /tmp/install.log
          
          echo ""
          echo "🎉 工具链构建完成！"
          echo "完成时间: $(date)"
          echo "构建目录大小: $(du -sh . | cut -f1)"
      
      # 7. 验证结果（修复ARM64检查）
      - name: ✅ 验证结果
        run: |
          echo "=== 验证编译结果 ==="
          if [ ! -d "${{ env.BUILD_DIR }}" ]; then
            echo "❌ 错误: 构建目录不存在"
            exit 1
          fi
          cd "${{ env.BUILD_DIR }}"
          echo "[1/6] 查找工具链目录..."
          TOOLCHAIN_DIR=$(find staging_dir -maxdepth 1 -type d -name "toolchain-*" 2>/dev/null | head -1)
          if [ -z "$TOOLCHAIN_DIR" ] || [ ! -d "$TOOLCHAIN_DIR" ]; then
            echo "❌ 未找到工具链目录"
            echo "staging_dir内容:"
            ls -la staging_dir/ 2>/dev/null || echo "staging_dir不存在"
            exit 1
          fi
          echo "✅ 工具链目录: $TOOLCHAIN_DIR"
          echo "目录大小: $(du -sh "$TOOLCHAIN_DIR" 2>/dev/null | cut -f1)"
          
          echo "[2/6] 检查目标架构..."
          if [[ "$TOOLCHAIN_DIR" == *"aarch64"* ]] || [[ "$TOOLCHAIN_DIR" == *"arm64"* ]]; then
            echo "✅ 检测到ARM64架构"
            TARGET_ARCH="aarch64"
          elif [[ "$TOOLCHAIN_DIR" == *"mips"* ]]; then
            echo "⚠️ 检测到MIPS架构，配置错误"
            TARGET_ARCH="mips"
            echo "尝试查找ARM64编译器..."
          else
            echo "⚠️ 未知架构: $TOOLCHAIN_DIR"
            TARGET_ARCH="unknown"
          fi
          
          echo "[3/6] 检查编译器..."
          echo "查找所有gcc编译器..."
          find "$TOOLCHAIN_DIR/bin" -name "*gcc" -type f 2>/dev/null | while read f; do
            echo "  - $(basename "$f")"
          done
          
          GCC_PATH=$(find "$TOOLCHAIN_DIR/bin" -name "*aarch64*gcc" -type f 2>/dev/null | head -1)
          if [ -z "$GCC_PATH" ]; then
            echo "未找到aarch64-gcc，查找其他ARM编译器..."
            GCC_PATH=$(find "$TOOLCHAIN_DIR/bin" -name "*arm*gcc" -type f 2>/dev/null | head -1)
          fi
          
          if [ -n "$GCC_PATH" ] && [ -x "$GCC_PATH" ]; then
            echo "✅ 找到编译器: $(basename "$GCC_PATH")"
            echo "完整路径: $GCC_PATH"
            echo "版本信息:"
            $GCC_PATH --version 2>&1 | head -1
            TARGET_TRIPLET=$($GCC_PATH -dumpmachine 2>/dev/null || basename "$GCC_PATH" | sed 's/-gcc//')
            echo "目标三元组: $TARGET_TRIPLET"
          else
            echo "❌ 未找到可用的编译器"
            GCC_PATH=$(find "$TOOLCHAIN_DIR/bin" -name "*gcc" -type f 2>/dev/null | head -1)
            if [ -n "$GCC_PATH" ] && [ -x "$GCC_PATH" ]; then
              echo "找到备用编译器: $(basename "$GCC_PATH")"
              $GCC_PATH --version 2>&1 | head -1
              TARGET_TRIPLET=$($GCC_PATH -dumpmachine 2>/dev/null || basename "$GCC_PATH" | sed 's/-gcc//')
              echo "目标三元组: $TARGET_TRIPLET"
            else
              echo "❌ 没有可用的编译器"
              exit 1
            fi
          fi
          
          echo "[4/6] 检查关键工具..."
          TOOL_PREFIX=$(basename "$GCC_PATH" | sed 's/gcc$//')
          for tool in gcc g++ ld ar strip; do
            file="$TOOLCHAIN_DIR/bin/${TOOL_PREFIX}${tool}"
            if [ -x "$file" ]; then
              echo "  ✅ $tool: $(basename "$file")"
            else
              echo "  ⚠️ $tool: 未找到或不可执行"
            fi
          done
          
          echo "[5/6] 测试交叉编译..."
          echo '#include <stdio.h>' > test-cross.c
          echo 'int main() {' >> test-cross.c
          echo '    printf("Cross-compiler test OK\\n");' >> test-cross.c
          echo '    return 0;' >> test-cross.c
          echo '}' >> test-cross.c
          
          if $GCC_PATH test-cross.c -o test-cross 2>/tmp/compile-test.log; then
            echo "✅ 交叉编译测试通过"
            echo "生成文件类型:"
            file test-cross 2>/dev/null || echo "无法识别文件"
            rm -f test-cross test-cross.c
          else
            echo "⚠️ 交叉编译测试失败"
            cat /tmp/compile-test.log
          fi
          
          echo "[6/6] 保存架构信息..."
          echo "TARGET_ARCH=$TARGET_ARCH" >> $GITHUB_ENV
          echo "TARGET_TRIPLET=$TARGET_TRIPLET" >> $GITHUB_ENV
          echo "GCC_PATH=$GCC_PATH" >> $GITHUB_ENV
          echo "✅ 验证完成"
      
      # 8. 保存到仓库
      - name: 💾 保存编译器
        run: |
          echo "=== 保存编译器到仓库 ==="
          if [ ! -d "${{ env.BUILD_DIR }}" ]; then
            echo "❌ 错误: 构建目录不存在"
            exit 1
          fi
          cd "${{ env.BUILD_DIR }}"
          TOOLCHAIN_DIR=$(find staging_dir -maxdepth 1 -type d -name "toolchain-*" 2>/dev/null | head -1)
          if [ ! -d "$TOOLCHAIN_DIR" ]; then
            echo "❌ 错误: 工具链目录不存在"
            exit 1
          fi
          
          echo "[1/6] 准备输出目录..."
          mkdir -p "${{ env.COMPILER_DIR }}"
          if [ -d "${{ env.COMPILER_DIR }}" ]; then
            echo "清空输出目录..."
            rm -rf "${{ env.COMPILER_DIR }}"/* 2>/dev/null || true
          fi
          
          echo "[2/6] 复制工具链文件..."
          echo "从: $TOOLCHAIN_DIR"
          echo "到: ${{ env.COMPILER_DIR }}"
          cp -r "$TOOLCHAIN_DIR"/* "${{ env.COMPILER_DIR }}/" 2>&1 | tail -20
          
          echo "[3/6] 创建构建信息..."
          echo "========================================" > "${{ env.COMPILER_DIR }}/BUILD-INFO.txt"
          echo "OpenWrt 交叉编译器 - 自动构建" >> "${{ env.COMPILER_DIR }}/BUILD-INFO.txt"
          echo "========================================" >> "${{ env.COMPILER_DIR }}/BUILD-INFO.txt"
          echo "" >> "${{ env.COMPILER_DIR }}/BUILD-INFO.txt"
          echo "构建时间: $(date)" >> "${{ env.COMPILER_DIR }}/BUILD-INFO.txt"
          echo "工作流: ${{ github.workflow }}" >> "${{ env.COMPILER_DIR }}/BUILD-INFO.txt"
          echo "运行ID: ${{ github.run_id }}" >> "${{ env.COMPILER_DIR }}/BUILD-INFO.txt"
          echo "提交: ${{ github.sha }}" >> "${{ env.COMPILER_DIR }}/BUILD-INFO.txt"
          echo "" >> "${{ env.COMPILER_DIR }}/BUILD-INFO.txt"
          echo "=========== 版本信息 ===========" >> "${{ env.COMPILER_DIR }}/BUILD-INFO.txt"
          echo "GCC: ${{ env.GCC_VERSION }}" >> "${{ env.COMPILER_DIR }}/BUILD-INFO.txt"
          echo "Binutils: ${{ env.BINUTILS_VERSION }}" >> "${{ env.COMPILER_DIR }}/BUILD-INFO.txt"
          echo "Linux内核: ${{ env.LINUX_VERSION }}" >> "${{ env.COMPILER_DIR }}/BUILD-INFO.txt"
          echo "Musl: ${{ env.MUSL_VERSION }}" >> "${{ env.COMPILER_DIR }}/BUILD-INFO.txt"
          echo "目标架构: ${{ env.TARGET_ARCH }}" >> "${{ env.COMPILER_DIR }}/BUILD-INFO.txt"
          echo "目标三元组: ${{ env.TARGET_TRIPLET }}" >> "${{ env.COMPILER_DIR }}/BUILD-INFO.txt"
          echo "" >> "${{ env.COMPILER_DIR }}/BUILD-INFO.txt"
          echo "=========== 文件统计 ===========" >> "${{ env.COMPILER_DIR }}/BUILD-INFO.txt"
          
          FILE_COUNT=$(find "${{ env.COMPILER_DIR }}" -type f 2>/dev/null | wc -l)
          DIR_SIZE=$(du -sh "${{ env.COMPILER_DIR }}" 2>/dev/null | cut -f1)
          echo "总文件数: $FILE_COUNT" >> "${{ env.COMPILER_DIR }}/BUILD-INFO.txt"
          echo "总大小: $DIR_SIZE" >> "${{ env.COMPILER_DIR }}/BUILD-INFO.txt"
          echo "" >> "${{ env.COMPILER_DIR }}/BUILD-INFO.txt"
          echo "=========== 使用方法 ===========" >> "${{ env.COMPILER_DIR }}/BUILD-INFO.txt"
          echo "在OpenWrt构建环境中:" >> "${{ env.COMPILER_DIR }}/BUILD-INFO.txt"
          echo "" >> "${{ env.COMPILER_DIR }}/BUILD-INFO.txt"
          echo "1. 设置环境变量:" >> "${{ env.COMPILER_DIR }}/BUILD-INFO.txt"
          echo "   export STAGING_DIR=/path/to/this/compiler" >> "${{ env.COMPILER_DIR }}/BUILD-INFO.txt"
          echo "   make" >> "${{ env.COMPILER_DIR }}/BUILD-INFO.txt"
          echo "" >> "${{ env.COMPILER_DIR }}/BUILD-INFO.txt"
          echo "2. 或者在make命令中直接指定:" >> "${{ env.COMPILER_DIR }}/BUILD-INFO.txt"
          echo "   make STAGING_DIR=/path/to/this/compiler" >> "${{ env.COMPILER_DIR }}/BUILD-INFO.txt"
          echo "" >> "${{ env.COMPILER_DIR }}/BUILD-INFO.txt"
          echo "=========== 主要工具 ===========" >> "${{ env.COMPILER_DIR }}/BUILD-INFO.txt"
          
          if [ -d "${{ env.COMPILER_DIR }}/bin" ]; then
            for tool in gcc g++ ld ar strip; do
              tool_path=$(find "${{ env.COMPILER_DIR }}/bin" -name "*$tool" -type f 2>/dev/null | head -1)
              if [ -n "$tool_path" ]; then
                echo "bin/$(basename "$tool_path")      # $tool" >> "${{ env.COMPILER_DIR }}/BUILD-INFO.txt"
              fi
            done
          fi
          
          echo "" >> "${{ env.COMPILER_DIR }}/BUILD-INFO.txt"
          echo "========================================" >> "${{ env.COMPILER_DIR }}/BUILD-INFO.txt"
          echo "此文件由GitHub Actions自动生成" >> "${{ env.COMPILER_DIR }}/BUILD-INFO.txt"
          echo "========================================" >> "${{ env.COMPILER_DIR }}/BUILD-INFO.txt"
          
          echo "[4/6] 创建版本文件..."
          echo "COMPILER_VERSION=${{ env.GCC_VERSION }}" > "${{ env.COMPILER_DIR }}/VERSION"
          echo "BUILD_DATE=$(date '+%Y-%m-%d')" >> "${{ env.COMPILER_DIR }}/VERSION"
          echo "TARGET_ARCH=${{ env.TARGET_ARCH }}" >> "${{ env.COMPILER_DIR }}/VERSION"
          echo "TARGET_TRIPLET=${{ env.TARGET_TRIPLET }}" >> "${{ env.COMPILER_DIR }}/VERSION"
          echo "LIBC=musl" >> "${{ env.COMPILER_DIR }}/VERSION"
          
          echo "[5/6] 验证复制结果..."
          echo "输出目录: ${{ env.COMPILER_DIR }}"
          echo "大小: $DIR_SIZE"
          echo "文件数: $FILE_COUNT"
          
          echo "[6/6] 最终测试..."
          if [ -n "${{ env.GCC_PATH }}" ] && [ -f "${{ env.GCC_PATH }}" ]; then
            echo "✅ 编译器测试:"
            "${{ env.GCC_PATH }}" --version 2>&1 | head -1
          else
            echo "⚠️  警告: 编译器路径未设置"
          fi
          echo "✅ 保存完成"
      
      # 9. 提交到仓库（修复commit message中的YAML问题）
      - name: 📤 提交更新
        run: |
          echo "=== 提交编译器到仓库 ==="
          cd "${{ github.workspace }}"
          git config user.email "github-actions@github.com"
          git config user.name "GitHub Actions"
          echo "[1/4] 检查变更..."
          
          if [ -d "${{ env.COMPILER_DIR }}" ] && [ "$(find "${{ env.COMPILER_DIR }}" -type f 2>/dev/null | wc -l)" -gt 10 ]; then
            echo "检测到新的编译器文件，准备提交..."
            SIZE=$(du -sh "${{ env.COMPILER_DIR }}" 2>/dev/null | cut -f1 || echo "未知")
            FILE_COUNT=$(find "${{ env.COMPILER_DIR }}" -type f 2>/dev/null | wc -l)
            DATE_TAG=$(date +%Y%m%d)
            ARCH="${{ env.TARGET_ARCH }}"
            
            echo "[2/4] 添加到Git..."
            git add -f "firmware-config/build-Compiler-file/compiled"
            
            echo "[3/4] 提交..."
            # 使用简单的commit message，避免YAML解析问题
            COMMIT_TITLE="build(compiler): 自动构建${ARCH}交叉编译器 v${{ env.GCC_VERSION }} [$DATE_TAG]"
            COMMIT_BODY="版本信息:
• GCC: ${{ env.GCC_VERSION }}
• Binutils: ${{ env.BINUTILS_VERSION }}
• Linux内核: ${{ env.LINUX_VERSION }}
• Musl: ${{ env.MUSL_VERSION }}
• 目标: ${ARCH} (${{ env.TARGET_TRIPLET }})

构建详情:
• 大小: $SIZE
• 文件数: $FILE_COUNT
• 时间: $(date '+%Y-%m-%d %H:%M:%S')
• 工作流: ${{ github.workflow }}
• 运行ID: #${{ github.run_id }}

用途: OpenWrt/ImmortalWrt固件构建
位置: firmware-config/build-Compiler-file/compiled/

Signed-off-by: GitHub Actions <actions@github.com>"
            
            git commit -m "$COMMIT_TITLE" -m "$COMMIT_BODY"
            
            echo "[4/4] 推送到仓库..."
            if git push; then
              echo "🎉 ${ARCH}编译器已成功提交到仓库"
            else
              echo "⚠️ 推送失败，但提交已保存在本地"
            fi
          else
            echo "✅ 没有足够的编译器文件，跳过提交"
          fi
      
      # 10. 上传日志（简化路径定义）
      - name: 📄 上传日志
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: compiler-build-logs-$(date +%Y%m%d-%H%M%S)
          path: |
            /tmp/deps-install.log
            /tmp/defconfig.log
            /tmp/download.log
            /tmp/toolchain-compile.log
            /tmp/binutils.log
            /tmp/gcc-initial.log
            /tmp/musl.log
            /tmp/gcc-final.log
            /tmp/install.log
            /tmp/compile-test.log
          retention-days: 7
