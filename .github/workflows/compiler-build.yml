# .github/workflows/compiler-matrix-build.yml
name: 🔧 交叉编译器构建

on:
  workflow_dispatch:
    inputs:
      target_arch:
        description: '目标架构（IPQ40xx选择arm_cortex-a7）'
        required: true
        default: 'arm_cortex-a7'
        type: choice
        options: ['aarch64', 'x86_64', 'mips', 'mipsel', 'arm_cortex-a7', 'arm_cortex-a53', 'riscv64']
      libc:
        description: 'C库类型（嵌入式推荐musl）'
        required: true
        default: 'musl'
        type: choice
        options: ['musl', 'glibc']
      gcc_version:
        description: 'GCC版本（OpenWrt 23.05支持12.3.0，输入auto自动选择）'
        required: true
        default: 'auto'
        type: string
      clean_build:
        description: '清理之前的构建缓存'
        required: true
        default: false
        type: boolean
      force_rebuild:
        description: '强制重新构建（即使已存在）'
        required: true
        default: false
        type: boolean

env:
  OPENWRT_VERSION: "23.05.2"
  COMPILER_BASE_DIR: "${{ github.workspace }}/firmware-config/build-Compiler-file/compilers"

jobs:
  show-device-info:
    runs-on: ubuntu-latest
    steps:
      - name: 📱 显示架构对应设备
        run: |
          echo "========================================"
          echo "          🎯 目标架构设备对应表           "
          echo "========================================"
          echo "arm_cortex-a7 (你的设备！)"
          echo "  高通IPQ40xx系列: 小米路由器4A千兆版等"
          echo "aarch64: 树莓派4B、电视盒子"
          echo "x86_64: PC、服务器"
          echo "mips/mipsel: 路由器"
          echo "arm_cortex-a53: WiFi6路由器"
          echo "riscv64: 开发板"
          echo "========================================"

  setup:
    runs-on: ubuntu-22.04
    outputs:
      gcc_version: ${{ steps.version-detection.outputs.gcc_version }}
      binutils_version: ${{ steps.version-detection.outputs.binutils_version }}
      linux_version: ${{ steps.version-detection.outputs.linux_version }}
      musl_version: ${{ steps.version-detection.outputs.musl_version }}
      glibc_version: ${{ steps.version-detection.outputs.glibc_version }}
      need_build: ${{ steps.check-existing.outputs.need_build }}
    steps:
      - name: 📥 检出仓库（用于检查现有编译器）
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          sparse-checkout: firmware-config/build-Compiler-file/compilers
          sparse-checkout-cone-mode: false

      - name: 🏗️ 初始化
        run: |
          echo "=== 交叉编译器构建系统 ==="
          mkdir -p "${{ env.COMPILER_BASE_DIR }}"
          echo "基础目录: ${{ env.COMPILER_BASE_DIR }}"
          echo "开始时间: $(date)"

      - name: 🔍 动态检测GCC版本
        id: version-detection
        run: |
          USER_VERSION="${{ github.event.inputs.gcc_version }}"
          if [ "$USER_VERSION" = "auto" ] || [ "$USER_VERSION" = "" ]; then
            GCC_VERSION="12.3.0"
            echo "选择GCC版本: $GCC_VERSION"
          else
            GCC_VERSION="$USER_VERSION"
            echo "使用指定GCC版本: $GCC_VERSION"
          fi
          BINUTILS_VERSION="2.40"
          LINUX_VERSION="5.15.133"
          MUSL_VERSION="1.2.4"
          GLIBC_VERSION="2.37"
          echo "gcc_version=$GCC_VERSION" >> $GITHUB_OUTPUT
          echo "binutils_version=$BINUTILS_VERSION" >> $GITHUB_OUTPUT
          echo "linux_version=$LINUX_VERSION" >> $GITHUB_OUTPUT
          echo "musl_version=$MUSL_VERSION" >> $GITHUB_OUTPUT
          echo "glibc_version=$GLIBC_VERSION" >> $GITHUB_OUTPUT

      - name: 🔎 检查现有编译器
        id: check-existing
        run: |
          ARCH="${{ github.event.inputs.target_arch }}"
          LIBC="${{ github.event.inputs.libc }}"
          GCC_VERSION='${{ steps.version-detection.outputs.gcc_version }}'
          if [ "$LIBC" = "musl" ]; then
            LIBC_DIR="musl"
          else
            LIBC_DIR="glibc"
          fi
          OUTPUT_DIR="${{ github.workspace }}/firmware-config/build-Compiler-file/compilers/${ARCH}/${LIBC_DIR}/gcc-${GCC_VERSION}"
          if [ -d "$OUTPUT_DIR" ] && [ -d "$OUTPUT_DIR/bin" ]; then
            GCC_FILES=$(find "$OUTPUT_DIR/bin" -name "*gcc*" 2>/dev/null | wc -l)
            if [ $GCC_FILES -gt 0 ]; then
              NEED_BUILD="false"
            else
              NEED_BUILD="true"
            fi
          else
            NEED_BUILD="true"
          fi
          if [ "${{ github.event.inputs.force_rebuild }}" = "true" ]; then
            NEED_BUILD="true"
          fi
          echo "need_build=$NEED_BUILD" >> $GITHUB_OUTPUT

  build-compiler:
    needs: [show-device-info, setup]
    if: needs.setup.outputs.need_build == 'true'
    runs-on: ubuntu-22.04
    timeout-minutes: 120
    
    steps:
      - name: 📥 检出仓库
        uses: actions/checkout@v4

      - name: 📋 显示构建信息
        run: |
          echo "========================================"
          echo "        交叉编译器构建信息              "
          echo "========================================"
          echo "目标架构: ${{ github.event.inputs.target_arch }}"
          echo "C库类型: ${{ github.event.inputs.libc }}"
          echo "GCC版本: ${{ needs.setup.outputs.gcc_version }}"
          echo "清理构建: ${{ github.event.inputs.clean_build }}"
          echo "强制重建: ${{ github.event.inputs.force_rebuild }}"
          echo "========================================"

      - name: 🏗️ 设置环境
        run: |
          echo "=== 设置构建环境 ==="
          ARCH="${{ github.event.inputs.target_arch }}"
          LIBC="${{ github.event.inputs.libc }}"
          case "$ARCH" in
            arm_cortex-a7)
              TARGET="armvirt"
              SUBTARGET="armv7"
              ARCH_NAME="arm"
              ARCH_DIR="arm_cortex-a7"
              PREFIX="arm-openwrt-linux"
              CPU_TYPE="cortex-a7"
              TUNE="cortex-a7"
              FLOAT_ABI="hard"
              ;;
            aarch64)
              TARGET="armvirt"
              SUBTARGET="64"
              ARCH_NAME="aarch64"
              ARCH_DIR="aarch64"
              PREFIX="aarch64-openwrt-linux"
              CPU_TYPE="cortex-a53"
              TUNE="cortex-a53"
              FLOAT_ABI=""
              ;;
            x86_64)
              TARGET="x86"
              SUBTARGET="64"
              ARCH_NAME="x86_64"
              ARCH_DIR="x86_64"
              PREFIX="x86_64-openwrt-linux"
              CPU_TYPE="x86-64"
              TUNE="generic"
              FLOAT_ABI=""
              ;;
            mips)
              TARGET="ramips"
              SUBTARGET="mt7621"
              ARCH_NAME="mips"
              ARCH_DIR="mips"
              PREFIX="mips-openwrt-linux"
              CPU_TYPE="24kc"
              TUNE="24kc"
              FLOAT_ABI="softfloat"
              ;;
            mipsel)
              TARGET="ramips"
              SUBTARGET="mt7621"
              ARCH_NAME="mipsel"
              ARCH_DIR="mipsel"
              PREFIX="mipsel-openwrt-linux"
              CPU_TYPE="24kc"
              TUNE="24kc"
              FLOAT_ABI="softfloat"
              ;;
            arm_cortex-a53)
              TARGET="rockchip"
              SUBTARGET="armv8"
              ARCH_NAME="aarch64"
              ARCH_DIR="arm_cortex-a53"
              PREFIX="aarch64-openwrt-linux"
              CPU_TYPE="cortex-a53"
              TUNE="cortex-a53"
              FLOAT_ABI=""
              ;;
            riscv64)
              TARGET="virt"
              SUBTARGET="rv64"
              ARCH_NAME="riscv64"
              ARCH_DIR="riscv64"
              PREFIX="riscv64-openwrt-linux"
              CPU_TYPE="generic"
              TUNE="generic"
              FLOAT_ABI="hard"
              ;;
            *)
              echo "❌ 不支持的架构: $ARCH"
              exit 1
              ;;
          esac
          if [ "$LIBC" = "musl" ]; then
            PREFIX="${PREFIX}-musl"
            LIBC_DIR="musl"
          else
            PREFIX="${PREFIX}-gnu"
            LIBC_DIR="glibc"
          fi
          GCC_VERSION='${{ needs.setup.outputs.gcc_version }}'
          BUILD_ID="${ARCH_DIR}_${LIBC_DIR}_gcc-${GCC_VERSION}_$(date +%Y%m%d_%H%M%S)"
          BUILD_DIR="/home/runner/compiler-build-${BUILD_ID}"
          OUTPUT_DIR="${{ env.COMPILER_BASE_DIR }}/${ARCH_DIR}/${LIBC_DIR}/gcc-${GCC_VERSION}"
          echo "BUILD_DIR=$BUILD_DIR" >> $GITHUB_ENV
          echo "OUTPUT_DIR=$OUTPUT_DIR" >> $GITHUB_ENV
          echo "TARGET=$TARGET" >> $GITHUB_ENV
          echo "SUBTARGET=$SUBTARGET" >> $GITHUB_ENV
          echo "ARCH_NAME=$ARCH_NAME" >> $GITHUB_ENV
          echo "ARCH_DIR=$ARCH_DIR" >> $GITHUB_ENV
          echo "LIBC_DIR=$LIBC_DIR" >> $GITHUB_ENV
          echo "PREFIX=$PREFIX" >> $GITHUB_ENV
          echo "CPU_TYPE=$CPU_TYPE" >> $GITHUB_ENV
          echo "TUNE=$TUNE" >> $GITHUB_ENV
          echo "FLOAT_ABI=$FLOAT_ABI" >> $GITHUB_ENV
          echo "GCC_VERSION=$GCC_VERSION" >> $GITHUB_ENV
          echo "BUILD_ID=$BUILD_ID" >> $GITHUB_ENV
          echo "🚨 环境变量设置完成"
          echo "构建目录: $BUILD_DIR"
          echo "输出目录: $OUTPUT_DIR"
          echo "编译器前缀: $PREFIX-"

      - name: 🔍 验证环境变量
        run: |
          echo "=== 验证环境变量 ==="
          echo "检查必需的环境变量:"
          REQUIRED_VARS="BUILD_DIR OUTPUT_DIR TARGET SUBTARGET ARCH_NAME ARCH_DIR LIBC_DIR PREFIX CPU_TYPE TUNE FLOAT_ABI GCC_VERSION BUILD_ID"
          ALL_VALID=true
          for var in $REQUIRED_VARS; do
            value=$(eval echo \$$var)
            if [ -z "$value" ]; then
              echo "❌ 环境变量 $var 未设置"
              ALL_VALID=false
            else
              echo "✅ $var=$value"
            fi
          done
          if [ "$ALL_VALID" = false ]; then
            echo "❌ 环境变量验证失败"
            exit 1
          fi
          echo "检查架构配置:"
          if [ "$ARCH_NAME" = "arm" ]; then
            if [ "$TARGET" != "armvirt" ] || [ "$SUBTARGET" != "armv7" ]; then
              echo "❌ ARM架构配置错误: TARGET应为armvirt, SUBTARGET应为armv7"
              echo "当前: TARGET=$TARGET, SUBTARGET=$SUBTARGET"
              exit 1
            else
              echo "✅ ARM架构配置正确"
            fi
          else
            echo "ℹ️ 非ARM架构，跳过特定检查"
          fi
          echo "检查C库配置:"
          if [ "$LIBC_DIR" = "musl" ]; then
            if [[ "$PREFIX" == *"musl" ]]; then
              echo "✅ Musl配置正确: PREFIX包含musl"
            else
              echo "❌ Musl配置错误: PREFIX应包含musl"
              echo "当前: PREFIX=$PREFIX"
              exit 1
            fi
          else
            if [[ "$PREFIX" == *"gnu" ]]; then
              echo "✅ Glibc配置正确: PREFIX包含gnu"
            else
              echo "❌ Glibc配置错误: PREFIX应包含gnu"
              echo "当前: PREFIX=$PREFIX"
              exit 1
            fi
          fi
          echo "检查路径配置:"
          if [[ "$OUTPUT_DIR" == *"firmware-config/build-Compiler-file/compilers"* ]]; then
            echo "✅ 输出目录路径正确"
          else
            echo "⚠️ 警告: 输出目录路径可能不正确"
            echo "当前: $OUTPUT_DIR"
          fi
          if [[ "$BUILD_DIR" == "/home/runner/compiler-build-"* ]]; then
            echo "✅ 构建目录路径正确"
          else
            echo "⚠️ 警告: 构建目录路径可能不正确"
          fi
          echo "✅ 所有环境变量验证通过"

      - name: 🔧 安装完整依赖
        run: |
          echo "=== 安装完整的构建依赖 ==="
          sudo apt-get update -y
          sudo apt-get install -y build-essential gcc g++ make flex bison
          sudo apt-get install -y libgmp-dev libmpfr-dev libmpc-dev libisl-dev
          sudo apt-get install -y python3 python3-dev python3-pip python3-distutils
          sudo apt-get install -y git wget curl rsync unzip patchutils bc bzip2 xz-utils
          sudo apt-get install -y zlib1g-dev liblzma-dev libssl-dev libexpat-dev
          sudo apt-get install -y libgdbm-dev libreadline-dev libsqlite3-dev libffi-dev
          sudo apt-get install -y libelf-dev libdw-dev lzma-dev zstd libzstd-dev
          sudo apt-get install -y autoconf automake libtool pkg-config texinfo gperf
          sudo apt-get install -y cmake ninja-build help2man jq file gawk gettext
          sudo apt-get install -y ncurses-term ncurses-base libncursesw5 libtinfo5
          sudo apt-get install -y dialog man-db locales locales-all
          export TERM=xterm-256color
          export TERMINFO=/usr/share/terminfo
          sudo locale-gen en_US.UTF-8
          export LANG=en_US.UTF-8
          export LC_ALL=en_US.UTF-8
          echo "✅ 依赖安装完成"

      - name: 🗑️ 清理构建缓存
        if: ${{ github.event.inputs.clean_build == 'true' }}
        run: |
          echo "清理之前的构建缓存..."
          rm -rf /home/runner/compiler-build-* 2>/dev/null || true
          echo "✅ 缓存清理完成"

      - name: 📥 下载OpenWrt源码
        timeout-minutes: 10
        run: |
          echo "=== 获取OpenWrt源码 ==="
          mkdir -p "${{ env.BUILD_DIR }}"
          cd "${{ env.BUILD_DIR }}"
          echo "克隆OpenWrt ${{ env.OPENWRT_VERSION }}..."
          git clone --depth 1 --branch v${{ env.OPENWRT_VERSION }} https://github.com/openwrt/openwrt.git . || {
            echo "Git克隆失败，尝试tar包..."
            wget -q -O openwrt.tar.gz "https://github.com/openwrt/openwrt/archive/refs/tags/v${{ env.OPENWRT_VERSION }}.tar.gz"
            tar -xzf openwrt.tar.gz --strip-components=1
            rm -f openwrt.tar.gz
          }
          if [ -f "README.md" ]; then
            echo "✅ OpenWrt源码准备完成"
          else
            echo "❌ 下载失败"
            exit 1
          fi

      - name: 🔧 修复环境
        run: |
          echo "=== 修复构建环境 ==="
          cd "${{ env.BUILD_DIR }}"
          export TERM=dumb
          export TERMINFO=/usr/share/terminfo
          export FORCE_UNSAFE_CONFIGURE=1
          export DEBIAN_FRONTEND=noninteractive
          git config --global http.postBuffer 524288000
          git config --global http.lowSpeedLimit 0
          git config --global http.lowSpeedTime 999999
          export GIT_SSL_NO_VERIFY=1
          export CURL_SSL_NO_VERIFY=1
          mkdir -p include/site tmp dl
          echo "✅ 环境修复完成"

      - name: 🛠️ 创建配置
        run: |
          echo "=== 创建构建配置 ==="
          cd "${{ env.BUILD_DIR }}"
          echo "CONFIG_TARGET_armvirt=y" > .config
          echo "CONFIG_TARGET_armvirt_armv7=y" >> .config
          echo 'CONFIG_TARGET_BOARD="armvirt"' >> .config
          echo 'CONFIG_TARGET_SUBTARGET="armv7"' >> .config
          echo 'CONFIG_TARGET_ARCH_PACKAGES="arm_cortex-a7"' >> .config
          echo 'CONFIG_TARGET_ARCH="arm"' >> .config
          echo 'CONFIG_TARGET_PREFIX="arm-openwrt-linux-musl-"' >> .config
          echo "CONFIG_BUILD_TOOLCHAIN=y" >> .config
          echo "CONFIG_TOOLCHAINOPTS=y" >> .config
          echo 'CONFIG_GCC_VERSION="12.3.0"' >> .config
          echo 'CONFIG_BINUTILS_VERSION="2.40"' >> .config
          echo 'CONFIG_LINUX_VERSION="5.15.133"' >> .config
          echo 'CONFIG_LIBC="musl"' >> .config
          echo 'CONFIG_CPU_TYPE="cortex-a7"' >> .config
          echo 'CONFIG_CPU_TUNE="cortex-a7"' >> .config
          echo 'CONFIG_FPU="hard"' >> .config
          echo "CONFIG_DEVEL=y" >> .config
          echo "CONFIG_BUILD_PATENTED=y" >> .config
          echo "CONFIG_CCACHE=n" >> .config
          echo "CONFIG_ALL=n" >> .config
          echo "CONFIG_ALL_KMODS=n" >> .config
          echo "CONFIG_ALL_NONSHARED=n" >> .config
          echo "CONFIG_BASE_FILES=y" >> .config
          echo "CONFIG_BUSYBOX_DEFAULT_FEATURE_EDITING=y" >> .config
          echo "CONFIG_TARGET_ROOTFS_INITRAMFS=y" >> .config
          echo "CONFIG_EXTERNAL_TOOLCHAIN=n" >> .config
          echo "CONFIG_BUILD_TOOLS=y" >> .config
          echo "CONFIG_TOOLCHAIN_BUILD=y" >> .config
          echo "CONFIG_SDK=y" >> .config
          echo "CONFIG_STRIP_KERNEL_EXPORTS=y" >> .config
          echo "CONFIG_USE_MUSL=y" >> .config
          echo "CONFIG_USE_GLIBC=n" >> .config
          echo "✅ 配置文件创建完成"

      - name: 📥 预下载关键源码包
        timeout-minutes: 25
        run: |
          echo "=== 预下载关键源码包 ==="
          cd "${{ env.BUILD_DIR }}"
          
          # 创建下载目录
          mkdir -p dl
          
          echo "下载关键工具链组件（使用多个镜像源）..."
          echo "开始时间: $(date)"
          
          # 设置下载重试
          MAX_RETRIES=3
          
          # 定义下载函数
          download_with_retry() {
            local url=$1
            local output=$2
            local retry=0
            
            while [ $retry -lt $MAX_RETRIES ]; do
              echo "尝试下载: $url (重试 $((retry+1))/$MAX_RETRIES)"
              if wget --timeout=60 --tries=3 -q -O "$output" "$url"; then
                echo "✅ 下载成功: $(basename "$output")"
                return 0
              fi
              retry=$((retry+1))
              if [ $retry -lt $MAX_RETRIES ]; then
                echo "等待3秒后重试..."
                sleep 3
              fi
            done
            echo "❌ 下载失败: $(basename "$output")"
            return 1
          }
          
          # GCC 12.3.0 (多个镜像源)
          echo "=== 下载 GCC 12.3.0 ==="
          GCC_URLS=(
            "https://mirrors.tuna.tsinghua.edu.cn/gnu/gcc/gcc-12.3.0/gcc-12.3.0.tar.xz"
            "https://mirrors.ustc.edu.cn/gnu/gcc/gcc-12.3.0/gcc-12.3.0.tar.xz"
            "https://ftp.gnu.org/gnu/gcc/gcc-12.3.0/gcc-12.3.0.tar.xz"
          )
          for url in "${GCC_URLS[@]}"; do
            if download_with_retry "$url" "dl/gcc-12.3.0.tar.xz"; then
              break
            fi
          done
          
          # Binutils 2.40
          echo "=== 下载 Binutils 2.40 ==="
          BINUTILS_URLS=(
            "https://mirrors.tuna.tsinghua.edu.cn/gnu/binutils/binutils-2.40.tar.xz"
            "https://mirrors.ustc.edu.cn/gnu/binutils/binutils-2.40.tar.xz"
            "https://ftp.gnu.org/gnu/binutils/binutils-2.40.tar.xz"
          )
          for url in "${BINUTILS_URLS[@]}"; do
            if download_with_retry "$url" "dl/binutils-2.40.tar.xz"; then
              break
            fi
          done
          
          # Linux 5.15.133
          echo "=== 下载 Linux 内核 5.15.133 ==="
          LINUX_URLS=(
            "https://mirrors.tuna.tsinghua.edu.cn/kernel/v5.x/linux-5.15.133.tar.xz"
            "https://cdn.kernel.org/pub/linux/kernel/v5.x/linux-5.15.133.tar.xz"
          )
          for url in "${LINUX_URLS[@]}"; do
            if download_with_retry "$url" "dl/linux-5.15.133.tar.xz"; then
              break
            fi
          done
          
          # Musl 1.2.4
          echo "=== 下载 Musl 1.2.4 ==="
          MUSL_URLS=(
            "https://musl.libc.org/releases/musl-1.2.4.tar.gz"
            "https://mirrors.tuna.tsinghua.edu.cn/musl/musl-1.2.4.tar.gz"
          )
          for url in "${MUSL_URLS[@]}"; do
            if download_with_retry "$url" "dl/musl-1.2.4.tar.gz"; then
              break
            fi
          done
          
          # 其他可能需要的包
          echo "=== 下载其他工具链组件 ==="
          OTHER_PACKAGES=(
            "https://mirrors.tuna.tsinghua.edu.cn/gnu/mpc/mpc-1.3.1.tar.gz|dl/mpc-1.3.1.tar.gz"
            "https://mirrors.tuna.tsinghua.edu.cn/gnu/mpfr/mpfr-4.2.0.tar.xz|dl/mpfr-4.2.0.tar.xz"
            "https://mirrors.tuna.tsinghua.edu.cn/gnu/gmp/gmp-6.2.1.tar.xz|dl/gmp-6.2.1.tar.xz"
          )
          
          for package in "${OTHER_PACKAGES[@]}"; do
            url=$(echo "$package" | cut -d'|' -f1)
            output=$(echo "$package" | cut -d'|' -f2)
            download_with_retry "$url" "$output" || true
          done
          
          echo "结束时间: $(date)"
          echo "下载完成，检查文件..."
          ls -lh dl/ 2>/dev/null | head -20 || echo "下载目录为空"
          echo "总文件数: $(ls -1 dl/ 2>/dev/null | wc -l)"
          echo "总大小: $(du -sh dl/ 2>/dev/null | cut -f1 || echo "未知")"
          
          if [ $(ls -1 dl/ 2>/dev/null | wc -l) -lt 3 ]; then
            echo "⚠️ 警告: 下载的文件数量较少，但继续构建..."
          else
            echo "✅ 关键源码包下载完成"
          fi

      - name: 📥 下载剩余源码包
        timeout-minutes: 15
        run: |
          echo "=== 下载剩余源码包 ==="
          cd "${{ env.BUILD_DIR }}"
          export FORCE_UNSAFE_CONFIGURE=1
          export TERM=dumb
          
          echo "开始时间: $(date)"
          echo "检查现有下载..."
          PRE_COUNT=$(ls -1 dl/ 2>/dev/null | wc -l)
          echo "预下载文件数: $PRE_COUNT"
          
          echo "使用单线程下载缺失的包..."
          make -j1 download V=s 2>&1 | tee download.log | tail -30
          
          echo "结束时间: $(date)"
          
          POST_COUNT=$(ls -1 dl/ 2>/dev/null | wc -l)
          ADDED_COUNT=$((POST_COUNT - PRE_COUNT))
          TOTAL_SIZE=$(du -sh dl/ 2>/dev/null | cut -f1 || echo "未知")
          
          echo "下载统计:"
          echo "预下载文件: $PRE_COUNT"
          echo "新增文件: $ADDED_COUNT"
          echo "总文件数: $POST_COUNT"
          echo "总大小: $TOTAL_SIZE"
          
          if [ $POST_COUNT -gt 5 ]; then
            echo "✅ 源码包下载完成"
          else
            echo "⚠️ 下载文件较少，但继续构建..."
          fi

      - name: 🔨 编译工具链
        timeout-minutes: 50
        run: |
          echo "=== 编译工具链 ==="
          cd "${{ env.BUILD_DIR }}"
          export FORCE_UNSAFE_CONFIGURE=1
          export TERM=dumb
          CPU_CORES=$(nproc)
          echo "使用 $CPU_CORES 个核心编译"
          echo "开始编译工具链..."
          echo "开始时间: $(date)"
          make toolchain/compile -j$CPU_CORES V=s 2>&1 | tee compile.log
          COMPILE_STATUS=${PIPESTATUS[0]}
          echo "结束时间: $(date)"
          echo "编译状态: $COMPILE_STATUS"
          if [ $COMPILE_STATUS -eq 0 ]; then
            echo "✅ 工具链编译成功"
          else
            echo "❌ 工具链编译失败"
            echo "最后30行编译日志:"
            tail -30 compile.log
            exit 1
          fi

      - name: 📦 安装工具链
        timeout-minutes: 15
        run: |
          echo "=== 安装工具链 ==="
          cd "${{ env.BUILD_DIR }}"
          export TERM=dumb
          CPU_CORES=$(nproc)
          echo "开始安装工具链..."
          echo "开始时间: $(date)"
          make toolchain/install -j$CPU_CORES V=s 2>&1 | tee install.log
          INSTALL_STATUS=${PIPESTATUS[0]}
          echo "结束时间: $(date)"
          echo "安装状态: $INSTALL_STATUS"
          if [ $INSTALL_STATUS -eq 0 ]; then
            echo "✅ 工具链安装成功"
          else
            echo "❌ 工具链安装失败"
            echo "最后30行安装日志:"
            tail -30 install.log
            exit 1
          fi

      - name: 🛠️ 错误分析与诊断
        if: failure()
        timeout-minutes: 5
        run: |
          echo "========================================"
          echo "          🔍 错误分析与诊断              "
          echo "========================================"
          echo "构建失败，开始诊断..."
          echo ""
          echo "1. 系统资源状态:"
          echo "磁盘空间:"
          df -h
          echo ""
          echo "内存使用:"
          free -h
          echo ""
          echo "2. 构建目录状态:"
          if [ -d "${{ env.BUILD_DIR }}" ]; then
            echo "构建目录大小: $(du -sh "${{ env.BUILD_DIR }}" 2>/dev/null || echo "未知")"
            echo "关键子目录:"
            ls -la "${{ env.BUILD_DIR }}/" | grep -E "staging|build|dl" || echo "未找到关键目录"
            echo ""
            echo "3. 下载目录状态:"
            if [ -d "${{ env.BUILD_DIR }}/dl" ]; then
              echo "下载文件数: $(ls -1 "${{ env.BUILD_DIR }}/dl" 2>/dev/null | wc -l)"
              echo "下载目录大小: $(du -sh "${{ env.BUILD_DIR }}/dl" 2>/dev/null || echo "未知")"
              echo "下载文件列表:"
              ls -lh "${{ env.BUILD_DIR }}/dl/" | head -10
            else
              echo "❌ 下载目录不存在"
            fi
            echo ""
            echo "4. 日志文件分析:"
            for log in "${{ env.BUILD_DIR }}"/*.log; do
              if [ -f "$log" ]; then
                echo "日志文件: $(basename "$log")"
                echo "文件大小: $(wc -l < "$log") 行"
                echo "最后错误:"
                grep -i "error\|failed\|recipe for target" "$log" | tail -3 || echo "未找到明确错误"
                echo ""
              fi
            done
          else
            echo "❌ 构建目录不存在"
          fi
          echo ""
          echo "5. 常见问题解决建议:"
          echo "   - 终端错误 (Error opening terminal):"
          echo "     确保安装了 ncurses-term: sudo apt-get install ncurses-term"
          echo "   - 下载失败:"
          echo "     检查网络连接，使用预下载方案"
          echo "     在 .config 中设置 CONFIG_DOWNLOAD_FOLDER 指定本地缓存"
          echo "   - 编译失败:"
          echo "     检查所有依赖是否完整安装"
          echo "     尝试减少并行任务: make -j2"
          echo "     清理构建缓存重新开始"
          echo "   - 超时问题:"
          echo "     增加工作流超时时间"
          echo "     分步构建，先下载再编译"
          echo ""
          echo "6. 调试命令参考:"
          echo "   - 查看完整日志: tail -100 ${{ env.BUILD_DIR }}/compile.log"
          echo "   - 检查配置: grep -E 'TARGET|ARCH|GCC' ${{ env.BUILD_DIR }}/.config"
          echo "   - 检查依赖: ldd ${{ env.BUILD_DIR }}/staging_dir/*/bin/*gcc 2>/dev/null | head -5"
          echo "========================================"

      - name: ✅ 验证构建结果
        run: |
          echo "=== 验证构建结果 ==="
          cd "${{ env.BUILD_DIR }}"
          TOOLCHAIN_DIR=$(find staging_dir -name "toolchain-*" -type d 2>/dev/null | head -1)
          if [ -z "$TOOLCHAIN_DIR" ]; then
            echo "❌ 未找到工具链目录"
            exit 1
          fi
          echo "✅ 工具链目录: $TOOLCHAIN_DIR"
          GCC_FILE=$(find "$TOOLCHAIN_DIR/bin" -name "*gcc" -type f 2>/dev/null | head -1)
          if [ -z "$GCC_FILE" ]; then
            echo "❌ 未找到编译器"
            exit 1
          fi
          echo "✅ 找到编译器: $(basename "$GCC_FILE")"
          echo "编译器版本:"
          "$GCC_FILE" --version 2>&1 | head -3
          COMPILER_NAME=$(basename "$GCC_FILE")
          TOOL_PREFIX=$(echo "$COMPILER_NAME" | sed 's/gcc$//')
          echo "TOOLCHAIN_DIR=$TOOLCHAIN_DIR" >> $GITHUB_ENV
          echo "COMPILER_PATH=$GCC_FILE" >> $GITHUB_ENV
          echo "TOOL_PREFIX=$TOOL_PREFIX" >> $GITHUB_ENV

      - name: 💾 保存编译器
        run: |
          echo "=== 保存编译器 ==="
          mkdir -p "${{ env.OUTPUT_DIR }}"
          if [ -d "${{ env.OUTPUT_DIR }}" ] && [ "$(ls -A "${{ env.OUTPUT_DIR }}" 2>/dev/null)" ]; then
            rm -rf "${{ env.OUTPUT_DIR }}"/*
          fi
          cp -r "$TOOLCHAIN_DIR"/* "${{ env.OUTPUT_DIR }}/" 2>/dev/null || true
          if [ -d "${{ env.OUTPUT_DIR }}/bin" ]; then
            file_count=$(find "${{ env.OUTPUT_DIR }}" -type f 2>/dev/null | wc -l)
            TOTAL_SIZE=$(du -sh "${{ env.OUTPUT_DIR }}" | cut -f1)
            echo "FILE_COUNT=$file_count" >> $GITHUB_ENV
            echo "TOTAL_SIZE=$TOTAL_SIZE" >> $GITHUB_ENV
            echo "✅ 工具链保存完成"
            echo "文件数: $file_count"
            echo "大小: $TOTAL_SIZE"
            echo '{"architecture": "'${{ env.ARCH_DIR }}'", "libc": "'${{ env.LIBC_DIR }}'", "gcc_version": "'${{ env.GCC_VERSION }}'", "build_date": "'$(date -Iseconds)'", "file_count": "'$file_count'", "total_size": "'$TOTAL_SIZE'", "prefix": "'${{ env.PREFIX }}'-"}' > "${{ env.OUTPUT_DIR }}/metadata.json"
          else
            echo "❌ 工具链保存失败"
            exit 1
          fi

      - name: 📤 提交到Git仓库
        run: |
          echo "=== 提交编译器到Git仓库 ==="
          cd "${{ github.workspace }}"
          git config user.email "github-actions@github.com"
          git config user.name "GitHub Actions"
          git pull origin main --no-edit || echo "拉取失败，继续..."
          git add -f "firmware-config/build-Compiler-file/compilers/*"
          if ! git diff --cached --quiet; then
            git commit -m "build(compiler): 添加 ${{ env.ARCH_DIR }}-${{ env.LIBC_DIR }} 交叉编译器" -m "GCC版本: ${{ env.GCC_VERSION }}" -m "大小: ${{ env.TOTAL_SIZE }}" -m "工作流: #${{ github.run_id }}"
            git push origin main
            echo "✅ 编译器已提交到仓库"
          else
            echo "ℹ️ 没有变更需要提交"
          fi

      - name: 📊 显示最终成果
        run: |
          echo "========================================"
          echo "          🎉 构建完成报告              "
          echo "========================================"
          echo "✅ 交叉编译器构建成功!"
          echo ""
          echo "📊 构建信息:"
          echo "  架构: ${{ env.ARCH_DIR }}"
          echo "  C库: ${{ env.LIBC_DIR }}"
          echo "  GCC版本: ${{ env.GCC_VERSION }}"
          echo "  编译器前缀: ${{ env.PREFIX }}-"
          echo ""
          echo "📦 成果统计:"
          echo "  总大小: ${{ env.TOTAL_SIZE }}"
          echo "  文件数量: ${{ env.FILE_COUNT }}"
          echo "  输出目录: ${{ env.OUTPUT_DIR }}"
          echo ""
          echo "🚀 使用方法:"
          echo "cd ${{ env.OUTPUT_DIR }}"
          echo 'export PATH="$(pwd)/bin:$PATH"'
          echo "${{ env.PREFIX }}-gcc --version"
          echo ""
          echo "完成时间: $(date)"
          echo "========================================"

      - name: 📄 上传构建日志
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-logs-${{ env.BUILD_ID }}
          path: |
            ${{ env.BUILD_DIR }}/download.log
            ${{ env.BUILD_DIR }}/compile.log
            ${{ env.BUILD_DIR }}/install.log
          retention-days: 7

  skip-build:
    needs: setup
    if: needs.setup.outputs.need_build == 'false'
    runs-on: ubuntu-latest
    steps:
      - name: ⏭️ 跳过构建
        run: |
          echo "========================================"
          echo "        ⏭️ 跳过构建报告                "
          echo "========================================"
          echo "✅ 编译器已存在且完整，跳过构建"
          echo "💡 如需重新构建，请启用'强制重新构建'选项"
          echo "========================================"
