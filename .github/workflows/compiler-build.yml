# .github/workflows/compiler-build.yml
name: 🔧 交叉编译器构建

on:
  workflow_dispatch:
    inputs:
      arch_type:
        description: '架构类型'
        required: true
        default: 'all'
        type: choice
        options: ['all', 'arm', 'mips', 'x86', 'riscv', 'aarch64']
      gcc_version:
        description: 'GCC版本'
        required: true
        default: '10.3.0'
        type: choice
        options: ['10.3.0', '11.3.0', '12.3.0', '13.2.0']

env:
  COMPILER_BASE_DIR: "${{ github.workspace }}/firmware-config/build-Compiler-file"
  BUILD_BASE_DIR: "/mnt/compiler-build"

jobs:
  setup:
    runs-on: ubuntu-22.04
    outputs:
      arch_list: ${{ steps.setup-arch.outputs.arch_list }}
      openwrt_version: ${{ steps.version-detection.outputs.openwrt_version }}
      gcc_version: ${{ steps.version-detection.outputs.gcc_version }}
      binutils_version: ${{ steps.version-detection.outputs.binutils_version }}
      linux_version: ${{ steps.version-detection.outputs.linux_version }}
      musl_version: ${{ steps.version-detection.outputs.musl_version }}
      glibc_version: ${{ steps.version-detection.outputs.glibc_version }}
      start_time: ${{ steps.get-time.outputs.start_time }}
    steps:
      - name: 📥 检出仓库
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: ⏰ 获取开始时间
        id: get-time
        run: |
          echo "start_time=$(date)" >> $GITHUB_OUTPUT

      - name: 🔧 设置构建目录权限
        run: |
          echo "=== 设置构建目录权限 ==="
          echo "创建并设置/mnt/compiler-build目录权限..."
          
          # 检查目录是否存在
          if [ ! -d "/mnt/compiler-build" ]; then
            echo "创建/mnt/compiler-build目录..."
            sudo mkdir -p /mnt/compiler-build
          fi
          
          # 设置权限
          echo "设置目录权限..."
          sudo chmod -R 777 /mnt/compiler-build
          
          # 设置所有权（可选，根据实际需要）
          echo "设置目录所有权..."
          sudo chown -R $USER:$USER /mnt/compiler-build 2>/dev/null || true
          
          echo "验证目录权限..."
          ls -ld /mnt/compiler-build
          echo "目录内容:"
          ls -la /mnt/compiler-build/ 2>/dev/null || echo "目录为空"
          
          echo "✅ 目录权限设置完成"

      - name: 🏗️ 初始化环境
        run: |
          echo "=== 初始化构建环境 ==="
          mkdir -p "${{ env.COMPILER_BASE_DIR }}"
          mkdir -p "${{ env.BUILD_BASE_DIR }}"
          echo "编译器基础目录: ${{ env.COMPILER_BASE_DIR }}"
          echo "构建基础目录: ${{ env.BUILD_BASE_DIR }}"
          echo "开始时间: $(date)"

      - name: 🔧 设置架构列表
        id: setup-arch
        run: |
          echo "=== 设置目标架构列表 ==="
          ARCH_TYPE="${{ github.event.inputs.arch_type }}"
          
          case "$ARCH_TYPE" in
            'all')
              ARCH_LIST='["aarch64", "x86_64", "mips", "mipsel", "arm_cortex-a7", "arm_cortex-a53", "riscv64"]'
              echo "✅ 选择所有架构类型"
              ;;
            'arm')
              ARCH_LIST='["arm_cortex-a7", "arm_cortex-a53"]'
              echo "✅ 选择ARM架构"
              ;;
            'mips')
              ARCH_LIST='["mips", "mipsel"]'
              echo "✅ 选择MIPS架构"
              ;;
            'x86')
              ARCH_LIST='["x86_64"]'
              echo "✅ 选择x86架构"
              ;;
            'riscv')
              ARCH_LIST='["riscv64"]'
              echo "✅ 选择RISC-V架构"
              ;;
            'aarch64')
              ARCH_LIST='["aarch64"]'
              echo "✅ 选择AArch64架构"
              ;;
            *)
              ARCH_LIST='["aarch64"]'
              echo "⚠️ 未知类型，默认选择AArch64"
              ;;
          esac
          
          echo "目标架构列表: $ARCH_LIST"
          echo "arch_list=$ARCH_LIST" >> $GITHUB_OUTPUT

      - name: 🔍 智能版本检测与选择
        id: version-detection
        run: |
          echo "=== 智能版本检测与选择 ==="
          GCC_VERSION="${{ github.event.inputs.gcc_version }}"
          
          echo "用户选择的GCC版本: $GCC_VERSION"
          
          case "$GCC_VERSION" in
            '10.3.0')
              OPENWRT_VERSION="21.02.5"
              BINUTILS_VERSION="2.36.1"
              LINUX_VERSION="5.4.188"
              MUSL_VERSION="1.2.2"
              GLIBC_VERSION="2.33"
              ;;
            '11.3.0')
              OPENWRT_VERSION="22.03.5"
              BINUTILS_VERSION="2.38"
              LINUX_VERSION="5.10.179"
              MUSL_VERSION="1.2.3"
              GLIBC_VERSION="2.35"
              ;;
            '12.3.0')
              OPENWRT_VERSION="23.05.2"
              BINUTILS_VERSION="2.40"
              LINUX_VERSION="5.15.133"
              MUSL_VERSION="1.2.4"
              GLIBC_VERSION="2.37"
              ;;
            '13.2.0')
              OPENWRT_VERSION="snapshot"
              BINUTILS_VERSION="2.41"
              LINUX_VERSION="6.1.69"
              MUSL_VERSION="1.2.5"
              GLIBC_VERSION="2.38"
              ;;
            *)
              OPENWRT_VERSION="23.05.2"
              BINUTILS_VERSION="2.40"
              LINUX_VERSION="5.15.133"
              MUSL_VERSION="1.2.4"
              GLIBC_VERSION="2.37"
              echo "⚠️ 未知GCC版本，使用默认配置"
              ;;
          esac
          
          echo "🎯 最终版本选择:"
          echo "- OpenWrt版本: $OPENWRT_VERSION"
          echo "- GCC版本: $GCC_VERSION"
          echo "- Binutils版本: $BINUTILS_VERSION"
          echo "- Linux内核: $LINUX_VERSION"
          echo "- Musl版本: $MUSL_VERSION"
          echo "- Glibc版本: $GLIBC_VERSION"
          
          echo "openwrt_version=$OPENWRT_VERSION" >> $GITHUB_OUTPUT
          echo "gcc_version=$GCC_VERSION" >> $GITHUB_OUTPUT
          echo "binutils_version=$BINUTILS_VERSION" >> $GITHUB_OUTPUT
          echo "linux_version=$LINUX_VERSION" >> $GITHUB_OUTPUT
          echo "musl_version=$MUSL_VERSION" >> $GITHUB_OUTPUT
          echo "glibc_version=$GLIBC_VERSION" >> $GITHUB_OUTPUT

  build-matrix:
    needs: setup
    runs-on: ubuntu-22.04
    strategy:
      matrix:
        arch: ${{ fromJSON(needs.setup.outputs.arch_list) }}
    steps:
      - name: 📥 检出仓库
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: 🔧 设置运行环境权限
        run: |
          echo "=== 设置运行环境权限 ==="
          echo "当前用户: $(whoami)"
          echo "用户ID: $(id -u)"
          echo "组ID: $(id -g)"
          
          echo "设置构建目录权限..."
          sudo mkdir -p /mnt/compiler-build
          sudo chmod -R 777 /mnt/compiler-build
          sudo chown -R $(whoami):$(whoami) /mnt/compiler-build 2>/dev/null || true
          
          echo "验证权限..."
          ls -ld /mnt/compiler-build
          touch /mnt/compiler-build/test-permission && echo "✅ 写入权限正常" && rm /mnt/compiler-build/test-permission

      - name: 🔍 检查现有编译器
        run: |
          echo "=== 检查现有编译器 ==="
          ARCH="${{ matrix.arch }}"
          GCC_VERSION='${{ needs.setup.outputs.gcc_version }}'
          BINUTILS_VERSION='${{ needs.setup.outputs.binutils_version }}'
          
          echo "检查架构: $ARCH"
          echo "GCC版本: $GCC_VERSION"
          echo "Binutils版本: $BINUTILS_VERSION"
          
          OUTPUT_DIR="${{ env.COMPILER_BASE_DIR }}/compilers/${ARCH}/musl/gcc-${GCC_VERSION}_binutils-${BINUTILS_VERSION}"
          
          echo "检查目录: $OUTPUT_DIR"
          
          if [ -d "$OUTPUT_DIR" ]; then
            echo "✅ 编译器目录已存在"
            
            REQUIRED_DIRS=("bin" "lib" "include")
            MISSING_ITEMS=()
            
            for dir in "${REQUIRED_DIRS[@]}"; do
              if [ ! -d "$OUTPUT_DIR/$dir" ]; then
                MISSING_ITEMS+=("目录 $dir")
              fi
            done
            
            if [ ${#MISSING_ITEMS[@]} -eq 0 ]; then
              echo "✅ 编译器文件完整"
              echo "SKIP_BUILD=true" >> $GITHUB_ENV
            else
              echo "⚠️ 编译器不完整，需要构建"
              echo "SKIP_BUILD=false" >> $GITHUB_ENV
            fi
          else
            echo "❌ 编译器目录不存在，需要构建"
            echo "SKIP_BUILD=false" >> $GITHUB_ENV
          fi

      - name: ⏭️ 跳过构建
        if: env.SKIP_BUILD == 'true'
        run: |
          echo "========================================"
          echo "        ⏭️ 跳过构建报告                "
          echo "========================================"
          echo "✅ 编译器已存在且完整，跳过构建"
          echo ""
          echo "🔧 现有编译器信息:"
          echo "- 架构: ${{ matrix.arch }}"
          echo "- GCC版本: ${{ needs.setup.outputs.gcc_version }}"
          echo "- Binutils版本: ${{ needs.setup.outputs.binutils_version }}"
          echo "- 输出目录: ${{ env.COMPILER_BASE_DIR }}/compilers/${{ matrix.arch }}/musl/gcc-${{ needs.setup.outputs.gcc_version }}_binutils-${{ needs.setup.outputs.binutils_version }}"
          echo "========================================"

      - name: 📋 显示构建信息
        if: env.SKIP_BUILD != 'true'
        run: |
          echo "========================================"
          echo "        交叉编译器构建信息              "
          echo "========================================"
          echo "目标架构: ${{ matrix.arch }}"
          echo "C库类型: musl"
          echo "GCC版本: ${{ needs.setup.outputs.gcc_version }}"
          echo "Binutils版本: ${{ needs.setup.outputs.binutils_version }}"
          echo "Linux内核: ${{ needs.setup.outputs.linux_version }}"
          echo "Musl版本: ${{ needs.setup.outputs.musl_version }}"
          echo "OpenWrt版本: ${{ needs.setup.outputs.openwrt_version }}"
          echo "构建目录: ${{ env.BUILD_BASE_DIR }}"
          echo "输出目录: ${{ env.COMPILER_BASE_DIR }}/compilers"
          echo "========================================"
          echo "开始时间: $(date)"

      - name: 🔧 设置架构环境
        if: env.SKIP_BUILD != 'true'
        run: |
          echo "=== 设置架构环境 ==="
          ARCH="${{ matrix.arch }}"
          
          case "$ARCH" in
            arm_cortex-a7)
              TARGET="ipq40xx"
              SUBTARGET="generic"
              ARCH_NAME="arm"
              PREFIX="arm-openwrt-linux-musl"
              CPU_TYPE="cortex-a7"
              TUNE="cortex-a7"
              ABI="32"
              FLOAT_ABI="hard"
              ;;
            aarch64)
              TARGET="armvirt"
              SUBTARGET="64"
              ARCH_NAME="aarch64"
              PREFIX="aarch64-openwrt-linux-musl"
              CPU_TYPE="cortex-a53"
              TUNE="cortex-a53"
              ABI="64"
              FLOAT_ABI=""
              ;;
            x86_64)
              TARGET="x86"
              SUBTARGET="64"
              ARCH_NAME="x86_64"
              PREFIX="x86_64-openwrt-linux-musl"
              CPU_TYPE="x86_64"
              TUNE="generic"
              ABI="64"
              FLOAT_ABI=""
              ;;
            mips)
              TARGET="ramips"
              SUBTARGET="mt7621"
              ARCH_NAME="mips"
              PREFIX="mips-openwrt-linux-musl"
              CPU_TYPE="24kc"
              TUNE="24kc"
              ABI="32"
              FLOAT_ABI="softfloat"
              ;;
            mipsel)
              TARGET="ramips"
              SUBTARGET="mt7621"
              ARCH_NAME="mipsel"
              PREFIX="mipsel-openwrt-linux-musl"
              CPU_TYPE="24kc"
              TUNE="24kc"
              ABI="32"
              FLOAT_ABI="softfloat"
              ;;
            arm_cortex-a53)
              TARGET="rockchip"
              SUBTARGET="armv8"
              ARCH_NAME="aarch64"
              PREFIX="aarch64-openwrt-linux-musl"
              CPU_TYPE="cortex-a53"
              TUNE="cortex-a53"
              ABI="64"
              FLOAT_ABI=""
              ;;
            riscv64)
              TARGET="virt"
              SUBTARGET="rv64"
              ARCH_NAME="riscv64"
              PREFIX="riscv64-openwrt-linux-musl"
              CPU_TYPE="generic"
              TUNE="generic"
              ABI="lp64d"
              FLOAT_ABI="hard"
              ;;
            *)
              echo "❌ 不支持的架构: $ARCH"
              exit 1
              ;;
          esac
          
          BUILD_ID="${ARCH}_musl_gcc-${{ needs.setup.outputs.gcc_version }}_$(date +%Y%m%d_%H%M%S)"
          BUILD_DIR="${{ env.BUILD_BASE_DIR }}/${BUILD_ID}"
          OUTPUT_DIR="${{ env.COMPILER_BASE_DIR }}/compilers/${ARCH}/musl/gcc-${{ needs.setup.outputs.gcc_version }}_binutils-${{ needs.setup.outputs.binutils_version }}"
          
          echo "📊 环境配置汇总:"
          echo "  构建ID: $BUILD_ID"
          echo "  构建目录: $BUILD_DIR"
          echo "  输出目录: $OUTPUT_DIR"
          echo "  目标平台: $TARGET/$SUBTARGET"
          echo "  架构名称: $ARCH_NAME"
          echo "  编译器前缀: $PREFIX-"
          echo "  CPU类型: $CPU_TYPE"
          echo "  调优: $TUNE"
          echo "  ABI: $ABI"
          
          echo "BUILD_DIR=$BUILD_DIR" >> $GITHUB_ENV
          echo "OUTPUT_DIR=$OUTPUT_DIR" >> $GITHUB_ENV
          echo "TARGET=$TARGET" >> $GITHUB_ENV
          echo "SUBTARGET=$SUBTARGET" >> $GITHUB_ENV
          echo "ARCH_NAME=$ARCH_NAME" >> $GITHUB_ENV
          echo "PREFIX=$PREFIX" >> $GITHUB_ENV
          echo "CPU_TYPE=$CPU_TYPE" >> $GITHUB_ENV
          echo "TUNE=$TUNE" >> $GITHUB_ENV
          echo "ABI=$ABI" >> $GITHUB_ENV
          echo "FLOAT_ABI=$FLOAT_ABI" >> $GITHUB_ENV
          echo "BUILD_ID=$BUILD_ID" >> $GITHUB_ENV

      - name: 🔧 安装依赖（根据版本调整）
        if: env.SKIP_BUILD != 'true'
        run: |
          echo "=== 安装构建依赖 ==="
          echo "开始时间: $(date)"
          echo "GCC版本: ${{ needs.setup.outputs.gcc_version }}"
          GCC_MAJOR=$(echo "${{ needs.setup.outputs.gcc_version }}" | cut -d. -f1)
          
          echo "更新软件包列表..."
          sudo apt-get update
          
          echo "安装基础构建工具..."
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y build-essential gcc g++ make flex bison git wget curl file gawk gettext rsync unzip autoconf automake autopoint libtool pkg-config texinfo gperf cmake ninja-build patchutils bc bzip2 xz-utils help2man jq info install-info liblzma-dev zstd libzstd-dev libexpat-dev libgdbm-dev libreadline-dev libsqlite3-dev libffi-dev libelf-dev libdw-dev libcap-dev libattr1-dev libaudit-dev libiberty-dev
          
          echo "安装版本特定的依赖..."
          if [ $GCC_MAJOR -ge 13 ]; then
            echo "GCC 13+ 依赖..."
            sudo DEBIAN_FRONTEND=noninteractive apt-get install -y libgmp-dev libmpfr-dev libmpc-dev libisl-dev libgnutls28-dev libcurl4-gnutls-dev python3 python3-dev python3-distutils libncurses5-dev libssl-dev gnutls-bin liblzma-dev zlib1g-dev
          elif [ $GCC_MAJOR -ge 11 ]; then
            echo "GCC 11-12 依赖..."
            sudo DEBIAN_FRONTEND=noninteractive apt-get install -y libgmp-dev libmpfr-dev libmpc-dev libisl-dev libgnutls28-dev libcurl4-gnutls-dev python3 python3-dev python3-distutils libncurses5-dev libssl-dev gnutls-bin liblzma-dev zlib1g-dev
          else
            echo "GCC 10及以下版本依赖..."
            sudo DEBIAN_FRONTEND=noninteractive apt-get install -y libgmp-dev libmpfr-dev libmpc-dev libisl-dev libgnutls28-dev python3 python3-dev libncurses5-dev libssl-dev liblzma-dev zlib1g-dev
          fi
          
          echo "✅ 依赖安装完成"
          echo "安装的包版本:"
          gcc --version | head -1
          make --version | head -1
          python3 --version
          echo "结束时间: $(date)"

      - name: 📥 下载OpenWrt源码
        if: env.SKIP_BUILD != 'true'
        run: |
          echo "=== 获取OpenWrt源码 ==="
          echo "开始时间: $(date)"
          mkdir -p "${{ env.BUILD_DIR }}"
          cd "${{ env.BUILD_DIR }}"
          echo "当前目录: $(pwd)"
          echo "克隆OpenWrt ${{ needs.setup.outputs.openwrt_version }}..."
          
          OPENWRT_VERSION="${{ needs.setup.outputs.openwrt_version }}"
          
          if [ "$OPENWRT_VERSION" = "snapshot" ]; then
            echo "使用主分支快照..."
            git clone --depth 1 https://github.com/openwrt/openwrt.git .
          else
            echo "使用版本标签 v$OPENWRT_VERSION"
            if git clone --depth 1 --branch "v$OPENWRT_VERSION" https://github.com/openwrt/openwrt.git .; then
              echo "✅ Git clone成功"
            else
              echo "尝试使用tar包..."
              wget --tries=3 --timeout=60 -O openwrt.tar.gz "https://github.com/openwrt/openwrt/archive/refs/tags/v$OPENWRT_VERSION.tar.gz"
              if [ $? -eq 0 ]; then
                tar -xzf openwrt.tar.gz --strip-components=1
                rm -f openwrt.tar.gz
                echo "✅ 使用tar包下载成功"
              else
                echo "❌ 下载失败"
                exit 1
              fi
            fi
          fi
          
          echo "源码大小: $(du -sh . | cut -f1)"
          echo "结束时间: $(date)"

      - name: 🛠️ 配置OpenWrt
        if: env.SKIP_BUILD != 'true'
        run: |
          echo "=== 配置OpenWrt ==="
          echo "开始时间: $(date)"
          cd "${{ env.BUILD_DIR }}"
          
          echo "步骤1: 更新feeds..."
          ./scripts/feeds update -a
          ./scripts/feeds install -a
          
          echo "步骤2: 创建配置..."
          # 基础配置
          echo "CONFIG_TARGET_${{ env.TARGET }}=y" > .config
          echo "CONFIG_TARGET_${{ env.TARGET }}_${{ env.SUBTARGET }}=y" >> .config
          echo "CONFIG_TARGET_ARCH=\"${{ env.ARCH_NAME }}\"" >> .config
          echo "CONFIG_TARGET_PREFIX=\"${{ env.PREFIX }}-\"" >> .config
          
          # 工具链配置
          echo "CONFIG_BUILD_TOOLCHAIN=y" >> .config
          echo "CONFIG_GCC_VERSION=\"${{ needs.setup.outputs.gcc_version }}\"" >> .config
          echo "CONFIG_BINUTILS_VERSION=\"${{ needs.setup.outputs.binutils_version }}\"" >> .config
          echo "CONFIG_LINUX_VERSION=\"${{ needs.setup.outputs.linux_version }}\"" >> .config
          echo "CONFIG_USE_MUSL=y" >> .config
          echo "CONFIG_MUSL_VERSION=\"${{ needs.setup.outputs.musl_version }}\"" >> .config
          
          # 简化配置
          echo "CONFIG_ALL=n" >> .config
          echo "CONFIG_ALL_KMODS=n" >> .config
          echo "CONFIG_ALL_NONSHARED=n" >> .config
          echo "CONFIG_DEVEL=y" >> .config
          echo "CONFIG_SDK=y" >> .config
          
          echo "步骤3: 应用配置..."
          yes "" | make defconfig
          
          echo "步骤4: 验证配置..."
          echo "当前配置摘要:"
          grep -E "CONFIG_TARGET_|CONFIG_GCC|CONFIG_BINUTILS|CONFIG_MUSL" .config
          
          echo "✅ 配置完成"
          echo "结束时间: $(date)"

      - name: 🛠️ 分步构建工具链
        if: env.SKIP_BUILD != 'true'
        timeout-minutes: 180
        run: |
          echo "=== 分步构建工具链 ==="
          cd "${{ env.BUILD_DIR }}"
          echo "工作目录: $(pwd)"
          echo "开始时间: $(date)"
          
          export FORCE_UNSAFE_CONFIGURE=1
          export MAKEFLAGS="-j$(nproc)"
          
          echo "系统信息:"
          echo "- CPU核心: $(nproc)"
          echo "- 内存: $(free -h | grep Mem | awk '{print $2}')"
          echo "- 磁盘空间:"
          df -h .
          
          echo "步骤1: 下载源码包..."
          echo "下载目录内容:"
          ls -lh dl/ 2>/dev/null || echo "下载目录为空"
          
          make download -j$(nproc)
          
          echo "步骤2: 构建host工具..."
          make tools/compile -j$(nproc) V=s 2>&1 | tail -100
          
          echo "步骤3: 安装host工具..."
          make tools/install -j$(nproc) V=s 2>&1 | tail -50
          
          echo "步骤4: 准备工具链..."
          make toolchain/prepare -j$(nproc) V=s 2>&1 | tail -100
          
          echo "步骤5: 编译工具链..."
          make toolchain/compile -j$(nproc) V=s 2>&1 | tee /tmp/compile.log | tail -100
          COMPILE_STATUS=${PIPESTATUS[0]}
          
          if [ $COMPILE_STATUS -eq 0 ]; then
            echo "✅ 工具链编译成功"
          else
            echo "⚠️ 编译有警告，尝试继续..."
          fi
          
          echo "步骤6: 安装工具链..."
          make toolchain/install -j$(nproc) V=s 2>&1 | tail -50
          
          echo "✅ 工具链构建完成"
          echo "总构建时间: $(date)"
          echo "构建目录大小: $(du -sh . | cut -f1)"

      - name: ✅ 验证构建结果
        if: env.SKIP_BUILD != 'true'
        run: |
          echo "=== 验证构建结果 ==="
          echo "开始时间: $(date)"
          cd "${{ env.BUILD_DIR }}"
          
          echo "查找工具链目录..."
          TOOLCHAIN_DIR=""
          for dir in $(find staging_dir -name "toolchain-*" -type d 2>/dev/null | sort); do
            if [ -d "$dir/bin" ]; then
              TOOLCHAIN_DIR="$dir"
              echo "✅ 找到工具链目录: $TOOLCHAIN_DIR"
              break
            fi
          done
          
          if [ -z "$TOOLCHAIN_DIR" ]; then
            echo "⚠️ 未找到标准工具链目录"
            TOOLCHAIN_DIR=$(find staging_dir -name "*toolchain*" -type d 2>/dev/null | head -1)
            if [ -n "$TOOLCHAIN_DIR" ]; then
              echo "找到备选工具链目录: $TOOLCHAIN_DIR"
            fi
          fi
          
          echo "查找编译器..."
          COMPILER_PATH=""
          if [ -n "$TOOLCHAIN_DIR" ] && [ -d "$TOOLCHAIN_DIR/bin" ]; then
            case "${{ env.ARCH_NAME }}" in
              "arm")
                COMPILER_PATH=$(find "$TOOLCHAIN_DIR/bin" -name "arm-openwrt-linux-*gcc" -type f 2>/dev/null | head -1)
                ;;
              "aarch64")
                COMPILER_PATH=$(find "$TOOLCHAIN_DIR/bin" -name "aarch64-openwrt-linux-*gcc" -type f 2>/dev/null | head -1)
                ;;
              "x86_64")
                COMPILER_PATH=$(find "$TOOLCHAIN_DIR/bin" -name "x86_64-openwrt-linux-*gcc" -type f 2>/dev/null | head -1)
                ;;
              "mips"|"mipsel")
                COMPILER_PATH=$(find "$TOOLCHAIN_DIR/bin" -name "mips*-openwrt-linux-*gcc" -type f 2>/dev/null | head -1)
                ;;
              "riscv64")
                COMPILER_PATH=$(find "$TOOLCHAIN_DIR/bin" -name "riscv64-openwrt-linux-*gcc" -type f 2>/dev/null | head -1)
                ;;
            esac
          fi
          
          if [ -n "$COMPILER_PATH" ] && [ -x "$COMPILER_PATH" ]; then
            echo "✅ 找到编译器文件"
            echo "编译器位置: $COMPILER_PATH"
            echo "编译器版本:"
            "$COMPILER_PATH" --version 2>&1 | head -3 || echo "⚠️ 无法获取版本信息"
            
            echo "快速测试编译..."
            echo '#include <stdio.h>' > test.c
            echo 'int main() { printf("Test OK\\n"); return 0; }' >> test.c
            
            if "$COMPILER_PATH" test.c -o test_program 2>&1; then
              echo "✅ 编译测试通过"
              if file test_program 2>/dev/null | grep -q "${{ env.ARCH_NAME }}"; then
                echo "✅ 生成了正确的目标架构文件"
                echo "BUILD_SUCCESS=true" >> $GITHUB_ENV
              fi
              rm -f test.c test_program
            else
              echo "⚠️ 编译测试失败"
              rm -f test.c test_program
            fi
          else
            echo "⚠️ 未找到编译器文件"
            echo "BUILD_SUCCESS=false" >> $GITHUB_ENV
          fi
          
          echo "计算工具链大小..."
          if [ -n "$TOOLCHAIN_DIR" ]; then
            TOOLCHAIN_SIZE=$(du -sh "$TOOLCHAIN_DIR" | cut -f1)
            echo "工具链大小: $TOOLCHAIN_SIZE"
            echo "TOOLCHAIN_SIZE=$TOOLCHAIN_SIZE" >> $GITHUB_ENV
            echo "TOOLCHAIN_DIR=$TOOLCHAIN_DIR" >> $GITHUB_ENV
          fi
          
          echo "✅ 验证完成"
          echo "结束时间: $(date)"

      - name: 💾 保存编译器
        if: env.SKIP_BUILD != 'true' && env.TOOLCHAIN_DIR != ''
        run: |
          echo "=== 保存编译器 ==="
          echo "开始时间: $(date)"
          
          echo "准备输出目录: ${{ env.OUTPUT_DIR }}"
          mkdir -p "${{ env.OUTPUT_DIR }}"
          
          if [ -d "${{ env.OUTPUT_DIR }}" ] && [ "$(ls -A "${{ env.OUTPUT_DIR }}" 2>/dev/null)" ]; then
            echo "清理现有输出目录..."
            rm -rf "${{ env.OUTPUT_DIR }}"/*
          fi
          
          echo "复制工具链文件..."
          if cp -r "${{ env.TOOLCHAIN_DIR }}"/* "${{ env.OUTPUT_DIR }}/" 2>/dev/null; then
            echo "✅ 复制完成"
          else
            echo "⚠️ 复制有错误，尝试逐项复制..."
            for dir in bin lib include libexec share; do
              if [ -d "${{ env.TOOLCHAIN_DIR }}/$dir" ]; then
                mkdir -p "${{ env.OUTPUT_DIR }}/$dir"
                cp -r "${{ env.TOOLCHAIN_DIR }}/$dir"/* "${{ env.OUTPUT_DIR }}/$dir/" 2>/dev/null || true
              fi
            done
          fi
          
          echo "创建元数据文件..."
          echo "{" > "${{ env.OUTPUT_DIR }}/metadata.json"
          echo "  \"architecture\": \"${{ matrix.arch }}\"," >> "${{ env.OUTPUT_DIR }}/metadata.json"
          echo "  \"arch_name\": \"${{ env.ARCH_NAME }}\"," >> "${{ env.OUTPUT_DIR }}/metadata.json"
          echo "  \"libc\": \"musl\"," >> "${{ env.OUTPUT_DIR }}/metadata.json"
          echo "  \"libc_version\": \"${{ needs.setup.outputs.musl_version }}\"," >> "${{ env.OUTPUT_DIR }}/metadata.json"
          echo "  \"gcc_version\": \"${{ needs.setup.outputs.gcc_version }}\"," >> "${{ env.OUTPUT_DIR }}/metadata.json"
          echo "  \"binutils_version\": \"${{ needs.setup.outputs.binutils_version }}\"," >> "${{ env.OUTPUT_DIR }}/metadata.json"
          echo "  \"linux_version\": \"${{ needs.setup.outputs.linux_version }}\"," >> "${{ env.OUTPUT_DIR }}/metadata.json"
          echo "  \"openwrt_version\": \"${{ needs.setup.outputs.openwrt_version }}\"," >> "${{ env.OUTPUT_DIR }}/metadata.json"
          echo "  \"build_date\": \"$(date -Iseconds)\"," >> "${{ env.OUTPUT_DIR }}/metadata.json"
          echo "  \"build_success\": \"${{ env.BUILD_SUCCESS == 'true' && 'true' || 'false' }}\"," >> "${{ env.OUTPUT_DIR }}/metadata.json"
          echo "  \"size\": \"${{ env.TOOLCHAIN_SIZE }}\"" >> "${{ env.OUTPUT_DIR }}/metadata.json"
          echo "}" >> "${{ env.OUTPUT_DIR }}/metadata.json"
          
          echo "计算成果大小..."
          TOTAL_SIZE=$(du -sh "${{ env.OUTPUT_DIR }}" | cut -f1)
          FILE_COUNT=$(find "${{ env.OUTPUT_DIR }}" -type f | wc -l)
          
          echo "📊 成果统计:"
          echo "- 输出目录: ${{ env.OUTPUT_DIR }}"
          echo "- 总大小: $TOTAL_SIZE"
          echo "- 文件数量: $FILE_COUNT"
          
          echo "TOTAL_SIZE=$TOTAL_SIZE" >> $GITHUB_ENV
          echo "FILE_COUNT=$FILE_COUNT" >> $GITHUB_ENV
          
          echo "✅ 编译器保存完成"
          echo "结束时间: $(date)"

      - name: 🔍 搜索验证编译器
        if: env.SKIP_BUILD != 'true'
        run: |
          echo "=== 搜索验证编译器 ==="
          echo "开始时间: $(date)"
          
          echo "在输出目录中搜索编译器..."
          OUTPUT_DIR="${{ env.OUTPUT_DIR }}"
          
          if [ -d "$OUTPUT_DIR/bin" ]; then
            echo "✅ 找到bin目录"
            echo "bin目录内容（前10个）:"
            ls -la "$OUTPUT_DIR/bin/" | head -10
            
            echo "搜索编译器文件..."
            COMPILER_FILES=$(find "$OUTPUT_DIR/bin" -name "*gcc" -type f 2>/dev/null)
            
            if [ -n "$COMPILER_FILES" ]; then
              echo "✅ 找到编译器文件:"
              for file in $COMPILER_FILES; do
                echo "- $(basename $file)"
                if [ -x "$file" ]; then
                  echo "  可执行，版本:"
                  "$file" --version 2>&1 | head -1 || echo "  无法获取版本"
                fi
              done
            else
              echo "⚠️ 未找到编译器文件"
            fi
          else
            echo "❌ 未找到bin目录"
          fi
          
          echo "验证编译功能..."
          if [ -f "$OUTPUT_DIR/bin/${{ env.PREFIX }}-gcc" ] && [ -x "$OUTPUT_DIR/bin/${{ env.PREFIX }}-gcc" ]; then
            TEST_DIR="/tmp/test_compile_${{ env.BUILD_ID }}"
            mkdir -p "$TEST_DIR"
            cd "$TEST_DIR"
            
            echo '#include <stdio.h>' > test.c
            echo 'int main() { printf("Compiler test passed\\n"); return 0; }' >> test.c
            
            if "$OUTPUT_DIR/bin/${{ env.PREFIX }}-gcc" test.c -o test_program; then
              echo "✅ 编译验证通过"
              echo "生成的文件类型:"
              file test_program
            else
              echo "⚠️ 编译验证失败"
            fi
            
            rm -rf "$TEST_DIR"
          fi
          
          echo "显示输出目录结构..."
          echo "输出目录总览:"
          du -sh "$OUTPUT_DIR"
          echo ""
          echo "目录结构:"
          find "$OUTPUT_DIR" -type d | sort | head -20
          
          echo "✅ 搜索验证完成"
          echo "结束时间: $(date)"

      - name: 📊 最终成果统计
        if: env.SKIP_BUILD != 'true'
        run: |
          echo "========================================"
          echo "          🎉 构建完成报告              "
          echo "========================================"
          
          if [ "${{ env.BUILD_SUCCESS }}" = "true" ]; then
            echo "✅ 交叉编译器构建成功!"
          else
            echo "⚠️ 交叉编译器构建可能存在问题"
          fi
          
          echo ""
          echo "📊 构建信息:"
          echo "- 架构: ${{ matrix.arch }} (${{ env.ARCH_NAME }})"
          echo "- C库: musl ${{ needs.setup.outputs.musl_version }}"
          echo "- GCC版本: ${{ needs.setup.outputs.gcc_version }}"
          echo "- Binutils版本: ${{ needs.setup.outputs.binutils_version }}"
          echo "- Linux内核: ${{ needs.setup.outputs.linux_version }}"
          echo "- OpenWrt版本: ${{ needs.setup.outputs.openwrt_version }}"
          echo "- 编译器前缀: ${{ env.PREFIX }}-"
          echo "- 构建状态: ${{ env.BUILD_SUCCESS == 'true' && '成功' || '可能有问题' }}"
          echo ""
          echo "📦 成果统计:"
          echo "- 总大小: ${{ env.TOTAL_SIZE }}"
          echo "- 文件数量: ${{ env.FILE_COUNT }}"
          echo "- 输出目录: ${{ env.OUTPUT_DIR }}"
          echo "- 工具链大小: ${{ env.TOOLCHAIN_SIZE }}"
          echo ""
          
          if [ "${{ env.BUILD_SUCCESS }}" = "true" ]; then
            echo "🚀 使用方法:"
            echo "cd ${{ env.OUTPUT_DIR }}"
            echo 'export PATH="$PWD/bin:$PATH"'
            echo "${{ env.PREFIX }}-gcc --version"
          else
            echo "⚠️ 注意: 编译器可能不完整"
            echo "建议检查构建日志"
          fi
          
          echo ""
          echo "⏰ 构建时间: $(date)"
          echo "========================================"

      - name: 📤 提交到Git仓库
        if: env.SKIP_BUILD != 'true' && env.TOTAL_SIZE != ''
        run: |
          echo "=== 提交编译器到Git仓库 ==="
          echo "开始时间: $(date)"
          cd "${{ github.workspace }}"
          
          git config user.email "github-actions@github.com"
          git config user.name "GitHub Actions"
          git config pull.rebase false
          
          echo "拉取最新代码..."
          git pull origin main --no-edit
          
          echo "添加编译器文件..."
          git add -f "firmware-config/build-Compiler-file/compilers/*"
          
          if ! git diff --cached --quiet; then
            echo "检测到变更，创建提交..."
            
            git commit -m "build(compiler): 添加 ${{ matrix.arch }} 交叉编译器" \
              -m "架构: ${{ matrix.arch }}" \
              -m "GCC: ${{ needs.setup.outputs.gcc_version }}" \
              -m "Binutils: ${{ needs.setup.outputs.binutils_version }}" \
              -m "Musl: ${{ needs.setup.outputs.musl_version }}" \
              -m "大小: ${{ env.TOTAL_SIZE }}" \
              -m "文件数: ${{ env.FILE_COUNT }}" \
              -m "状态: ${{ env.BUILD_SUCCESS == 'true' && '成功' || '部分完成' }}"
            
            echo "推送到仓库..."
            if git push origin main; then
              echo "✅ 编译器已成功提交到仓库"
            else
              echo "⚠️ 推送失败，重新尝试..."
              git pull origin main --rebase
              git push origin main
            fi
          else
            echo "没有变更需要提交"
          fi
          
          echo "结束时间: $(date)"

      - name: 🧹 清理构建目录
        if: always() && env.SKIP_BUILD != 'true'
        run: |
          echo "=== 清理构建目录 ==="
          echo "开始时间: $(date)"
          
          if [ -d "${{ env.BUILD_DIR }}" ]; then
            echo "清理构建目录: ${{ env.BUILD_DIR }}"
            rm -rf "${{ env.BUILD_DIR }}"
            echo "✅ 构建目录已清理"
          else
            echo "构建目录不存在，无需清理"
          fi
          
          echo "结束时间: $(date)"

  summary:
    needs: build-matrix
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: 📊 构建总结
        run: |
          echo "========================================"
          echo "           🎯 构建任务完成              "
          echo "========================================"
          echo "架构类型: ${{ github.event.inputs.arch_type }}"
          echo "GCC版本: ${{ github.event.inputs.gcc_version }}"
          echo "开始时间: ${{ needs.setup.outputs.start_time }}"
          echo "完成时间: $(date)"
          echo "========================================"
          echo "所有构建任务已完成，请查看上方详细报告"
          echo "========================================"
