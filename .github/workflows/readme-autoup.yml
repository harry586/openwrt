# 【readme-autoup.yml-01】
# 更新频率：可手动触发或每周一早上8点自动运行
# 更新说明：自动扫描仓库目录，更新 README.md 中的项目结构表
# readme-autoup.yml-01-end

name: 自动更新 README 目录结构

on:
  schedule:
    - cron: '0 0 * * 1'  # 每周一早上8点（UTC+8）
  workflow_dispatch:      # 支持手动触发

jobs:
  update-readme:
    name: 更新 README 目录表
    runs-on: ubuntu-22.04
    permissions:
      contents: write

    steps:
      #【readme-autoup.yml-02】
      - name: "01. 生成时间戳（UTC+8时区）"
        run: |
          echo "=== 步骤01: 生成时间戳（UTC+8时区） ==="
          
          sudo timedatectl set-timezone Asia/Shanghai 2>/dev/null || true
          
          UPDATE_TIME=$(date +'%Y-%m-%d %H:%M:%S')
          echo "🕐 更新时间: $UPDATE_TIME"
          
          TIMESTAMP_LOCAL=$(date +'%Y%m%d_%H%M%S')
          echo "TIMESTAMP_LOCAL=$TIMESTAMP_LOCAL" >> $GITHUB_ENV
          echo "UPDATE_TIME=$UPDATE_TIME" >> $GITHUB_ENV
          
          echo "✅ 生成时间戳: $TIMESTAMP_LOCAL"
      #【readme-autoup.yml-02-end】

      #【readme-autoup.yml-03】
      - name: "02. 检出代码"
        uses: actions/checkout@v4
      #【readme-autoup.yml-03-end】

      #【readme-autoup.yml-04】
      - name: "03. 安装 tree 命令"
        run: sudo apt-get install -y tree
      #【readme-autoup.yml-04-end】

      #【readme-autoup.yml-05】
      - name: "04. 生成目录树并更新 README"
        run: |
          echo "=== 步骤04: 生成目录树并更新 README ==="
          
          # 生成目录树
          TREE_OUTPUT=$(tree . -I '.git|.github|__pycache__|*.pyc' --dirsfirst -F -n)
          
          # 删除从"## 📁 项目结构"到文件末尾的所有内容
          sed -i '/^## 📁 项目结构/,$d' README.md
          
          # 追加新的项目结构内容（严格控制格式，不留多余空行）
          cat >> README.md <<- 'EOF'
          
          ## 📁 项目结构
          
          EOF
          
          echo "\`\`\`" >> README.md
          echo "$TREE_OUTPUT" >> README.md
          echo "\`\`\`" >> README.md
          echo "" >> README.md
          echo "注意，自定义文件名是英文的，方便复制、运行" >> README.md
          echo "" >> README.md
          printf "> 最后更新时间: %s" "$UPDATE_TIME" >> README.md
          
          echo "✅ README.md 更新完成，最后更新时间: $UPDATE_TIME"
      #【readme-autoup.yml-05-end】

      #【readme-autoup.yml-06】
      - name: "05. 检查是否有变更"
        id: git-check
        run: |
          echo "=== 步骤05: 检查 README 变更 ==="
          
          if git diff --quiet README.md; then
            echo "changed=false" >> $GITHUB_OUTPUT
            echo "📝 README.md 没有变更，跳过提交"
          else
            echo "changed=true" >> $GITHUB_OUTPUT
            echo "📝 README.md 已更新，准备提交"
            
            # 显示变更内容
            echo "📋 变更内容:"
            git diff README.md | cat
          fi
      #【readme-autoup.yml-06-end】

      #【readme-autoup.yml-07】
      - name: "06. 提交更改（如果有）"
        if: steps.git-check.outputs.changed == 'true'
        run: |
          echo "=== 步骤06: 提交更改 ==="
          
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add README.md
          
          # 使用时间戳生成提交信息
          git commit -m "docs: 自动更新项目目录结构

          更新说明：
          - 自动扫描仓库目录结构
          - 同步更新 README.md 中的项目结构表
          - 最后更新时间: $UPDATE_TIME
          - 时间戳: $TIMESTAMP_LOCAL

          [skip ci]"
          
          git push
          echo "✅ 更改已提交并推送"
      #【readme-autoup.yml-07-end】

      #【readme-autoup.yml-08】
      - name: "07. 更新总结"
        if: always()
        run: |
          echo "=== 步骤07: 更新总结 ==="
          echo "========================================"
          echo "📊 README 目录结构更新总结"
          echo "========================================"
          echo "🕐 更新时间: $UPDATE_TIME"
          echo "🔢 时间戳: $TIMESTAMP_LOCAL"
          echo "📁 更新状态: ${{ steps.git-check.outputs.changed == 'true' && '已更新' || '无变更' }}"
          echo "========================================"
      #【readme-autoup.yml-08-end】

#【readme-autoup.yml-99】
# 工作流文件结束
# 最后更新时间: 使用步骤01生成的时间戳
#【readme-autoup.yml-99-end】
