# 【readme-autoup.yml-01】
# 更新频率：可手动触发或每周一早上8点自动运行
# 更新说明：自动扫描仓库目录，更新 README.md 中的项目结构表
# readme-autoup.yml-01-end

name: 自动更新 README 目录结构

on:
  schedule:
    - cron: '0 0 * * 1'  # 每周一早上8点（UTC+8）
  workflow_dispatch:      # 支持手动触发

jobs:
  update-readme:
    name: 更新 README 目录表
    runs-on: ubuntu-22.04
    permissions:
      contents: write

    steps:
      # 【readme-autoup.yml-02】检出代码
      - name: 检出代码
        uses: actions/checkout@v4
      # readme-autoup.yml-02-end

      # 【readme-autoup.yml-03】安装 tree 命令
      - name: 安装 tree
        run: sudo apt-get install -y tree
      # readme-autoup.yml-03-end

      # 【readme-autoup.yml-04】生成目录树并更新 README
      - name: 更新 README 中的项目结构
        run: |
          # 生成目录树，使用 -n 参数关闭颜色输出，避免特殊字符
          TREE_OUTPUT=$(tree . -I '.git|.github|__pycache__|*.pyc' --dirsfirst -F -n)
          
          # 创建临时文件
          cp README.md README.md.tmp
          
          # 使用 awk 处理文件替换 - 只替换第一个项目结构部分
          awk -v tree="$TREE_OUTPUT" '
          BEGIN { section=0; replaced=0; }
          {
            # 找到项目结构开始标记
            if ($0 ~ /^## 📁 项目结构/ && replaced==0) {
              print $0;                    # 打印标题
              print "";                     # 空行
              print "\`\`\`";                # 代码块开始
              print tree;                   # 打印目录树
              print "\`\`\`";                # 代码块结束
              print "";                      # 空行
              print "注意，自定义文件名是英文的，方便复制、运行";  # 打印说明
              section=1;                     # 进入跳过模式
              replaced=1;                    # 标记已替换
              next;
            }
            
            # 如果处于跳过模式，跳过直到找到说明行
            if (section==1) {
              if ($0 ~ /注意，自定义文件名是英文的，方便复制、运行/) {
                section=0;                    # 结束跳过模式
              }
              next;                           # 跳过当前行
            }
            
            # 打印其他所有行
            print $0;
          }' README.md.tmp > README.md.new
          
          # 替换原文件
          mv README.md.new README.md
          rm -f README.md.tmp
      # readme-autoup.yml-04-end

      # 【readme-autoup.yml-05】检查是否有变更
      - name: 检查 README 变更
        id: git-check
        run: |
          if git diff --quiet README.md; then
            echo "changed=false" >> $GITHUB_OUTPUT
            echo "README.md 没有变更，跳过提交"
          else
            echo "changed=true" >> $GITHUB_OUTPUT
            echo "README.md 已更新，准备提交"
          fi
      # readme-autoup.yml-05-end

      # 【readme-autoup.yml-06】提交更改（如果有）
      - name: 提交更改
        if: steps.git-check.outputs.changed == 'true'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add README.md
          git commit -m "docs: 自动更新项目目录结构

          更新内容：
          - 自动扫描并更新项目目录结构
          - 保持文档与实际仓库结构同步

          [skip ci]"
          git push
      # readme-autoup.yml-06-end
