name: 🛠️ 通用IPK包编译工作流（多包支持+源码压缩包）

on:
  workflow_dispatch:
    inputs:
      package_names:
        description: '📦 要编译的IPK包名 (用顿号、分隔，如: luci-app-filetransfer、luci-app-turboacc)。留空则只编译源码压缩包'
        required: false
        type: string
        default: ''
      source_packages:
        description: '📁 源码压缩包文件名 (用顿号、分隔，如: luci-app-filetransfer.zip、my-package.tar.gz)。文件需放在firmware-config/packages/目录'
        required: false
        type: string
        default: ''
      build_all_packages:
        description: '📦 编译packages目录内的所有压缩包 (优先级高于指定压缩包)'
        required: false
        default: false
        type: boolean
      version_selection:
        description: '🔄 OpenWrt版本选择'
        required: true
        type: choice
        default: '21.02'
        options:
        - '23.05'
        - '21.02'
      extra_deps:
        description: '🔧 额外依赖包 (用顿号、分隔，如: luci-base、luci-compat)'
        required: false
        type: string
        default: ''
      clean_build:
        description: '🧹 全新编译 (清理缓存重新编译)'
        required: false
        default: true
        type: boolean

env:
  BUILD_DIR: /mnt/openwrt-build-ipk

jobs:
  build-ipk:
    runs-on: ubuntu-22.04
    
    steps:
    - name: 检出仓库
      uses: actions/checkout@v4

    - name: 设置脚本执行权限
      run: |
        echo "=== 设置脚本执行权限 ==="
        find . -name "*.sh" -exec chmod +x {} \; 2>/dev/null || true
        echo "✅ 脚本执行权限设置完成"

    - name: 初始空间检查
      run: |
        echo "=== 初始磁盘空间检查 ==="
        df -h
        AVAILABLE_SPACE=$(df /mnt --output=avail | tail -1)
        AVAILABLE_GB=$((AVAILABLE_SPACE / 1024 / 1024))
        echo "/mnt 可用空间: ${AVAILABLE_GB}G"
        if [ $AVAILABLE_GB -lt 20 ]; then
          echo "错误: /mnt 空间不足20G"
          exit 1
        fi

    - name: 检查源码压缩包
      run: |
        echo "=== 检查源码压缩包 ==="
        if [ -d "firmware-config/packages" ]; then
          echo "📦 可用的源码压缩包:"
          find firmware-config/packages -name "*.zip" -o -name "*.tar.gz" -o -name "*.tgz" -o -name "*.tar.bz2" -o -name "*.tar.xz" 2>/dev/null | while read file; do
            echo "  - $(basename "$file")"
          done
        else
          echo "📁 firmware-config/packages 目录不存在"
        fi

    - name: 设置编译环境
      run: |
        echo "=== 设置编译环境 ==="
        firmware-config/scripts/build_ipk_main.sh setup_environment

    - name: 创建构建目录
      run: |
        echo "=== 创建构建目录 ==="
        firmware-config/scripts/build_ipk_main.sh create_build_dir

    - name: 初始化构建环境
      run: |
        echo "=== 初始化构建环境 ==="
        firmware-config/scripts/build_ipk_main.sh initialize_build_env "${{ github.event.inputs.version_selection }}"

    - name: 配置Feeds
      run: |
        echo "=== 配置Feeds ==="
        firmware-config/scripts/build_ipk_main.sh configure_feeds

    - name: 下载自定义包
      run: |
        echo "=== 下载自定义包 ==="
        firmware-config/scripts/build_ipk_main.sh download_custom_packages "${{ github.event.inputs.package_names }}"

    - name: 处理源码压缩包
      run: |
        echo "=== 处理源码压缩包 ==="
        firmware-config/scripts/build_ipk_main.sh process_source_packages "${{ github.event.inputs.source_packages }}" "${{ github.event.inputs.build_all_packages }}"

    - name: 编译前空间检查
      run: |
        echo "=== 编译前空间检查 ==="
        firmware-config/scripts/build_ipk_main.sh pre_build_space_check

    - name: 生成IPK配置
      run: |
        echo "=== 生成IPK配置 ==="
        firmware-config/scripts/build_ipk_main.sh generate_config "${{ github.event.inputs.package_names }}" "${{ github.event.inputs.extra_deps }}"

    - name: 应用配置
      run: |
        echo "=== 应用配置 ==="
        firmware-config/scripts/build_ipk_main.sh apply_config

    - name: 修复网络环境
      run: |
        echo "=== 修复网络环境 ==="
        firmware-config/scripts/build_ipk_main.sh fix_network

    - name: 下载依赖包
      run: |
        echo "=== 下载依赖包 ==="
        firmware-config/scripts/build_ipk_main.sh download_dependencies

    - name: 编译IPK包
      run: |
        echo "=== 编译IPK包 ==="
        firmware-config/scripts/build_ipk_main.sh build_ipk "${{ github.event.inputs.package_names }}" "${{ github.event.inputs.clean_build }}"

    - name: 创建安装脚本
      run: |
        echo "=== 创建安装脚本 ==="
        firmware-config/scripts/build_ipk_main.sh create_install_script

    - name: 上传IPK文件
      uses: actions/upload-artifact@v4
      with:
        name: ipk-packages-${{ github.event.inputs.version_selection }}
        path: ${{ env.BUILD_DIR }}/ipk_output/
        retention-days: 30

    - name: 上传编译日志
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: ipk-build-log-${{ github.event.inputs.version_selection }}
        path: ${{ env.BUILD_DIR }}/build_ipk.log
        retention-days: 30

    - name: 清理目录
      if: always()
      run: |
        echo "=== 清理IPK构建目录 ==="
        firmware-config/scripts/build_ipk_main.sh cleanup
