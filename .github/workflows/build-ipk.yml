name: 🛠️ 通用IPK包编译工作流（全平台支持）

on:
  workflow_dispatch:
    inputs:
      package_name:
        description: '📦 要编译的IPK包名 (如: luci-app-filetransfer)'
        required: true
        default: 'luci-app-filetransfer'
        type: string
      version_selection:
        description: '🔄 OpenWrt版本选择'
        required: true
        type: choice
        default: '21.02'
        options:
        - '23.05'
        - '21.02'
      extra_deps:
        description: '🔧 额外依赖包 (用逗号分隔，如: luci-base,luci-compat)'
        required: false
        type: string
        default: ''
      clean_build:
        description: '🧹 全新编译 (清理缓存重新编译)'
        required: false
        default: true
        type: boolean

env:
  BUILD_DIR: /mnt/openwrt-build-ipk

jobs:
  build-ipk:
    runs-on: ubuntu-22.04
    
    steps:
    - name: 检出仓库
      uses: actions/checkout@v4

    - name: 设置脚本执行权限
      run: |
        echo "=== 设置脚本执行权限 ==="
        chmod +x scripts/build_ipk.sh
        echo "✅ 脚本执行权限设置完成"

    - name: 初始空间检查
      run: |
        echo "=== 初始磁盘空间检查 ==="
        df -h
        AVAILABLE_SPACE=$(df /mnt --output=avail | tail -1)
        AVAILABLE_GB=$((AVAILABLE_SPACE / 1024 / 1024))
        echo "/mnt 可用空间: ${AVAILABLE_GB}G"
        if [ $AVAILABLE_GB -lt 20 ]; then
          echo "错误: /mnt 空间不足20G"
          exit 1
        fi

    - name: 编译IPK包
      run: |
        scripts/build_ipk.sh \
          "${{ github.event.inputs.package_name }}" \
          "${{ github.event.inputs.version_selection }}" \
          "${{ github.event.inputs.extra_deps }}" \
          "${{ github.event.inputs.clean_build }}"

    - name: 上传IPK文件
      uses: actions/upload-artifact@v4
      with:
        name: ipk-${{ github.event.inputs.package_name }}-${{ github.event.inputs.version_selection }}
        path: ${{ env.BUILD_DIR }}/ipk_output/
        retention-days: 30

    - name: 上传编译日志
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: ipk-build-log-${{ github.event.inputs.package_name }}-${{ github.event.inputs.version_selection }}
        path: ${{ env.BUILD_DIR }}/build_ipk.log
        retention-days: 30

    - name: 清理目录
      if: always()
      run: |
        echo "=== 清理IPK构建目录 ==="
        sudo rm -rf $BUILD_DIR || echo "⚠️ 清理构建目录失败"
        echo "✅ 构建目录已清理"
