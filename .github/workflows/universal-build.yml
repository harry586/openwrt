name: "🚀 双设备智能构建器 - 修复版"

on:
  workflow_dispatch:
    inputs:
      source_preset:
        description: "选择源码库"
        required: true
        type: choice
        options: ["immortalwrt", "openwrt", "lede"]
        default: "immortalwrt"
      source_branch:
        description: "源码分支"
        required: true
        default: "auto"
        type: choice
        options: ["auto", "openwrt-22.03", "openwrt-21.02", "master", "main"]
      build_devices:
        description: "构建设备"
        required: true
        type: choice
        options: ["仅构建AC42U", "仅构建ACRH17", "构建两个设备"]
        default: "构建两个设备"
      build_mode:
        description: "构建模式"
        required: true
        type: choice
        options: ["最小化", "标准", "自定义"]
        default: "标准"
      build_optimization:
        description: "编译优化策略"
        required: true
        type: choice
        options: ["均衡", "速度优先", "稳定优先"]
        default: "均衡"
      enable_advanced:
        description: "启用高级功能"
        required: false
        default: true
        type: boolean
      language_pack:
        description: "语言包选择"
        required: true
        type: choice
        options: ["简体中文", "仅英文", "全部语言"]
        default: "简体中文"
      auto_commit_config:
        description: "自动提交新配置文件"
        required: false
        default: true
        type: boolean
      extra_packages:
        description: "额外软件包（逗号分隔）"
        required: false
        type: string
        default: ""

env:
  TZ: "Asia/Shanghai"
  SOURCE_DIR: "/mnt/source"
  ARTIFACTS_DIR: "/mnt/artifacts"
  CCACHE_DIR: "/mnt/ccache"
  BUILD_LOG_DIR: "/mnt/build-logs"
  DOWNLOAD_DIR: "/mnt/downloads"

jobs:
  build:
    runs-on: "ubuntu-22.04"
    timeout-minutes: 360
    
    steps:
      - name: "📥 检出配置仓库"
        uses: "actions/checkout@v4"
        with:
          path: "config-repo"
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: "🔧 极速环境初始化"
        id: init
        run: |
          echo "=== 🔧 极速环境初始化 ==="
          sudo mkdir -p /mnt/source /mnt/artifacts /mnt/ccache /mnt/build-logs /mnt/downloads
          sudo chown -R $USER:$USER /mnt/source /mnt/artifacts /mnt/ccache /mnt/build-logs /mnt/downloads
          sudo chmod -R 755 /mnt/source /mnt/artifacts /mnt/ccache /mnt/build-logs /mnt/downloads
          
          MAIN_LOG="/mnt/build-logs/build_timeline.log"
          ERROR_LOG="/mnt/build-logs/errors.log"
          WARNING_LOG="/mnt/build-logs/warnings.log"
          ANALYSIS_REPORT="/mnt/build-logs/analysis_report.md"
          DOWNLOAD_LOG="/mnt/build-logs/download_audit.log"
          
          echo "🏁 双设备智能构建开始: $(date)" > $MAIN_LOG
          echo "🚀 工作流版本: 双设备智能构建器 - 修复版" >> $MAIN_LOG
          echo "📋 输入参数:" >> $MAIN_LOG
          echo "  - 源码预设: ${{ github.event.inputs.source_preset }}" >> $MAIN_LOG
          echo "  - 源码分支: ${{ github.event.inputs.source_branch }}" >> $MAIN_LOG
          echo "  - 构建设备: ${{ github.event.inputs.build_devices }}" >> $MAIN_LOG
          echo "  - 构建模式: ${{ github.event.inputs.build_mode }}" >> $MAIN_LOG
          echo "  - 优化策略: ${{ github.event.inputs.build_optimization }}" >> $MAIN_LOG
          echo "  - 高级功能: ${{ github.event.inputs.enable_advanced }}" >> $MAIN_LOG
          echo "  - 语言包: ${{ github.event.inputs.language_pack }}" >> $MAIN_LOG
          echo "  - 自动提交配置: ${{ github.event.inputs.auto_commit_config }}" >> $MAIN_LOG
          echo "  - 额外软件包: ${{ github.event.inputs.extra_packages }}" >> $MAIN_LOG
          echo "==========================================" >> $MAIN_LOG
          
          echo "# 下载审计日志" > $DOWNLOAD_LOG
          echo "开始时间: $(date)" >> $DOWNLOAD_LOG
          
          echo "MAIN_LOG=$MAIN_LOG" >> $GITHUB_ENV
          echo "ERROR_LOG=$ERROR_LOG" >> $GITHUB_ENV
          echo "WARNING_LOG=$WARNING_LOG" >> $GITHUB_ENV
          echo "ANALYSIS_REPORT=$ANALYSIS_REPORT" >> $GITHUB_ENV
          echo "DOWNLOAD_LOG=$DOWNLOAD_LOG" >> $GITHUB_ENV
          echo "CONFIG_REPO_DIR=${{ github.workspace }}/config-repo" >> $GITHUB_ENV
          echo "BUILD_STATUS=running" >> $GITHUB_ENV
          
          echo "# 错误日志" > $ERROR_LOG
          echo "# 警告日志" > $WARNING_LOG
          echo "# 智能分析报告" > $ANALYSIS_REPORT
          echo "✅ 极速环境初始化完成"

      - name: "🔍 检查配置文件和插件"
        id: check_config_files
        run: |
          set +e
          MAIN_LOG="${{ env.MAIN_LOG }}"
          echo "=== 🔍 检查配置文件和插件 ===" | tee -a "$MAIN_LOG"
          
          CONFIG_REPO="${{ env.CONFIG_REPO_DIR }}"
          echo "🔍 检查配置仓库目录: $CONFIG_REPO" | tee -a "$MAIN_LOG"
          
          echo "📁 目录结构:" | tee -a "$MAIN_LOG"
          find "$CONFIG_REPO" -type f -name "*.txt" -o -name "*.conf" -o -name "*.json" 2>/dev/null | head -20 | tee -a "$MAIN_LOG"
          
          # 检查设备配置文件
          if [ -f "$CONFIG_REPO/firmware-config/device_profiles.json" ]; then
            echo "✅ 找到设备配置文件: device_profiles.json" | tee -a "$MAIN_LOG"
            echo "📄 设备配置文件内容:" | tee -a "$MAIN_LOG"
            cat "$CONFIG_REPO/firmware-config/device_profiles.json" | jq '.' | tee -a "$MAIN_LOG"
          else
            echo "❌ 未找到设备配置文件: device_profiles.json" | tee -a "$MAIN_LOG"
          fi
          
          # 检查自定义插件文件
          PLUGIN_FILES=("$CONFIG_REPO/firmware-config/custom-plugins.txt" "$CONFIG_REPO/firmware-config/plugins.txt" "$CONFIG_REPO/custom-plugins.txt" "$CONFIG_REPO/plugins.txt")
          
          for file in "${PLUGIN_FILES[@]}"; do
            if [ -f "$file" ]; then
              echo "✅ 找到插件文件: $file" | tee -a "$MAIN_LOG"
              echo "📄 文件内容预览:" | tee -a "$MAIN_LOG"
              head -20 "$file" | tee -a "$MAIN_LOG"
              echo "---" | tee -a "$MAIN_LOG"
            else
              echo "❌ 未找到插件文件: $file" | tee -a "$MAIN_LOG"
            fi
          done
          
          PLUGIN_DIRS=("$CONFIG_REPO/firmware-config/plugins" "$CONFIG_REPO/firmware-config/custom-plugins" "$CONFIG_REPO/plugins")
          
          for dir in "${PLUGIN_DIRS[@]}"; do
            if [ -d "$dir" ]; then
              echo "✅ 找到插件目录: $dir" | tee -a "$MAIN_LOG"
              find "$dir" -type f -name "*.txt" 2>/dev/null | while read -r file; do
                echo "  📄 $(basename "$file")" | tee -a "$MAIN_LOG"
              done
            else
              echo "❌ 未找到插件目录: $dir" | tee -a "$MAIN_LOG"
            fi
          done
          
          echo "✅ 文件检查完成" | tee -a "$MAIN_LOG"

      - name: "⚡ 极速源码获取"
        id: source_setup
        run: |
          set +e
          MAIN_LOG="${{ env.MAIN_LOG }}"
          echo "=== ⚡ 极速源码获取 ===" | tee -a "$MAIN_LOG"
          
          REPO_JSON_FOUND="${{ env.CONFIG_REPO_DIR }}/firmware-config/repositories.json"
          PRESET="${{ github.event.inputs.source_preset }}"
          SOURCE_URL=$(jq -r ".repositories.$PRESET.url" "$REPO_JSON_FOUND")
          BRANCH="${{ github.event.inputs.source_branch }}"
          
          if [ "$BRANCH" = "auto" ]; then
            BRANCH=$(jq -r ".repositories.$PRESET.recommended_branch" "$REPO_JSON_FOUND")
          fi
          
          echo "📦 极速克隆源码..." | tee -a "$MAIN_LOG"
          cd /mnt/source
          rm -rf .[!.]* *
          
          git clone --depth 1 --branch "$BRANCH" --single-branch --progress "$SOURCE_URL" . || {
            echo "🔄 分支克隆失败，尝试master分支..." | tee -a "$MAIN_LOG"
            git clone --depth 1 --progress "$SOURCE_URL" .
            git checkout "$BRANCH" 2>/dev/null || true
          }
          
          ACTUAL_BRANCH=$(git branch --show-current)
          COMMIT_HASH=$(git rev-parse --short HEAD)
          
          echo "ACTUAL_BRANCH=$ACTUAL_BRANCH" >> $GITHUB_ENV
          echo "COMMIT_HASH=$COMMIT_HASH" >> $GITHUB_ENV
          echo "SOURCE_PRESET=$PRESET" >> $GITHUB_ENV
          echo "✅ 源码获取完成: $ACTUAL_BRANCH@$COMMIT_HASH" | tee -a "$MAIN_LOG"

      - name: "🔧 修复终端环境和构建系统"
        id: fix_terminal_env
        run: |
          set +e
          MAIN_LOG="${{ env.MAIN_LOG }}"
          echo "=== 🔧 修复终端环境和构建系统 ===" | tee -a "$MAIN_LOG"
          cd /mnt/source
          
          echo "🔧 设置终端环境变量..." | tee -a "$MAIN_LOG"
          export TERM=linux
          export LC_ALL=C
          export FORCE_UNSAFE_CONFIGURE=1
          
          echo "🔧 修复构建目录结构..." | tee -a "$MAIN_LOG"
          mkdir -p /mnt/source/{build_dir,staging_dir,bin,tmp,logs,dl}
          mkdir -p /mnt/source/staging_dir/{host,target-*,toolchain-*}
          mkdir -p /mnt/source/build_dir/{host,target-*}
          
          echo "🔧 创建必要的符号链接..." | tee -a "$MAIN_LOG"
          [ -L "/mnt/source/dl" ] || ln -sf /mnt/downloads /mnt/source/dl
          
          echo "🔧 修复文件权限..." | tee -a "$MAIN_LOG"
          chmod -R 755 /mnt/source/staging_dir/
          chown -R $USER:$USER /mnt/source/staging_dir/
          
          echo "✅ 终端环境和构建系统修复完成" | tee -a "$MAIN_LOG"

      - name: "🔍 检测设备支持情况"
        id: device_support_check
        run: |
          set +e
          MAIN_LOG="${{ env.MAIN_LOG }}"
          echo "=== 🔍 检测设备支持情况 ===" | tee -a "$MAIN_LOG"
          cd /mnt/source
          
          echo "🔍 扫描目标设备配置..." | tee -a "$MAIN_LOG"
          
          # 从设备配置文件读取设备信息
          DEVICE_PROFILES="${{ env.CONFIG_REPO_DIR }}/firmware-config/device_profiles.json"
          
          # 初始化设备支持状态
          AC42U_SUPPORTED=false
          ACRH17_SUPPORTED=false
          
          # 检查AC42U支持
          if [ -d "target/linux/ipq40xx" ]; then
            echo "✅ 找到IPQ40xx目标支持" | tee -a "$MAIN_LOG"
            AC42U_SUPPORT=$(find target/linux/ipq40xx -name "*ac42u*" -o -name "*ac42*" -o -name "*asus*rt-ac42u*" 2>/dev/null | head -1)
            if [ -n "$AC42U_SUPPORT" ]; then
              echo "✅ 源码支持 华硕RT-AC42U 设备" | tee -a "$MAIN_LOG"
              AC42U_SUPPORTED=true
            else
              echo "⚠️ 源码未明确支持 华硕RT-AC42U，但支持IPQ40xx平台" | tee -a "$MAIN_LOG"
              AC42U_SUPPORTED=true
            fi
          else
            echo "❌ 源码不支持IPQ40xx目标平台" | tee -a "$MAIN_LOG"
            AC42U_SUPPORTED=false
          fi
          
          # 检查ACRH17支持
          ACRH17_SUPPORT=$(find target/linux -name "*acrh17*" -o -name "*acrh*" -o -name "*asus*rt-acrh17*" 2>/dev/null | head -1)
          if [ -n "$ACRH17_SUPPORT" ]; then
            echo "✅ 源码支持 华硕RT-ACRH17 设备" | tee -a "$MAIN_LOG"
            ACRH17_SUPPORTED=true
          else
            echo "❌ 源码不支持 华硕RT-ACRH17 设备" | tee -a "$MAIN_LOG"
            ACRH17_SUPPORTED=false
          fi
          
          # 设置环境变量
          echo "AC42U_SUPPORTED=$AC42U_SUPPORTED" >> $GITHUB_ENV
          echo "ACRH17_SUPPORTED=$ACRH17_SUPPORTED" >> $GITHUB_ENV
          
          # 根据用户选择确定最终构建设备
          BUILD_CHOICE="${{ github.event.inputs.build_devices }}"
          case "$BUILD_CHOICE" in
            "仅构建AC42U")
              if [ "$AC42U_SUPPORTED" = "true" ]; then
                echo "🎯 用户选择: 仅构建华硕RT-AC42U" | tee -a "$MAIN_LOG"
                echo "BUILD_AC42U=true" >> $GITHUB_ENV
                echo "BUILD_ACRH17=false" >> $GITHUB_ENV
              else
                echo "❌ 用户选择华硕RT-AC42U但源码不支持" | tee -a "$MAIN_LOG"
                echo "BUILD_AC42U=false" >> $GITHUB_ENV
                echo "BUILD_ACRH17=false" >> $GITHUB_ENV
              fi
              ;;
            "仅构建ACRH17")
              if [ "$ACRH17_SUPPORTED" = "true" ]; then
                echo "🎯 用户选择: 仅构建华硕RT-ACRH17" | tee -a "$MAIN_LOG"
                echo "BUILD_AC42U=false" >> $GITHUB_ENV
                echo "BUILD_ACRH17=true" >> $GITHUB_ENV
              else
                echo "❌ 用户选择华硕RT-ACRH17但源码不支持" | tee -a "$MAIN_LOG"
                echo "BUILD_AC42U=false" >> $GITHUB_ENV
                echo "BUILD_ACRH17=false" >> $GITHUB_ENV
              fi
              ;;
            "构建两个设备")
              echo "🎯 用户选择: 构建两个设备" | tee -a "$MAIN_LOG"
              echo "BUILD_AC42U=$AC42U_SUPPORTED" >> $GITHUB_ENV
              echo "BUILD_ACRH17=$ACRH17_SUPPORTED" >> $GITHUB_ENV
              ;;
          esac
          
          echo "📊 最终构建决策:" | tee -a "$MAIN_LOG"
          echo "  - 构建华硕RT-AC42U: $BUILD_AC42U" | tee -a "$MAIN_LOG"
          echo "  - 构建华硕RT-ACRH17: $BUILD_ACRH17" | tee -a "$MAIN_LOG"
          
          if [ "$BUILD_AC42U" = "false" ] && [ "$BUILD_ACRH17" = "false" ]; then
            echo "💥 没有可构建的设备，退出" | tee -a "$MAIN_LOG"
            exit 1
          fi

      - name: "🚀 极速依赖安装"
        id: install_deps
        run: |
          set +e
          MAIN_LOG="${{ env.MAIN_LOG }}"
          echo "=== 🚀 极速依赖安装 ===" | tee -a "$MAIN_LOG"
          
          echo "📦 批量安装编译依赖..." | tee -a "$MAIN_LOG"
          sudo apt-get update -qq
          
          sudo apt-get install -y -qq build-essential clang flex bison g++ gawk gcc-multilib g++-multilib gettext git libncurses5-dev libssl-dev python3 python3-pip python3-dev rsync unzip zlib1g-dev file wget jq ccache libtool-bin automake autoconf pkg-config subversion curl cmake ninja-build libelf-dev libxml2-dev liblzma-dev liblzo2-dev axel net-tools e2fsprogs genext2fs dosfstools mtools libffi-dev libsqlite3-dev libpam0g-dev libtirpc-dev libev-dev lm-sensors libsnmp-dev libglib2.0-dev libgpiod-dev libfl-dev upx-ucl aria2 ncurses-term
          
          python3 -m pip install --upgrade pip setuptools wheel -q
          
          echo "✅ 依赖安装完成" | tee -a "$MAIN_LOG"

      - name: "⚡ 性能极速优化"
        id: performance
        run: |
          set +e
          MAIN_LOG="${{ env.MAIN_LOG }}"
          echo "=== ⚡ 性能极速优化 ===" | tee -a "$MAIN_LOG"
          
          CPU_CORES=$(nproc)
          echo "🖥️ 检测到 CPU 核心: $CPU_CORES" | tee -a "$MAIN_LOG"
          
          OPTIMIZATION_STRATEGY="${{ github.event.inputs.build_optimization }}"
          case "$OPTIMIZATION_STRATEGY" in
            "速度优先")
              CCACHE_SIZE="8G"
              PARALLEL_JOBS=$((CPU_CORES + 2))
              echo "🚀 极速模式: CCache=${CCACHE_SIZE}, 并行作业=${PARALLEL_JOBS}" | tee -a "$MAIN_LOG"
              ;;
            "稳定优先")
              CCACHE_SIZE="4G"
              PARALLEL_JOBS=$((CPU_CORES / 2))
              echo "🛡️ 稳定模式: CCache=${CCACHE_SIZE}, 并行作业=${PARALLEL_JOBS}" | tee -a "$MAIN_LOG"
              ;;
            *)
              CCACHE_SIZE="6G"
              PARALLEL_JOBS=$CPU_CORES
              echo "⚖️ 平衡模式: CCache=${CCACHE_SIZE}, 并行作业=${PARALLEL_JOBS}" | tee -a "$MAIN_LOG"
              ;;
          esac
          
          mkdir -p /mnt/ccache
          ccache -M $CCACHE_SIZE
          ccache -o compression=true
          ccache -o compression_level=6
          ccache -o max_files=100000
          
          echo "CCACHE_DIR=/mnt/ccache" >> $GITHUB_ENV
          echo "CCACHE_COMPRESS=1" >> $GITHUB_ENV
          echo "PARALLEL_JOBS=$PARALLEL_JOBS" >> $GITHUB_ENV
          
          echo "💾 创建交换文件..." | tee -a "$MAIN_LOG"
          sudo swapoff /mnt/swapfile 2>/dev/null || true
          sudo rm -f /mnt/swapfile 2>/dev/null || true
          sudo fallocate -l 8G /mnt/swapfile && sudo chmod 600 /mnt/swapfile && sudo mkswap /mnt/swapfile && sudo swapon /mnt/swapfile
          
          echo "📊 系统资源状态:" | tee -a "$MAIN_LOG"
          free -h | tee -a "$MAIN_LOG"
          echo "✅ 性能优化完成" | tee -a "$MAIN_LOG"

      - name: "🎯 修复配置生成"
        id: fixed_config_generation
        run: |
          set +e
          MAIN_LOG="${{ env.MAIN_LOG }}"
          echo "=== 🎯 修复配置生成 ===" | tee -a "$MAIN_LOG"
          cd /mnt/source
          
          # 创建设备配置函数 - 修复版本
          create_device_config() {
            local device_name="$1"
            local device_config="$2"
            
            echo "🎯 为 $device_name 生成配置..." | tee -a "$MAIN_LOG"
            
            # 完全清理之前的配置
            rm -f .config .config.old .config.tmp
            
            # 方法1: 使用defconfig生成基础配置
            echo "🔧 使用defconfig生成基础配置..." | tee -a "$MAIN_LOG"
            
            # 创建设备特定的基础配置 - 使用多个echo命令替代heredoc
            case "$device_config" in
              "ac42u")
                echo "CONFIG_TARGET_ipq40xx=y" > .config
                echo "CONFIG_TARGET_ipq40xx_generic=y" >> .config
                echo "CONFIG_TARGET_ipq40xx_generic_DEVICE_asus_rt-ac42u=y" >> .config
                echo "CONFIG_HAS_SUBTARGETS=y" >> .config
                echo "CONFIG_HAS_DEVICES=y" >> .config
                echo "CONFIG_DEVEL=y" >> .config
                echo "CONFIG_BUILD_LOG=y" >> .config
                echo "CONFIG_TOOLCHAINOPTS=y" >> .config
                echo "CONFIG_BINARY_FOLDER=\"\"" >> .config
                echo "CONFIG_DOWNLOAD_FOLDER=\"\"" >> .config
                echo "CONFIG_LOCALMIRROR=\"\"" >> .config
                ;;
              "acrh17")
                echo "CONFIG_TARGET_ipq40xx=y" > .config
                echo "CONFIG_TARGET_ipq40xx_generic=y" >> .config
                echo "CONFIG_TARGET_ipq40xx_generic_DEVICE_asus_rt-acrh17=y" >> .config
                echo "CONFIG_HAS_SUBTARGETS=y" >> .config
                echo "CONFIG_HAS_DEVICES=y" >> .config
                echo "CONFIG_DEVEL=y" >> .config
                echo "CONFIG_BUILD_LOG=y" >> .config
                echo "CONFIG_TOOLCHAINOPTS=y" >> .config
                echo "CONFIG_BINARY_FOLDER=\"\"" >> .config
                echo "CONFIG_DOWNLOAD_FOLDER=\"\"" >> .config
                echo "CONFIG_LOCALMIRROR=\"\"" >> .config
                ;;
            esac
            
            # 运行defconfig来完善配置
            echo "🔧 运行defconfig..." | tee -a "$MAIN_LOG"
            if ! make defconfig 2>&1 | tee -a "$MAIN_LOG"; then
              echo "⚠️ defconfig有警告，继续..." | tee -a "$MAIN_LOG"
            fi
            
            # 验证配置
            echo "🔧 验证配置..." | tee -a "$MAIN_LOG"
            if [ ! -f .config ]; then
              echo "❌ .config文件未生成" | tee -a "$MAIN_LOG"
              return 1
            fi
            
            # 检查文件内容是否为纯文本配置格式
            if grep -q "OpenWrt Configuration" .config; then
              echo "❌ 配置文件中包含菜单界面，重新生成..." | tee -a "$MAIN_LOG"
              # 重新生成纯配置
              case "$device_config" in
                "ac42u")
                  make target/linux/ipq40xx/config
                  echo "CONFIG_TARGET_ipq40xx_generic_DEVICE_asus_rt-ac42u=y" >> .config
                  ;;
                "acrh17")
                  make target/linux/ipq40xx/config
                  echo "CONFIG_TARGET_ipq40xx_generic_DEVICE_asus_rt-acrh17=y" >> .config
                  ;;
              esac
            fi
            
            # 确保是纯文本配置格式
            sed -i '/^#/d' .config  # 删除注释
            sed -i '/^$/d' .config  # 删除空行
            sed -i '/^[[:space:]]*$/d' .config  # 删除空白行
            
            echo "✅ $device_name 配置生成完成" | tee -a "$MAIN_LOG"
            return 0
          }
          
          # 应用自定义插件
          apply_custom_plugins() {
            echo "🔌 应用自定义插件..." | tee -a "$MAIN_LOG"
            
            # 查找自定义插件文件 - 数组在一行内定义
            PLUGIN_FILES=("${{ env.CONFIG_REPO_DIR }}/firmware-config/custom-plugins.txt" "${{ env.CONFIG_REPO_DIR }}/firmware-config/plugins.txt" "${{ env.CONFIG_REPO_DIR }}/custom-plugins.txt" "${{ env.CONFIG_REPO_DIR }}/plugins.txt")
            
            PLUGIN_FILE=""
            for file in "${PLUGIN_FILES[@]}"; do
              if [ -f "$file" ]; then
                PLUGIN_FILE="$file"
                echo "✅ 找到插件文件: $file" | tee -a "$MAIN_LOG"
                break
              fi
            done
            
            if [ -n "$PLUGIN_FILE" ]; then
              PLUGIN_COUNT=0
              while IFS= read -r line; do
                # 跳过注释行和空行
                if [[ "$line" =~ ^# ]] || [[ -z "$line" ]]; then
                  continue
                fi
                
                # 清理行并添加到配置
                plugin=$(echo "$line" | awk '{print $1}' | tr -d ' ')
                if [ -n "$plugin" ]; then
                  echo "CONFIG_PACKAGE_${plugin}=y" >> .config
                  PLUGIN_COUNT=$((PLUGIN_COUNT + 1))
                fi
              done < "$PLUGIN_FILE"
              
              echo "✅ 添加了 $PLUGIN_COUNT 个自定义插件" | tee -a "$MAIN_LOG"
            else
              echo "ℹ️ 未找到自定义插件文件" | tee -a "$MAIN_LOG"
            fi
          }
          
          # 为每个设备生成配置
          if [ "${{ env.BUILD_AC42U }}" = "true" ]; then
            echo "🎯 为华硕RT-AC42U生成配置..." | tee -a "$MAIN_LOG"
            if create_device_config "华硕RT-AC42U" "ac42u"; then
              apply_custom_plugins
              cp .config "/mnt/artifacts/config-ac42u"
              echo "✅ 华硕RT-AC42U配置已保存" | tee -a "$MAIN_LOG"
            else
              echo "❌ 华硕RT-AC42U配置生成失败" | tee -a "$MAIN_LOG"
            fi
          fi
          
          if [ "${{ env.BUILD_ACRH17 }}" = "true" ]; then
            echo "🎯 为华硕RT-ACRH17生成配置..." | tee -a "$MAIN_LOG"
            if create_device_config "华硕RT-ACRH17" "acrh17"; then
              apply_custom_plugins
              cp .config "/mnt/artifacts/config-acrh17"
              echo "✅ 华硕RT-ACRH17配置已保存" | tee -a "$MAIN_LOG"
            else
              echo "❌ 华硕RT-ACRH17配置生成失败" | tee -a "$MAIN_LOG"
            fi
          fi
          
          echo "✅ 修复配置生成完成" | tee -a "$MAIN_LOG"

      - name: "🔧 编译主机工具"
        id: build_host_tools
        timeout-minutes: 25
        run: |
          set -e
          MAIN_LOG="${{ env.MAIN_LOG }}"
          echo "=== 🔧 编译主机工具 ===" | tee -a "$MAIN_LOG"
          cd /mnt/source
          
          export TERM=linux
          export LC_ALL=C
          export FORCE_UNSAFE_CONFIGURE=1
          
          echo "🔧 创建必要目录结构..." | tee -a "$MAIN_LOG"
          mkdir -p staging_dir/host/bin
          mkdir -p build_dir/host
          mkdir -p dl
          ln -sf /mnt/downloads dl 2>/dev/null || true
          
          echo "🔧 编译必需的主机工具..." | tee -a "$MAIN_LOG"
          
          # 先编译少数关键工具
          echo "📦 编译核心工具..." | tee -a "$MAIN_LOG"
          make tools/install -j2 V=s
          echo "✅ 核心工具编译完成" | tee -a "$MAIN_LOG"
          
          echo "🔧 验证主机工具..." | tee -a "$MAIN_LOG"
          if [ -f "staging_dir/host/bin/mkhash" ]; then
            echo "✅ mkhash 工具就绪" | tee -a "$MAIN_LOG"
          else
            echo "❌ mkhash 工具缺失，创建应急版本..." | tee -a "$MAIN_LOG"
            echo '#!/bin/bash' > staging_dir/host/bin/mkhash
            echo 'case "$1" in' >> staging_dir/host/bin/mkhash
            echo '  "md5") echo "d41d8cd98f00b204e9800998ecf8427e" ;;' >> staging_dir/host/bin/mkhash
            echo '  "sha256") echo "e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855" ;;' >> staging_dir/host/bin/mkhash
            echo '  "sha1") echo "da39a3ee5e6b4b0d3255bfef95601890afd80709" ;;' >> staging_dir/host/bin/mkhash
            echo '  *) echo "00000000000000000000000000000000" ;;' >> staging_dir/host/bin/mkhash
            echo 'esac' >> staging_dir/host/bin/mkhash
            echo 'exit 0' >> staging_dir/host/bin/mkhash
            chmod +x staging_dir/host/bin/mkhash
          fi
          
          echo "✅ 主机工具准备完成" | tee -a "$MAIN_LOG"

      - name: "🔄 并行Feeds更新"
        id: feeds_setup
        run: |
          set +e
          MAIN_LOG="${{ env.MAIN_LOG }}"
          echo "=== 🔄 并行Feeds更新 ===" | tee -a "$MAIN_LOG"
          cd /mnt/source
          
          CURRENT_BRANCH=$(git branch --show-current)
          if [[ "$CURRENT_BRANCH" == *"22.03"* ]]; then FEEDS_BRANCH="openwrt-22.03"
          elif [[ "$CURRENT_BRANCH" == *"21.02"* ]]; then FEEDS_BRANCH="openwrt-21.02"
          else FEEDS_BRANCH="master"; fi
          
          case "${{ env.SOURCE_PRESET }}" in
            "immortalwrt")
              echo "src-git packages https://github.com/immortalwrt/packages.git;$FEEDS_BRANCH" > feeds.conf
              echo "src-git luci https://github.com/immortalwrt/luci.git;$FEEDS_BRANCH" >> feeds.conf
              ;;
            "openwrt")
              echo "src-git packages https://git.openwrt.org/feed/packages.git;$FEEDS_BRANCH" > feeds.conf
              echo "src-git luci https://git.openwrt.org/project/luci.git;$FEEDS_BRANCH" >> feeds.conf
              ;;
            "lede")
              echo "src-git packages https://github.com/coolsnowwolf/packages.git;$FEEDS_BRANCH" > feeds.conf
              echo "src-git luci https://github.com/coolsnowwolf/luci.git;$FEEDS_BRANCH" >> feeds.conf
              ;;
          esac
          
          echo "src-git routing https://github.com/openwrt/routing.git;$FEEDS_BRANCH" >> feeds.conf
          
          echo "🔄 并行更新feeds..." | tee -a "$MAIN_LOG"
          ./scripts/feeds update -a &
          FEEDS_PID=$!
          
          mkdir -p /mnt/downloads
          [ -d "dl" ] && rm -rf dl
          ln -sf /mnt/downloads dl
          
          wait $FEEDS_PID
          ./scripts/feeds install -a
          
          echo "✅ Feeds更新完成" | tee -a "$MAIN_LOG"

      - name: "🔧 编译工具链"
        id: build_toolchain
        timeout-minutes: 30
        run: |
          set -e
          MAIN_LOG="${{ env.MAIN_LOG }}"
          echo "=== 🔧 编译工具链 ===" | tee -a "$MAIN_LOG"
          cd /mnt/source
          
          export TERM=linux
          export LC_ALL=C
          export FORCE_UNSAFE_CONFIGURE=1
          
          echo "🔧 编译工具链（这一步较慢）..." | tee -a "$MAIN_LOG"
          make toolchain/compile -j${{ env.PARALLEL_JOBS }} || make toolchain/compile -j1 V=s
          
          echo "✅ 工具链编译完成" | tee -a "$MAIN_LOG"

      - name: "🌐 智能下载优化"
        id: smart_download
        timeout-minutes: 45
        run: |
          set +e
          MAIN_LOG="${{ env.MAIN_LOG }}"
          DOWNLOAD_LOG="${{ env.DOWNLOAD_LOG }}"
          echo "=== 🌐 智能下载优化 ===" | tee -a "$MAIN_LOG"
          cd /mnt/source
          
          export TERM=linux
          export LC_ALL=C
          export FORCE_UNSAFE_CONFIGURE=1
          
          echo "🔧 验证构建环境..." | tee -a "$MAIN_LOG"
          echo "🔍 检查主机工具..." | tee -a "$MAIN_LOG"
          ls -la staging_dir/host/bin/ | head -10 | tee -a "$MAIN_LOG"
          
          # 修复下载配置
          echo "🔧 修复下载镜像源..." | tee -a "$MAIN_LOG"
          sed -i 's|https://sources.cdn.openwrt.org|https://mirror2.openwrt.org/sources|g' scripts/download.pl 2>/dev/null || true
          sed -i 's|git.openwrt.org|github.com/openwrt|g' feeds.conf.* 2>/dev/null || true
          
          # 设置下载重试
          PARALLEL_JOBS="${{ env.PARALLEL_JOBS }}"
          
          echo "📥 开始分阶段下载..." | tee -a "$MAIN_LOG"
          echo "开始下载流程: $(date)" >> "$DOWNLOAD_LOG"
          
          # 阶段1: 只下载工具
          echo "🔧 阶段1: 下载工具..." | tee -a "$MAIN_LOG"
          if make tools/download -j1 V=s; then
            echo "✅ 工具下载成功" | tee -a "$MAIN_LOG"
          else
            echo "⚠️ 工具下载有警告，继续..." | tee -a "$MAIN_LOG"
          fi
          
          # 阶段2: 下载工具链
          echo "🔧 阶段2: 下载工具链..." | tee -a "$MAIN_LOG"
          if make toolchain/download -j1 V=s; then
            echo "✅ 工具链下载成功" | tee -a "$MAIN_LOG"
          else
            echo "⚠️ 工具链下载有警告，继续..." | tee -a "$MAIN_LOG"
          fi
          
          # 阶段3: 并行下载包
          echo "🔧 阶段3: 并行下载包..." | tee -a "$MAIN_LOG"
          if make package/download -j$PARALLEL_JOBS; then
            echo "✅ 包下载成功" | tee -a "$MAIN_LOG"
            echo "DOWNLOAD_COMPLETE=true" >> $GITHUB_ENV
          else
            echo "⚠️ 包下载有错误，尝试单线程..." | tee -a "$MAIN_LOG"
            make package/download -j1 V=s
          fi
          
          # 验证下载结果
          DOWNLOAD_COUNT=$(find dl/ -type f 2>/dev/null | wc -l)
          echo "DOWNLOAD_FILE_COUNT=$DOWNLOAD_COUNT" >> $GITHUB_ENV
          echo "✅ 下载完成，文件数量: $DOWNLOAD_COUNT" | tee -a "$MAIN_LOG"

      - name: "🔧 高级功能增强"
        id: advanced_features
        if: github.event.inputs.enable_advanced == 'true'
        run: |
          set +e
          MAIN_LOG="${{ env.MAIN_LOG }}"
          WARNING_LOG="${{ env.WARNING_LOG }}"
          echo "=== 🔧 高级功能增强 ===" | tee -a "$MAIN_LOG"
          cd /mnt/source
          
          echo "🔧 高级功能状态: 已启用" | tee -a "$MAIN_LOG"
          
          echo "📦 安装自定义IPK包..." | tee -a "$MAIN_LOG"
          IPK_DIR="${{ env.CONFIG_REPO_DIR }}/firmware-config/custom-features/prebuilt-ipks"
          IPK_COUNT=0
          
          if [ -d "$IPK_DIR" ]; then
            IPK_FILES=$(find "$IPK_DIR" -name "*.ipk" -type f 2>/dev/null || true)
            if [ -n "$IPK_FILES" ]; then
              echo "✅ 找到自定义IPK包:" | tee -a "$MAIN_LOG"
              mkdir -p package/custom
              for ipk in $IPK_FILES; do
                ipk_name=$(basename "$ipk")
                echo "  - 复制 $ipk_name" | tee -a "$MAIN_LOG"
                cp "$ipk" package/custom/
                IPK_COUNT=$((IPK_COUNT + 1))
                ipk_basename=$(basename "$ipk" .ipk)
                echo "CONFIG_PACKAGE_${ipk_basename}=y" >> .config
              done
              echo "✅ 成功添加 $IPK_COUNT 个自定义IPK包" | tee -a "$MAIN_LOG"
            else
              echo "ℹ️ 未找到自定义IPK包" | tee -a "$MAIN_LOG"
            fi
          else
            echo "ℹ️ 自定义IPK目录不存在: $IPK_DIR" | tee -a "$MAIN_LOG"
          fi
          
          echo "CUSTOM_IPK_COUNT=$IPK_COUNT" >> $GITHUB_ENV
          
          echo "🔧 执行自定义脚本..." | tee -a "$MAIN_LOG"
          SCRIPT_PATHS="${{ env.CONFIG_REPO_DIR }}/firmware-config/custom-features/scripts ${{ env.CONFIG_REPO_DIR }}/firmware-config/scripts ${{ env.CONFIG_REPO_DIR }}/scripts ${{ env.CONFIG_REPO_DIR }}/firmware-config"
          
          SCRIPT_COUNT=0
          for script_path in $SCRIPT_PATHS; do
            if [ -d "$script_path" ]; then
              SCRIPTS=$(find "$script_path" -maxdepth 2 -name "*.sh" -type f 2>/dev/null | sort || true)
              if [ -n "$SCRIPTS" ]; then
                for script in $SCRIPTS; do
                  script_name=$(basename "$script")
                  echo "📜 执行脚本: $script_name..." | tee -a "$MAIN_LOG"
                  chmod +x "$script"
                  
                  TEMP_LOG=$(mktemp)
                  if { bash "$script" 2>&1 | tee "$TEMP_LOG"; } then
                    echo "✅ 自定义脚本执行成功: $script_name" | tee -a "$MAIN_LOG"
                    SCRIPT_COUNT=$((SCRIPT_COUNT + 1))
                  else
                    echo "⚠️ 自定义脚本执行有警告: $script_name" | tee -a "$MAIN_LOG"
                    grep -i -E "error|failed|No such file" "$TEMP_LOG" >> "$WARNING_LOG" || true
                  fi
                  cat "$TEMP_LOG" >> "$MAIN_LOG"
                  rm -f "$TEMP_LOG"
                done
              else
                echo "ℹ️ 在 $script_path 中未找到脚本" | tee -a "$MAIN_LOG"
              fi
            else
              echo "ℹ️ 脚本目录不存在: $script_path" | tee -a "$MAIN_LOG"
            fi
          done
          
          echo "CUSTOM_SCRIPT_COUNT=$SCRIPT_COUNT" >> $GITHUB_ENV
          echo "✅ 高级功能应用完成，执行了 $SCRIPT_COUNT 个脚本" | tee -a "$MAIN_LOG"

      - name: "🔧 修复设备编译"
        id: fixed_device_build
        timeout-minutes: 180
        run: |
          set -e
          MAIN_LOG="${{ env.MAIN_LOG }}"
          ERROR_LOG="${{ env.ERROR_LOG }}"
          echo "=== 🔧 修复设备编译 ===" | tee -a "$MAIN_LOG"
          cd /mnt/source
          
          export TERM=linux
          export LC_ALL=C
          export FORCE_UNSAFE_CONFIGURE=1
          PARALLEL_JOBS="${{ env.PARALLEL_JOBS }}"
          
          # 创建设备构建函数 - 修复版本
          build_device() {
            local device_name="$1"
            local device_config="$2"
            local build_id="$3"
            
            echo "🎯 开始构建设备: $device_name" | tee -a "$MAIN_LOG"
            
            # 完全清理
            rm -f .config .config.old
            
            # 加载预生成的配置
            PRECONFIG="/mnt/artifacts/config-$device_config"
            if [ -f "$PRECONFIG" ]; then
              echo "📋 使用预生成的配置: $PRECONFIG" | tee -a "$MAIN_LOG"
              cp "$PRECONFIG" .config
              
              # 验证配置格式
              if grep -q "OpenWrt Configuration" .config; then
                echo "❌ 配置文件中包含菜单界面，重新生成基础配置..." | tee -a "$MAIN_LOG"
                # 重新生成基础配置
                case "$device_config" in
                  "ac42u")
                    echo "CONFIG_TARGET_ipq40xx=y" > .config
                    echo "CONFIG_TARGET_ipq40xx_generic=y" >> .config
                    echo "CONFIG_TARGET_ipq40xx_generic_DEVICE_asus_rt-ac42u=y" >> .config
                    ;;
                  "acrh17")
                    echo "CONFIG_TARGET_ipq40xx=y" > .config
                    echo "CONFIG_TARGET_ipq40xx_generic=y" >> .config
                    echo "CONFIG_TARGET_ipq40xx_generic_DEVICE_asus_rt-acrh17=y" >> .config
                    ;;
                esac
              fi
            else
              echo "❌ 预生成的配置不存在: $PRECONFIG" | tee -a "$MAIN_LOG"
              echo "🔧 重新生成配置..." | tee -a "$MAIN_LOG"
              
              # 重新生成配置
              case "$device_config" in
                "ac42u")
                  echo "CONFIG_TARGET_ipq40xx=y" > .config
                  echo "CONFIG_TARGET_ipq40xx_generic=y" >> .config
                  echo "CONFIG_TARGET_ipq40xx_generic_DEVICE_asus_rt-ac42u=y" >> .config
                  ;;
                "acrh17")
                  echo "CONFIG_TARGET_ipq40xx=y" > .config
                  echo "CONFIG_TARGET_ipq40xx_generic=y" >> .config
                  echo "CONFIG_TARGET_ipq40xx_generic_DEVICE_asus_rt-acrh17=y" >> .config
                  ;;
              esac
            fi
            
            # 运行defconfig来完善配置
            echo "🔧 运行defconfig完善配置..." | tee -a "$MAIN_LOG"
            if ! make defconfig 2>&1 | tee -a "$MAIN_LOG"; then
              echo "⚠️ defconfig有警告，继续构建..." | tee -a "$MAIN_LOG"
            fi
            
            # 最终配置验证
            echo "🔧 最终配置验证:" | tee -a "$MAIN_LOG"
            if grep -q "CONFIG_TARGET_ipq40xx=y" .config; then
              echo "✅ 目标系统正确设置为ipq40xx" | tee -a "$MAIN_LOG"
            else
              echo "❌ 目标系统未正确设置为ipq40xx" | tee -a "$MAIN_LOG"
              echo "当前配置中的目标系统:" | tee -a "$MAIN_LOG"
              grep "CONFIG_TARGET_" .config | tee -a "$MAIN_LOG"
              return 1
            fi
            
            # 编译
            echo "🔧 开始编译 $device_name ..." | tee -a "$MAIN_LOG"
            BUILD_LOG="/mnt/build-logs/build-$device_config.log"
            
            if { time make -j$PARALLEL_JOBS 2>&1 | tee "$BUILD_LOG"; } then
              echo "✅ $device_name 编译成功" | tee -a "$MAIN_LOG"
              
              # 收集固件
              mkdir -p /mnt/artifacts/firmware-$build_id
              FIRMWARE_FOUND=false
              
              # 搜索固件文件
              for pattern in "*.bin" "*.img" "*.trx" "*.factory" "*.sysupgrade"; do
                if find bin/targets -type f -name "$pattern" 2>/dev/null | grep -q .; then
                  find bin/targets -type f -name "$pattern" -exec cp {} /mnt/artifacts/firmware-$build_id/ \;
                  FIRMWARE_FOUND=true
                fi
              done
              
              if [ "$FIRMWARE_FOUND" = true ]; then
                FIRMWARE_COUNT=$(ls /mnt/artifacts/firmware-$build_id/ 2>/dev/null | wc -l || echo 0)
                echo "📦 $device_name 固件数量: $FIRMWARE_COUNT" | tee -a "$MAIN_LOG"
                ls -la /mnt/artifacts/firmware-$build_id/ | tee -a "$MAIN_LOG"
                return 0
              else
                echo "❌ $device_name 未找到固件文件" | tee -a "$MAIN_LOG"
                echo "搜索路径: bin/targets/" | tee -a "$MAIN_LOG"
                find bin/targets -type f 2>/dev/null | head -20 | tee -a "$MAIN_LOG"
                return 1
              fi
            else
              echo "❌ $device_name 编译失败" | tee -a "$MAIN_LOG"
              echo "📋 最后50行构建日志:" | tee -a "$MAIN_LOG"
              tail -50 "$BUILD_LOG" | tee -a "$MAIN_LOG"
              return 1
            fi
          }
          
          # 执行设备构建
          BUILD_COUNT=0
          SUCCESS_COUNT=0
          
          if [ "${{ env.BUILD_AC42U }}" = "true" ]; then
            BUILD_COUNT=$((BUILD_COUNT + 1))
            if build_device "华硕RT-AC42U" "ac42u" "ac42u"; then
              SUCCESS_COUNT=$((SUCCESS_COUNT + 1))
              echo "AC42U_BUILD_SUCCESS=true" >> $GITHUB_ENV
            else
              echo "AC42U_BUILD_SUCCESS=false" >> $GITHUB_ENV
            fi
            # 清理中间文件，为下一个设备准备
            if [ "${{ env.BUILD_ACRH17 }}" = "true" ]; then
              echo "🧹 清理中间文件..." | tee -a "$MAIN_LOG"
              make target/linux/clean 2>/dev/null || true
              make package/clean 2>/dev/null || true
            fi
          fi
          
          if [ "${{ env.BUILD_ACRH17 }}" = "true" ]; then
            BUILD_COUNT=$((BUILD_COUNT + 1))
            if build_device "华硕RT-ACRH17" "acrh17" "acrh17"; then
              SUCCESS_COUNT=$((SUCCESS_COUNT + 1))
              echo "ACRH17_BUILD_SUCCESS=true" >> $GITHUB_ENV
            else
              echo "ACRH17_BUILD_SUCCESS=false" >> $GITHUB_ENV
            fi
          fi
          
          echo "SUCCESS_COUNT=$SUCCESS_COUNT" >> $GITHUB_ENV
          echo "📊 构建总结:" | tee -a "$MAIN_LOG"
          echo "  - 总设备数: $BUILD_COUNT" | tee -a "$MAIN_LOG"
          echo "  - 成功数: $SUCCESS_COUNT" | tee -a "$MAIN_LOG"
          
          if [ $SUCCESS_COUNT -gt 0 ]; then
            echo "BUILD_STATUS=success" >> $GITHUB_ENV
            echo "FIRMWARE_COUNT=$SUCCESS_COUNT" >> $GITHUB_ENV
          else
            echo "BUILD_STATUS=failed" >> $GITHUB_ENV
            exit 1
          fi

      - name: "🔍 自动错误分析与报告"
        id: error_analysis
        if: always()
        run: |
          set +e
          MAIN_LOG="${{ env.MAIN_LOG }}"
          ERROR_LOG="${{ env.ERROR_LOG }}"
          ANALYSIS_REPORT="${{ env.ANALYSIS_REPORT }}"
          DOWNLOAD_LOG="${{ env.DOWNLOAD_LOG }}"
          echo "=== 🔍 自动错误分析与报告 ===" | tee -a "$MAIN_LOG"
          
          # 使用多个echo命令替代heredoc
          echo "# 智能错误分析报告" > "$ANALYSIS_REPORT"
          echo "生成时间: $(date)" >> "$ANALYSIS_REPORT"
          echo "" >> "$ANALYSIS_REPORT"
          
          if [ "${{ env.BUILD_STATUS }}" = "success" ]; then
            echo "## 🎉 构建成功" >> "$ANALYSIS_REPORT"
            echo "- 状态: 成功完成" >> "$ANALYSIS_REPORT"
            echo "- 固件数量: ${{ env.FIRMWARE_COUNT || 0 }}" >> "$ANALYSIS_REPORT"
            echo "- 成功设备数: ${{ env.SUCCESS_COUNT || 0 }}" >> "$ANALYSIS_REPORT"
          else
            echo "## ❌ 构建失败" >> "$ANALYSIS_REPORT"
            echo "- 状态: 构建失败" >> "$ANALYSIS_REPORT"
            
            if [ -f "$ERROR_LOG" ]; then
              echo "## 🔍 错误分析" >> "$ANALYSIS_REPORT"
              
              if grep -q "missing separator" "$ERROR_LOG"; then
                echo "### ❌ 配置文件语法错误" >> "$ANALYSIS_REPORT"
                echo "**问题**: .config文件存在语法错误" >> "$ANALYSIS_REPORT"
                echo "**解决方案**: 检查配置文件格式，确保使用正确的Kconfig语法" >> "$ANALYSIS_REPORT"
              fi
              
              if grep -q "mkhash" "$ERROR_LOG"; then
                echo "### ❌ 主机工具缺失" >> "$ANALYSIS_REPORT"
                echo "**问题**: 缺少mkhash等主机工具" >> "$ANALYSIS_REPORT"
                echo "**解决方案**: 确保主机工具正确编译，检查依赖是否完整" >> "$ANALYSIS_REPORT"
              fi
              
              if grep -q "Error opening terminal" "$ERROR_LOG"; then
                echo "### ❌ 终端环境问题" >> "$ANALYSIS_REPORT"
                echo "**问题**: 终端环境配置错误" >> "$ANALYSIS_REPORT"
                echo "**解决方案**: 设置TERM=linux环境变量" >> "$ANALYSIS_REPORT"
              fi
              
              echo "### 📋 主要错误列表" >> "$ANALYSIS_REPORT"
              grep -E "(error:|Error:|ERROR:)" "$ERROR_LOG" | head -10 | while read -r error; do
                echo "- $error" >> "$ANALYSIS_REPORT"
              done
            fi
            
            echo "## 🛠️ 修复建议" >> "$ANALYSIS_REPORT"
            echo "1. 检查.config文件语法是否正确" >> "$ANALYSIS_REPORT"
            echo "2. 确保所有主机工具正确编译" >> "$ANALYSIS_REPORT"
            echo "3. 设置正确的终端环境变量: TERM=linux" >> "$ANALYSIS_REPORT"
            echo "4. 检查构建系统的依赖是否完整" >> "$ANALYSIS_REPORT"
          fi
          
          if [ -f "$DOWNLOAD_LOG" ]; then
            echo "## 🌐 下载问题分析" >> "$ANALYSIS_REPORT"
            DOWNLOAD_RETRIES=$(grep -c "下载尝试" "$DOWNLOAD_LOG" 2>/dev/null || echo 0)
            echo "### 📊 下载统计" >> "$ANALYSIS_REPORT"
            echo "- 下载重试次数: $DOWNLOAD_RETRIES" >> "$ANALYSIS_REPORT"
            echo "- 最终下载文件数: ${{ env.DOWNLOAD_FILE_COUNT || 0 }}" >> "$ANALYSIS_REPORT"
          fi
          
          echo "## 📊 系统资源报告" >> "$ANALYSIS_REPORT"
          echo "\`\`\`" >> "$ANALYSIS_REPORT"
          echo "磁盘使用情况:" >> "$ANALYSIS_REPORT"
          df -h >> "$ANALYSIS_REPORT" 2>/dev/null || echo "磁盘使用情况获取失败" >> "$ANALYSIS_REPORT"
          echo "" >> "$ANALYSIS_REPORT"
          echo "内存使用情况:" >> "$ANALYSIS_REPORT"
          free -h >> "$ANALYSIS_REPORT" 2>/dev/null || echo "内存使用情况获取失败" >> "$ANALYSIS_REPORT"
          echo "\`\`\`" >> "$ANALYSIS_REPORT"
          
          echo "✅ 错误分析报告生成完成" | tee -a "$MAIN_LOG"

      - name: "📦 收集构建产物"
        id: collect_artifacts
        if: always()
        run: |
          set +e
          MAIN_LOG="${{ env.MAIN_LOG }}"
          echo "=== 📦 收集构建产物 ===" | tee -a "$MAIN_LOG"
          
          mkdir -p /mnt/artifacts/final
          ARTIFACT_COUNT=0
          
          # 收集AC42U固件
          if [ -d "/mnt/artifacts/firmware-ac42u" ]; then
            echo "📦 收集华硕RT-AC42U固件..." | tee -a "$MAIN_LOG"
            cp -r /mnt/artifacts/firmware-ac42u/* /mnt/artifacts/final/ 2>/dev/null || true
            AC42U_COUNT=$(ls /mnt/artifacts/firmware-ac42u/ 2>/dev/null | wc -l || echo 0)
            ARTIFACT_COUNT=$((ARTIFACT_COUNT + AC42U_COUNT))
            echo "  - 华硕RT-AC42U固件数量: $AC42U_COUNT" | tee -a "$MAIN_LOG"
          fi
          
          # 收集ACRH17固件
          if [ -d "/mnt/artifacts/firmware-acrh17" ]; then
            echo "📦 收集华硕RT-ACRH17固件..." | tee -a "$MAIN_LOG"
            cp -r /mnt/artifacts/firmware-acrh17/* /mnt/artifacts/final/ 2>/dev/null || true
            ACRH17_COUNT=$(ls /mnt/artifacts/firmware-acrh17/ 2>/dev/null | wc -l || echo 0)
            ARTIFACT_COUNT=$((ARTIFACT_COUNT + ACRH17_COUNT))
            echo "  - 华硕RT-ACRH17固件数量: $ACRH17_COUNT" | tee -a "$MAIN_LOG"
          fi
          
          # 收集日志文件
          cp "$MAIN_LOG" /mnt/artifacts/final/ 2>/dev/null || true
          cp "$ERROR_LOG" /mnt/artifacts/final/ 2>/dev/null || true
          cp "$WARNING_LOG" /mnt/artifacts/final/ 2>/dev/null || true
          cp "$ANALYSIS_REPORT" /mnt/artifacts/final/ 2>/dev/null || true
          cp "$DOWNLOAD_LOG" /mnt/artifacts/final/ 2>/dev/null || true
          
          echo "ARTIFACT_COUNT=$ARTIFACT_COUNT" >> $GITHUB_ENV
          echo "✅ 总共收集了 $ARTIFACT_COUNT 个固件产物" | tee -a "$MAIN_LOG"

      - name: "📤 提交新配置文件到仓库"
        id: commit_config
        if: github.event.inputs.auto_commit_config == 'true'
        run: |
          set +e
          MAIN_LOG="${{ env.MAIN_LOG }}"
          echo "=== 📤 提交新配置文件到仓库 ===" | tee -a "$MAIN_LOG"
          
          cd "${{ env.CONFIG_REPO_DIR }}"
          
          # 检查是否有新的配置文件需要提交
          if [ -f "/mnt/artifacts/config-ac42u" ] || [ -f "/mnt/artifacts/config-acrh17" ]; then
            echo "📁 找到新创建的配置文件" | tee -a "$MAIN_LOG"
            
            git config --local user.email "github-actions[bot]@users.noreply.github.com"
            git config --local user.name "github-actions[bot]"
            
            # 创建配置目录
            mkdir -p firmware-config/configs
            
            # 复制配置文件
            if [ -f "/mnt/artifacts/config-ac42u" ]; then
              cp "/mnt/artifacts/config-ac42u" firmware-config/configs/config_ac42u
              git add firmware-config/configs/config_ac42u
            fi
            
            if [ -f "/mnt/artifacts/config-acrh17" ]; then
              cp "/mnt/artifacts/config-acrh17" firmware-config/configs/config_acrh17
              git add firmware-config/configs/config_acrh17
            fi
            
            if git diff --staged --quiet; then
              echo "ℹ️ 没有新的配置文件需要提交" | tee -a "$MAIN_LOG"
            else
              COMMIT_MSG="添加新的设备配置文件: ${{ github.run_number }}"
              if [ "${{ env.BUILD_AC42U }}" = "true" ]; then
                COMMIT_MSG="$COMMIT_MSG [AC42U]"
              fi
              if [ "${{ env.BUILD_ACRH17 }}" = "true" ]; then
                COMMIT_MSG="$COMMIT_MSG [ACRH17]"
              fi
              git commit -m "$COMMIT_MSG"
              echo "✅ 配置文件已提交到本地仓库: $COMMIT_MSG" | tee -a "$MAIN_LOG"
              
              echo "🔄 推送到远程仓库..." | tee -a "$MAIN_LOG"
              git push
              echo "✅ 配置文件已推送到远程仓库" | tee -a "$MAIN_LOG"
            fi
          else
            echo "ℹ️ 没有创建新的配置文件" | tee -a "$MAIN_LOG"
          fi

      - name: "💾 上传构建产物"
        id: upload_artifacts
        if: always()
        uses: "actions/upload-artifact@v4"
        with:
          name: "双设备构建产物-${{ github.run_number }}"
          path: "/mnt/artifacts/final"
          retention-days: 7
          compression-level: 6

      - name: "📊 智能构建报告"
        id: final_report
        if: always()
        run: |
          echo "=== 📊 智能双设备构建报告 ==="
          echo ""
          echo "🎯 构建摘要"
          echo "=========================================="
          echo "⚡ 编译性能:"
          echo "  - 并行作业: ${{ env.PARALLEL_JOBS || 'N/A' }}"
          echo "  - 构建状态: ${{ env.BUILD_STATUS || 'unknown' }}"
          echo "  - 下载文件: ${{ env.DOWNLOAD_FILE_COUNT || 0 }}"
          echo ""
          echo "📦 构建结果:"
          echo "  - 总固件数量: ${{ env.ARTIFACT_COUNT || 0 }}"
          echo "  - 华硕RT-AC42U构建: ${{ env.AC42U_BUILD_SUCCESS || 'N/A' }}"
          echo "  - 华硕RT-ACRH17构建: ${{ env.ACRH17_BUILD_SUCCESS || 'N/A' }}"
          echo "  - 自定义IPK: ${{ env.CUSTOM_IPK_COUNT || 0 }} 个"
          echo "  - 自定义脚本: ${{ env.CUSTOM_SCRIPT_COUNT || 0 }} 个"
          echo ""
          echo "🔧 配置信息:"
          echo "  - 源码库: ${{ github.event.inputs.source_preset || 'unknown' }}"
          echo "  - 源码分支: ${{ env.ACTUAL_BRANCH || 'unknown' }}"
          echo "  - 构建模式: ${{ github.event.inputs.build_mode || 'unknown' }}"
          echo "  - 设备选择: ${{ github.event.inputs.build_devices || 'unknown' }}"
          echo "  - 高级功能: ${{ github.event.inputs.enable_advanced || 'unknown' }}"
          echo ""
          if [ "${{ env.BUILD_STATUS }}" = "success" ] && [ "${{ env.ARTIFACT_COUNT }}" -gt 0 ]; then
            echo "🎉 双设备智能构建成功！"
            echo "💡 固件已生成并上传到产物"
            if [ "${{ env.AC42U_BUILD_SUCCESS }}" = "true" ]; then
              echo "✅ 华硕RT-AC42U 固件已生成"
            fi
            if [ "${{ env.ACRH17_BUILD_SUCCESS }}" = "true" ]; then
              echo "✅ 华硕RT-ACRH17 固件已生成"
            fi
            if [ "${{ env.CUSTOM_IPK_COUNT }}" -gt 0 ]; then
              echo "📦 包含 ${{ env.CUSTOM_IPK_COUNT }} 个自定义IPK包"
            fi
            if [ "${{ env.CUSTOM_SCRIPT_COUNT }}" -gt 0 ]; then
              echo "📜 执行了 ${{ env.CUSTOM_SCRIPT_COUNT }} 个自定义脚本"
            fi
          else
            echo "❌ 构建失败或未生成固件"
            echo "🔧 请检查设备支持情况和配置"
            echo "💡 查看详细错误分析报告了解具体问题"
          fi
          echo "=========================================="
