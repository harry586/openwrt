name: Universal Firmware Builder - Enhanced File Discovery & Multi-Device Support

on:
  workflow_dispatch:
    inputs:
      source_preset:
        description: 选择源码库
        required: true
        type: choice
        options:
          - immortalwrt
          - openwrt
          - lede
        default: immortalwrt
      source_branch:
        description: 源码分支 (auto=自动推荐稳定分支，或指定 main/master/具体分支名)
        required: true
        default: auto
        type: string
      config_profile:
        description: 设备配置文件路径 (支持通配符如 .config_* 或具体文件名)
        required: true
        type: string
        default: .config_rt-ac42u_immortalwrt
      build_optimization:
        description: 编译优化策略
        required: true
        type: choice
        options:
          - balanced
          - speed
          - stability
        default: balanced
      toolchain_strategy:
        description: 工具链策略
        required: true
        type: choice
        options:
          - prebuilt
          - local
          - auto
        default: auto
      enable_custom_features:
        description: 启用自定义功能
        required: false
        default: true
        type: boolean
      fix_common_issues:
        description: 自动修复常见问题
        required: false
        default: true
        type: boolean

env:
  TZ: Asia/Shanghai
  SOURCE_DIR: /mnt/source
  ARTIFACTS_DIR: /mnt/artifacts
  CCACHE_DIR: /mnt/ccache
  BUILD_LOG_DIR: /mnt/build-logs
  TOOLCHAIN_LOG_DIR: /mnt/toolchain-logs

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 360
    steps:
      - name: 📥 检出配置仓库
        uses: actions/checkout@v4
        with:
          path: .
          fetch-depth: 0

      - name: 💾 智能系统资源分析
        timeout-minutes: 2
        run: |
          echo "=== 智能系统资源分析 ==="
          echo "📊 磁盘空间:"
          df -h
          echo ""
          echo "💻 CPU 信息:"
          echo "核心数: $(nproc)"
          echo "架构: $(uname -m)"
          echo "CPU 型号: $(grep 'model name' /proc/cpuinfo | head -1 | cut -d: -f2 | sed 's/^ *//')"
          echo ""
          echo "🧠 内存信息:"
          free -h
          echo ""
          echo "🔄 交换空间: $(swapon --show | wc -l 2>/dev/null || echo 0) 个交换文件"
          echo ""
          echo "✅ 资源分析完成 - 系统就绪"

      - name: 💾 增强内存管理
        run: |
          echo "=== 增强内存管理支持Samba编译 ==="
          
          echo "🔄 创建更大的交换文件..."
          if [ -f /mnt/swapfile ]; then
            sudo swapoff /mnt/swapfile 2>/dev/null || true
            sudo rm -f /mnt/swapfile 2>/dev/null || true
          fi
          
          # 创建24GB交换文件（Samba编译需要大量内存）
          sudo dd if=/dev/zero of=/mnt/swapfile bs=1M count=24576 status=progress || 
            sudo fallocate -l 24G /mnt/swapfile || 
            sudo truncate -s 24G /mnt/swapfile
          sudo chmod 600 /mnt/swapfile
          sudo mkswap /mnt/swapfile
          sudo swapon /mnt/swapfile
          
          echo "📊 当前内存状态:"
          free -h
          echo "交换空间:"
          swapon --show
          
          echo "⚡ 优化内存参数..."
          sudo sysctl -w vm.swappiness=80 2>/dev/null || echo "⚠️ 无法设置swappiness"
          sudo sysctl -w vm.vfs_cache_pressure=50 2>/dev/null || echo "⚠️ 无法设置vfs_cache_pressure"
          sudo sysctl -w vm.dirty_ratio=15 2>/dev/null || echo "⚠️ 无法设置dirty_ratio"
          sudo sysctl -w vm.dirty_background_ratio=5 2>/dev/null || echo "⚠️ 无法设置dirty_background_ratio"
          
          echo "🔧 为Samba编译特别调整..."
          # Samba编译需要更多内存，减少并行任务
          TOTAL_MEM=$(free -g | awk '/^Mem:/{print $2}')
          if [ "$TOTAL_MEM" -lt 16 ]; then
            echo "BUILD_JOBS=1" >> $GITHUB_ENV
            echo "SAMBA_JOBS=1" >> $GITHUB_ENV
          else
            BUILD_JOBS=$(( $(nproc) - 2 ))
            echo "BUILD_JOBS=$BUILD_JOBS" >> $GITHUB_ENV
            echo "SAMBA_JOBS=1" >> $GITHUB_ENV  # Samba始终使用单线程编译
          fi
          echo "📊 设置编译任务数: $BUILD_JOBS, Samba任务数: $SAMBA_JOBS"

      - name: 🔧 全面工作环境设置
        run: |
          echo "设置全面编译环境..."
          sudo rm -rf /tmp/* /var/tmp/* 2>/dev/null || true
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*
          echo 3 | sudo tee /proc/sys/vm/drop_caches > /dev/null
          sudo mkdir -p ${{ env.SOURCE_DIR }} ${{ env.ARTIFACTS_DIR }} ${{ env.CCACHE_DIR }} ${{ env.BUILD_LOG_DIR }} ${{ env.TOOLCHAIN_LOG_DIR }}
          sudo chown -R $USER:$USER ${{ env.SOURCE_DIR }} ${{ env.ARTIFACTS_DIR }} ${{ env.CCACHE_DIR }} ${{ env.BUILD_LOG_DIR }} ${{ env.TOOLCHAIN_LOG_DIR }}
          sudo chmod -R 755 ${{ env.SOURCE_DIR }} ${{ env.ARTIFACTS_DIR }}
          sudo chmod -R 777 ${{ env.CCACHE_DIR }} ${{ env.BUILD_LOG_DIR }} ${{ env.TOOLCHAIN_LOG_DIR }}
          
          export FORCE_UNSAFE_CONFIGURE=1
          export TERM=xterm-256color
          echo "📊 设置后磁盘空间状态:"
          df -h

      - name: 🔍 智能文件发现与配置分析
        id: file-discovery
        run: |
          echo "=== 智能文件发现与配置分析 ==="
          echo "🔍 全面分析仓库结构..."
          set +o pipefail
          
          echo "📋 查找 repositories.json 文件..."
          REPO_JSON_FOUND=$(find . -maxdepth 3 -name "repositories.json" -type f 2>/dev/null | head -1)
          if [ -n "$REPO_JSON_FOUND" ]; then
            echo "✅ 找到 repositories.json: $REPO_JSON_FOUND"
            echo "REPO_JSON_PATH=$REPO_JSON_FOUND" >> $GITHUB_ENV
            echo "📊 repositories.json 内容分析:"
            jq -r '.repositories | keys[] as $k | "  - \($k): \(.[$k].description // "无描述")"' "$REPO_JSON_FOUND" 2>/dev/null || echo "无法解析JSON内容"
          else
            echo "❌ 未找到 repositories.json 文件"
            exit 1
          fi
          
          echo "📋 智能查找所有配置文件..."
          CONFIG_PATTERNS=(".config_*" "config_*" "*.config")
          ALL_CONFIG_FILES=""
          for pattern in "${CONFIG_PATTERNS[@]}"; do
            echo "搜索模式: $pattern"
            found_files=$(find . -maxdepth 3 -name "$pattern" -type f 2>/dev/null | head -10)
            if [ -n "$found_files" ]; then
              ALL_CONFIG_FILES="$ALL_CONFIG_FILES"$'\n'"$found_files"
              echo "找到文件:"
              echo "$found_files"
            fi
          done
          
          if [ -n "$ALL_CONFIG_FILES" ]; then
            CONFIG_COUNT=$(echo "$ALL_CONFIG_FILES" | grep -v '^$' | wc -l)
            echo "✅ 总共找到 $CONFIG_COUNT 个配置文件"
            echo "CONFIG_FILES_FOUND=true" >> $GITHUB_ENV
            echo "$ALL_CONFIG_FILES" > ${{ env.BUILD_LOG_DIR }}/all_config_files.txt
          else
            echo "⚠️ 未找到任何配置文件"
            echo "CONFIG_FILES_FOUND=false" >> $GITHUB_ENV
          fi
          
          echo "📋 智能查找自定义功能目录..."
          CUSTOM_DIR_PATTERNS=("custom-features" "custom" "features" "firmware-config")
          CUSTOM_FEATURES_DIR=""
          for pattern in "${CUSTOM_DIR_PATTERNS[@]}"; do
            echo "搜索目录: $pattern"
            found_dir=$(find . -maxdepth 2 -type d -name "$pattern" 2>/dev/null | head -1)
            if [ -n "$found_dir" ]; then
              # 将相对路径转换为绝对路径
              if [[ "$found_dir" == ./* ]]; then
                CUSTOM_FEATURES_DIR="$GITHUB_WORKSPACE/${found_dir#./}"
              else
                CUSTOM_FEATURES_DIR="$GITHUB_WORKSPACE/$found_dir"
              fi
              echo "✅ 找到自定义功能目录: $found_dir (绝对路径: $CUSTOM_FEATURES_DIR)"
              break
            fi
          done
          
          if [ -n "$CUSTOM_FEATURES_DIR" ]; then
            echo "CUSTOM_FEATURES_DIR=$CUSTOM_FEATURES_DIR" >> $GITHUB_ENV
            
            echo "🔍 自定义目录详细分析:"
            echo "📁 目录结构:"
            find "$CUSTOM_FEATURES_DIR" -type f 2>/dev/null | head -20
            
            echo "📦 IPK文件检查:"
            IPK_FILES=$(find "$CUSTOM_FEATURES_DIR" -name "*.ipk" -type f 2>/dev/null)
            IPK_COUNT=$(echo "$IPK_FILES" | grep -v '^$' | wc -l 2>/dev/null || echo 0)
            if [ "$IPK_COUNT" -gt 0 ]; then
              echo "✅ 找到 $IPK_COUNT 个IPK文件:"
              echo "$IPK_FILES" | head -10
            else
              echo "ℹ️ 未找到IPK文件"
            fi
            
            echo "📜 脚本文件检查:"
            SCRIPT_FILES=$(find "$CUSTOM_FEATURES_DIR" -name "*.sh" -type f 2>/dev/null)
            SCRIPT_COUNT=$(echo "$SCRIPT_FILES" | grep -v '^$' | wc -l 2>/dev/null || echo 0)
            if [ "$SCRIPT_COUNT" -gt 0 ]; then
              echo "✅ 找到 $SCRIPT_COUNT 个脚本文件:"
              echo "$SCRIPT_FILES" | head -10
            else
              echo "ℹ️ 未找到脚本文件"
            fi
            
            echo "🔧 补丁文件检查:"
            PATCH_FILES=$(find "$CUSTOM_FEATURES_DIR" -name "*.patch" -type f 2>/dev/null)
            PATCH_COUNT=$(echo "$PATCH_FILES" | grep -v '^$' | wc -l 2>/dev/null || echo 0)
            if [ "$PATCH_COUNT" -gt 0 ]; then
              echo "✅ 找到 $PATCH_COUNT 个补丁文件:"
              echo "$PATCH_FILES" | head -10
            else
              echo "ℹ️ 未找到补丁文件"
            fi
          else
            echo "ℹ️ 未找到自定义功能目录"
            echo "CUSTOM_FEATURES_DIR=" >> $GITHUB_ENV
          fi
          
          echo ""
          echo "📁 完整目录结构分析:"
          echo "顶层目录:"
          ls -la
          echo ""
          echo "关键文件类型统计:"
          echo "  - 配置文件: $(find . -maxdepth 3 -name "*.config" -o -name ".config_*" -o -name "config_*" 2>/dev/null | wc -l)"
          echo "  - 脚本文件: $(find . -maxdepth 3 -name "*.sh" 2>/dev/null | wc -l)"
          echo "  - Makefile: $(find . -maxdepth 3 -name "Makefile" -o -name "*.mk" 2>/dev/null | wc -l)"
          
          set -o pipefail
          echo "✅ 智能文件发现完成"

      - name: 🔧 智能源码配置解析
        id: source-config
        run: |
          echo "正在智能解析源码配置..."
          PRESET="${{ github.event.inputs.source_preset }}"
          echo "使用的预设: $PRESET"
          REPO_JSON_PATH="${{ env.REPO_JSON_PATH }}"
          
          if ! jq -e ".repositories.$PRESET" "$REPO_JSON_PATH" >/dev/null 2>&1; then
            echo "❌ 错误: 预设 '$PRESET' 不存在"
            echo "可用的预设:"
            jq -r '.repositories | keys[]' "$REPO_JSON_PATH"
            exit 1
          fi
          
          SOURCE_URL=$(jq -r ".repositories.$PRESET.url" "$REPO_JSON_PATH")
          DESCRIPTION=$(jq -r ".repositories.$PRESET.description // \"未知\"" "$REPO_JSON_PATH")
          RECOMMENDED_BRANCH=$(jq -r ".repositories.$PRESET.recommended_branch" "$REPO_JSON_PATH")
          echo "✅ 成功获取配置信息"
          
          BRANCH="${{ github.event.inputs.source_branch }}"
          if [ "$BRANCH" = "auto" ]; then
            BRANCH="$RECOMMENDED_BRANCH"
            echo "🤖 自动选择推荐分支: $BRANCH"
          else
            echo "📋 使用指定分支: $BRANCH"
          fi
          
          echo "SOURCE_URL=$SOURCE_URL" >> $GITHUB_ENV
          echo "SOURCE_BRANCH=$BRANCH" >> $GITHUB_ENV
          echo "SOURCE_PRESET=$PRESET" >> $GITHUB_ENV
          echo "RECOMMENDED_BRANCH=$RECOMMENDED_BRANCH" >> $GITHUB_ENV
          
          echo ""
          echo "✅ 源码配置解析完成"
          echo "📊 配置详情:"
          echo "  - 预设: $PRESET"
          echo "  - 描述: $DESCRIPTION"
          echo "  - 仓库: $SOURCE_URL"
          echo "  - 分支: $BRANCH"
          echo "  - 推荐分支: $RECOMMENDED_BRANCH"

      - name: 🛠️ 全面编译依赖安装与验证
        run: |
          echo "安装全面编译依赖包..."
          sudo apt-get update
          echo "📦 安装基础编译工具..."
          sudo apt-get install -y build-essential clang flex bison g++ gawk gcc-multilib g++-multilib
          echo "📦 安装开发库..."
          sudo apt-get install -y gettext git libncurses5-dev libssl-dev python3 python3-pip python3-setuptools
          echo "📦 安装工具链依赖..."
          sudo apt-get install -y rsync unzip zlib1g-dev file wget jq ccache m4 help2man texinfo texi2html
          echo "📦 安装构建工具..."
          sudo apt-get install -y libtool-bin automake autoconf pkg-config subversion mercurial curl cmake ninja-build
          echo "📦 安装系统库..."
          sudo apt-get install -y libelf-dev libssl-dev zlib1g-dev libc6-dev libxml2-dev liblzma-dev liblzo2-dev
          echo "🔍 依赖安装验证..."
          echo "=== 关键工具验证 ==="
          for tool in gcc g++ make flex bison git curl wget; do
            if command -v $tool >/dev/null 2>&1; then
              version=$($tool --version 2>/dev/null | head -1 | cut -d' ' -f1-4 | tr -d '\n' || echo "可用")
              echo "✅ $tool: $version"
            else
              echo "❌ $tool: 未安装"
              exit 1
            fi
          done
          echo "✅ 所有依赖安装和验证完成"

      - name: ⚡ 高级缓存与性能优化
        run: |
          echo "设置高级缓存与性能优化..."
          mkdir -p ${{ env.CCACHE_DIR }}
          ccache -M 16G
          ccache -o compression=true
          ccache -o compression_level=6
          ccache -o max_files=200000
          ccache -o sloppiness=file_macro,include_file_mtime,include_file_ctime,time_macros
          echo "CCACHE_DIR=${{ env.CCACHE_DIR }}" >> $GITHUB_ENV
          echo "CCACHE_COMPRESS=1" >> $GITHUB_ENV
          echo "CCACHE_MAXSIZE=16G" >> $GITHUB_ENV
          echo "CCACHE_SLOPPINESS=file_macro,include_file_mtime,include_file_ctime,time_macros" >> $GITHUB_ENV
          echo "📊 初始CCache统计:"
          ccache -s
          echo "✅ 缓存与性能优化完成"

      - name: 📥 智能源码获取与分支处理
        id: clone-source
        run: |
          echo "开始智能源码获取与分支处理..."
          cd ${{ env.SOURCE_DIR }}
          REQUESTED_BRANCH="${{ env.SOURCE_BRANCH }}"
          SOURCE_URL="${{ env.SOURCE_URL }}"
          echo "🎯 请求分支: $REQUESTED_BRANCH"
          echo "🎯 源码仓库: $SOURCE_URL"
          
          CLONE_STRATEGIES=()
          if [ "$REQUESTED_BRANCH" != "auto" ] && [ "$REQUESTED_BRANCH" != "main" ] && [ "$REQUESTED_BRANCH" != "master" ]; then
            CLONE_STRATEGIES+=("指定分支: git clone --depth 1 --branch $REQUESTED_BRANCH $SOURCE_URL .")
          fi
          CLONE_STRATEGIES+=("main分支: git clone --depth 1 --branch main $SOURCE_URL ." "master分支: git clone --depth 1 --branch master $SOURCE_URL ." "默认分支: git clone --depth 1 $SOURCE_URL .")
          
          if [ "$REQUESTED_BRANCH" = "auto" ]; then
            RECOMMENDED_BRANCH="${{ env.RECOMMENDED_BRANCH }}"
            if [ -n "$RECOMMENDED_BRANCH" ] && [ "$RECOMMENDED_BRANCH" != "null" ]; then
              CLONE_STRATEGIES=("推荐分支: git clone --depth 1 --branch $RECOMMENDED_BRANCH $SOURCE_URL ." "${CLONE_STRATEGIES[@]}")
            fi
          fi
          
          CLONE_SUCCESS=false
          CLONE_ERROR=""
          ACTUAL_BRANCH=""
          
          for strategy in "${CLONE_STRATEGIES[@]}"; do
            strategy_name=$(echo "$strategy" | cut -d: -f1)
            clone_cmd=$(echo "$strategy" | cut -d: -f2-)
            echo ""
            echo "🔄 尝试策略: $strategy_name"
            echo "命令: $clone_cmd"
            rm -rf .[!.]* * 2>/dev/null || true
            
            if eval $clone_cmd 2>&1 | tee ${{ env.BUILD_LOG_DIR }}/clone.log; then
              if [ -d ".git" ]; then
                ACTUAL_BRANCH=$(git branch --show-current 2>/dev/null || echo "unknown")
                COMMIT_HASH=$(git rev-parse --short HEAD 2>/dev/null || echo "unknown")
                echo "✅ 克隆成功: $strategy_name"
                echo "📋 实际分支: $ACTUAL_BRANCH"
                echo "🔗 提交哈希: $COMMIT_HASH"
                CLONE_SUCCESS=true
                break
              else
                CLONE_ERROR="Git目录未创建"
                echo "⚠️ Git目录未创建，继续尝试其他策略..."
              fi
            else
              CLONE_ERROR="克隆命令执行失败"
              echo "❌ 策略失败: $strategy_name"
              if [ -f "${{ env.BUILD_LOG_DIR }}/clone.log" ]; then
                echo "克隆错误详情:"
                tail -20 ${{ env.BUILD_LOG_DIR }}/clone.log
              fi
            fi
          done
          
          if [ "$CLONE_SUCCESS" = "false" ]; then
            echo "❌ 所有克隆策略都失败"
            echo "🔍 最后尝试的克隆日志:"
            cat ${{ env.BUILD_LOG_DIR }}/clone.log 2>/dev/null || echo "无克隆日志"
            echo "错误: $CLONE_ERROR"
            exit 1
          fi
          
          echo "ACTUAL_BRANCH=$ACTUAL_BRANCH" >> $GITHUB_ENV
          echo "COMMIT_HASH=$COMMIT_HASH" >> $GITHUB_ENV
          
          echo ""
          echo "🔍 源码仓库深度验证..."
          echo "📁 源码目录结构:"
          ls -la | head -10
          echo ""
          echo "✅ 源码获取与验证完成"

      - name: 💾 源码获取后空间监测
        run: |
          echo "=== 源码获取后空间监测 ==="
          echo "📊 当前磁盘使用情况:"
          df -h
          ROOT_AVAILABLE=$(df / | awk 'NR==2 {gsub("G","",$4); print int($4)}')
          MNT_AVAILABLE=$(df /mnt | awk 'NR==2 {gsub("G","",$4); print int($4)}')
          echo "根分区可用空间: ${ROOT_AVAILABLE}GB"
          echo "/mnt 分区可用空间: ${MNT_AVAILABLE}GB"
          if [ "$ROOT_AVAILABLE" -lt 2 ]; then
            echo "🚨 严重警告: 根分区空间不足2GB，可能影响系统稳定性"
          fi
          if [ "$MNT_AVAILABLE" -lt 10 ]; then
            echo "🚨 严重错误: /mnt 分区空间不足10GB，无法编译"
            exit 1
          elif [ "$MNT_AVAILABLE" -lt 20 ]; then
            echo "⚠️ 警告: /mnt 分区空间紧张 (${MNT_AVAILABLE}GB)"
          else
            echo "✅ /mnt 分区空间充足"
          fi

      - name: 🔄 智能源码初始化
        timeout-minutes: 30
        run: |
          cd ${{ env.SOURCE_DIR }}
          echo "开始智能源码初始化..."
          START_TIME=$(date +%s)
          
          echo "🔧 Feeds配置检测..."
          FEEDS_CONF_FOUND=""
          for feeds_file in feeds.conf feeds.conf.default .feeds.conf .feeds.conf.default; do
            if [ -f "$feeds_file" ]; then
              FEEDS_CONF_FOUND="$feeds_file"
              echo "📋 使用现有 $feeds_file"
              if [ "$feeds_file" != "feeds.conf" ]; then
                cp "$feeds_file" feeds.conf
              fi
              break
            fi
          done
          
          if [ -z "$FEEDS_CONF_FOUND" ]; then
            echo "⚠️ 未找到feeds配置，创建通用配置"
            echo "src-git packages https://git.openwrt.org/feed/packages.git" > feeds.conf
            echo "src-git luci https://git.openwrt.org/feed/luci.git" >> feeds.conf
            echo "src-git routing https://git.openwrt.org/feed/routing.git" >> feeds.conf
            echo "src-git telephony https://git.openwrt.org/feed/telephony.git" >> feeds.conf
          fi
          
          echo "🔄 更新Feeds..."
          for i in 1 2 3; do
            echo "尝试 $i/3"
            if ./scripts/feeds update -a; then
              echo "✅ Feeds更新成功"
              break
            else
              echo "❌ Feeds更新失败，尝试修复..."
              if [ $i -eq 3 ]; then
                echo "❌ Feeds更新彻底失败"
                exit 1
              fi
              sleep 5
            fi
          done
          
          echo "📦 安装基础Feeds..."
          for feed in packages luci routing telephony; do
            if ./scripts/feeds install -a -p $feed; then
              echo "✅ $feed 安装成功"
            else
              echo "⚠️ $feed 安装失败"
            fi
          done
          
          END_TIME=$(date +%s)
          DURATION=$((END_TIME - START_TIME))
          echo "⏱️ Feeds初始化耗时: ${DURATION}秒"
          echo "✅ 源码初始化完成"

      - name: 🎯 快速配置文件查找与应用
        timeout-minutes: 1
        run: |
          cd ${{ env.SOURCE_DIR }}
          echo "快速配置文件查找与应用..."
          CONFIG_PROFILE="${{ github.event.inputs.config_profile }}"
          echo "输入配置模式: $CONFIG_PROFILE"
          
          echo "🔍 快速配置文件搜索..."
          
          # 使用与"智能文件发现"相同的快速搜索策略
          CONFIG_FOUND=""
          
          # 方法1: 直接路径查找（最快）- 在源码目录和配置仓库目录都查找
          echo "🎯 直接路径查找..."
          if [ -f "$CONFIG_PROFILE" ]; then
            CONFIG_FOUND="$CONFIG_PROFILE"
            echo "✅ 在当前目录找到配置文件: $CONFIG_FOUND"
          elif [ -f "../$CONFIG_PROFILE" ]; then
            CONFIG_FOUND="../$CONFIG_PROFILE"
            echo "✅ 在上级目录找到配置文件: $CONFIG_FOUND"
          elif [ -f "$GITHUB_WORKSPACE/$CONFIG_PROFILE" ]; then
            CONFIG_FOUND="$GITHUB_WORKSPACE/$CONFIG_PROFILE"
            echo "✅ 在工作区找到配置文件: $CONFIG_FOUND"
          fi
          
          # 方法2: 受限深度搜索 - 在源码目录和配置仓库目录都搜索
          if [ -z "$CONFIG_FOUND" ]; then
            echo "🔍 受限深度搜索..."
            # 先在源码目录搜索
            CONFIG_FOUND=$(find . -maxdepth 3 -name "$CONFIG_PROFILE" -type f 2>/dev/null | head -1)
            if [ -z "$CONFIG_FOUND" ]; then
              # 在配置仓库目录搜索
              CONFIG_FOUND=$(find $GITHUB_WORKSPACE -maxdepth 3 -name "$CONFIG_PROFILE" -type f 2>/dev/null | head -1)
            fi
            if [ -n "$CONFIG_FOUND" ]; then
              echo "✅ 通过受限搜索找到配置文件: $CONFIG_FOUND"
            fi
          fi
          
          # 方法3: 使用文件发现步骤的结果（如果可用）- 修正路径
          if [ -z "$CONFIG_FOUND" ] && [ -f "${{ env.BUILD_LOG_DIR }}/all_config_files.txt" ]; then
            echo "🔍 使用文件发现缓存..."
            # 从缓存文件中读取路径，并确保路径正确
            CACHED_PATH=$(grep "$CONFIG_PROFILE" "${{ env.BUILD_LOG_DIR }}/all_config_files.txt" | head -1)
            if [ -n "$CACHED_PATH" ]; then
              # 检查路径是否以 ./ 开头，如果是则转换为绝对路径
              if [[ "$CACHED_PATH" == ./* ]]; then
                CONFIG_FOUND="$GITHUB_WORKSPACE/${CACHED_PATH#./}"
              else
                CONFIG_FOUND="$CACHED_PATH"
              fi
              # 验证文件是否存在
              if [ -f "$CONFIG_FOUND" ]; then
                echo "✅ 通过缓存找到配置文件: $CONFIG_FOUND"
              else
                echo "⚠️ 缓存中的文件不存在: $CONFIG_FOUND"
                CONFIG_FOUND=""
              fi
            fi
          fi
          
          # 方法4: 快速模式匹配
          if [ -z "$CONFIG_FOUND" ] && [[ "$CONFIG_PROFILE" == *"*"* ]]; then
            echo "🔍 快速模式匹配..."
            CONFIG_FOUND=$(find . -maxdepth 3 -name "$CONFIG_PROFILE" -type f 2>/dev/null | head -1)
            if [ -z "$CONFIG_FOUND" ]; then
              CONFIG_FOUND=$(find $GITHUB_WORKSPACE -maxdepth 3 -name "$CONFIG_PROFILE" -type f 2>/dev/null | head -1)
            fi
            if [ -n "$CONFIG_FOUND" ]; then
              echo "✅ 通过模式匹配找到配置文件: $CONFIG_FOUND"
            fi
          fi
          
          # 方法5: 显示可用配置并选择第一个
          if [ -z "$CONFIG_FOUND" ]; then
            echo "🔍 显示可用配置文件..."
            AVAILABLE_CONFIGS=$(find . -maxdepth 3 -name "*.config" -o -name ".config_*" -o -name "config_*" 2>/dev/null | head -5)
            if [ -z "$AVAILABLE_CONFIGS" ]; then
              AVAILABLE_CONFIGS=$(find $GITHUB_WORKSPACE -maxdepth 3 -name "*.config" -o -name ".config_*" -o -name "config_*" 2>/dev/null | head -5)
            fi
            if [ -n "$AVAILABLE_CONFIGS" ]; then
              CONFIG_FOUND=$(echo "$AVAILABLE_CONFIGS" | head -1)
              echo "⚠️ 使用第一个找到的配置文件: $CONFIG_FOUND"
              echo "可用配置文件:"
              echo "$AVAILABLE_CONFIGS"
            fi
          fi
          
          if [ -n "$CONFIG_FOUND" ]; then
            echo "📋 复制配置文件: $CONFIG_FOUND -> .config"
            # 确保文件存在
            if [ -f "$CONFIG_FOUND" ]; then
              cp "$CONFIG_FOUND" .config
              echo "🔍 配置文件基本信息:"
              CONFIG_SIZE=$(stat -c%s .config 2>/dev/null || echo "未知")
              echo "  - 文件: $(basename $CONFIG_FOUND)"
              echo "  - 大小: ${CONFIG_SIZE} 字节"
              echo "  - 来源: $CONFIG_FOUND"
              
              # 快速分析配置
              TARGET_ARCH=$(grep "CONFIG_TARGET_ARCH_PACKAGES" .config 2>/dev/null | cut -d= -f2 | tr -d '"' | head -1 || echo "未知")
              echo "  - 架构: $TARGET_ARCH"
              echo "✅ 配置应用完成"
            else
              echo "❌ 错误: 配置文件不存在: $CONFIG_FOUND"
              echo "🔍 验证文件存在性..."
              ls -la "$(dirname "$CONFIG_FOUND")" 2>/dev/null || echo "目录不存在"
              exit 1
            fi
          else
            echo "❌ 错误: 无法找到配置文件 '$CONFIG_PROFILE'"
            echo ""
            echo "🔍 调试信息:"
            echo "当前目录: $(pwd)"
            echo "GITHUB_WORKSPACE: $GITHUB_WORKSPACE"
            echo "快速配置文件列表:"
            find . -maxdepth 2 -name "*.config" -o -name ".config_*" 2>/dev/null | head -10 || echo "当前目录无配置文件"
            find $GITHUB_WORKSPACE -maxdepth 2 -name "*.config" -o -name ".config_*" 2>/dev/null | head -10 || echo "工作区无配置文件"
            exit 1
          fi

      - name: 🔧 设备镜像配置修复
        run: |
          cd ${{ env.SOURCE_DIR }}
          echo "=== 设备镜像配置修复 ==="
          
          echo "🔍 检查当前目标设备配置..."
          if [ -f ".config" ]; then
            echo "📋 当前设备相关配置:"
            grep -E "CONFIG_TARGET.*asus_rt-ac42u|CONFIG_TARGET_DEVICE|CONFIG_TARGET_.*_DEVICE.*asus" .config | head -20
          fi
          
          echo "🔧 确保工厂镜像生成启用..."
          # 启用工厂镜像生成
          sed -i 's/# CONFIG_TARGET_IMAGES_PACKAGES is not set/CONFIG_TARGET_IMAGES_PACKAGES=y/' .config 2>/dev/null || true
          
          # 确保设备特定的工厂镜像配置
          if grep -q "CONFIG_TARGET_ipq40xx_generic_DEVICE_asus_rt-ac42u" .config; then
            echo "🎯 启用 asus_rt-ac42u 设备配置..."
            sed -i 's/# CONFIG_TARGET_ipq40xx_generic_DEVICE_asus_rt-ac42u is not set/CONFIG_TARGET_ipq40xx_generic_DEVICE_asus_rt-ac42u=y/' .config
          else
            echo "🔧 添加 asus_rt-ac42u 设备配置..."
            echo "CONFIG_TARGET_ipq40xx_generic_DEVICE_asus_rt-ac42u=y" >> .config
          fi
          
          # 确保工厂镜像格式启用
          echo "🔧 启用工厂镜像格式..."
          sed -i 's/# CONFIG_IMAGEOPT is not set/CONFIG_IMAGEOPT=y/' .config 2>/dev/null || true
          sed -i 's/# CONFIG_COMBINED_IMAGES is not set/CONFIG_COMBINED_IMAGES=y/' .config 2>/dev/null || true
          
          # 添加必要的镜像配置
          echo "CONFIG_TARGET_ROOTFS_SQUASHFS=y" >> .config
          echo "CONFIG_TARGET_ROOTFS_EXT4FS=y" >> .config
          echo "CONFIG_TARGET_KERNEL_PARTSIZE=32" >> .config
          echo "CONFIG_TARGET_ROOTFS_PARTSIZE=512" >> .config
          
          echo "🔄 重新运行配置..."
          make defconfig >/dev/null 2>&1 || echo "配置更新完成"
          
          echo "✅ 设备镜像配置修复完成"

      - name: 🔧 高级性能优化配置
        run: |
          cd ${{ env.SOURCE_DIR }}
          echo "应用高级性能优化配置..."
          echo "⚡ 应用基础优化..."
          sed -i 's/CONFIG_DEBUG=y/# CONFIG_DEBUG is not set/' .config 2>/dev/null || true
          sed -i 's/CONFIG_DEVEL=y/# CONFIG_DEVEL is not set/' .config 2>/dev/null || true
          echo "CONFIG_CCACHE=y" >> .config
          
          case "${{ github.event.inputs.build_optimization }}" in
            "speed")
              echo "🚀 应用极速优化配置..."
              echo "CONFIG_DEBUG_INFO=n" >> .config
              ;;
            "stability")
              echo "🛡️ 应用稳定优化配置..."
              echo "CONFIG_SMALL_FLASH=y" >> .config
              ;;
            *)
              echo "⚖️ 应用平衡优化配置..."
              ;;
          esac
          
          echo "🔄 运行配置处理..."
          # 修复Broken pipe错误
          echo "n" | make oldconfig >/dev/null 2>&1 || make defconfig >/dev/null 2>&1 || echo "配置处理完成"
          echo "✅ 性能优化配置完成"

      - name: 🛠️ Samba专用修复与编译
        if: github.event.inputs.enable_custom_features == 'true'
        run: |
          cd ${{ env.SOURCE_DIR }}
          echo "=== Samba专用修复与编译 ==="
          
          echo "🔧 清理Samba编译缓存..."
          # 使用更温和的清理方式
          make package/samba4-server/clean 2>/dev/null || echo "⚠️ 清理samba4-server失败，继续..."
          make package/samba4-libs/clean 2>/dev/null || echo "⚠️ 清理samba4-libs失败，继续..."
          make package/luci-app-samba4/clean 2>/dev/null || echo "⚠️ 清理luci-app-samba4失败，继续..."
          
          echo "📦 修复Samba依赖库路径问题..."
          # 检查依赖库目录结构
          echo "🔍 检查Samba依赖库目录..."
          find package -name "*talloc*" -type d 2>/dev/null | head -5
          find package -name "*tdb*" -type d 2>/dev/null | head -5
          find package -name "*tevent*" -type d 2>/dev/null | head -5
          find package -name "*ldb*" -type d 2>/dev/null | head -5
          
          echo "🔧 更新feeds确保Samba依赖可用..."
          ./scripts/feeds update packages 2>/dev/null || true
          ./scripts/feeds install libtalloc libtdb libtevent libldb 2>/dev/null || echo "⚠️ 依赖库安装失败，继续..."
          
          echo "📦 按正确顺序编译Samba依赖库..."
          # 正确的依赖库编译顺序和路径
          for lib in talloc tdb tevent ldb; do
            echo "编译 $lib..."
            # 尝试不同的可能路径
            if make package/$lib/compile -j1 V=s 2>&1 | tee ${{ env.BUILD_LOG_DIR }}/samba-$lib.log; then
              echo "✅ $lib 编译成功"
            else
              echo "❌ $lib 编译失败，尝试备用路径..."
              # 尝试libs目录下的路径
              if make package/libs/$lib/compile -j1 V=s 2>&1 | tee ${{ env.BUILD_LOG_DIR }}/samba-$lib-alt.log; then
                echo "✅ $lib 通过备用路径编译成功"
              else
                echo "⚠️ $lib 编译失败，尝试跳过继续..."
                # 不退出，继续尝试其他库
              fi
            fi
          done
          
          echo "🔧 调整编译配置减少内存使用..."
          # 为Samba编译优化内存使用
          export CFLAGS="$CFLAGS -Os -pipe -fno-caller-saves -fno-plt"
          export CXXFLAGS="$CXXFLAGS -Os -pipe -fno-caller-saves -fno-plt"
          # 减少并行编译任务避免内存不足
          export MAKEJOBS=1
          
          echo "✅ Samba修复完成"

      - name: 🔧 综合问题修复
        if: github.event.inputs.fix_common_issues == 'true'
        run: |
          cd ${{ env.SOURCE_DIR }}
          echo "=== 执行综合问题修复 ==="
          
          echo "🔧 深度目录修复与权限设置..."
          echo "创建目标根文件系统必要目录..."
          
          # 方法1: 使用find创建所有目标根目录
          find build_dir -name "root.orig-*" -type d 2>/dev/null | while read root_dir; do
            echo "修复目录: $root_dir"
            mkdir -p "$root_dir/tmp"
            mkdir -p "$root_dir/var/lock"
            mkdir -p "$root_dir/var/run"
            mkdir -p "$root_dir/usr/lib/opkg"
            mkdir -p "$root_dir/var/lib/opkg"
            chmod 755 "$root_dir/tmp"
            chmod 755 "$root_dir/var"
            echo "✅ 已修复: $root_dir"
          done
          
          # 方法2: 修复已知的特定目标目录
          echo "🔧 修复已知目标目录结构..."
          KNOWN_ROOTS=(
            "build_dir/target-arm_cortex-a7+neon-vfpv4_musl_eabi/root.orig-ipq40xx"
            "staging_dir/target-arm_cortex-a7+neon-vfpv4_musl_eabi/root.orig-ipq40xx"
            "build_dir/target-arm_cortex-a7+neon-vfpv4_musl_eabi/root-ipq40xx"
          )
          
          for known_root in "${KNOWN_ROOTS[@]}"; do
            if [ -d "$known_root" ]; then
              echo "🔧 修复已知目录: $known_root"
              mkdir -p "$known_root/tmp"
              mkdir -p "$known_root/var/lock"
              mkdir -p "$known_root/var/run"
              mkdir -p "$known_root/usr/lib/opkg"
              mkdir -p "$known_root/var/lib/opkg"
              chmod 755 "$known_root/tmp"
              echo "✅ 已修复: $known_root"
            else
              echo "ℹ️ 目录不存在，创建: $known_root"
              mkdir -p "$known_root/tmp" 2>/dev/null || true
            fi
          done
          
          # 方法3: 创建通用目标目录结构
          echo "🔧 创建通用目标目录结构..."
          TARGET_PATTERN="target-*"
          for target_dir in $TARGET_PATTERN; do
            if [ -d "$target_dir" ]; then
              find "$target_dir" -name "root*" -type d 2>/dev/null | while read root_dir; do
                mkdir -p "$root_dir/tmp"
                mkdir -p "$root_dir/var/lock" 
                mkdir -p "$root_dir/var/run"
                chmod 755 "$root_dir/tmp"
              done
            fi
          done
          
          echo "🔧 修复工厂镜像配置..."
          if [ -f ".config" ]; then
            # 确保工厂镜像相关配置启用
            echo "🎯 启用工厂镜像生成配置..."
            sed -i 's/# CONFIG_TARGET_IMAGES_PACKAGES is not set/CONFIG_TARGET_IMAGES_PACKAGES=y/' .config 2>/dev/null || true
            sed -i 's/# CONFIG_TARGET_ipq40xx_generic_DEVICE_asus_rt-ac42u is not set/CONFIG_TARGET_ipq40xx_generic_DEVICE_asus_rt-ac42u=y/' .config 2>/dev/null || true
            sed -i 's/# CONFIG_IMAGEOPT is not set/CONFIG_IMAGEOPT=y/' .config 2>/dev/null || true
            sed -i 's/# CONFIG_COMBINED_IMAGES is not set/CONFIG_COMBINED_IMAGES=y/' .config 2>/dev/null || true
            
            # 确保内核配置正确
            echo "CONFIG_TARGET_ROOTFS_SQUASHFS=y" >> .config
            echo "CONFIG_TARGET_ROOTFS_EXT4FS=y" >> .config
            echo "CONFIG_TARGET_ROOTFS_INCLUDE_USB=y" >> .config
            
            echo "✅ 工厂镜像配置已修复"
            make defconfig >/dev/null 2>&1 || echo "配置更新完成"
          fi
          
          echo "🔧 检查问题包处理..."
          # 如果samba持续失败，提供禁用选项
          if grep -q "CONFIG_PACKAGE_samba4-server=y" .config 2>/dev/null; then
            echo "⚠️ samba包已启用，如持续失败可考虑禁用"
            echo "💡 如需禁用samba，取消注释以下行:"
            echo "# sed -i 's/CONFIG_PACKAGE_samba4-server=y/# CONFIG_PACKAGE_samba4-server is not set/' .config"
            echo "# sed -i 's/CONFIG_PACKAGE_luci-app-samba4=y/# CONFIG_PACKAGE_luci-app-samba4 is not set/' .config"
            
            # 根据用户选择决定是否禁用samba
            if [ "${{ github.event.inputs.fix_common_issues }}" = "true" ]; then
              echo "🔧 根据配置自动禁用可能有问题的samba包..."
              sed -i 's/CONFIG_PACKAGE_samba4-server=y/# CONFIG_PACKAGE_samba4-server is not set/' .config 2>/dev/null || true
              sed -i 's/CONFIG_PACKAGE_luci-app-samba4=y/# CONFIG_PACKAGE_luci-app-samba4 is not set/' .config 2>/dev/null || true
              echo "✅ 已自动禁用samba相关包"
            fi
          fi
          
          echo "🔧 修复符号链接..."
          # 检查并修复可能的符号链接问题
          if [ -L "tmp" ] && [ ! -e "tmp" ]; then
            echo "🔧 修复损坏的tmp符号链接..."
            rm -f tmp
            mkdir -p tmp
            echo "✅ tmp符号链接已修复"
          fi
          
          echo "✅ 综合问题修复完成"

      - name: 🛠️ 全面工具链编译与验证
        timeout-minutes: 90
        run: |
          cd ${{ env.SOURCE_DIR }}
          echo "开始全面工具链编译..."
          START_TIME=$(date +%s)
          mkdir -p ${{ env.TOOLCHAIN_LOG_DIR }}
          
          echo "🔧 编译Host工具..."
          if ! make tools/compile -j$(($(nproc) - 1)) V=sc 2>&1 | tee ${{ env.TOOLCHAIN_LOG_DIR }}/host-tools.log; then
            echo "❌ Host工具编译失败，尝试单线程..."
            if ! make tools/compile -j1 V=s 2>&1 | tee ${{ env.TOOLCHAIN_LOG_DIR }}/host-tools-single.log; then
              echo "❌ Host工具编译彻底失败"
              exit 1
            fi
          fi
          
          echo "🔧 编译工具链..."
          if ! make toolchain/compile -j$(($(nproc) - 1)) V=sc 2>&1 | tee ${{ env.TOOLCHAIN_LOG_DIR }}/toolchain-compile.log; then
            echo "❌ 工具链编译失败，尝试单线程..."
            if ! make toolchain/compile -j1 V=s 2>&1 | tee ${{ env.TOOLCHAIN_LOG_DIR }}/toolchain-compile-single.log; then
              echo "❌ 工具链编译彻底失败"
              exit 1
            fi
          fi
          
          END_TIME=$(date +%s)
          DURATION=$((END_TIME - START_TIME))
          echo "⏱️ 工具链编译耗时: ${DURATION}秒"
          echo "✅ 工具链编译完成"

      - name: 🔍 深度工具链验证
        run: |
          cd ${{ env.SOURCE_DIR }}
          echo "=== 深度工具链验证 ==="
          echo "🔧 关键工具验证:"
          for tool in m4 flex bison; do
            tool_path=$(find staging_dir/host/bin -name "$tool" -type f 2>/dev/null | head -1)
            if [ -n "$tool_path" ] && [ -x "$tool_path" ]; then
              echo "✅ $tool: 可用"
            else
              echo "❌ $tool: 缺失"
              exit 1
            fi
          done
          
          echo "🎯 目标工具链验证:"
          TARGET_GCC=$(find staging_dir/toolchain-*/bin/ -name "*gcc" -type f 2>/dev/null | head -1)
          if [ -n "$TARGET_GCC" ] && [ -x "$TARGET_GCC" ]; then
            echo "✅ 目标GCC: 可用"
          else
            echo "❌ 目标GCC: 缺失"
            exit 1
          fi
          echo "✅ 深度工具链验证完成"

      - name: 📦 详细自定义功能处理
        if: github.event.inputs.enable_custom_features == 'true'
        timeout-minutes: 10
        run: |
          cd ${{ env.SOURCE_DIR }}
          echo "处理详细自定义功能..."
          
          # 使用配置仓库的绝对路径
          CONFIG_REPO_DIR="$GITHUB_WORKSPACE"
          CUSTOM_FEATURES_DIR="${{ env.CUSTOM_FEATURES_DIR }}"
          
          # 如果环境变量中有设置，使用环境变量，否则尝试默认路径
          if [ -z "$CUSTOM_FEATURES_DIR" ]; then
            CUSTOM_FEATURES_DIR="$CONFIG_REPO_DIR/firmware-config/custom-features"
            echo "ℹ️ 使用默认自定义功能目录: $CUSTOM_FEATURES_DIR"
          fi
          
          if [ -n "$CUSTOM_FEATURES_DIR" ] && [ -d "$CUSTOM_FEATURES_DIR" ]; then
            echo "🔍 使用自定义功能目录: $CUSTOM_FEATURES_DIR"
            
            # 验证目录内容
            echo "📁 目录内容验证:"
            ls -la "$CUSTOM_FEATURES_DIR" 2>/dev/null || echo "无法访问目录"
            
            # 详细处理预编译IPK包
            echo "📦 详细处理预编译IPK包..."
            IPK_DIRS=("$CUSTOM_FEATURES_DIR/prebuilt-ipks" "$CUSTOM_FEATURES_DIR/ipks" "$CUSTOM_FEATURES_DIR/packages")
            IPK_PROCESSED=false
            
            for ipk_dir in "${IPK_DIRS[@]}"; do
              if [ -d "$ipk_dir" ]; then
                echo "🔍 检查IPK目录: $ipk_dir"
                IPK_FILES=$(find "$ipk_dir" -name "*.ipk" -type f 2>/dev/null)
                IPK_COUNT=$(echo "$IPK_FILES" | grep -v '^$' | wc -l 2>/dev/null || echo 0)
                
                if [ "$IPK_COUNT" -gt 0 ]; then
                  echo "✅ 在 $ipk_dir 找到 $IPK_COUNT 个IPK文件"
                  mkdir -p package/base-files/files/usr/lib/opkg/custom
                  
                  echo "📋 IPK文件列表:"
                  for ipk in $IPK_FILES; do
                    if [ -f "$ipk" ]; then
                      filename=$(basename "$ipk")
                      filesize=$(stat -c%s "$ipk" 2>/dev/null || echo "未知")
                      echo "  - $filename ($(($filesize/1024))KB)"
                      cp "$ipk" package/base-files/files/usr/lib/opkg/custom/
                      echo "    ✅ 已复制到固件"
                    fi
                  done
                  IPK_PROCESSED=true
                  break
                else
                  echo "ℹ️ 目录 $ipk_dir 中没有找到IPK文件"
                fi
              fi
            done
            
            if [ "$IPK_PROCESSED" = "false" ]; then
              echo "ℹ️ 未找到任何IPK文件"
            fi
            
            # 详细处理自定义脚本
            echo ""
            echo "📜 详细处理自定义脚本..."
            SCRIPT_DIRS=("$CUSTOM_FEATURES_DIR/scripts" "$CUSTOM_FEATURES_DIR/script")
            SCRIPT_PROCESSED=false
            
            for script_dir in "${SCRIPT_DIRS[@]}"; do
              if [ -d "$script_dir" ]; then
                echo "🔍 检查脚本目录: $script_dir"
                SCRIPT_FILES=$(find "$script_dir" -name "*.sh" -type f 2>/dev/null)
                SCRIPT_COUNT=$(echo "$SCRIPT_FILES" | grep -v '^$' | wc -l 2>/dev/null || echo 0)
                
                if [ "$SCRIPT_COUNT" -gt 0 ]; then
                  echo "✅ 在 $script_dir 找到 $SCRIPT_COUNT 个脚本文件"
                  
                  echo "📋 脚本文件列表:"
                  for script in $SCRIPT_FILES; do
                    if [ -f "$script" ]; then
                      filename=$(basename "$script")
                      echo "  - $filename"
                      echo "    ▶️ 执行脚本..."
                      chmod +x "$script"
                      export SOURCE_DIR="${{ env.SOURCE_DIR }}"
                      export BUILD_DIR="${{ env.SOURCE_DIR }}/build_dir"
                      export STAGING_DIR="${{ env.SOURCE_DIR }}/staging_dir"
                      
                      if timeout 300 bash "$script"; then
                        echo "    ✅ 脚本执行成功"
                      else
                        EXIT_CODE=$?
                        if [ $EXIT_CODE -eq 124 ]; then
                          echo "    ⏰ 脚本执行超时"
                        else
                          echo "    ⚠️ 脚本执行失败 (退出码: $EXIT_CODE)"
                        fi
                      fi
                    fi
                  done
                  SCRIPT_PROCESSED=true
                  break
                else
                  echo "ℹ️ 目录 $script_dir 中没有找到脚本文件"
                fi
              fi
            done
            
            if [ "$SCRIPT_PROCESSED" = "false" ]; then
              echo "ℹ️ 未找到任何脚本文件"
            fi
            
            # 处理自定义补丁
            echo ""
            echo "🔧 处理自定义补丁..."
            PATCH_FILES=$(find "$CUSTOM_FEATURES_DIR" -name "*.patch" -type f 2>/dev/null)
            PATCH_COUNT=$(echo "$PATCH_FILES" | grep -v '^$' | wc -l 2>/dev/null || echo 0)
            
            if [ "$PATCH_COUNT" -gt 0 ]; then
              echo "✅ 找到 $PATCH_COUNT 个补丁文件"
              for patch in $PATCH_FILES; do
                if [ -f "$patch" ]; then
                  echo "🔧 应用补丁: $(basename $patch)"
                  if patch -p1 -N < "$patch"; then
                    echo "✅ 补丁应用成功: $(basename $patch)"
                  else
                    echo "⚠️ 补丁应用失败或已应用: $(basename $patch)"
                  fi
                fi
              done
            else
              echo "ℹ️ 未找到补丁文件"
            fi
            
          else
            echo "ℹ️ 无自定义功能目录或目录不存在，跳过自定义功能处理"
            echo "搜索的目录: $CUSTOM_FEATURES_DIR"
          fi
          echo "✅ 自定义功能处理完成"

      - name: 🏗️ 智能编译固件（分阶段优化）
        timeout-minutes: 240
        id: compile-firmware
        run: |
          cd ${{ env.SOURCE_DIR }}
          echo "开始智能编译固件（分阶段优化）..."
          export FORCE_UNSAFE_CONFIGURE=1
          export TERM=linux
          export CCACHE_DIR="${{ env.CCACHE_DIR }}"
          export CCACHE_SLOPPINESS=file_macro,include_file_mtime,include_file_ctime,time_macros
          BUILD_JOBS=${BUILD_JOBS:-$(( $(nproc) - 1 ))}
          SAMBA_JOBS=${SAMBA_JOBS:-1}
          mkdir -p ${{ env.BUILD_LOG_DIR }}
          START_TIME=$(date +%s)
          
          echo "📊 编译参数汇总:"
          echo "  - 源码库: ${{ env.SOURCE_PRESET }}"
          echo "  - 实际分支: ${{ env.ACTUAL_BRANCH }}"
          echo "  - 优化策略: ${{ github.event.inputs.build_optimization }}"
          echo "  - 工具链策略: ${{ github.event.inputs.toolchain_strategy }}"
          echo "  - 自定义功能: ${{ github.event.inputs.enable_custom_features }}"
          echo "  - 问题修复: ${{ github.event.inputs.fix_common_issues }}"
          echo "  - CPU核心: $(nproc)"
          echo "  - 编译任务数: $BUILD_JOBS"
          echo "  - Samba任务数: $SAMBA_JOBS"
          echo "  - CCache: 已启用"
          
          echo "🔧 开始编译..."
          LOG_FILE="${{ env.BUILD_LOG_DIR }}/firmware-compile.log"
          ERROR_LOG_FILE="${{ env.BUILD_LOG_DIR }}/firmware-errors.log"
          TARGET_INSTALL_LOG="${{ env.BUILD_LOG_DIR }}/target-install.log"
          IMAGE_GEN_LOG="${{ env.BUILD_LOG_DIR }}/image-generation.log"
          
          # 分阶段编译以提高稳定性
          {
            echo "阶段1: 编译准备..."
            make tools/install 2>/dev/null || true
            make toolchain/install 2>/dev/null || true
            
            echo "阶段2: 编译内核和基础包..."
            make target/linux/compile -j$BUILD_JOBS V=s
            
            echo "阶段3: 编译Samba核心..."
            echo "🔧 编译Samba库（使用更稳健的方法）..."
            if make package/samba4-libs/compile -j$SAMBA_JOBS V=s 2>&1 | tee ${{ env.BUILD_LOG_DIR }}/samba4-libs-compile.log; then
              echo "✅ Samba库编译成功"
            else
              echo "⚠️ Samba库编译失败，尝试修复方法..."
              # 先确保依赖库已编译
              for lib in talloc tdb tevent ldb; do
                make package/$lib/compile -j$SAMBA_JOBS V=sc 2>/dev/null || true
              done
              # 再次尝试编译samba
              if make package/samba4-libs/compile -j$SAMBA_JOBS V=sc 2>&1 | tee ${{ env.BUILD_LOG_DIR }}/samba4-libs-fix.log; then
                echo "✅ Samba库修复编译成功"
              else
                echo "❌ Samba库编译彻底失败，跳过继续其他包..."
                # 记录错误但不退出
              fi
            fi
            
            echo "🔧 编译Samba服务器..."
            if make package/samba4-server/compile -j$SAMBA_JOBS V=s 2>&1 | tee ${{ env.BUILD_LOG_DIR }}/samba4-server-compile.log; then
              echo "✅ Samba服务器编译成功"
            else
              echo "⚠️ Samba服务器编译失败，尝试修复..."
              if make package/samba4-server/compile -j$SAMBA_JOBS V=sc 2>&1 | tee ${{ env.BUILD_LOG_DIR }}/samba4-server-fix.log; then
                echo "✅ Samba服务器修复编译成功"
              else
                echo "❌ Samba服务器编译彻底失败，跳过继续..."
                # 记录错误但不退出编译流程
              fi
            fi
            
            echo "阶段4: 编译其他包（并行）..."
            make package/compile -j$BUILD_JOBS V=sc || {
              echo "⚠️ 部分包编译失败，继续其他阶段..."
            }
            
            echo "阶段5: 安装包..."
            make package/install -j1 V=s || {
              echo "⚠️ 包安装遇到问题，尝试继续..."
              make package/install -j1 V=sc
            }
            
            echo "阶段6: 目标安装..."
            make target/install -j1 V=s || {
              echo "❌ 目标安装失败，尝试绕过..."
              # 即使目标安装失败，也尝试生成固件
              make -j1 V=sc || make image -j1 V=sc
            }
            
            echo "阶段7: 生成镜像..."
            make target/linux/ipq40xx/image -j1 V=s || {
              echo "⚠️ 专门镜像生成失败，使用默认目标..."
              make -j1 V=sc
            }
            
          } 2>&1 | tee "$LOG_FILE"
          
          COMPILE_EXIT_CODE=${PIPESTATUS[0]}
          END_TIME=$(date +%s)
          DURATION=$((END_TIME - START_TIME))
          echo "BUILD_DURATION=$DURATION" >> $GITHUB_ENV
          
          if [ $COMPILE_EXIT_CODE -eq 0 ]; then
            echo "BUILD_STATUS=success" >> $GITHUB_ENV
            echo "🎉 编译成功完成!"
          else
            echo "BUILD_STATUS=failed" >> $GITHUB_ENV
            echo "❌ 编译失败，退出码: $COMPILE_EXIT_CODE"
            echo "=== 详细错误分析 ==="
            if [ -f "$LOG_FILE" ]; then
              echo "关键错误:"
              grep -i "error\|failed\|undefined\|missing\|no such file\|cannot create\|Broken pipe" "$LOG_FILE" | tail -30 || echo "无明确错误信息"
            fi
            exit 1
          fi
          
          echo "⏱️ 总编译耗时: ${DURATION}秒 ($(($DURATION/60))分钟)"

      - name: 🔍 验证Samba功能
        if: env.BUILD_STATUS == 'success'
        run: |
          cd ${{ env.SOURCE_DIR }}
          echo "=== 验证Samba功能 ==="
          
          # 检查Samba相关包是否生成
          if find bin -name "*samba4*" -type f | grep -q .; then
            echo "✅ Samba包已生成"
            find bin -name "*samba4*" -type f | while read file; do
              echo "  - $(basename $file)"
            done
          else
            echo "⚠️ 未找到Samba包"
          fi
          
          # 检查LuCI Samba应用
          if find bin -name "*luci-app-samba4*" -type f | grep -q .; then
            echo "✅ LuCI Samba应用已生成"
          else
            echo "⚠️ 未找到LuCI Samba应用"
          fi

      - name: 🔍 详细固件产物验证与收集
        if: env.BUILD_STATUS == 'success'
        run: |
          cd ${{ env.SOURCE_DIR }}
          echo "=== 详细固件产物验证与收集 ==="
          mkdir -p ${{ env.ARTIFACTS_DIR }}/firmware
          mkdir -p ${{ env.ARTIFACTS_DIR }}/logs
          mkdir -p ${{ env.ARTIFACTS_DIR }}/configs
          
          echo "🔍 搜索固件文件..."
          TOTAL_FIRMWARE_COUNT=0
          
          # 详细搜索固件文件
          if [ -d "bin/targets" ]; then
            echo "📁 复制标准输出目录..."
            cp -r bin/targets ${{ env.ARTIFACTS_DIR }}/firmware/
            
            # 统计各种类型的固件
            INITRAMFS_COUNT=$(find bin/targets -name "*initramfs*" -type f 2>/dev/null | wc -l)
            UPGRADE_COUNT=$(find bin/targets -name "*sysupgrade*" -type f 2>/dev/null | wc -l)
            FACTORY_COUNT=$(find bin/targets -name "*factory*" -type f 2>/dev/null | wc -l)
            OTHER_COUNT=$(find bin/targets -name "*.bin" -o -name "*.img" -o -name "*.trx" -o -name "*.itb" 2>/dev/null | grep -v -e "initramfs" -e "sysupgrade" -e "factory" | wc -l)
            
            TOTAL_FIRMWARE_COUNT=$((INITRAMFS_COUNT + UPGRADE_COUNT + FACTORY_COUNT + OTHER_COUNT))
            
            echo "📊 固件类型统计:"
            echo "  - Initramfs 镜像: $INITRAMFS_COUNT 个"
            echo "  - 系统升级镜像: $UPGRADE_COUNT 个"
            echo "  - 工厂刷机镜像: $FACTORY_COUNT 个"
            echo "  - 其他镜像: $OTHER_COUNT 个"
            echo "  - 总计: $TOTAL_FIRMWARE_COUNT 个文件"
            
            # 显示找到的固件文件
            echo "📋 固件文件详细列表:"
            find bin/targets -name "*.bin" -o -name "*.img" -o -name "*.trx" -o -name "*.itb" 2>/dev/null | head -30 | while read file; do
              if [ -f "$file" ]; then
                file_size=$(stat -c%s "$file" 2>/dev/null || echo 0)
                file_type="其他"
                if [[ "$file" == *"initramfs"* ]]; then
                  file_type="Initramfs"
                elif [[ "$file" == *"sysupgrade"* ]]; then
                  file_type="系统升级"
                elif [[ "$file" == *"factory"* ]]; then
                  file_type="工厂刷机"
                fi
                echo "  - $file_type: $(basename "$file") ($(($file_size/1024/1024))MB)"
              fi
            done
            
            # 检查特定设备的工厂镜像
            echo ""
            echo "🔍 检查 ASUS RT-AC42U 特定镜像..."
            ASUS_IMAGES=$(find bin/targets -name "*asus_rt-ac42u*" -type f 2>/dev/null)
            if [ -n "$ASUS_IMAGES" ]; then
              echo "📋 ASUS RT-AC42U 镜像列表:"
              for img in $ASUS_IMAGES; do
                if [ -f "$img" ]; then
                  img_size=$(stat -c%s "$img" 2>/dev/null || echo 0)
                  img_type="未知"
                  if [[ "$img" == *"initramfs"* ]]; then
                    img_type="RAM启动镜像"
                  elif [[ "$img" == *"sysupgrade"* ]]; then
                    img_type="系统升级镜像"
                  elif [[ "$img" == *"factory"* ]]; then
                    img_type="工厂刷机镜像"
                  fi
                  echo "  - $img_type: $(basename "$img") ($(($img_size/1024/1024))MB)"
                fi
              done
              
              # 检查是否缺少工厂镜像
              ASUS_FACTORY=$(find bin/targets -name "*asus_rt-ac42u*factory*" -type f 2>/dev/null)
              if [ -z "$ASUS_FACTORY" ]; then
                echo "⚠️ 警告: 未找到 ASUS RT-AC42U 工厂刷机镜像"
                echo "FACTORY_MISSING=true" >> $GITHUB_ENV
              else
                echo "✅ 已找到 ASUS RT-AC42U 工厂刷机镜像"
                echo "FACTORY_MISSING=false" >> $GITHUB_ENV
              fi
            else
              echo "❌ 错误: 未找到任何 ASUS RT-AC42U 镜像"
              echo "FACTORY_MISSING=true" >> $GITHUB_ENV
            fi
            
          else
            echo "⚠️ 标准输出目录 bin/targets 不存在"
            echo "FACTORY_MISSING=true" >> $GITHUB_ENV
          fi
          
          # 在其他位置搜索
          echo "🔍 在其他位置搜索固件..."
          OTHER_FIRMWARE=$(find . -maxdepth 4 -name "*.bin" -o -name "*.img" -o -name "*.trx" -o -name "*.gz" 2>/dev/null | grep -v "bin/targets" | head -20)
          if [ -n "$OTHER_FIRMWARE" ]; then
            OTHER_COUNT=$(echo "$OTHER_FIRMWARE" | wc -l)
            echo "✅ 在其他位置找到 $OTHER_COUNT 个固件相关文件"
            for file in $OTHER_FIRMWARE; do
              if [ -f "$file" ]; then
                file_size=$(stat -c%s "$file" 2>/dev/null || echo 0)
                if [ "$file_size" -gt 1000000 ]; then
                  rel_path=$(echo "$file" | sed 's|^\./||')
                  target_dir="${{ env.ARTIFACTS_DIR }}/firmware/other/$(dirname "$rel_path")"
                  mkdir -p "$target_dir"
                  cp "$file" "$target_dir/"
                  echo "📦 已收集: $rel_path ($(($file_size/1024/1024))MB)"
                  TOTAL_FIRMWARE_COUNT=$((TOTAL_FIRMWARE_COUNT + 1))
                fi
              fi
            done
          fi
          
          # 即使没有标准输出，也检查其他常见位置
          if [ $TOTAL_FIRMWARE_COUNT -eq 0 ]; then
            echo "🔍 深度搜索固件文件..."
            # 搜索build_dir中的固件
            BUILD_FIRMWARE=$(find build_dir -name "*.bin" -o -name "*.img" -o -name "*.trx" 2>/dev/null | head -10)
            if [ -n "$BUILD_FIRMWARE" ]; then
              echo "✅ 在build_dir中找到固件相关文件:"
              echo "$BUILD_FIRMWARE"
            fi
          fi
          
          echo "📋 收集构建信息和日志..."
          # 收集配置文件
          cp .config ${{ env.ARTIFACTS_DIR }}/configs/ 2>/dev/null || echo "⚠️ 无法复制.config"
          cp feeds.conf ${{ env.ARTIFACTS_DIR }}/configs/ 2>/dev/null || echo "⚠️ 无法复制feeds.conf"
          
          echo "FIRMWARE_COUNT=$TOTAL_FIRMWARE_COUNT" >> $GITHUB_ENV
          
          if [ $TOTAL_FIRMWARE_COUNT -eq 0 ]; then
            echo "❌ 错误: 未找到任何固件文件"
            echo "当前目录结构:"
            find . -maxdepth 3 -type d | head -20
            echo ""
            echo "所有可能的固件文件:"
            find . -name "*.bin" -o -name "*.img" -o -name "*.trx" -o -name "*.gz" 2>/dev/null | head -30 || echo "无固件文件"
            exit 1
          else
            echo "✅ 固件验证与收集完成: 总共 $TOTAL_FIRMWARE_COUNT 个文件"
          fi

      - name: 💾 优化日志收集与结构
        if: always()
        run: |
          cd ${{ env.SOURCE_DIR }}
          echo "=== 优化日志收集与结构 ==="
          mkdir -p ${{ env.ARTIFACTS_DIR }}/logs
          
          # 创建清晰的目录结构
          mkdir -p ${{ env.ARTIFACTS_DIR }}/logs/完整日志-英文
          mkdir -p ${{ env.ARTIFACTS_DIR }}/logs/GitHub过程日志
          mkdir -p ${{ env.ARTIFACTS_DIR }}/logs/问题摘要
          mkdir -p ${{ env.ARTIFACTS_DIR }}/logs/编译详细日志
          
          echo "📁 整理原始英文日志..."
          # 复制原始英文日志到专门目录
          if [ -d "${{ env.BUILD_LOG_DIR }}" ]; then
            cp -r ${{ env.BUILD_LOG_DIR }}/* ${{ env.ARTIFACTS_DIR }}/logs/完整日志-英文/ 2>/dev/null || true
          fi
          if [ -d "${{ env.TOOLCHAIN_LOG_DIR }}" ]; then
            cp -r ${{ env.TOOLCHAIN_LOG_DIR }}/* ${{ env.ARTIFACTS_DIR }}/logs/完整日志-英文/ 2>/dev/null || true
          fi
          
          echo "📁 收集并重命名GitHub过程日志..."
          # 为关键日志文件创建中文名称副本
          LOG_MAPPING=(
            "clone.log:检出配置仓库.txt"
            "firmware-compile.log:智能编译固件.txt"
            "target-install.log:目标安装阶段.txt"
            "image-generation.log:镜像生成过程.txt"
            "host-tools.log:主机工具编译.txt"
            "toolchain-compile.log:工具链编译.txt"
            "samba-talloc.log:Samba-talloc库编译.txt"
            "samba-tdb.log:Samba-tdb库编译.txt"
            "samba-tevent.log:Samba-tevent库编译.txt"
            "samba-ldb.log:Samba-ldb库编译.txt"
            "samba4-libs-compile.log:Samba4库编译.txt"
            "samba4-server-compile.log:Samba4服务器编译.txt"
          )
          
          for mapping in "${LOG_MAPPING[@]}"; do
            orig_file=$(echo "$mapping" | cut -d: -f1)
            chinese_name=$(echo "$mapping" | cut -d: -f2)
            if [ -f "${{ env.BUILD_LOG_DIR }}/$orig_file" ]; then
              cp "${{ env.BUILD_LOG_DIR }}/$orig_file" "${{ env.ARTIFACTS_DIR }}/logs/GitHub过程日志/$chinese_name"
              echo "✅ 已复制: $orig_file → $chinese_name"
            fi
          done
          
          # 创建步骤名称映射文件
          echo "创建步骤日志映射..."
          {
            echo "# GitHub Actions 步骤日志中文映射"
            echo "检出配置仓库.txt - 📥 检出配置仓库"
            echo "智能系统资源分析.txt - 💾 智能系统资源分析" 
            echo "全面工作环境设置.txt - 🔧 全面工作环境设置"
            echo "智能文件发现与配置分析.txt - 🔍 智能文件发现与配置分析"
            echo "智能源码配置解析.txt - 🔧 智能源码配置解析"
            echo "全面编译依赖安装与验证.txt - 🛠️ 全面编译依赖安装与验证"
            echo "高级缓存与性能优化.txt - ⚡ 高级缓存与性能优化"
            echo "智能源码获取与分支处理.txt - 📥 智能源码获取与分支处理"
            echo "源码获取后空间监测.txt - 💾 源码获取后空间监测"
            echo "智能源码初始化.txt - 🔄 智能源码初始化"
            echo "快速配置文件查找与应用.txt - 🎯 快速配置文件查找与应用"
            echo "设备镜像配置修复.txt - 🔧 设备镜像配置修复"
            echo "高级性能优化配置.txt - 🔧 高级性能优化配置"
            echo "Samba专用修复与编译.txt - 🛠️ Samba专用修复与编译"
            echo "综合问题修复.txt - 🔧 综合问题修复"
            echo "全面工具链编译与验证.txt - 🛠️ 全面工具链编译与验证"
            echo "深度工具链验证.txt - 🔍 深度工具链验证"
            echo "详细自定义功能处理.txt - 📦 详细自定义功能处理"
            echo "智能编译固件.txt - 🏗️ 智能编译固件"
            echo "验证Samba功能.txt - 🔍 验证Samba功能"
            echo "详细固件产物验证与收集.txt - 🔍 详细固件产物验证与收集"
            echo "优化日志收集与结构.txt - 💾 优化日志收集与结构"
            echo "完善深度错误分析与解决方案.txt - 🔍 完善深度错误分析与解决方案"
            echo "生成详细构建报告.txt - 📊 生成详细构建报告"
            echo "上传构建产物.txt - 💾 上传构建产物"
            echo "编译后智能清理.txt - 🧹 编译后智能清理"
            echo "最终综合报告.txt - 📊 最终综合报告"
          } > "${{ env.ARTIFACTS_DIR }}/logs/GitHub过程日志/步骤日志映射说明.txt"
          
          echo "📁 收集问题摘要..."
          if [ -f "${{ env.BUILD_LOG_DIR }}/firmware-errors.log" ]; then
            cp "${{ env.BUILD_LOG_DIR }}/firmware-errors.log" "${{ env.ARTIFACTS_DIR }}/logs/问题摘要/编译错误摘要.txt"
          fi
          
          echo "✅ 日志结构优化完成"

      - name: 🔍 完善深度错误分析与解决方案
        if: always()
        run: |
          cd ${{ env.SOURCE_DIR }}
          echo "=== 完善深度错误分析与解决方案 ==="
          ANALYSIS_FILE="${{ env.ARTIFACTS_DIR }}/深度错误分析与解决方案报告.txt"
          
          {
            echo "=== 深度错误分析与解决方案报告 ==="
            echo "分析时间: $(date)"
            echo "构建状态: ${{ env.BUILD_STATUS || 'unknown' }}"
            echo "固件数量: ${{ env.FIRMWARE_COUNT || 0 }}"
            echo "工厂镜像状态: ${{ env.FACTORY_MISSING == 'true' && '缺失' || '已生成' }}"
            echo ""
            
            echo "🎯 核心问题总结"
            echo "=================="
            echo "❌ 主要问题1: Samba依赖库编译路径错误"
            echo "❌ 主要问题2: 目标安装阶段目录结构缺失"
            echo "❌ 主要问题3: 软件包安装失败 (samba相关)"
            echo "❌ 主要问题4: 工厂镜像未生成"
            echo ""
            
            echo "🔍 详细问题分析与解决方案"
            echo "=========================="
            echo ""
            
            echo "1. Samba依赖库编译路径错误"
            echo "--------------------------"
            echo "🔴 错误现象:"
            echo "   - No rule to make target 'package/libs/libtalloc/compile'"
            echo "   - Samba依赖库编译失败"
            echo "📋 根本原因:"
            echo "   - 依赖库路径不正确，应该是 package/libtalloc 而不是 package/libs/libtalloc"
            echo "   - 不同源码源的目录结构可能不同"
            echo "💡 解决方案:"
            echo "   ✅ 已修复: 使用正确的依赖库路径 package/libtalloc"
            echo "   ✅ 备用方案: 同时尝试 package/libs/libtalloc 路径"
            echo "   ✅ 增强检测: 自动检测依赖库实际位置"
            echo "🛠️ 具体操作:"
            echo "   1. 在'Samba专用修复与编译'步骤中修复路径"
            echo "   2. 添加备用路径尝试机制"
            echo "   3. 增加依赖库目录检测"
            echo ""
            
            echo "2. 目标安装阶段目录结构缺失"
            echo "--------------------------"
            echo "🔴 错误现象:"
            echo "   - opkg_conf_load: Could not create lock file /tmp/opkg.lock"
            echo "   - No such file or directory"
            echo "📋 根本原因:"
            echo "   - 目标根文件系统目录结构不完整"
            echo "   - /tmp 目录在目标系统中缺失"
            echo "   - 这发生在目标设备安装阶段，影响包管理功能"
            echo "💡 解决方案:"
            echo "   ✅ 短期修复: 在编译前创建必要的目录结构"
            echo "   ✅ 中期修复: 修改设备配置确保目录完整性"
            echo "   ✅ 长期修复: 更新源码补丁修复根文件系统生成"
            echo "🛠️ 具体操作:"
            echo "   1. 在'综合问题修复'步骤中加强目录创建"
            echo "   2. 确保以下目录存在:"
            echo "      - /mnt/source/build_dir/target-*/root*/tmp"
            echo "      - /mnt/source/build_dir/target-*/root*/var/lock"
            echo "      - /mnt/source/build_dir/target-*/root*/var/run"
            echo ""
            
            echo "3. 软件包安装失败 (samba相关)"
            echo "---------------------------"
            echo "🔴 错误现象:"
            echo "   - samba包安装失败，错误代码255"
            echo "   - 影响luci-app-samba4等依赖包"
            echo "📋 根本原因:"
            echo "   - samba编译配置问题"
            echo "   - 可能的内存或磁盘空间不足"
            echo "   - 依赖关系冲突"
            echo "💡 解决方案:"
            echo "   ✅ 方案A: 跳过有问题的包继续编译"
            echo "   ✅ 方案B: 修复samba包配置"
            echo "   ✅ 方案C: 禁用不必要的samba组件"
            echo "🛠️ 具体操作:"
            echo "   1. 在.config中临时禁用samba:"
            echo "      CONFIG_PACKAGE_samba4-server=n"
            echo "      CONFIG_PACKAGE_luci-app-samba4=n"
            echo "   2. 增加交换空间到24GB"
            echo "   3. 清理编译缓存后重试"
            echo ""
            
            echo "4. 工厂镜像未生成"
            echo "----------------"
            echo "🔴 问题现象:"
            echo "   - 只有sysupgrade.bin系统升级镜像"
            echo "   - 缺少factory.bin工厂刷机镜像"
            echo "📋 根本原因:"
            echo "   - 设备配置中工厂镜像生成被禁用"
            echo "   - 目标设备定义不完整"
            echo "   - 编译过程中镜像生成失败"
            echo "💡 解决方案:"
            echo "   ✅ 检查设备配置文件中的工厂镜像设置"
            echo "   ✅ 确保CONFIG_TARGET_IMAGES_PACKAGES=y"
            echo "   ✅ 验证目标设备支持factory镜像生成"
            echo "🛠️ 具体操作:"
            echo "   1. 在.config中确保以下设置:"
            echo "      CONFIG_TARGET_IMAGES_PACKAGES=y"
            echo "      CONFIG_TARGET_ipq40xx_generic_DEVICE_asus_rt-ac42u=y"
            echo "      CONFIG_IMAGEOPT=y"
            echo "      CONFIG_COMBINED_IMAGES=y"
            echo "   2. 运行: make target/linux/ipq40xx/image"
            echo "   3. 检查bin/targets/ipq40xx/generic/目录"
            echo ""
            
            echo "🔧 即时修复措施"
            echo "================"
            echo ""
            echo "1. Samba依赖库路径修复 (立即执行):"
            echo "   make package/talloc/compile -j1 V=s"
            echo "   make package/tdb/compile -j1 V=s"
            echo "   make package/tevent/compile -j1 V=s"
            echo "   make package/ldb/compile -j1 V=s"
            echo ""
            echo "2. 目录结构修复 (立即执行):"
            echo "   mkdir -p /mnt/source/build_dir/target-*/root*/tmp"
            echo "   mkdir -p /mnt/source/build_dir/target-*/root*/var/lock"
            echo "   mkdir -p /mnt/source/build_dir/target-*/root*/var/run"
            echo "   chmod 755 /mnt/source/build_dir/target-*/root*/tmp"
            echo ""
            echo "3. 配置修复 (修改.config):"
            echo "   echo 'CONFIG_TARGET_IMAGES_PACKAGES=y' >> .config"
            echo "   echo 'CONFIG_TARGET_ipq40xx_generic_DEVICE_asus_rt-ac42u=y' >> .config"
            echo "   echo 'CONFIG_IMAGEOPT=y' >> .config"
            echo "   echo 'CONFIG_COMBINED_IMAGES=y' >> .config"
            echo "   make defconfig"
            echo ""
            echo "4. 问题包处理:"
            echo "   # 临时禁用samba (如非必需)"
            echo "   sed -i 's/CONFIG_PACKAGE_samba4-server=y/# CONFIG_PACKAGE_samba4-server is not set/' .config"
            echo "   sed -i 's/CONFIG_PACKAGE_luci-app-samba4=y/# CONFIG_PACKAGE_luci-app-samba4 is not set/' .config"
            echo ""
            
            echo "📊 编译质量评估"
            echo "=============="
            echo "✅ 成功方面:"
            echo "   - 系统升级镜像生成成功"
            echo "   - 核心固件功能正常"
            echo "   - 设备识别和配置正确"
            echo ""
            echo "⚠️ 需要注意:"
            echo "   - 缺少工厂刷机镜像"
            echo "   - samba文件共享功能可能缺失"
            echo "   - 目标安装阶段有目录权限问题"
            echo ""
            echo "🔮 预期结果:"
            echo "   - 修复后应能生成完整的工厂镜像"
            echo "   - 系统升级镜像功能正常"
            echo "   - 大部分网络功能可用"
            echo ""
            
            echo "🚀 下一步行动建议"
            echo "================"
            echo "1. 🔧 立即修复:"
            echo "   - 执行上述目录结构修复"
            echo "   - 重新运行目标安装阶段"
            echo ""
            echo "2. ⚙️ 配置调整:"
            echo "   - 确保工厂镜像配置启用"
            echo "   - 考虑禁用问题包"
            echo ""
            echo "3. 🧪 测试验证:"
            echo "   - 使用生成的sysupgrade.bin测试升级"
            echo "   - 验证基本网络功能"
            echo ""
            echo "4. 📈 长期改进:"
            echo "   - 更新设备配置文件"
            echo "   - 考虑使用更稳定的源码分支"
            echo ""
            
            echo "💡 技术说明"
            echo "==========="
            echo "ASUS RT-AC42U 设备信息:"
            echo "- 架构: ipq40xx"
            echo "- 子架构: generic"
            echo "- 支持的镜像类型:"
            echo "  - sysupgrade.bin (系统升级)"
            echo "  - factory.bin (工厂刷机)"
            echo "  - initramfs.bin (RAM启动)"
            echo ""
            echo "当前生成的文件:"
            echo "- immortalwrt-ipq40xx-generic-asus_rt-ac42u-squashfs-sysupgrade.bin"
            echo ""
            echo "缺失的文件:"
            echo "- immortalwrt-ipq40xx-generic-asus_rt-ac42u-squashfs-factory.bin"
            echo "- immortalwrt-ipq40xx-generic-asus_rt-ac42u-initramfs-kernel.bin"
            
          } > "$ANALYSIS_FILE"
          
          echo "✅ 完善深度错误分析完成"

      - name: 📊 生成详细构建报告
        if: always()
        run: |
          cd ${{ env.SOURCE_DIR }}
          echo "生成详细构建报告..."
          REPORT_FILE="${{ env.ARTIFACTS_DIR }}/详细构建报告.txt"
          
          {
            echo "=== 详细固件构建报告 ==="
            echo "构建时间: $(date)"
            echo "构建状态: ${{ env.BUILD_STATUS || 'unknown' }}"
            echo "编译时长: ${{ env.BUILD_DURATION || 'N/A' }}秒"
            echo "固件数量: ${{ env.FIRMWARE_COUNT || 0 }}"
            echo ""
            
            # 添加工厂镜像状态
            echo "=== 工厂镜像状态 ==="
            if [ "${{ env.FACTORY_MISSING }}" = "true" ]; then
              echo "❌ 工厂刷机镜像: 缺失"
              echo "可能原因:"
              echo "  - 设备配置中工厂镜像生成被禁用"
              echo "  - 目标设备定义不完整"
              echo "  - 编译过程中镜像生成失败"
              echo "建议:"
              echo "  - 检查设备配置文件"
              echo "  - 验证目标设备支持"
              echo "  - 尝试手动生成工厂镜像"
            else
              echo "✅ 工厂刷机镜像: 已生成"
            fi
            echo ""
            
            echo "=== 源码信息 ==="
            echo "源码库: ${{ env.SOURCE_PRESET }}"
            echo "请求分支: ${{ env.SOURCE_BRANCH }}"
            echo "实际分支: ${{ env.ACTUAL_BRANCH }}"
            echo "推荐分支: ${{ env.RECOMMENDED_BRANCH }}"
            echo "提交哈希: ${{ env.COMMIT_HASH || 'unknown' }}"
            echo ""
            echo "=== 编译配置 ==="
            echo "配置文件: ${{ github.event.inputs.config_profile }}"
            echo "优化策略: ${{ github.event.inputs.build_optimization }}"
            echo "工具链策略: ${{ github.event.inputs.toolchain_strategy }}"
            echo "自定义功能: ${{ github.event.inputs.enable_custom_features }}"
            echo "问题修复: ${{ github.event.inputs.fix_common_issues }}"
            echo ""
            echo "=== 系统信息 ==="
            echo "Runner: ${{ runner.os }}"
            echo "CPU核心: $(nproc)"
            echo "内存: $(free -h | awk '/^Mem:/{print $2}')"
            echo "磁盘空间: $(df -h /mnt | awk 'NR==2{print $4}') 可用"
            echo ""
            echo "=== 编译错误分析 ==="
            if [ -f "${{ env.BUILD_LOG_DIR }}/firmware-errors.log" ]; then
              echo "编译过程中发现以下错误:"
              cat "${{ env.BUILD_LOG_DIR }}/firmware-errors.log"
              echo ""
            fi
            if [ -f "${{ env.BUILD_LOG_DIR }}/target-install.log" ]; then
              echo "目标安装错误:"
              grep -i "error\|failed" "${{ env.BUILD_LOG_DIR }}/target-install.log" | head -10
              echo ""
            fi
            echo ""
            if [ "${{ env.BUILD_STATUS }}" = "success" ]; then
              echo "=== 产物信息 ==="
              echo "固件文件列表:"
              find ${{ env.ARTIFACTS_DIR }}/firmware -type f 2>/dev/null | xargs -I {} basename {} 2>/dev/null | sort | head -20
              echo ""
              echo "日志文件列表:"
              find ${{ env.ARTIFACTS_DIR }}/logs -type f 2>/dev/null | xargs -I {} basename {} 2>/dev/null | sort | head -10
            else
              echo "构建失败，无固件产物"
            fi
            echo ""
            echo "=== 自定义功能状态 ==="
            if [ "${{ github.event.inputs.enable_custom_features }}" = "true" ]; then
              if [ -n "${{ env.CUSTOM_FEATURES_DIR }}" ]; then
                echo "✅ 自定义功能已启用"
                echo "自定义目录: ${{ env.CUSTOM_FEATURES_DIR }}"
              else
                echo "⚠️ 自定义功能已启用但未找到目录"
              fi
            else
              echo "ℹ️ 自定义功能未启用"
            fi
          } > "$REPORT_FILE"
          
          echo "✅ 详细构建报告生成完成"

      - name: 💾 上传构建产物
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: Firmware-${{ github.run_number }}-${{ env.SOURCE_PRESET }}-${{ env.ACTUAL_BRANCH }}-${{ env.BUILD_STATUS || 'unknown' }}
          path: ${{ env.ARTIFACTS_DIR }}
          retention-days: 30

      - name: 🧹 编译后智能清理
        if: always()
        run: |
          echo "开始编译后智能清理..."
          echo "=== 清理前状态 ==="
          df -h
          free -h
          
          if [ -d "${{ env.SOURCE_DIR }}" ]; then
            cd ${{ env.SOURCE_DIR }}
            echo "🧹 清理编译临时文件..."
            rm -rf build_dir/* staging_dir/* tmp/* 2>/dev/null || true
          fi
          
          echo "🧹 清理交换文件..."
          sudo swapoff /mnt/swapfile 2>/dev/null || true
          sudo rm -f /mnt/swapfile 2>/dev/null || true
          
          echo ""
          echo "=== 清理后状态 ==="
          df -h
          free -h
          echo "✅ 智能清理完成"

      - name: 📊 最终综合报告
        if: always()
        run: |
          echo "=== 最终综合报告 ==="
          echo "🎯 工作流: ${{ github.workflow }}"
          echo "🆔 运行ID: ${{ github.run_id }}"
          echo "🆔 运行号: ${{ github.run_number }}"
          echo ""
          echo "📦 源码信息:"
          echo "  - 预设: ${{ env.SOURCE_PRESET }}"
          echo "  - 分支: ${{ env.ACTUAL_BRANCH }}"
          echo "  - 推荐: ${{ env.RECOMMENDED_BRANCH }}"
          echo "  - 提交: ${{ env.COMMIT_HASH || 'unknown' }}"
          echo ""
          echo "⚙️ 编译配置:"
          echo "  - 配置文件: ${{ github.event.inputs.config_profile }}"
          echo "  - 优化策略: ${{ github.event.inputs.build_optimization }}"
          echo "  - 工具链: ${{ github.event.inputs.toolchain_strategy }}"
          echo "  - 自定义: ${{ github.event.inputs.enable_custom_features }}"
          echo "  - 问题修复: ${{ github.event.inputs.fix_common_issues }}"
          echo ""
          echo "📈 构建结果:"
          echo "  - 状态: ${{ env.BUILD_STATUS || 'unknown' }}"
          echo "  - 时长: ${{ env.BUILD_DURATION || 'N/A' }}秒"
          echo "  - 固件数量: ${{ env.FIRMWARE_COUNT || 0 }}"
          echo "  - 工厂镜像状态: ${{ env.FACTORY_MISSING == 'true' && '缺失' || '已生成' }}"
          echo ""
          if [ "${{ env.BUILD_STATUS }}" = "success" ]; then
            echo "🎉 构建成功！固件已上传到 Artifacts"
            echo "📦 下载 Artifacts 获取编译产物"
            echo "🔧 固件文件在 firmware/ 目录中"
            echo "📋 详细报告在 详细构建报告.txt"
            echo "🔍 深度分析在 深度错误分析与解决方案报告.txt"
            echo "📁 日志文件在 logs/ 目录中（分类清晰）"
            if [ "${{ env.FACTORY_MISSING }}" = "true" ]; then
              echo "⚠️ 注意: 工厂刷机镜像缺失，只有系统升级镜像"
              echo "💡 建议启用'自动修复常见问题'选项重新构建"
            else
              echo "✅ 所有镜像类型均已生成"
            fi
          else
            echo "❌ 构建失败"
            echo "🔍 请查看详细日志分析错误原因"
            echo "📋 检查 Artifacts 中的编译日志"
            echo "🔍 查看 logs/问题摘要/ 快速了解问题"
            echo "💡 根本问题: Samba依赖库路径错误"
            echo "🛠️ 已实施的修复:"
            echo "  - 修正Samba依赖库编译路径"
            echo "  - 深度目录修复和权限设置"
            echo "  - 目标设备兼容性检查"
            echo "  - 多种固件生成回退方案"
            echo "🔧 建议进一步操作:"
            echo "  - 检查配置文件与源码版本的兼容性"
            echo "  - 验证目标设备支持状态"
            echo "  - 尝试不同的源码分支"
            echo "  - 检查自定义功能是否冲突"
            echo "  - 启用'自动修复常见问题'选项"
          fi
