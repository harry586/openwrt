name: "🚀 超快速固件构建器 - 修复兼容版"

on:
  workflow_dispatch:
    inputs:
      source_preset:
        description: "选择源码库"
        required: true
        type: choice
        options: ["immortalwrt", "openwrt", "lede"]
        default: "immortalwrt"
      source_branch:
        description: "源码分支"
        required: true
        default: "auto"
        type: choice
        options: ["auto", "openwrt-22.03", "openwrt-21.02", "master", "main"]
      config_profile:
        description: "设备配置文件名称"
        required: true
        type: string
        default: "generic-x86-64"
      build_optimization:
        description: "编译优化策略"
        required: true
        type: choice
        options: ["balanced", "speed", "stability"]
        default: "balanced"
      enable_advanced:
        description: "启用高级功能"
        required: false
        default: true
        type: boolean
      language_pack:
        description: "语言包选择"
        required: true
        type: choice
        options: ["zh-cn", "en-only", "all"]
        default: "zh-cn"
      plugin_preset:
        description: "插件预设"
        required: true
        type: choice
        options: ["minimal", "standard", "full", "custom"]
        default: "standard"
      skip_tests:
        description: "跳过测试阶段"
        required: false
        default: true
        type: boolean

env:
  TZ: "Asia/Shanghai"
  SOURCE_DIR: "/mnt/source"
  ARTIFACTS_DIR: "/mnt/artifacts"
  CCACHE_DIR: "/mnt/ccache"
  BUILD_LOG_DIR: "/mnt/build-logs"
  DOWNLOAD_DIR: "/mnt/downloads"

jobs:
  build:
    runs-on: "ubuntu-22.04"
    timeout-minutes: 180
    
    steps:
      - name: "📥 检出配置仓库"
        uses: "actions/checkout@v4"
        with:
          path: "config-repo"
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: "🔧 极速环境初始化"
        id: init
        run: |
          echo "=== 🔧 极速环境初始化 ==="
          sudo mkdir -p /mnt/source /mnt/artifacts /mnt/ccache /mnt/build-logs /mnt/downloads
          sudo chown -R $USER:$USER /mnt/source /mnt/artifacts /mnt/ccache /mnt/build-logs /mnt/downloads
          sudo chmod -R 755 /mnt/source /mnt/artifacts /mnt/ccache /mnt/build-logs /mnt/downloads
          
          MAIN_LOG="/mnt/build-logs/build_timeline.log"
          ERROR_LOG="/mnt/build-logs/errors.log"
          WARNING_LOG="/mnt/build-logs/warnings.log"
          ANALYSIS_REPORT="/mnt/build-logs/analysis_report.md"
          
          echo "🏁 极速构建开始: $(date)" > $MAIN_LOG
          echo "🚀 工作流版本: 超快速固件构建器 - 修复兼容版" >> $MAIN_LOG
          echo "MAIN_LOG=$MAIN_LOG" >> $GITHUB_ENV
          echo "ERROR_LOG=$ERROR_LOG" >> $GITHUB_ENV
          echo "WARNING_LOG=$WARNING_LOG" >> $GITHUB_ENV
          echo "ANALYSIS_REPORT=$ANALYSIS_REPORT" >> $GITHUB_ENV
          echo "CONFIG_REPO_DIR=${{ github.workspace }}/config-repo" >> $GITHUB_ENV
          echo "BUILD_STATUS=running" >> $GITHUB_ENV
          
          echo "# 错误日志" > $ERROR_LOG
          echo "# 警告日志" > $WARNING_LOG
          echo "# 智能分析报告" > $ANALYSIS_REPORT
          echo "✅ 极速环境初始化完成"

      - name: "🚀 极速依赖安装"
        id: install_deps
        run: |
          set +e
          MAIN_LOG="${{ env.MAIN_LOG }}"
          echo "=== 🚀 极速依赖安装 ===" | tee -a "$MAIN_LOG"
          
          echo "📦 批量安装编译依赖..." | tee -a "$MAIN_LOG"
          sudo apt-get update -qq
          
          sudo apt-get install -y -qq \
            build-essential clang flex bison g++ gawk gcc-multilib g++-multilib \
            gettext git libncurses5-dev libssl-dev python3 python3-pip python3-dev \
            rsync unzip zlib1g-dev file wget jq ccache libtool-bin automake \
            autoconf pkg-config subversion curl cmake ninja-build libelf-dev \
            libxml2-dev liblzma-dev liblzo2-dev axel net-tools e2fsprogs \
            genext2fs dosfstools mtools libffi-dev libsqlite3-dev libpam0g-dev \
            libtirpc-dev libev-dev lm-sensors libsnmp-dev libglib2.0-dev \
            libgpiod-dev libfl-dev upx-ucl
          
          python3 -m pip install --upgrade pip setuptools wheel -q
          
          echo "✅ 依赖安装完成" | tee -a "$MAIN_LOG"

      - name: "⚡ 性能极速优化"
        id: performance
        run: |
          set +e
          MAIN_LOG="${{ env.MAIN_LOG }}"
          echo "=== ⚡ 性能极速优化 ===" | tee -a "$MAIN_LOG"
          
          CPU_CORES=$(nproc)
          echo "🖥️ 检测到 CPU 核心: $CPU_CORES" | tee -a "$MAIN_LOG"
          
          OPTIMIZATION_STRATEGY="${{ github.event.inputs.build_optimization }}"
          case "$OPTIMIZATION_STRATEGY" in
            "speed")
              CCACHE_SIZE="8G"
              PARALLEL_JOBS=$((CPU_CORES + 2))
              echo "🚀 极速模式: CCache=${CCACHE_SIZE}, 并行作业=${PARALLEL_JOBS}" | tee -a "$MAIN_LOG"
              ;;
            "stability")
              CCACHE_SIZE="4G"
              PARALLEL_JOBS=$((CPU_CORES / 2))
              echo "🛡️ 稳定模式: CCache=${CCACHE_SIZE}, 并行作业=${PARALLEL_JOBS}" | tee -a "$MAIN_LOG"
              ;;
            *)
              CCACHE_SIZE="6G"
              PARALLEL_JOBS=$CPU_CORES
              echo "⚖️ 平衡模式: CCache=${CCACHE_SIZE}, 并行作业=${PARALLEL_JOBS}" | tee -a "$MAIN_LOG"
              ;;
          esac
          
          mkdir -p /mnt/ccache
          ccache -M $CCACHE_SIZE
          ccache -o compression=true
          ccache -o compression_level=6
          ccache -o max_files=100000
          
          echo "CCACHE_DIR=/mnt/ccache" >> $GITHUB_ENV
          echo "CCACHE_COMPRESS=1" >> $GITHUB_ENV
          echo "PARALLEL_JOBS=$PARALLEL_JOBS" >> $GITHUB_ENV
          
          # 创建交换文件
          echo "💾 创建交换文件..." | tee -a "$MAIN_LOG"
          sudo swapoff /mnt/swapfile 2>/dev/null || true
          sudo rm -f /mnt/swapfile 2>/dev/null || true
          sudo fallocate -l 8G /mnt/swapfile && sudo chmod 600 /mnt/swapfile && sudo mkswap /mnt/swapfile && sudo swapon /mnt/swapfile
          
          echo "📊 系统资源状态:" | tee -a "$MAIN_LOG"
          free -h | tee -a "$MAIN_LOG"
          echo "✅ 性能优化完成" | tee -a "$MAIN_LOG"

      - name: "⚡ 极速源码获取"
        id: source_setup
        run: |
          set +e
          MAIN_LOG="${{ env.MAIN_LOG }}"
          echo "=== ⚡ 极速源码获取 ===" | tee -a "$MAIN_LOG"
          
          REPO_JSON_FOUND="${{ env.CONFIG_REPO_DIR }}/firmware-config/repositories.json"
          PRESET="${{ github.event.inputs.source_preset }}"
          SOURCE_URL=$(jq -r ".repositories.$PRESET.url" "$REPO_JSON_FOUND")
          BRANCH="${{ github.event.inputs.source_branch }}"
          
          if [ "$BRANCH" = "auto" ]; then
            BRANCH=$(jq -r ".repositories.$PRESET.recommended_branch" "$REPO_JSON_FOUND")
          fi
          
          echo "📦 极速克隆源码..." | tee -a "$MAIN_LOG"
          cd /mnt/source
          rm -rf .[!.]* *
          
          git clone --depth 1 --branch "$BRANCH" --single-branch --progress "$SOURCE_URL" . || {
            echo "🔄 分支克隆失败，尝试master分支..." | tee -a "$MAIN_LOG"
            git clone --depth 1 --progress "$SOURCE_URL" .
            git checkout "$BRANCH" 2>/dev/null || true
          }
          
          ACTUAL_BRANCH=$(git branch --show-current)
          COMMIT_HASH=$(git rev-parse --short HEAD)
          
          echo "ACTUAL_BRANCH=$ACTUAL_BRANCH" >> $GITHUB_ENV
          echo "COMMIT_HASH=$COMMIT_HASH" >> $GITHUB_ENV
          echo "SOURCE_PRESET=$PRESET" >> $GITHUB_ENV
          echo "✅ 源码获取完成: $ACTUAL_BRANCH@$COMMIT_HASH" | tee -a "$MAIN_LOG"

      - name: "🔧 智能配置预处理"
        id: pre_config
        run: |
          set +e
          MAIN_LOG="${{ env.MAIN_LOG }}"
          echo "=== 🔧 智能配置预处理 ===" | tee -a "$MAIN_LOG"
          cd /mnt/source
          
          # 应用设备配置
          CONFIG_PROFILE="${{ github.event.inputs.config_profile }}"
          CONFIG_DIR="${{ env.CONFIG_REPO_DIR }}/firmware-config/configs"
          CONFIG_FILE="$CONFIG_DIR/config_$CONFIG_PROFILE"
          
          if [ -f "$CONFIG_FILE" ]; then
            cp "$CONFIG_FILE" .config
            echo "✅ 应用配置文件: $CONFIG_PROFILE" | tee -a "$MAIN_LOG"
          else
            # 根据源码分支自动生成兼容配置
            CURRENT_BRANCH="${{ env.ACTUAL_BRANCH }}"
            echo "🔧 自动生成兼容配置..." | tee -a "$MAIN_LOG"
            
            if [[ "$CURRENT_BRANCH" == *"21.02"* ]]; then
              echo "🔧 检测到 21.02 分支，生成兼容配置..." | tee -a "$MAIN_LOG"
              {
                echo "# 自动生成配置 - $CONFIG_PROFILE (21.02兼容)"
                echo "CONFIG_TARGET_x86=y"
                echo "CONFIG_TARGET_x86_64=y"
                echo "CONFIG_TARGET_x86_64_DEVICE_generic=y"
                echo "CONFIG_TARGET_ROOTFS_SQUASHFS=y"
                echo "CONFIG_TARGET_ROOTFS_EXT4FS=y"
                echo "CONFIG_TARGET_ROOTFS_TARGZ=y"
                echo "CONFIG_PACKAGE_luci=y"
                echo "CONFIG_PACKAGE_luci-base=y"
                echo "CONFIG_PACKAGE_luci-compat=y"
                echo "CONFIG_LUCI_LANG_zh-cn=y"
                echo "CONFIG_BUSYBOX_CUSTOM=y"
                echo "CONFIG_PACKAGE_busybox=y"
              } > .config
            elif [[ "$CURRENT_BRANCH" == *"22.03"* ]]; then
              echo "🔧 检测到 22.03 分支，生成兼容配置..." | tee -a "$MAIN_LOG"
              {
                echo "# 自动生成配置 - $CONFIG_PROFILE (22.03兼容)"
                echo "CONFIG_TARGET_x86=y"
                echo "CONFIG_TARGET_x86_64=y"
                echo "CONFIG_TARGET_x86_64_DEVICE_generic=y"
                echo "CONFIG_TARGET_ROOTFS_SQUASHFS=y"
                echo "CONFIG_TARGET_ROOTFS_EXT4FS=y"
                echo "CONFIG_TARGET_ROOTFS_TARGZ=y"
                echo "CONFIG_PACKAGE_luci=y"
                echo "CONFIG_PACKAGE_luci-base=y"
                echo "CONFIG_PACKAGE_luci-compat=y"
                echo "CONFIG_LUCI_LANG_zh_Hans=y"
                echo "CONFIG_BUSYBOX_CUSTOM=y"
                echo "CONFIG_PACKAGE_busybox=y"
              } > .config
            else
              echo "🔧 生成通用配置..." | tee -a "$MAIN_LOG"
              {
                echo "# 自动生成配置 - $CONFIG_PROFILE"
                echo "CONFIG_TARGET_x86=y"
                echo "CONFIG_TARGET_x86_64=y"
                echo "CONFIG_TARGET_x86_64_DEVICE_generic=y"
                echo "CONFIG_TARGET_ROOTFS_SQUASHFS=y"
                echo "CONFIG_TARGET_ROOTFS_EXT4FS=y"
                echo "CONFIG_TARGET_ROOTFS_TARGZ=y"
                echo "CONFIG_PACKAGE_luci=y"
                echo "CONFIG_PACKAGE_luci-base=y"
                echo "CONFIG_PACKAGE_luci-compat=y"
                echo "CONFIG_BUSYBOX_CUSTOM=y"
                echo "CONFIG_PACKAGE_busybox=y"
              } > .config
            fi
          fi
          
          # 预配置优化选项
          echo "⚡ 预配置优化选项..." | tee -a "$MAIN_LOG"
          {
            echo "# 编译优化"
            echo "CONFIG_CCACHE=y"
            echo "CONFIG_BUILD_LOG=y"
            echo "CONFIG_SDK=y"
            echo "# CONFIG_DEBUG=y is not set"
          } >> .config
          
          # 禁用已知问题包
          echo "🚫 禁用已知问题包..." | tee -a "$MAIN_LOG"
          for pkg in 6in4 6rd 6to4 luci-app-6in4 luci-app-6rd; do
            sed -i "s/CONFIG_PACKAGE_${pkg}=y/# CONFIG_PACKAGE_${pkg} is not set/" .config
          done
          
          echo "✅ 配置预处理完成" | tee -a "$MAIN_LOG"

      - name: "🔄 并行Feeds更新"
        id: feeds_setup
        run: |
          set +e
          MAIN_LOG="${{ env.MAIN_LOG }}"
          echo "=== 🔄 并行Feeds更新 ===" | tee -a "$MAIN_LOG"
          cd /mnt/source
          
          CURRENT_BRANCH=$(git branch --show-current)
          if [[ "$CURRENT_BRANCH" == *"22.03"* ]]; then FEEDS_BRANCH="openwrt-22.03"
          elif [[ "$CURRENT_BRANCH" == *"21.02"* ]]; then FEEDS_BRANCH="openwrt-21.02"
          else FEEDS_BRANCH="master"; fi
          
          case "${{ env.SOURCE_PRESET }}" in
            "immortalwrt")
              echo "src-git packages https://github.com/immortalwrt/packages.git;$FEEDS_BRANCH" > feeds.conf
              echo "src-git luci https://github.com/immortalwrt/luci.git;$FEEDS_BRANCH" >> feeds.conf
              ;;
            "openwrt")
              echo "src-git packages https://git.openwrt.org/feed/packages.git;$FEEDS_BRANCH" > feeds.conf
              echo "src-git luci https://git.openwrt.org/project/luci.git;$FEEDS_BRANCH" >> feeds.conf
              ;;
            "lede")
              echo "src-git packages https://github.com/coolsnowwolf/packages.git;$FEEDS_BRANCH" > feeds.conf
              echo "src-git luci https://github.com/coolsnowwolf/luci.git;$FEEDS_BRANCH" >> feeds.conf
              ;;
          esac
          
          echo "src-git routing https://github.com/openwrt/routing.git;$FEEDS_BRANCH" >> feeds.conf
          
          echo "🔄 并行更新feeds..." | tee -a "$MAIN_LOG"
          ./scripts/feeds update -a &
          FEEDS_PID=$!
          
          mkdir -p /mnt/downloads
          [ -d "dl" ] && rm -rf dl
          ln -sf /mnt/downloads dl
          
          wait $FEEDS_PID
          ./scripts/feeds install -a
          
          echo "✅ Feeds更新完成" | tee -a "$MAIN_LOG"

      - name: "🎯 完整插件配置"
        id: plugin_setup
        run: |
          set +e
          MAIN_LOG="${{ env.MAIN_LOG }}"
          echo "=== 🎯 完整插件配置 ===" | tee -a "$MAIN_LOG"
          cd /mnt/source
          
          PRESET="${{ github.event.inputs.plugin_preset }}"
          echo "🎯 插件预设: $PRESET" | tee -a "$MAIN_LOG"
          
          # 确保配置目录存在
          mkdir -p package/custom feeds/luci/applications
          
          enable_plugin() {
            local plugin=$1
            local config_name="CONFIG_PACKAGE_${plugin}"
            
            if grep -q "${config_name}=y" .config; then
              echo "✅ 已启用: $plugin" | tee -a "$MAIN_LOG"
              return 0
            fi
            
            if grep -q "${config_name} is not set" .config; then
              sed -i "s/# ${config_name} is not set/${config_name}=y/" .config
              echo "✅ 启用: $plugin" | tee -a "$MAIN_LOG"
            else
              echo "${config_name}=y" >> .config
              echo "✅ 新增: $plugin" | tee -a "$MAIN_LOG"
            fi
          }
          
          case "$PRESET" in
            "minimal")
              echo "🔧 最小化配置..." | tee -a "$MAIN_LOG"
              enable_plugin "luci-app-upnp"
              ;;
            "standard")
              echo "🔧 标准配置..." | tee -a "$MAIN_LOG"
              # 网络核心插件
              enable_plugin "luci-app-upnp"
              enable_plugin "luci-app-wireguard"
              enable_plugin "luci-app-samba4"
              enable_plugin "luci-app-turboacc"
              
              # 网络优化插件
              enable_plugin "luci-app-arpbind"
              enable_plugin "luci-app-cpulimit"
              enable_plugin "luci-app-eqosplus"
              
              # 系统工具插件
              enable_plugin "luci-app-diskman"
              enable_plugin "luci-app-hd-idle"
              enable_plugin "luci-app-parentcontrol"
              enable_plugin "luci-app-wechatpush"
              
              # DNS和网络服务
              enable_plugin "luci-app-smartdns"
              enable_plugin "luci-app-vlmcsd"
              enable_plugin "luci-app-vsftpd"
              enable_plugin "luci-app-ttyd"
              enable_plugin "luci-app-sqm-autorate"
              enable_plugin "automount"
              ;;
            "full")
              echo "🔧 完整配置..." | tee -a "$MAIN_LOG"
              for plugin in luci-app-upnp luci-app-wireguard luci-app-samba4 luci-app-turboacc \
                           luci-app-arpbind luci-app-cpulimit luci-app-diskman luci-app-eqosplus \
                           luci-app-hd-idle luci-app-parentcontrol luci-app-wechatpush \
                           luci-app-smartdns luci-app-vlmcsd luci-app-vsftpd luci-app-ttyd \
                           luci-app-sqm-autorate luci-app-adblock luci-app-wol luci-app-ddns \
                           luci-app-qos luci-app-statistics luci-app-vpn-policy-routing \
                           luci-app-nft-qos luci-app-uhttpd luci-app-firewall luci-app-olsr \
                           luci-app-olsr-services luci-app-olsr-viz luci-app-bcp38 \
                           luci-app-commands luci-app-diag-core luci-app-nlbwmon \
                           luci-app-splash luci-app-radicale2; do
                enable_plugin "$plugin"
              done
              ;;
            "custom")
              echo "🔧 自定义配置..." | tee -a "$MAIN_LOG"
              CUSTOM_PLUGINS_FILE="${{ env.CONFIG_REPO_DIR }}/firmware-config/custom-plugins.txt"
              if [ -f "$CUSTOM_PLUGINS_FILE" ]; then
                echo "📋 读取自定义插件列表..." | tee -a "$MAIN_LOG"
                while IFS= read -r plugin; do
                  if [ -n "$plugin" ] && [[ ! "$plugin" =~ ^# ]]; then
                    enable_plugin "$plugin"
                  fi
                done < "$CUSTOM_PLUGINS_FILE"
              else
                echo "ℹ️ 未找到自定义插件列表，使用标准配置" | tee -a "$MAIN_LOG"
                # 回退到标准配置
                for plugin in luci-app-upnp luci-app-wireguard luci-app-samba4 luci-app-turboacc; do
                  enable_plugin "$plugin"
                done
              fi
              ;;
          esac
          
          echo "✅ 插件配置完成" | tee -a "$MAIN_LOG"

      - name: "🌐 语言包配置"
        id: language_setup
        run: |
          set +e
          MAIN_LOG="${{ env.MAIN_LOG }}"
          echo "=== 🌐 语言包配置 ===" | tee -a "$MAIN_LOG"
          cd /mnt/source
          
          LANGUAGE="${{ github.event.inputs.language_pack }}"
          echo "🎯 语言包选择: $LANGUAGE" | tee -a "$MAIN_LOG"
          
          CURRENT_BRANCH="${{ env.ACTUAL_BRANCH }}"
          
          case "$LANGUAGE" in
            "zh-cn")
              echo "🔧 启用中文语言包..." | tee -a "$MAIN_LOG"
              if [[ "$CURRENT_BRANCH" == *"21.02"* ]]; then
                sed -i 's/# CONFIG_PACKAGE_luci-i18n-base-zh-cn is not set/CONFIG_PACKAGE_luci-i18n-base-zh-cn=y/' .config 2>/dev/null || echo "CONFIG_PACKAGE_luci-i18n-base-zh-cn=y" >> .config
              else
                sed -i 's/# CONFIG_PACKAGE_luci-i18n-base-zh_Hans is not set/CONFIG_PACKAGE_luci-i18n-base-zh_Hans=y/' .config 2>/dev/null || echo "CONFIG_PACKAGE_luci-i18n-base-zh_Hans=y" >> .config
              fi
              ;;
            "en-only")
              echo "🔧 仅保留英文..." | tee -a "$MAIN_LOG"
              sed -i 's/CONFIG_PACKAGE_luci-i18n-[^e][^n].*=y/# &/' .config
              ;;
            "all")
              echo "🔧 启用所有语言包..." | tee -a "$MAIN_LOG"
              for lang in $(grep "CONFIG_PACKAGE_luci-i18n-" .config | grep "is not set" | cut -d' ' -f2); do
                sed -i "s/# $lang is not set/$lang=y/" .config
              done
              ;;
          esac
          
          echo "✅ 语言包配置完成" | tee -a "$MAIN_LOG"

      - name: "🔧 高级功能增强"
        id: advanced_features
        if: github.event.inputs.enable_advanced == 'true'
        run: |
          set +e
          MAIN_LOG="${{ env.MAIN_LOG }}"
          WARNING_LOG="${{ env.WARNING_LOG }}"
          echo "=== 🔧 高级功能增强 ===" | tee -a "$MAIN_LOG"
          cd /mnt/source
          
          echo "🔧 高级功能状态: 已启用" | tee -a "$MAIN_LOG"
          
          # 自定义IPK包处理
          echo "📦 安装自定义IPK包..." | tee -a "$MAIN_LOG"
          IPK_DIR="${{ env.CONFIG_REPO_DIR }}/firmware-config/custom-features/prebuilt-ipks"
          IPK_COUNT=0
          
          if [ -d "$IPK_DIR" ]; then
            echo "🔍 查找自定义IPK包..." | tee -a "$MAIN_LOG"
            IPK_FILES=$(find "$IPK_DIR" -name "*.ipk" -type f 2>/dev/null || true)
            if [ -n "$IPK_FILES" ]; then
              echo "✅ 找到自定义IPK包:" | tee -a "$MAIN_LOG"
              mkdir -p package/custom
              for ipk in $IPK_FILES; do
                ipk_name=$(basename "$ipk")
                echo "  - 复制 $ipk_name" | tee -a "$MAIN_LOG"
                cp "$ipk" package/custom/
                IPK_COUNT=$((IPK_COUNT + 1))
                
                # 确保IPK包被包含在构建中
                ipk_basename=$(basename "$ipk" .ipk)
                echo "CONFIG_PACKAGE_${ipk_basename}=y" >> .config
              done
              echo "✅ 成功添加 $IPK_COUNT 个自定义IPK包" | tee -a "$MAIN_LOG"
            else
              echo "ℹ️ 未找到自定义IPK包" | tee -a "$MAIN_LOG"
            fi
          else
            echo "ℹ️ 自定义IPK目录不存在: $IPK_DIR" | tee -a "$MAIN_LOG"
          fi
          
          echo "CUSTOM_IPK_COUNT=$IPK_COUNT" >> $GITHUB_ENV
          
          # 自定义脚本执行
          echo "🔧 执行自定义脚本..." | tee -a "$MAIN_LOG"
          SCRIPT_PATHS=(
            "${{ env.CONFIG_REPO_DIR }}/firmware-config/custom-features/scripts"
            "${{ env.CONFIG_REPO_DIR }}/firmware-config/scripts" 
            "${{ env.CONFIG_REPO_DIR }}/scripts"
            "${{ env.CONFIG_REPO_DIR }}/firmware-config"
          )
          
          SCRIPT_COUNT=0
          for script_path in "${SCRIPT_PATHS[@]}"; do
            if [ -d "$script_path" ]; then
              echo "🔍 在 $script_path 中查找脚本..." | tee -a "$MAIN_LOG"
              SCRIPTS=$(find "$script_path" -maxdepth 2 -name "*.sh" -type f 2>/dev/null | sort || true)
              if [ -n "$SCRIPTS" ]; then
                for script in $SCRIPTS; do
                  script_name=$(basename "$script")
                  echo "📜 执行脚本: $script_name..." | tee -a "$MAIN_LOG"
                  chmod +x "$script"
                  
                  TEMP_LOG=$(mktemp)
                  if { bash "$script" 2>&1 | tee "$TEMP_LOG"; } then
                    echo "✅ 自定义脚本执行成功: $script_name" | tee -a "$MAIN_LOG"
                    SCRIPT_COUNT=$((SCRIPT_COUNT + 1))
                  else
                    echo "⚠️ 自定义脚本执行有警告: $script_name" | tee -a "$MAIN_LOG"
                    grep -i -E "error|failed|No such file" "$TEMP_LOG" >> "$WARNING_LOG" || true
                  fi
                  cat "$TEMP_LOG" >> "$MAIN_LOG"
                  rm -f "$TEMP_LOG"
                done
              else
                echo "ℹ️ 在 $script_path 中未找到脚本" | tee -a "$MAIN_LOG"
              fi
            else
              echo "ℹ️ 脚本目录不存在: $script_path" | tee -a "$MAIN_LOG"
            fi
          done
          
          echo "CUSTOM_SCRIPT_COUNT=$SCRIPT_COUNT" >> $GITHUB_ENV
          echo "✅ 高级功能应用完成，执行了 $SCRIPT_COUNT 个脚本" | tee -a "$MAIN_LOG"

      - name: "🔄 最终配置处理"
        id: final_config
        run: |
          set +e
          MAIN_LOG="${{ env.MAIN_LOG }}"
          echo "=== 🔄 最终配置处理 ===" | tee -a "$MAIN_LOG"
          cd /mnt/source
          
          echo "🔧 运行defconfig..." | tee -a "$MAIN_LOG"
          make defconfig
          
          echo "📋 最终配置检查..." | tee -a "$MAIN_LOG"
          echo "启用的LUCI应用:" | tee -a "$MAIN_LOG"
          grep "CONFIG_PACKAGE_luci-app" .config | grep "=y" | tee -a "$MAIN_LOG"
          
          echo "✅ 最终配置完成" | tee -a "$MAIN_LOG"

      - name: "🔧 配置验证和修复"
        id: config_validation
        run: |
          set +e
          MAIN_LOG="${{ env.MAIN_LOG }}"
          echo "=== 🔧 配置验证和修复 ===" | tee -a "$MAIN_LOG"
          cd /mnt/source
          
          # 确保基础配置正确
          echo "🔧 验证基础配置..." | tee -a "$MAIN_LOG"
          
          # 检查目标架构
          if ! grep -q "CONFIG_TARGET_x86_64=y" .config; then
            echo "⚠️ 缺少x86_64目标，添加基础配置..." | tee -a "$MAIN_LOG"
            cat >> .config << 'EOF'
CONFIG_TARGET_x86=y
CONFIG_TARGET_x86_64=y
CONFIG_TARGET_x86_64_DEVICE_generic=y
CONFIG_TARGET_ROOTFS_SQUASHFS=y
CONFIG_TARGET_ROOTFS_EXT4FS=y
CONFIG_TARGET_ROOTFS_TARGZ=y
CONFIG_PACKAGE_luci=y
CONFIG_PACKAGE_luci-base=y
CONFIG_PACKAGE_luci-compat=y
CONFIG_BUSYBOX_CUSTOM=y
CONFIG_PACKAGE_busybox=y
EOF
          fi
          
          # 运行defconfig修复配置
          echo "🔄 运行defconfig修复配置..." | tee -a "$MAIN_LOG"
          if ! make defconfig; then
            echo "❌ defconfig失败，尝试手动修复..." | tee -a "$MAIN_LOG"
            # 备份配置
            cp .config .config.backup
            # 清理可能的问题配置
            sed -i '/^CONFIG_PACKAGE_kmod-/d' .config
            sed -i '/^CONFIG_PACKAGE_luci-app-/d' .config
            make defconfig
          fi
          
          echo "✅ 配置验证完成" | tee -a "$MAIN_LOG"

      - name: "🚀 并行下载优化"
        id: download_deps
        run: |
          set +e
          MAIN_LOG="${{ env.MAIN_LOG }}"
          echo "=== 🚀 并行下载优化 ===" | tee -a "$MAIN_LOG"
          cd /mnt/source
          
          # 修复下载源
          sed -i 's|https://sources.cdn.openwrt.org|https://mirror2.openwrt.org/sources|g' scripts/download.pl
          sed -i 's|git.openwrt.org|github.com/openwrt|g' feeds.conf.* 2>/dev/null || true
          
          echo "📥 开始并行下载..." | tee -a "$MAIN_LOG"
          PARALLEL_JOBS="${{ env.PARALLEL_JOBS }}"
          
          # 多次重试下载
          for i in 1 2 3; do
            echo "🔄 下载尝试 $i/3..." | tee -a "$MAIN_LOG"
            if make download -j$PARALLEL_JOBS; then
              echo "✅ 下载成功" | tee -a "$MAIN_LOG"
              break
            else
              echo "⚠️ 下载尝试 $i 失败" | tee -a "$MAIN_LOG"
              if [ $i -eq 3 ]; then
                echo "❌ 所有下载尝试都失败" | tee -a "$MAIN_LOG"
                exit 1
              fi
              sleep 10
            fi
          done
          
          # 检查下载文件
          DOWNLOAD_COUNT=$(find dl/ -type f 2>/dev/null | wc -l)
          echo "📦 下载文件数量: $DOWNLOAD_COUNT" | tee -a "$MAIN_LOG"
          
          if [ "$DOWNLOAD_COUNT" -lt 10 ]; then
            echo "⚠️ 下载文件数量过少，可能有问题" | tee -a "$MAIN_LOG"
          fi

      - name: "⚡ 智能增量编译"
        id: smart_build
        timeout-minutes: 150
        run: |
          set +e
          MAIN_LOG="${{ env.MAIN_LOG }}"
          ERROR_LOG="${{ env.ERROR_LOG }}"
          WARNING_LOG="${{ env.WARNING_LOG }}"
          echo "=== ⚡ 智能增量编译 ===" | tee -a "$MAIN_LOG"
          cd /mnt/source
          
          export FORCE_UNSAFE_CONFIGURE=1
          export LC_ALL=C
          PARALLEL_JOBS="${{ env.PARALLEL_JOBS }}"
          SKIP_TESTS="${{ github.event.inputs.skip_tests }}"
          
          echo "🔧 编译配置: 并行作业=$PARALLEL_JOBS, 跳过测试=$SKIP_TESTS" | tee -a "$MAIN_LOG"
          
          # 首先检查配置
          echo "🔍 检查配置完整性..." | tee -a "$MAIN_LOG"
          if ! make defconfig; then
            echo "❌ 配置检查失败" | tee -a "$MAIN_LOG"
            exit 1
          fi
          
          if [ "$SKIP_TESTS" = "true" ]; then
            EXTRA_FLAGS="IGNORE_ERRORS=m"
          else
            EXTRA_FLAGS=""
          fi
          
          echo "🛠️ 阶段1: 编译工具链..." | tee -a "$MAIN_LOG"
          if ! make toolchain/compile -j$PARALLEL_JOBS $EXTRA_FLAGS 2>&1 | tee -a "$MAIN_LOG"; then
            echo "❌ 工具链编译失败" | tee -a "$ERROR_LOG"
            # 继续尝试，因为有些错误可能不影响整体构建
          fi
          
          echo "🛠️ 阶段2: 编译内核..." | tee -a "$MAIN_LOG"
          if ! make target/linux/compile -j$PARALLEL_JOBS $EXTRA_FLAGS 2>&1 | tee -a "$MAIN_LOG"; then
            echo "❌ 内核编译失败" | tee -a "$ERROR_LOG"
            # 继续尝试，因为有些错误可能不影响整体构建
          fi
          
          # 完整编译使用更严格的设置
          echo "🚀 阶段3: 完整编译..." | tee -a "$MAIN_LOG"
          if make -j$PARALLEL_JOBS $EXTRA_FLAGS 2>&1 | tee -a "$MAIN_LOG"; then
            echo "✅ 编译成功" | tee -a "$MAIN_LOG"
            echo "BUILD_STATUS=success" >> $GITHUB_ENV
          else
            echo "❌ 编译失败，查看详细错误..." | tee -a "$MAIN_LOG"
            # 尝试单线程编译以获取准确错误
            echo "🔄 尝试单线程编译定位问题..." | tee -a "$MAIN_LOG"
            make -j1 V=s 2>&1 | tee -a "$ERROR_LOG" || true
            echo "BUILD_STATUS=failed" >> $GITHUB_ENV
          fi
          
          # 检查产物
          if find bin/targets -type f \( -name "*.bin" -o -name "*.img" -o -name "*.trx" \) 2>/dev/null | grep -q .; then
            echo "🎉 找到固件文件！" | tee -a "$MAIN_LOG"
            echo "BUILD_STATUS=success" >> $GITHUB_ENV
            find bin/targets -type f \( -name "*.bin" -o -name "*.img" -o -name "*.trx" \) -exec ls -la {} \; | tee -a "$MAIN_LOG"
          else
            echo "❌ 未找到固件文件" | tee -a "$MAIN_LOG"
            echo "BUILD_STATUS=failed" >> $GITHUB_ENV
            # 检查可能的构建目录
            echo "🔍 检查构建目录..." | tee -a "$MAIN_LOG"
            find bin -type f -name "*" 2>/dev/null | head -20 | tee -a "$MAIN_LOG"
          fi
          
          echo "📊 CCache统计:" | tee -a "$MAIN_LOG"
          ccache -s | tee -a "$MAIN_LOG"

      - name: "🔍 快速固件分析"
        id: firmware_analysis
        if: always()
        run: |
          set +e
          MAIN_LOG="${{ env.MAIN_LOG }}"
          echo "=== 🔍 快速固件分析 ===" | tee -a "$MAIN_LOG"
          cd /mnt/source
          
          FIRMWARE_COUNT=0
          TOTAL_SIZE=0
          
          if [ -d "bin/targets" ]; then
            while IFS= read -r file; do
              if [ -f "$file" ]; then
                FIRMWARE_COUNT=$((FIRMWARE_COUNT + 1))
                size=$(du -b "$file" 2>/dev/null | cut -f1)
                TOTAL_SIZE=$((TOTAL_SIZE + size))
                echo "📦 固件: $(basename "$file") ($(du -h "$file" | cut -f1))" | tee -a "$MAIN_LOG"
              fi
            done < <(find bin/targets -type f \( -name "*.bin" -o -name "*.img" -o -name "*.trx" \) 2>/dev/null)
          fi
          
          TOTAL_SIZE_MB=$((TOTAL_SIZE / 1024 / 1024))
          echo "FIRMWARE_COUNT=$FIRMWARE_COUNT" >> $GITHUB_ENV
          echo "FIRMWARE_SIZE_MB=$TOTAL_SIZE_MB" >> $GITHUB_ENV
          
          echo "📊 分析结果: $FIRMWARE_COUNT 个固件, ${TOTAL_SIZE_MB}MB" | tee -a "$MAIN_LOG"

      - name: "📦 极速产物收集"
        id: collect_artifacts
        if: always()
        run: |
          set +e
          MAIN_LOG="${{ env.MAIN_LOG }}"
          echo "=== 📦 极速产物收集 ===" | tee -a "$MAIN_LOG"
          
          mkdir -p /mnt/artifacts/firmware
          ARTIFACT_COUNT=0
          
          if [ -d "/mnt/source/bin/targets" ]; then
            find /mnt/source/bin/targets -type f \( -name "*.bin" -o -name "*.img" -o -name "*.trx" \) -exec cp {} /mnt/artifacts/firmware/ \;
            ARTIFACT_COUNT=$(ls /mnt/artifacts/firmware/ | wc -l)
          fi
          
          cp "$MAIN_LOG" /mnt/artifacts/ 2>/dev/null || true
          cp "$ERROR_LOG" /mnt/artifacts/ 2>/dev/null || true
          cp "$WARNING_LOG" /mnt/artifacts/ 2>/dev/null || true
          cp "$ANALYSIS_REPORT" /mnt/artifacts/ 2>/dev/null || true
          
          echo "ARTIFACT_COUNT=$ARTIFACT_COUNT" >> $GITHUB_ENV
          echo "✅ 收集了 $ARTIFACT_COUNT 个产物" | tee -a "$MAIN_LOG"

      - name: "💾 上传构建产物"
        id: upload_artifacts
        if: always()
        uses: "actions/upload-artifact@v4"
        with:
          name: "Build-Artifacts-${{ github.run_number }}"
          path: |
            /mnt/artifacts
          retention-days: 7
          compression-level: 6

      - name: "📊 极速构建报告"
        id: final_report
        if: always()
        run: |
          echo "=== 📊 极速构建报告 ==="
          echo ""
          echo "🎯 构建摘要"
          echo "=========================================="
          echo "⚡ 编译性能:"
          echo "  - 并行作业: ${{ env.PARALLEL_JOBS || 'N/A' }}"
          echo "  - CCache命中率: 查看详细日志"
          echo ""
          echo "📦 构建结果:"
          echo "  - 状态: ${{ env.BUILD_STATUS || 'unknown' }}"
          echo "  - 固件数量: ${{ env.FIRMWARE_COUNT || 0 }}"
          echo "  - 固件大小: ${{ env.FIRMWARE_SIZE_MB || 'unknown' }}MB"
          echo "  - 产物数量: ${{ env.ARTIFACT_COUNT || 0 }}"
          echo ""
          echo "🔧 高级功能:"
          echo "  - 自定义IPK: ${{ env.CUSTOM_IPK_COUNT || 0 }} 个"
          echo "  - 自定义脚本: ${{ env.CUSTOM_SCRIPT_COUNT || 0 }} 个"
          echo ""
          echo "⏱️ 构建时间:"
          echo "  - 开始: 见构建日志"
          echo "  - 结束: $(date)"
          echo ""
          if [ "${{ env.BUILD_STATUS }}" = "success" ] && [ "${{ env.FIRMWARE_COUNT }}" -gt 0 ]; then
            echo "🎉 极速构建成功！"
            echo "💡 固件已生成并上传到产物"
          else
            echo "❌ 构建失败或未生成固件"
            echo "🔧 请查看详细日志了解问题"
          fi
          echo "=========================================="
