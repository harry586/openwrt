name: "🚀 超快速固件构建器 - 修复版"

on:
  workflow_dispatch:
    inputs:
      source_preset:
        description: "选择源码库"
        required: true
        type: choice
        options: 
          - "immortalwrt"
          - "openwrt"
          - "lede"
        default: "immortalwrt"
      source_branch:
        description: "源码分支"
        required: true
        default: "auto"
        type: choice
        options: 
          - "auto"
          - "openwrt-22.03"
          - "openwrt-21.02"
          - "master"
          - "main"
      config_profile:
        description: "设备配置文件名称"
        required: true
        type: string
        default: "ac42u"
      build_mode:
        description: "构建模式"
        required: true
        type: choice
        options: 
          - "最小化"
          - "标准"
          - "自定义"
        default: "标准"
      build_optimization:
        description: "编译优化策略"
        required: true
        type: choice
        options: 
          - "均衡"
          - "速度优先"
          - "稳定优先"
        default: "均衡"
      enable_advanced:
        description: "启用高级功能"
        required: false
        default: true
        type: boolean
      language_pack:
        description: "语言包选择"
        required: true
        type: choice
        options: 
          - "简体中文"
          - "仅英文"
          - "全部语言"
        default: "简体中文"
      auto_commit_config:
        description: "自动提交新配置文件"
        required: false
        default: true
        type: boolean
      extra_packages:
        description: "额外软件包（逗号分隔）"
        required: false
        type: string
        default: ""

env:
  TZ: "Asia/Shanghai"
  SOURCE_DIR: "/mnt/source"
  ARTIFACTS_DIR: "/mnt/artifacts"
  CCACHE_DIR: "/mnt/ccache"
  BUILD_LOG_DIR: "/mnt/build-logs"
  DOWNLOAD_DIR: "/mnt/downloads"

jobs:
  build:
    runs-on: "ubuntu-22.04"
    timeout-minutes: 180
    
    steps:
      - name: "📥 检出配置仓库"
        uses: "actions/checkout@v4"
        with:
          path: "config-repo"
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: "🔧 极速环境初始化"
        id: init
        run: |
          echo "=== 🔧 极速环境初始化 ==="
          sudo mkdir -p /mnt/source /mnt/artifacts /mnt/ccache /mnt/build-logs /mnt/downloads
          sudo chown -R $USER:$USER /mnt/source /mnt/artifacts /mnt/ccache /mnt/build-logs /mnt/downloads
          sudo chmod -R 755 /mnt/source /mnt/artifacts /mnt/ccache /mnt/build-logs /mnt/downloads
          
          MAIN_LOG="/mnt/build-logs/build_timeline.log"
          ERROR_LOG="/mnt/build-logs/errors.log"
          WARNING_LOG="/mnt/build-logs/warnings.log"
          ANALYSIS_REPORT="/mnt/build-logs/analysis_report.md"
          DOWNLOAD_LOG="/mnt/build-logs/download_audit.log"
          
          echo "🏁 极速构建开始: $(date)" > $MAIN_LOG
          echo "🚀 工作流版本: 超快速固件构建器 - 修复版" >> $MAIN_LOG
          echo "📋 输入参数:" >> $MAIN_LOG
          echo "  - 源码预设: ${{ github.event.inputs.source_preset }}" >> $MAIN_LOG
          echo "  - 源码分支: ${{ github.event.inputs.source_branch }}" >> $MAIN_LOG
          echo "  - 配置方案: ${{ github.event.inputs.config_profile }}" >> $MAIN_LOG
          echo "  - 构建模式: ${{ github.event.inputs.build_mode }}" >> $MAIN_LOG
          echo "  - 优化策略: ${{ github.event.inputs.build_optimization }}" >> $MAIN_LOG
          echo "  - 高级功能: ${{ github.event.inputs.enable_advanced }}" >> $MAIN_LOG
          echo "  - 语言包: ${{ github.event.inputs.language_pack }}" >> $MAIN_LOG
          echo "  - 自动提交配置: ${{ github.event.inputs.auto_commit_config }}" >> $MAIN_LOG
          echo "  - 额外软件包: ${{ github.event.inputs.extra_packages }}" >> $MAIN_LOG
          echo "==========================================" >> $MAIN_LOG
          
          echo "# 下载审计日志" > $DOWNLOAD_LOG
          echo "开始时间: $(date)" >> $DOWNLOAD_LOG
          
          echo "MAIN_LOG=$MAIN_LOG" >> $GITHUB_ENV
          echo "ERROR_LOG=$ERROR_LOG" >> $GITHUB_ENV
          echo "WARNING_LOG=$WARNING_LOG" >> $GITHUB_ENV
          echo "ANALYSIS_REPORT=$ANALYSIS_REPORT" >> $GITHUB_ENV
          echo "DOWNLOAD_LOG=$DOWNLOAD_LOG" >> $GITHUB_ENV
          echo "CONFIG_REPO_DIR=${{ github.workspace }}/config-repo" >> $GITHUB_ENV
          echo "BUILD_STATUS=running" >> $GITHUB_ENV
          
          echo "# 错误日志" > $ERROR_LOG
          echo "# 警告日志" > $WARNING_LOG
          echo "# 智能分析报告" > $ANALYSIS_REPORT
          echo "✅ 极速环境初始化完成"

      - name: "🔍 检查配置文件和插件"
        id: check_config_files
        run: |
          set +e
          MAIN_LOG="${{ env.MAIN_LOG }}"
          echo "=== 🔍 检查配置文件和插件 ===" | tee -a "$MAIN_LOG"
          
          CONFIG_REPO="${{ env.CONFIG_REPO_DIR }}"
          echo "🔍 检查配置仓库目录: $CONFIG_REPO" | tee -a "$MAIN_LOG"
          
          # 列出所有可能的相关文件
          echo "📁 目录结构:" | tee -a "$MAIN_LOG"
          find "$CONFIG_REPO" -type f -name "*.txt" -o -name "*.conf" 2>/dev/null | head -20 | tee -a "$MAIN_LOG"
          
          # 检查特定的插件文件
          PLUGIN_FILES=("$CONFIG_REPO/firmware-config/custom-plugins.txt" "$CONFIG_REPO/firmware-config/plugins.txt" "$CONFIG_REPO/custom-plugins.txt" "$CONFIG_REPO/plugins.txt")
          
          for file in "${PLUGIN_FILES[@]}"; do
            if [ -f "$file" ]; then
              echo "✅ 找到插件文件: $file" | tee -a "$MAIN_LOG"
              echo "📄 文件内容预览:" | tee -a "$MAIN_LOG"
              head -10 "$file" | tee -a "$MAIN_LOG"
              echo "---" | tee -a "$MAIN_LOG"
            else
              echo "❌ 未找到插件文件: $file" | tee -a "$MAIN_LOG"
            fi
          done
          
          # 检查插件目录
          PLUGIN_DIRS=("$CONFIG_REPO/firmware-config/plugins" "$CONFIG_REPO/firmware-config/custom-plugins" "$CONFIG_REPO/plugins")
          
          for dir in "${PLUGIN_DIRS[@]}"; do
            if [ -d "$dir" ]; then
              echo "✅ 找到插件目录: $dir" | tee -a "$MAIN_LOG"
              find "$dir" -type f -name "*.txt" 2>/dev/null | while read -r file; do
                echo "  📄 $(basename "$file")" | tee -a "$MAIN_LOG"
              done
            else
              echo "❌ 未找到插件目录: $dir" | tee -a "$MAIN_LOG"
            fi
          done
          
          echo "✅ 文件检查完成" | tee -a "$MAIN_LOG"

      - name: "🚀 极速依赖安装"
        id: install_deps
        run: |
          set +e
          MAIN_LOG="${{ env.MAIN_LOG }}"
          echo "=== 🚀 极速依赖安装 ===" | tee -a "$MAIN_LOG"
          
          echo "📦 批量安装编译依赖..." | tee -a "$MAIN_LOG"
          sudo apt-get update -qq
          
          sudo apt-get install -y -qq build-essential clang flex bison g++ gawk gcc-multilib g++-multilib gettext git libncurses5-dev libssl-dev python3 python3-pip python3-dev rsync unzip zlib1g-dev file wget jq ccache libtool-bin automake autoconf pkg-config subversion curl cmake ninja-build libelf-dev libxml2-dev liblzma-dev liblzo2-dev axel net-tools e2fsprogs genext2fs dosfstools mtools libffi-dev libsqlite3-dev libpam0g-dev libtirpc-dev libev-dev lm-sensors libsnmp-dev libglib2.0-dev libgpiod-dev libfl-dev upx-ucl aria2 ncurses-term
          
          python3 -m pip install --upgrade pip setuptools wheel -q
          
          echo "✅ 依赖安装完成" | tee -a "$MAIN_LOG"

      - name: "⚡ 性能极速优化"
        id: performance
        run: |
          set +e
          MAIN_LOG="${{ env.MAIN_LOG }}"
          echo "=== ⚡ 性能极速优化 ===" | tee -a "$MAIN_LOG"
          
          CPU_CORES=$(nproc)
          echo "🖥️ 检测到 CPU 核心: $CPU_CORES" | tee -a "$MAIN_LOG"
          
          # 将中文选项映射为内部值
          OPTIMIZATION_STRATEGY="${{ github.event.inputs.build_optimization }}"
          case "$OPTIMIZATION_STRATEGY" in
            "速度优先")
              CCACHE_SIZE="8G"
              PARALLEL_JOBS=$((CPU_CORES + 2))
              echo "🚀 极速模式: CCache=${CCACHE_SIZE}, 并行作业=${PARALLEL_JOBS}" | tee -a "$MAIN_LOG"
              ;;
            "稳定优先")
              CCACHE_SIZE="4G"
              PARALLEL_JOBS=$((CPU_CORES / 2))
              echo "🛡️ 稳定模式: CCache=${CCACHE_SIZE}, 并行作业=${PARALLEL_JOBS}" | tee -a "$MAIN_LOG"
              ;;
            *)
              CCACHE_SIZE="6G"
              PARALLEL_JOBS=$CPU_CORES
              echo "⚖️ 平衡模式: CCache=${CCACHE_SIZE}, 并行作业=${PARALLEL_JOBS}" | tee -a "$MAIN_LOG"
              ;;
          esac
          
          mkdir -p /mnt/ccache
          ccache -M $CCACHE_SIZE
          ccache -o compression=true
          ccache -o compression_level=6
          ccache -o max_files=100000
          
          echo "CCACHE_DIR=/mnt/ccache" >> $GITHUB_ENV
          echo "CCACHE_COMPRESS=1" >> $GITHUB_ENV
          echo "PARALLEL_JOBS=$PARALLEL_JOBS" >> $GITHUB_ENV
          
          echo "💾 创建交换文件..." | tee -a "$MAIN_LOG"
          sudo swapoff /mnt/swapfile 2>/dev/null || true
          sudo rm -f /mnt/swapfile 2>/dev/null || true
          sudo fallocate -l 8G /mnt/swapfile && sudo chmod 600 /mnt/swapfile && sudo mkswap /mnt/swapfile && sudo swapon /mnt/swapfile
          
          echo "📊 系统资源状态:" | tee -a "$MAIN_LOG"
          free -h | tee -a "$MAIN_LOG"
          echo "✅ 性能优化完成" | tee -a "$MAIN_LOG"

      - name: "⚡ 极速源码获取"
        id: source_setup
        run: |
          set +e
          MAIN_LOG="${{ env.MAIN_LOG }}"
          echo "=== ⚡ 极速源码获取 ===" | tee -a "$MAIN_LOG"
          
          REPO_JSON_FOUND="${{ env.CONFIG_REPO_DIR }}/firmware-config/repositories.json"
          PRESET="${{ github.event.inputs.source_preset }}"
          SOURCE_URL=$(jq -r ".repositories.$PRESET.url" "$REPO_JSON_FOUND")
          BRANCH="${{ github.event.inputs.source_branch }}"
          
          if [ "$BRANCH" = "auto" ]; then
            BRANCH=$(jq -r ".repositories.$PRESET.recommended_branch" "$REPO_JSON_FOUND")
          fi
          
          echo "📦 极速克隆源码..." | tee -a "$MAIN_LOG"
          cd /mnt/source
          rm -rf .[!.]* *
          
          git clone --depth 1 --branch "$BRANCH" --single-branch --progress "$SOURCE_URL" . || {
            echo "🔄 分支克隆失败，尝试master分支..." | tee -a "$MAIN_LOG"
            git clone --depth 1 --progress "$SOURCE_URL" .
            git checkout "$BRANCH" 2>/dev/null || true
          }
          
          ACTUAL_BRANCH=$(git branch --show-current)
          COMMIT_HASH=$(git rev-parse --short HEAD)
          
          echo "ACTUAL_BRANCH=$ACTUAL_BRANCH" >> $GITHUB_ENV
          echo "COMMIT_HASH=$COMMIT_HASH" >> $GITHUB_ENV
          echo "SOURCE_PRESET=$PRESET" >> $GITHUB_ENV
          echo "✅ 源码获取完成: $ACTUAL_BRANCH@$COMMIT_HASH" | tee -a "$MAIN_LOG"

      - name: "🔧 修复终端环境和构建系统"
        id: fix_terminal_env
        run: |
          set +e
          MAIN_LOG="${{ env.MAIN_LOG }}"
          echo "=== 🔧 修复终端环境和构建系统 ===" | tee -a "$MAIN_LOG"
          cd /mnt/source
          
          echo "🔧 设置终端环境变量..." | tee -a "$MAIN_LOG"
          export TERM=linux
          export LC_ALL=C
          export FORCE_UNSAFE_CONFIGURE=1
          
          echo "🔧 修复构建目录结构..." | tee -a "$MAIN_LOG"
          mkdir -p /mnt/source/{build_dir,staging_dir,bin,tmp,logs,dl}
          mkdir -p /mnt/source/staging_dir/{host,target-*,toolchain-*}
          mkdir -p /mnt/source/build_dir/{host,target-*}
          
          echo "🔧 创建必要的符号链接..." | tee -a "$MAIN_LOG"
          [ -L "/mnt/source/dl" ] || ln -sf /mnt/downloads /mnt/source/dl
          
          echo "🔧 修复文件权限..." | tee -a "$MAIN_LOG"
          chmod -R 755 /mnt/source/staging_dir/
          chown -R $USER:$USER /mnt/source/staging_dir/
          
          echo "✅ 终端环境和构建系统修复完成" | tee -a "$MAIN_LOG"

      - name: "🎛️ 模式配置文件处理"
        id: mode_config
        run: |
          set +e
          MAIN_LOG="${{ env.MAIN_LOG }}"
          echo "=== 🎛️ 模式配置文件处理 ===" | tee -a "$MAIN_LOG"
          
          # 将中文选项映射为内部值
          BUILD_MODE="${{ github.event.inputs.build_mode }}"
          case "$BUILD_MODE" in
            "最小化") MODE_INTERNAL="minimal" ;;
            "标准") MODE_INTERNAL="standard" ;;
            "自定义") MODE_INTERNAL="custom" ;;
            *) MODE_INTERNAL="standard" ;;
          esac
          
          CONFIG_REPO="${{ env.CONFIG_REPO_DIR }}"
          MODE_CONFIG_DIR="$CONFIG_REPO/firmware-config/modes"
          
          echo "🎯 构建模式: $BUILD_MODE ($MODE_INTERNAL)" | tee -a "$MAIN_LOG"
          
          mkdir -p "$MODE_CONFIG_DIR"
          
          if [ "$MODE_INTERNAL" = "minimal" ]; then
            echo "🔧 创建最小化模式配置..." | tee -a "$MAIN_LOG"
            echo "# 最小化构建模式配置" > "$MODE_CONFIG_DIR/minimal.conf"
            echo "CONFIG_PACKAGE_base-files=y" >> "$MODE_CONFIG_DIR/minimal.conf"
            echo "CONFIG_PACKAGE_busybox=y" >> "$MODE_CONFIG_DIR/minimal.conf"
            echo "CONFIG_PACKAGE_dropbear=y" >> "$MODE_CONFIG_DIR/minimal.conf"
            echo "CONFIG_PACKAGE_firewall=y" >> "$MODE_CONFIG_DIR/minimal.conf"
            echo "CONFIG_PACKAGE_fstools=y" >> "$MODE_CONFIG_DIR/minimal.conf"
            echo "CONFIG_PACKAGE_libc=y" >> "$MODE_CONFIG_DIR/minimal.conf"
            echo "CONFIG_PACKAGE_libgcc=y" >> "$MODE_CONFIG_DIR/minimal.conf"
            echo "CONFIG_PACKAGE_logd=y" >> "$MODE_CONFIG_DIR/minimal.conf"
            echo "CONFIG_PACKAGE_mtd=y" >> "$MODE_CONFIG_DIR/minimal.conf"
            echo "CONFIG_PACKAGE_netifd=y" >> "$MODE_CONFIG_DIR/minimal.conf"
            echo "CONFIG_PACKAGE_opkg=y" >> "$MODE_CONFIG_DIR/minimal.conf"
            echo "CONFIG_PACKAGE_procd=y" >> "$MODE_CONFIG_DIR/minimal.conf"
            echo "CONFIG_PACKAGE_swconfig=y" >> "$MODE_CONFIG_DIR/minimal.conf"
            echo "CONFIG_PACKAGE_ubox=y" >> "$MODE_CONFIG_DIR/minimal.conf"
            echo "CONFIG_PACKAGE_ubus=y" >> "$MODE_CONFIG_DIR/minimal.conf"
            echo "CONFIG_PACKAGE_ubusd=y" >> "$MODE_CONFIG_DIR/minimal.conf"
            echo "CONFIG_PACKAGE_uci=y" >> "$MODE_CONFIG_DIR/minimal.conf"
            echo "CONFIG_PACKAGE_uclient-fetch=y" >> "$MODE_CONFIG_DIR/minimal.conf"
            echo "CONFIG_PACKAGE_urandom-seed=y" >> "$MODE_CONFIG_DIR/minimal.conf"
            echo "CONFIG_PACKAGE_usign=y" >> "$MODE_CONFIG_DIR/minimal.conf"
            echo "📄 最小化模式配置内容:" | tee -a "$MAIN_LOG"
            cat "$MODE_CONFIG_DIR/minimal.conf" | tee -a "$MAIN_LOG"
            
          elif [ "$MODE_INTERNAL" = "standard" ]; then
            echo "🔧 创建标准模式配置..." | tee -a "$MAIN_LOG"
            echo "# 标准构建模式配置" > "$MODE_CONFIG_DIR/standard.conf"
            echo "CONFIG_PACKAGE_base-files=y" >> "$MODE_CONFIG_DIR/standard.conf"
            echo "CONFIG_PACKAGE_busybox=y" >> "$MODE_CONFIG_DIR/standard.conf"
            echo "CONFIG_PACKAGE_dropbear=y" >> "$MODE_CONFIG_DIR/standard.conf"
            echo "CONFIG_PACKAGE_firewall=y" >> "$MODE_CONFIG_DIR/standard.conf"
            echo "CONFIG_PACKAGE_fstools=y" >> "$MODE_CONFIG_DIR/standard.conf"
            echo "CONFIG_PACKAGE_libc=y" >> "$MODE_CONFIG_DIR/standard.conf"
            echo "CONFIG_PACKAGE_libgcc=y" >> "$MODE_CONFIG_DIR/standard.conf"
            echo "CONFIG_PACKAGE_logd=y" >> "$MODE_CONFIG_DIR/standard.conf"
            echo "CONFIG_PACKAGE_mtd=y" >> "$MODE_CONFIG_DIR/standard.conf"
            echo "CONFIG_PACKAGE_netifd=y" >> "$MODE_CONFIG_DIR/standard.conf"
            echo "CONFIG_PACKAGE_opkg=y" >> "$MODE_CONFIG_DIR/standard.conf"
            echo "CONFIG_PACKAGE_procd=y" >> "$MODE_CONFIG_DIR/standard.conf"
            echo "CONFIG_PACKAGE_swconfig=y" >> "$MODE_CONFIG_DIR/standard.conf"
            echo "CONFIG_PACKAGE_ubox=y" >> "$MODE_CONFIG_DIR/standard.conf"
            echo "CONFIG_PACKAGE_ubus=y" >> "$MODE_CONFIG_DIR/standard.conf"
            echo "CONFIG_PACKAGE_ubusd=y" >> "$MODE_CONFIG_DIR/standard.conf"
            echo "CONFIG_PACKAGE_uci=y" >> "$MODE_CONFIG_DIR/standard.conf"
            echo "CONFIG_PACKAGE_uclient-fetch=y" >> "$MODE_CONFIG_DIR/standard.conf"
            echo "CONFIG_PACKAGE_urandom-seed=y" >> "$MODE_CONFIG_DIR/standard.conf"
            echo "CONFIG_PACKAGE_usign=y" >> "$MODE_CONFIG_DIR/standard.conf"
            echo "# 标准模式额外包" >> "$MODE_CONFIG_DIR/standard.conf"
            echo "CONFIG_PACKAGE_luci=y" >> "$MODE_CONFIG_DIR/standard.conf"
            echo "CONFIG_PACKAGE_luci-base=y" >> "$MODE_CONFIG_DIR/standard.conf"
            echo "CONFIG_PACKAGE_luci-compat=y" >> "$MODE_CONFIG_DIR/standard.conf"
            echo "CONFIG_PACKAGE_luci-i18n-base-en=y" >> "$MODE_CONFIG_DIR/standard.conf"
            echo "📄 标准模式配置内容:" | tee -a "$MAIN_LOG"
            cat "$MODE_CONFIG_DIR/standard.conf" | tee -a "$MAIN_LOG"
            
          else
            echo "🔧 创建自定义模式配置..." | tee -a "$MAIN_LOG"
            echo "# 自定义构建模式配置" > "$MODE_CONFIG_DIR/custom.conf"
            echo "CONFIG_PACKAGE_base-files=y" >> "$MODE_CONFIG_DIR/custom.conf"
            echo "CONFIG_PACKAGE_busybox=y" >> "$MODE_CONFIG_DIR/custom.conf"
            echo "CONFIG_PACKAGE_dropbear=y" >> "$MODE_CONFIG_DIR/custom.conf"
            echo "CONFIG_PACKAGE_firewall=y" >> "$MODE_CONFIG_DIR/custom.conf"
            echo "CONFIG_PACKAGE_fstools=y" >> "$MODE_CONFIG_DIR/custom.conf"
            echo "CONFIG_PACKAGE_libc=y" >> "$MODE_CONFIG_DIR/custom.conf"
            echo "CONFIG_PACKAGE_libgcc=y" >> "$MODE_CONFIG_DIR/custom.conf"
            echo "CONFIG_PACKAGE_logd=y" >> "$MODE_CONFIG_DIR/custom.conf"
            echo "CONFIG_PACKAGE_netifd=y" >> "$MODE_CONFIG_DIR/custom.conf"
            echo "CONFIG_PACKAGE_opkg=y" >> "$MODE_CONFIG_DIR/custom.conf"
            echo "CONFIG_PACKAGE_procd=y" >> "$MODE_CONFIG_DIR/custom.conf"
            echo "CONFIG_PACKAGE_ubox=y" >> "$MODE_CONFIG_DIR/custom.conf"
            echo "CONFIG_PACKAGE_ubus=y" >> "$MODE_CONFIG_DIR/custom.conf"
            echo "CONFIG_PACKAGE_ubusd=y" >> "$MODE_CONFIG_DIR/custom.conf"
            echo "CONFIG_PACKAGE_uci=y" >> "$MODE_CONFIG_DIR/custom.conf"
            echo "CONFIG_PACKAGE_uclient-fetch=y" >> "$MODE_CONFIG_DIR/custom.conf"
            echo "CONFIG_PACKAGE_urandom-seed=y" >> "$MODE_CONFIG_DIR/custom.conf"
            echo "CONFIG_PACKAGE_usign=y" >> "$MODE_CONFIG_DIR/custom.conf"
            echo "📄 自定义模式配置内容:" | tee -a "$MAIN_LOG"
            cat "$MODE_CONFIG_DIR/custom.conf" | tee -a "$MAIN_LOG"
          fi
          
          echo "BUILD_MODE=$MODE_INTERNAL" >> $GITHUB_ENV
          echo "MODE_CONFIG_DIR=$MODE_CONFIG_DIR" >> $GITHUB_ENV
          echo "✅ 模式配置文件创建完成: $BUILD_MODE ($MODE_INTERNAL)" | tee -a "$MAIN_LOG"

      - name: "📝 智能配置创建与处理（修复版）"
        id: config_creation
        run: |
          set +e
          MAIN_LOG="${{ env.MAIN_LOG }}"
          echo "=== 📝 智能配置创建与处理（修复版） ===" | tee -a "$MAIN_LOG"
          cd /mnt/source
          
          CONFIG_PROFILE="${{ github.event.inputs.config_profile }}"
          CONFIG_DIR="${{ env.CONFIG_REPO_DIR }}/firmware-config/configs"
          CONFIG_FILE="$CONFIG_DIR/config_$CONFIG_PROFILE"
          MODE_CONFIG="${{ env.MODE_CONFIG_DIR }}/${{ env.BUILD_MODE }}.conf"
          
          echo "🔍 查找配置文件: $CONFIG_FILE" | tee -a "$MAIN_LOG"
          echo "🎛️ 应用模式配置: $MODE_CONFIG" | tee -a "$MAIN_LOG"
          
          # 清理可能存在的旧配置文件
          rm -f .config .config.old
          
          if [ -f "$CONFIG_FILE" ]; then
            echo "✅ 应用现有配置文件: $CONFIG_PROFILE" | tee -a "$MAIN_LOG"
            # 验证配置文件语法
            if grep -q "missing separator" "$CONFIG_FILE"; then
              echo "❌ 配置文件存在语法错误，重新创建..." | tee -a "$MAIN_LOG"
              rm -f "$CONFIG_FILE"
              echo "CONFIG_SOURCE=new" >> $GITHUB_ENV
            else
              cp "$CONFIG_FILE" .config
              echo "CONFIG_SOURCE=existing" >> $GITHUB_ENV
            fi
          else
            echo "CONFIG_SOURCE=new" >> $GITHUB_ENV
          fi
          
          if [ "${{ env.CONFIG_SOURCE }}" = "new" ]; then
            echo "🔧 创建新配置..." | tee -a "$MAIN_LOG"
            mkdir -p "$CONFIG_DIR"
            
            echo "🎯 智能分析设备名称: $CONFIG_PROFILE" | tee -a "$MAIN_LOG"
            
            # 创建基础配置文件
            echo "# 设备配置 - $CONFIG_PROFILE" > "$CONFIG_FILE"
            echo "# 自动生成于 $(date)" >> "$CONFIG_FILE"
            
            TARGET_CONFIG=""
            # 增强设备检测 - 使用精确匹配
            case "$CONFIG_PROFILE" in
              ac42u|rt-ac42u|asus-ac42u|ASUS-RT-AC42U)
                echo "# ASUS RT-AC42U 设备 (IPQ40xx)" >> "$CONFIG_FILE"
                echo "CONFIG_TARGET_ipq40xx=y" >> "$CONFIG_FILE"
                echo "CONFIG_TARGET_ipq40xx_generic=y" >> "$CONFIG_FILE"
                echo "CONFIG_TARGET_ipq40xx_generic_DEVICE_asus_rt-ac42u=y" >> "$CONFIG_FILE"
                echo "CONFIG_TARGET_DEVICE_ipq40xx_generic_DEVICE_asus_rt-ac42u=y" >> "$CONFIG_FILE"
                TARGET_CONFIG="ipq40xx_asus_rt-ac42u"
                echo "🎯 设备类型: IPQ40xx (ASUS RT-AC42U)" | tee -a "$MAIN_LOG"
                ;;
              x86|x64|generic-x64|amd64)
                echo "# x86/x64 通用设备" >> "$CONFIG_FILE"
                echo "CONFIG_TARGET_x86=y" >> "$CONFIG_FILE"
                echo "CONFIG_TARGET_x86_64=y" >> "$CONFIG_FILE"
                echo "CONFIG_TARGET_x86_64_DEVICE_generic=y" >> "$CONFIG_FILE"
                TARGET_CONFIG="x86_64_generic"
                echo "🎯 设备类型: x86_64 通用" | tee -a "$MAIN_LOG"
                ;;
              mt7621|newifi-d2|r6220)
                echo "# MT7621 设备" >> "$CONFIG_FILE"
                echo "CONFIG_TARGET_ramips=y" >> "$CONFIG_FILE"
                echo "CONFIG_TARGET_ramips_mt7621=y" >> "$CONFIG_FILE"
                TARGET_CONFIG="ramips_mt7621"
                echo "🎯 设备类型: MT7621" | tee -a "$MAIN_LOG"
                ;;
              *)
                # 使用默认配置，但优先尝试IPQ40xx
                echo "# 自动检测设备配置 - $CONFIG_PROFILE" >> "$CONFIG_FILE"
                echo "CONFIG_TARGET_ipq40xx=y" >> "$CONFIG_FILE"
                echo "CONFIG_TARGET_ipq40xx_generic=y" >> "$CONFIG_FILE"
                TARGET_CONFIG="ipq40xx_generic"
                echo "🎯 设备类型: 自动选择 IPQ40xx 通用" | tee -a "$MAIN_LOG"
                ;;
            esac
            
            # 添加通用配置
            echo "CONFIG_TARGET_ROOTFS_SQUASHFS=y" >> "$CONFIG_FILE"
            echo "CONFIG_TARGET_IMAGES_GZIP=y" >> "$CONFIG_FILE"
            echo "CONFIG_TARGET_IMAGES_INCLUDE_FACTORY=y" >> "$CONFIG_FILE"
            echo "CONFIG_TARGET_IMAGES_INCLUDE_SYSUPGRADE=y" >> "$CONFIG_FILE"
            echo "CONFIG_TARGET_KERNEL_PARTSIZE=16" >> "$CONFIG_FILE"
            echo "CONFIG_TARGET_ROOTFS_PARTSIZE=96" >> "$CONFIG_FILE"
            echo "CONFIG_CCACHE=y" >> "$CONFIG_FILE"
            echo "CONFIG_BUILD_LOG=y" >> "$CONFIG_FILE"
            echo "CONFIG_SDK=y" >> "$CONFIG_FILE"
            echo "# CONFIG_DEBUG=y is not set" >> "$CONFIG_FILE"
            
            cp "$CONFIG_FILE" .config
            echo "TARGET_CONFIG=$TARGET_CONFIG" >> $GITHUB_ENV
            echo "✅ 已创建智能配置文件: $CONFIG_FILE" | tee -a "$MAIN_LOG"
            echo "🎯 目标配置类型: $TARGET_CONFIG" | tee -a "$MAIN_LOG"
            
            # 显示生成的配置内容
            echo "📄 生成的配置内容预览:" | tee -a "$MAIN_LOG"
            grep -E "CONFIG_TARGET|CONFIG_PACKAGE" "$CONFIG_FILE" | head -20 | tee -a "$MAIN_LOG"
          fi
          
          # 显示当前.config内容
          echo "🔍 当前.config文件内容:" | tee -a "$MAIN_LOG"
          grep -E "CONFIG_TARGET" .config | tee -a "$MAIN_LOG" || echo "❌ 未找到目标配置" | tee -a "$MAIN_LOG"
          
          # 应用模式配置
          if [ -f "$MODE_CONFIG" ]; then
            echo "🔧 应用模式配置文件..." | tee -a "$MAIN_LOG"
            if ! grep -q "missing separator" "$MODE_CONFIG"; then
              cat "$MODE_CONFIG" >> .config
              echo "✅ 模式配置应用完成: ${{ env.BUILD_MODE }}" | tee -a "$MAIN_LOG"
            else
              echo "❌ 模式配置文件存在语法错误，跳过" | tee -a "$MAIN_LOG"
            fi
          else
            echo "⚠️ 模式配置文件不存在: $MODE_CONFIG" | tee -a "$MAIN_LOG"
          fi
          
          echo "🔧 预处理配置文件..." | tee -a "$MAIN_LOG"
          # 确保配置格式正确
          if [ -f .config ]; then
            # 清理可能的格式问题
            sed -i 's/^[ \t]*//' .config
            sed -i '/^$/d' .config
            sed -i 's/\r$//' .config
            
            # 确保每行都是有效的配置格式
            grep -E "^(# |CONFIG_|=|is not set)" .config > .config.clean || true
            mv .config.clean .config 2>/dev/null || true
          fi
          
          # 配置额外软件包（从多个来源）
          echo "📦 配置额外软件包..." | tee -a "$MAIN_LOG"
          ADDED_COUNT=0
          
          # 来源1：输入参数
          if [ -n "${{ github.event.inputs.extra_packages }}" ]; then
            echo "🔧 从输入参数加载额外软件包..." | tee -a "$MAIN_LOG"
            IFS=',' read -ra PKGS <<< "${{ github.event.inputs.extra_packages }}"
            for pkg in "${PKGS[@]}"; do
              CLEAN_PKG=$(echo "$pkg" | sed 's/^[[:space:]]*//;s/[[:space:]]*$//')
              if [ -n "$CLEAN_PKG" ]; then
                if ! grep -q "CONFIG_PACKAGE_${CLEAN_PKG}=y" .config; then
                  echo "CONFIG_PACKAGE_${CLEAN_PKG}=y" >> .config
                  echo "✅ 从输入参数添加包: $CLEAN_PKG" | tee -a "$MAIN_LOG"
                  ADDED_COUNT=$((ADDED_COUNT + 1))
                else
                  echo "ℹ️ 包已存在: $CLEAN_PKG" | tee -a "$MAIN_LOG"
                fi
              fi
            done
          fi
          
          # 来源2：自定义插件文件
          CUSTOM_PLUGINS_FILE="${{ env.CONFIG_REPO_DIR }}/firmware-config/custom-plugins.txt"
          CUSTOM_PLUGINS_FILE2="${{ env.CONFIG_REPO_DIR }}/firmware-config/plugins.txt"
          CUSTOM_PLUGINS_FILE3="${{ env.CONFIG_REPO_DIR }}/custom-plugins.txt"
          
          for PLUGIN_FILE in "$CUSTOM_PLUGINS_FILE" "$CUSTOM_PLUGINS_FILE2" "$CUSTOM_PLUGINS_FILE3"; do
            if [ -f "$PLUGIN_FILE" ]; then
              echo "🔧 从插件文件加载: $PLUGIN_FILE" | tee -a "$MAIN_LOG"
              
              # 读取插件文件内容
              PLUGIN_CONTENT=$(cat "$PLUGIN_FILE" 2>/dev/null | tr -d '\r' | grep -v '^#' | grep -v '^$' || true)
              
              if [ -n "$PLUGIN_CONTENT" ]; then
                echo "📋 插件文件内容:" | tee -a "$MAIN_LOG"
                echo "$PLUGIN_CONTENT" | tee -a "$MAIN_LOG"
                
                # 处理逗号分隔或换行分隔的包列表
                if echo "$PLUGIN_CONTENT" | grep -q ","; then
                  # 逗号分隔
                  IFS=',' read -ra PKGS <<< "$PLUGIN_CONTENT"
                else
                  # 换行分隔
                  IFS=$'\n' read -d '' -ra PKGS <<< "$PLUGIN_CONTENT" || true
                fi
                
                for pkg in "${PKGS[@]}"; do
                  CLEAN_PKG=$(echo "$pkg" | sed 's/^[[:space:]]*//;s/[[:space:]]*$//')
                  if [ -n "$CLEAN_PKG" ] && [[ "$CLEAN_PKG" != *"#"* ]]; then
                    if ! grep -q "CONFIG_PACKAGE_${CLEAN_PKG}=y" .config; then
                      echo "CONFIG_PACKAGE_${CLEAN_PKG}=y" >> .config
                      echo "✅ 从插件文件添加包: $CLEAN_PKG" | tee -a "$MAIN_LOG"
                      ADDED_COUNT=$((ADDED_COUNT + 1))
                    else
                      echo "ℹ️ 包已存在: $CLEAN_PKG" | tee -a "$MAIN_LOG"
                    fi
                  fi
                done
                echo "✅ 从 $PLUGIN_FILE 成功加载插件" | tee -a "$MAIN_LOG"
              else
                echo "ℹ️ 插件文件为空: $PLUGIN_FILE" | tee -a "$MAIN_LOG"
              fi
              break
            else
              echo "🔍 检查插件文件: $PLUGIN_FILE (未找到)" | tee -a "$MAIN_LOG"
            fi
          done
          
          # 来源3：检查插件目录
          PLUGIN_DIRS="${{ env.CONFIG_REPO_DIR }}/firmware-config/plugins ${{ env.CONFIG_REPO_DIR }}/firmware-config/custom-plugins ${{ env.CONFIG_REPO_DIR }}/plugins"
          
          for PLUGIN_DIR in $PLUGIN_DIRS; do
            if [ -d "$PLUGIN_DIR" ]; then
              echo "🔧 扫描插件目录: $PLUGIN_DIR" | tee -a "$MAIN_LOG"
              PLUGIN_FILES=$(find "$PLUGIN_DIR" -name "*.txt" -type f 2>/dev/null || true)
              
              if [ -n "$PLUGIN_FILES" ]; then
                for plugin_file in $PLUGIN_FILES; do
                  echo "📁 处理插件文件: $(basename "$plugin_file")" | tee -a "$MAIN_LOG"
                  PLUGIN_CONTENT=$(cat "$plugin_file" 2>/dev/null | tr -d '\r' | grep -v '^#' | grep -v '^$' || true)
                  
                  if [ -n "$PLUGIN_CONTENT" ]; then
                    IFS=$'\n' read -d '' -ra PKGS <<< "$PLUGIN_CONTENT" || true
                    for pkg in "${PKGS[@]}"; do
                      CLEAN_PKG=$(echo "$pkg" | sed 's/^[[:space:]]*//;s/[[:space:]]*$//')
                      if [ -n "$CLEAN_PKG" ] && [[ "$CLEAN_PKG" != *"#"* ]]; then
                        if ! grep -q "CONFIG_PACKAGE_${CLEAN_PKG}=y" .config; then
                          echo "CONFIG_PACKAGE_${CLEAN_PKG}=y" >> .config
                          echo "✅ 从目录插件文件添加包: $CLEAN_PKG" | tee -a "$MAIN_LOG"
                          ADDED_COUNT=$((ADDED_COUNT + 1))
                        fi
                      fi
                    done
                  fi
                done
              else
                echo "ℹ️ 插件目录中未找到.txt文件: $PLUGIN_DIR" | tee -a "$MAIN_LOG"
              fi
            fi
          done
          
          echo "EXTRA_PACKAGES_ADDED=$ADDED_COUNT" >> $GITHUB_ENV
          
          if [ "$ADDED_COUNT" -gt 0 ]; then
            echo "✅ 成功添加 $ADDED_COUNT 个额外软件包" | tee -a "$MAIN_LOG"
          else
            echo "ℹ️ 未找到额外软件包" | tee -a "$MAIN_LOG"
          fi
          
          # 验证最终配置文件语法
          echo "🔍 验证配置文件语法..." | tee -a "$MAIN_LOG"
          if grep -q "missing separator" .config; then
            echo "❌ 配置文件存在语法错误，显示错误行:" | tee -a "$MAIN_LOG"
            grep -n "missing separator" .config | head -5 | tee -a "$MAIN_LOG"
            echo "🔧 自动修复语法错误..." | tee -a "$MAIN_LOG"
            
            # 具体修复措施
            sed -i '/missing separator/d' .config
            sed -i 's/^[ \t]*//' .config
            sed -i 's/[ \t]*$//' .config
            
            # 重新验证
            if grep -q "missing separator" .config; then
              echo "💥 无法修复语法错误，重建配置..." | tee -a "$MAIN_LOG"
              # 重建基础配置
              echo "# 自动重建的基础配置" > .config
              echo "CONFIG_TARGET_ipq40xx=y" >> .config
              echo "CONFIG_TARGET_ipq40xx_generic=y" >> .config
              echo "CONFIG_TARGET_ipq40xx_generic_DEVICE_asus_rt-ac42u=y" >> .config
              echo "CONFIG_TARGET_ROOTFS_SQUASHFS=y" >> .config
              echo "CONFIG_PACKAGE_base-files=y" >> .config
              echo "CONFIG_PACKAGE_busybox=y" >> .config
              echo "CONFIG_PACKAGE_procd=y" >> .config
            fi
          fi
          
          echo "✅ 配置创建与处理完成" | tee -a "$MAIN_LOG"

      - name: "🔧 强制设备配置应用"
        id: force_device_config
        run: |
          set +e
          MAIN_LOG="${{ env.MAIN_LOG }}"
          echo "=== 🔧 强制设备配置应用 ===" | tee -a "$MAIN_LOG"
          cd /mnt/source
          
          CONFIG_PROFILE="${{ github.event.inputs.config_profile }}"
          
          echo "🎯 强制应用设备配置: $CONFIG_PROFILE" | tee -a "$MAIN_LOG"
          
          # 清除可能存在的错误配置
          sed -i '/CONFIG_TARGET_x86/d' .config
          sed -i '/CONFIG_TARGET_x86_64/d' .config
          sed -i '/CONFIG_TARGET_ramips/d' .config
          
          # 根据设备名称强制设置配置
          case "$CONFIG_PROFILE" in
            ac42u|rt-ac42u|asus-ac42u|ASUS-RT-AC42U)
              echo "🔧 强制设置 IPQ40xx 配置..." | tee -a "$MAIN_LOG"
              echo "CONFIG_TARGET_ipq40xx=y" >> .config
              echo "CONFIG_TARGET_ipq40xx_generic=y" >> .config
              echo "CONFIG_TARGET_ipq40xx_generic_DEVICE_asus_rt-ac42u=y" >> .config
              echo "CONFIG_TARGET_DEVICE_ipq40xx_generic_DEVICE_asus_rt-ac42u=y" >> .config
              ;;
            *)
              echo "🔧 强制设置 IPQ40xx 通用配置..." | tee -a "$MAIN_LOG"
              echo "CONFIG_TARGET_ipq40xx=y" >> .config
              echo "CONFIG_TARGET_ipq40xx_generic=y" >> .config
              ;;
          esac
          
          echo "🔍 验证强制配置结果:" | tee -a "$MAIN_LOG"
          grep -E "CONFIG_TARGET" .config | tee -a "$MAIN_LOG"
          
          echo "✅ 强制设备配置完成" | tee -a "$MAIN_LOG"

      - name: "🔧 修复主机工具和配置（彻底修复版）"
        id: fix_host_tools
        timeout-minutes: 45
        run: |
          set +e
          MAIN_LOG="${{ env.MAIN_LOG }}"
          echo "=== 🔧 修复主机工具和配置（彻底修复版） ===" | tee -a "$MAIN_LOG"
          cd /mnt/source
          
          export TERM=linux
          export LC_ALL=C
          export FORCE_UNSAFE_CONFIGURE=1
          
          echo "🔧 强制创建主机工具目录结构..." | tee -a "$MAIN_LOG"
          mkdir -p staging_dir/host/bin
          mkdir -p staging_dir/toolchain-*
          mkdir -p build_dir/host
          touch staging_dir/host/.prereq-build
          
          echo "🔧 设置环境变量避免交互..." | tee -a "$MAIN_LOG"
          export BUILD_LOG=1
          export SCAN_CPUS=1
          
          echo "🔧 预编译绝对必要的主机工具..." | tee -a "$MAIN_LOG"
          
          # 首先编译mkhash，这是最关键的
          echo "🎯 优先编译mkhash工具..." | tee -a "$MAIN_LOG"
          if make tools/mkhash/compile -j1 V=0 2>&1 | tee -a "$MAIN_LOG"; then
            echo "✅ mkhash编译成功" | tee -a "$MAIN_LOG"
            if make tools/mkhash/install -j1 V=0 2>&1 | tee -a "$MAIN_LOG"; then
              echo "✅ mkhash安装成功" | tee -a "$MAIN_LOG"
            else
              echo "⚠️ mkhash安装有警告" | tee -a "$MAIN_LOG"
            fi
          else
            echo "❌ mkhash编译失败，尝试替代方案..." | tee -a "$MAIN_LOG"
            cd tools/mkhash
            if gcc -o mkhash src/mkhash.c 2>&1 | tee -a "$MAIN_LOG"; then
              echo "✅ 手动编译mkhash成功" | tee -a "$MAIN_LOG"
              mkdir -p ../../staging_dir/host/bin
              cp mkhash ../../staging_dir/host/bin/
              cd ../..
            else
              echo "💥 mkhash编译彻底失败" | tee -a "$MAIN_LOG"
              cd ../..
              exit 1
            fi
          fi
          
          # 验证mkhash是否可用
          echo "🔍 验证mkhash工具..." | tee -a "$MAIN_LOG"
          if staging_dir/host/bin/mkhash 2>&1 | head -5 | tee -a "$MAIN_LOG"; then
            echo "✅ mkhash验证成功" | tee -a "$MAIN_LOG"
          else
            echo "❌ mkhash验证失败" | tee -a "$MAIN_LOG"
          fi
          
          # 编译其他关键主机工具
          echo "🔧 编译其他关键主机工具..." | tee -a "$MAIN_LOG"
          CRITICAL_TOOLS="tar xz findutils sed gawk grep bash coreutils install ccache"
          
          for tool in $CRITICAL_TOOLS; do
            echo "🛠️ 编译 $tool..." | tee -a "$MAIN_LOG"
            if timeout 600 make tools/$tool/compile -j1 V=0 2>&1 | tee -a "$MAIN_LOG"; then
              echo "✅ $tool 编译成功" | tee -a "$MAIN_LOG"
              make tools/$tool/install -j1 V=0 2>&1 | tee -a "$MAIN_LOG" || true
            else
              echo "⚠️ $tool 编译有警告，继续..." | tee -a "$MAIN_LOG"
            fi
          done
          
          echo "🔧 编译配置工具..." | tee -a "$MAIN_LOG"
          if make -C scripts/config -j1 2>&1 | tee -a "$MAIN_LOG"; then
            echo "✅ 配置工具编译成功" | tee -a "$MAIN_LOG"
          else
            echo "⚠️ 配置工具编译有警告" | tee -a "$MAIN_LOG"
          fi
          
          echo "🔧 修复文件权限和所有权..." | tee -a "$MAIN_LOG"
          chmod -R 755 staging_dir/
          chown -R $USER:$USER staging_dir/
          chmod -R 755 build_dir/
          chown -R $USER:$USER build_dir/
          
          echo "📊 主机工具状态检查:" | tee -a "$MAIN_LOG"
          if [ -f "staging_dir/host/bin/mkhash" ]; then
            echo "✅ mkhash: 已安装" | tee -a "$MAIN_LOG"
          else
            echo "❌ mkhash: 缺失" | tee -a "$MAIN_LOG"
          fi
          
          echo "✅ 主机工具修复完成" | tee -a "$MAIN_LOG"

      - name: "🔧 配置完整性验证与修复（保护设备配置）"
        id: config_validation
        timeout-minutes: 10
        run: |
          set +e
          MAIN_LOG="${{ env.MAIN_LOG }}"
          echo "=== 🔧 配置完整性验证与修复（保护设备配置） ===" | tee -a "$MAIN_LOG"
          cd /mnt/source
          
          echo "🔍 验证配置完整性..." | tee -a "$MAIN_LOG"
          
          # 备份设备配置
          echo "🔧 备份设备配置..." | tee -a "$MAIN_LOG"
          grep -E "CONFIG_TARGET.*ipq40xx" .config > /tmp/device_config.backup || true
          grep -E "CONFIG_TARGET.*asus.*ac42u" .config >> /tmp/device_config.backup || true
          
          # 备份原始配置
          cp .config .config.backup
          
          echo "🔧 使用oldconfig而非defconfig..." | tee -a "$MAIN_LOG"
          export TERM=dumb
          export LC_ALL=C
          
          echo "🔧 执行oldconfig（带超时）..." | tee -a "$MAIN_LOG"
          if yes "" | timeout 120 make oldconfig; then
            echo "✅ oldconfig成功完成" | tee -a "$MAIN_LOG"
          else
            echo "⚠️ oldconfig有警告或超时，继续..." | tee -a "$MAIN_LOG"
          fi
          
          # 恢复设备配置（如果被覆盖）
          echo "🔧 检查并恢复设备配置..." | tee -a "$MAIN_LOG"
          if ! grep -q "CONFIG_TARGET_ipq40xx=y" .config; then
            echo "⚠️ 设备配置被覆盖，恢复中..." | tee -a "$MAIN_LOG"
            cat /tmp/device_config.backup >> .config
          fi
          
          # 确保设备配置存在
          if ! grep -q "CONFIG_TARGET_ipq40xx=y" .config; then
            echo "🔧 确保IPQ40xx配置存在..." | tee -a "$MAIN_LOG"
            echo "CONFIG_TARGET_ipq40xx=y" >> .config
            echo "CONFIG_TARGET_ipq40xx_generic=y" >> .config
          fi
          
          echo "🔍 最终配置检查:" | tee -a "$MAIN_LOG"
          TARGET_TYPE=$(grep "CONFIG_TARGET.*=y" .config | head -1 | cut -d'=' -f1 | cut -d'_' -f3 || echo "unknown")
          TARGET_COUNT=$(grep -c "CONFIG_TARGET.*=y" .config 2>/dev/null || echo 0)
          
          echo "✅ 目标系统类型: $TARGET_TYPE" | tee -a "$MAIN_LOG"
          echo "✅ 目标配置数量: $TARGET_COUNT" | tee -a "$MAIN_LOG"
          
          if [ "$TARGET_TYPE" != "ipq40xx" ]; then
            echo "❌ 目标系统类型错误，期望: ipq40xx, 实际: $TARGET_TYPE" | tee -a "$MAIN_LOG"
            echo "🔧 强制设置为IPQ40xx..." | tee -a "$MAIN_LOG"
            sed -i '/CONFIG_TARGET_/d' .config
            echo "CONFIG_TARGET_ipq40xx=y" >> .config
            echo "CONFIG_TARGET_ipq40xx_generic=y" >> .config
            echo "CONFIG_TARGET_ipq40xx_generic_DEVICE_asus_rt-ac42u=y" >> .config
          fi
          
          # 验证关键配置
          BASE_FILES=$(grep -c "CONFIG_PACKAGE_base-files=y" .config 2>/dev/null || echo 0)
          BUSYBOX=$(grep -c "CONFIG_PACKAGE_busybox=y" .config 2>/dev/null || echo 0)
          
          echo "✅ 基础文件系统: $BASE_FILES" | tee -a "$MAIN_LOG"
          echo "✅ BusyBox: $BUSYBOX" | tee -a "$MAIN_LOG"
          
          if [ "$TARGET_COUNT" -eq 0 ] || [ "$BASE_FILES" -eq 0 ] || [ "$BUSYBOX" -eq 0 ]; then
            echo "❌ 关键配置缺失，显示前30行配置..." | tee -a "$MAIN_LOG"
            head -30 .config | tee -a "$MAIN_LOG"
            echo "💥 配置验证失败" | tee -a "$MAIN_LOG"
            exit 1
          fi
          
          # 显示最终配置
          echo "🔍 最终目标配置:" | tee -a "$MAIN_LOG"
          grep -E "CONFIG_TARGET" .config | tee -a "$MAIN_LOG"
          
          echo "✅ 配置验证完成" | tee -a "$MAIN_LOG"

      - name: "🐛 配置调试信息"
        id: config_debug
        run: |
          set +e
          MAIN_LOG="${{ env.MAIN_LOG }}"
          echo "=== 🐛 配置调试信息 ===" | tee -a "$MAIN_LOG"
          cd /mnt/source
          
          echo "🔍 当前工作目录: $(pwd)" | tee -a "$MAIN_LOG"
          echo "🔍 配置文件状态:" | tee -a "$MAIN_LOG"
          ls -la .config* 2>/dev/null | tee -a "$MAIN_LOG" || echo "无配置文件" | tee -a "$MAIN_LOG"
          
          echo "🔍 完整目标配置信息:" | tee -a "$MAIN_LOG"
          grep -E "CONFIG_TARGET" .config | tee -a "$MAIN_LOG" || echo "无目标配置" | tee -a "$MAIN_LOG"
          
          echo "🔍 设备相关配置:" | tee -a "$MAIN_LOG"
          grep -E "ipq40xx|ac42u|asus" .config | tee -a "$MAIN_LOG" || echo "无设备相关配置" | tee -a "$MAIN_LOG"
          
          echo "✅ 配置调试完成" | tee -a "$MAIN_LOG"

      - name: "🔄 并行Feeds更新"
        id: feeds_setup
        run: |
          set +e
          MAIN_LOG="${{ env.MAIN_LOG }}"
          echo "=== 🔄 并行Feeds更新 ===" | tee -a "$MAIN_LOG"
          cd /mnt/source
          
          CURRENT_BRANCH=$(git branch --show-current)
          if [[ "$CURRENT_BRANCH" == *"22.03"* ]]; then FEEDS_BRANCH="openwrt-22.03"
          elif [[ "$CURRENT_BRANCH" == *"21.02"* ]]; then FEEDS_BRANCH="openwrt-21.02"
          else FEEDS_BRANCH="master"; fi
          
          case "${{ env.SOURCE_PRESET }}" in
            "immortalwrt")
              echo "src-git packages https://github.com/immortalwrt/packages.git;$FEEDS_BRANCH" > feeds.conf
              echo "src-git luci https://github.com/immortalwrt/luci.git;$FEEDS_BRANCH" >> feeds.conf
              ;;
            "openwrt")
              echo "src-git packages https://git.openwrt.org/feed/packages.git;$FEEDS_BRANCH" > feeds.conf
              echo "src-git luci https://git.openwrt.org/project/luci.git;$FEEDS_BRANCH" >> feeds.conf
              ;;
            "lede")
              echo "src-git packages https://github.com/coolsnowwolf/packages.git;$FEEDS_BRANCH" > feeds.conf
              echo "src-git luci https://github.com/coolsnowwolf/luci.git;$FEEDS_BRANCH" >> feeds.conf
              ;;
          esac
          
          echo "src-git routing https://github.com/openwrt/routing.git;$FEEDS_BRANCH" >> feeds.conf
          
          echo "🔄 并行更新feeds..." | tee -a "$MAIN_LOG"
          ./scripts/feeds update -a &
          FEEDS_PID=$!
          
          mkdir -p /mnt/downloads
          [ -d "dl" ] && rm -rf dl
          ln -sf /mnt/downloads dl
          
          wait $FEEDS_PID
          ./scripts/feeds install -a
          
          echo "✅ Feeds更新完成" | tee -a "$MAIN_LOG"

      - name: "🌐 语言包配置"
        id: language_setup
        run: |
          set +e
          MAIN_LOG="${{ env.MAIN_LOG }}"
          echo "=== 🌐 语言包配置 ===" | tee -a "$MAIN_LOG"
          cd /mnt/source
          
          # 将中文选项映射为内部值
          LANGUAGE="${{ github.event.inputs.language_pack }}"
          case "$LANGUAGE" in
            "简体中文") LANG_INTERNAL="zh-cn" ;;
            "仅英文") LANG_INTERNAL="en-only" ;;
            "全部语言") LANG_INTERNAL="all" ;;
            *) LANG_INTERNAL="zh-cn" ;;
          esac
          
          echo "🎯 语言包选择: $LANGUAGE ($LANG_INTERNAL)" | tee -a "$MAIN_LOG"
          
          CURRENT_BRANCH="${{ env.ACTUAL_BRANCH }}"
          
          case "$LANG_INTERNAL" in
            "zh-cn")
              echo "🔧 启用中文语言包..." | tee -a "$MAIN_LOG"
              if [[ "$CURRENT_BRANCH" == *"21.02"* ]]; then
                sed -i 's/# CONFIG_PACKAGE_luci-i18n-base-zh-cn is not set/CONFIG_PACKAGE_luci-i18n-base-zh-cn=y/' .config 2>/dev/null || echo "CONFIG_PACKAGE_luci-i18n-base-zh-cn=y" >> .config
              else
                sed -i 's/# CONFIG_PACKAGE_luci-i18n-base-zh_Hans is not set/CONFIG_PACKAGE_luci-i18n-base-zh_Hans=y/' .config 2>/dev/null || echo "CONFIG_PACKAGE_luci-i18n-base-zh_Hans=y" >> .config
              fi
              ;;
            "en-only")
              echo "🔧 仅保留英文..." | tee -a "$MAIN_LOG"
              sed -i 's/CONFIG_PACKAGE_luci-i18n-[^e][^n].*=y/# &/' .config
              ;;
            "all")
              echo "🔧 启用所有语言包..." | tee -a "$MAIN_LOG"
              for lang in $(grep "CONFIG_PACKAGE_luci-i18n-" .config | grep "is not set" | cut -d' ' -f2); do
                sed -i "s/# $lang is not set/$lang=y/" .config
              done
              ;;
          esac
          
          echo "✅ 语言包配置完成" | tee -a "$MAIN_LOG"

      - name: "🌐 智能下载优化"
        id: smart_download
        timeout-minutes: 30
        run: |
          set +e
          MAIN_LOG="${{ env.MAIN_LOG }}"
          DOWNLOAD_LOG="${{ env.DOWNLOAD_LOG }}"
          echo "=== 🌐 智能下载优化 ===" | tee -a "$MAIN_LOG"
          cd /mnt/source
          
          export TERM=linux
          export LC_ALL=C
          export FORCE_UNSAFE_CONFIGURE=1
          
          echo "🔧 修复下载配置..." | tee -a "$MAIN_LOG"
          sed -i 's|https://sources.cdn.openwrt.org|https://mirror2.openwrt.org/sources|g' scripts/download.pl 2>/dev/null || true
          sed -i 's|git.openwrt.org|github.com/openwrt|g' feeds.conf.* 2>/dev/null || true
          
          PARALLEL_JOBS="${{ env.PARALLEL_JOBS }}"
          
          echo "📥 开始智能下载流程..." | tee -a "$MAIN_LOG"
          echo "开始下载流程: $(date)" >> "$DOWNLOAD_LOG"
          
          DOWNLOAD_SUCCESS=false
          DOWNLOAD_RETRY_COUNT=0
          MAX_RETRIES=3
          
          while [ $DOWNLOAD_RETRY_COUNT -lt $MAX_RETRIES ] && [ "$DOWNLOAD_SUCCESS" = false ]; do
            DOWNLOAD_RETRY_COUNT=$((DOWNLOAD_RETRY_COUNT + 1))
            echo "🔄 下载尝试 $DOWNLOAD_RETRY_COUNT/$MAX_RETRIES..." | tee -a "$MAIN_LOG"
            echo "下载尝试 $DOWNLOAD_RETRY_COUNT: $(date)" >> "$DOWNLOAD_LOG"
            
            if [ $DOWNLOAD_RETRY_COUNT -eq 1 ]; then
              JOBS=$PARALLEL_JOBS
            else
              JOBS=1
            fi
            
            TEMP_DOWNLOAD_LOG=$(mktemp)
            set +e
            make download -j$JOBS FORCE_UNSAFE_CONFIGURE=1 2>&1 | tee "$TEMP_DOWNLOAD_LOG"
            DOWNLOAD_RESULT=${PIPESTATUS[0]}
            set -e
            
            cat "$TEMP_DOWNLOAD_LOG" >> "$MAIN_LOG"
            echo "=== 下载尝试 $DOWNLOAD_RETRY_COUNT 详细日志 ===" >> "$DOWNLOAD_LOG"
            cat "$TEMP_DOWNLOAD_LOG" >> "$DOWNLOAD_LOG"
            
            if [ $DOWNLOAD_RESULT -eq 0 ]; then
              echo "✅ 下载成功" | tee -a "$MAIN_LOG"
              echo "下载成功" >> "$DOWNLOAD_LOG"
              DOWNLOAD_SUCCESS=true
            else
              echo "❌ 下载尝试 $DOWNLOAD_RETRY_COUNT 失败" | tee -a "$MAIN_LOG"
              echo "下载失败，退出码: $DOWNLOAD_RESULT" >> "$DOWNLOAD_LOG"
              
              if grep -q "missing separator" "$TEMP_DOWNLOAD_LOG"; then
                echo "❌ 配置文件语法错误" | tee -a "$MAIN_LOG"
                echo "错误类型: 配置文件语法错误" >> "$DOWNLOAD_LOG"
                grep -n "missing separator" "$TEMP_DOWNLOAD_LOG" | head -5 >> "$DOWNLOAD_LOG"
                break
              elif grep -q "mkhash" "$TEMP_DOWNLOAD_LOG"; then
                echo "❌ 主机工具缺失" | tee -a "$MAIN_LOG"
                echo "错误类型: 主机工具缺失" >> "$DOWNLOAD_LOG"
              fi
              
              if [ $DOWNLOAD_RETRY_COUNT -lt $MAX_RETRIES ]; then
                RETRY_DELAY=$((DOWNLOAD_RETRY_COUNT * 10))
                echo "⏳ 等待 ${RETRY_DELAY}秒后重试..." | tee -a "$MAIN_LOG"
                echo "等待 ${RETRY_DELAY}秒后重试" >> "$DOWNLOAD_LOG"
                sleep $RETRY_DELAY
              fi
            fi
            
            rm -f "$TEMP_DOWNLOAD_LOG"
          done
          
          if [ "$DOWNLOAD_SUCCESS" = false ]; then
            echo "💥 所有下载尝试都失败" | tee -a "$MAIN_LOG"
            echo "最终状态: 所有下载尝试失败" >> "$DOWNLOAD_LOG"
            head -100 .config >> "$DOWNLOAD_LOG"
            exit 1
          fi
          
          echo "🔍 验证下载完整性..." | tee -a "$MAIN_LOG"
          echo "开始完整性验证: $(date)" >> "$DOWNLOAD_LOG"
          
          DOWNLOAD_COUNT=$(find dl/ -type f 2>/dev/null | wc -l)
          echo "📦 下载文件数量: $DOWNLOAD_COUNT" | tee -a "$MAIN_LOG"
          echo "总下载文件数: $DOWNLOAD_COUNT" >> "$DOWNLOAD_LOG"
          
          if [ "$DOWNLOAD_COUNT" -lt 10 ]; then
            echo "⚠️ 下载文件数量过少，可能有问题" | tee -a "$MAIN_LOG"
            echo "警告: 下载文件数量过少" >> "$DOWNLOAD_LOG"
          else
            echo "✅ 下载文件数量正常" | tee -a "$MAIN_LOG"
          fi
          
          echo "DOWNLOAD_COMPLETE=true" >> $GITHUB_ENV
          echo "DOWNLOAD_FILE_COUNT=$DOWNLOAD_COUNT" >> $GITHUB_ENV
          echo "DOWNLOAD_RETRY_COUNT=$DOWNLOAD_RETRY_COUNT" >> $GITHUB_ENV
          echo "✅ 智能下载完成" | tee -a "$MAIN_LOG"

      - name: "🔧 高级功能增强"
        id: advanced_features
        if: github.event.inputs.enable_advanced == 'true'
        run: |
          set +e
          MAIN_LOG="${{ env.MAIN_LOG }}"
          WARNING_LOG="${{ env.WARNING_LOG }}"
          echo "=== 🔧 高级功能增强 ===" | tee -a "$MAIN_LOG"
          cd /mnt/source
          
          echo "🔧 高级功能状态: 已启用" | tee -a "$MAIN_LOG"
          
          echo "📦 安装自定义IPK包..." | tee -a "$MAIN_LOG"
          IPK_DIR="${{ env.CONFIG_REPO_DIR }}/firmware-config/custom-features/prebuilt-ipks"
          IPK_COUNT=0
          
          if [ -d "$IPK_DIR" ]; then
            IPK_FILES=$(find "$IPK_DIR" -name "*.ipk" -type f 2>/dev/null || true)
            if [ -n "$IPK_FILES" ]; then
              echo "✅ 找到自定义IPK包:" | tee -a "$MAIN_LOG"
              mkdir -p package/custom
              for ipk in $IPK_FILES; do
                ipk_name=$(basename "$ipk")
                echo "  - 复制 $ipk_name" | tee -a "$MAIN_LOG"
                cp "$ipk" package/custom/
                IPK_COUNT=$((IPK_COUNT + 1))
                ipk_basename=$(basename "$ipk" .ipk)
                echo "CONFIG_PACKAGE_${ipk_basename}=y" >> .config
              done
              echo "✅ 成功添加 $IPK_COUNT 个自定义IPK包" | tee -a "$MAIN_LOG"
            else
              echo "ℹ️ 未找到自定义IPK包" | tee -a "$MAIN_LOG"
            fi
          else
            echo "ℹ️ 自定义IPK目录不存在: $IPK_DIR" | tee -a "$MAIN_LOG"
          fi
          
          echo "CUSTOM_IPK_COUNT=$IPK_COUNT" >> $GITHUB_ENV
          
          echo "🔧 执行自定义脚本..." | tee -a "$MAIN_LOG"
          SCRIPT_PATHS="${{ env.CONFIG_REPO_DIR }}/firmware-config/custom-features/scripts ${{ env.CONFIG_REPO_DIR }}/firmware-config/scripts ${{ env.CONFIG_REPO_DIR }}/scripts ${{ env.CONFIG_REPO_DIR }}/firmware-config"
          
          SCRIPT_COUNT=0
          for script_path in $SCRIPT_PATHS; do
            if [ -d "$script_path" ]; then
              SCRIPTS=$(find "$script_path" -maxdepth 2 -name "*.sh" -type f 2>/dev/null | sort || true)
              if [ -n "$SCRIPTS" ]; then
                for script in $SCRIPTS; do
                  script_name=$(basename "$script")
                  echo "📜 执行脚本: $script_name..." | tee -a "$MAIN_LOG"
                  chmod +x "$script"
                  
                  TEMP_LOG=$(mktemp)
                  if { bash "$script" 2>&1 | tee "$TEMP_LOG"; } then
                    echo "✅ 自定义脚本执行成功: $script_name" | tee -a "$MAIN_LOG"
                    SCRIPT_COUNT=$((SCRIPT_COUNT + 1))
                  else
                    echo "⚠️ 自定义脚本执行有警告: $script_name" | tee -a "$MAIN_LOG"
                    grep -i -E "error|failed|No such file" "$TEMP_LOG" >> "$WARNING_LOG" || true
                  fi
                  cat "$TEMP_LOG" >> "$MAIN_LOG"
                  rm -f "$TEMP_LOG"
                done
              else
                echo "ℹ️ 在 $script_path 中未找到脚本" | tee -a "$MAIN_LOG"
              fi
            else
              echo "ℹ️ 脚本目录不存在: $script_path" | tee -a "$MAIN_LOG"
            fi
          done
          
          echo "CUSTOM_SCRIPT_COUNT=$SCRIPT_COUNT" >> $GITHUB_ENV
          echo "✅ 高级功能应用完成，执行了 $SCRIPT_COUNT 个脚本" | tee -a "$MAIN_LOG"

      - name: "⚡ 智能编译执行"
        id: smart_build
        timeout-minutes: 150
        run: |
          set -e
          MAIN_LOG="${{ env.MAIN_LOG }}"
          ERROR_LOG="${{ env.ERROR_LOG }}"
          ANALYSIS_REPORT="${{ env.ANALYSIS_REPORT }}"
          echo "=== ⚡ 智能编译执行 ===" | tee -a "$MAIN_LOG"
          cd /mnt/source
          
          export TERM=linux
          export LC_ALL=C
          export FORCE_UNSAFE_CONFIGURE=1
          PARALLEL_JOBS="${{ env.PARALLEL_JOBS }}"
          
          echo "🔧 编译配置: 并行作业=$PARALLEL_JOBS" | tee -a "$MAIN_LOG"
          
          echo "🔍 编译前最终检查..." | tee -a "$MAIN_LOG"
          make defconfig
          
          mkdir -p logs
          
          compile_with_recovery() {
            local stage_name="$1"
            local make_target="$2"
            local max_retries=2
            local retry_count=0
            
            while [ $retry_count -le $max_retries ]; do
              echo "🛠️ $stage_name (尝试 $((retry_count + 1))/$((max_retries + 1)))..." | tee -a "$MAIN_LOG"
              
              TEMP_LOG=$(mktemp)
              set +e
              if [ $retry_count -eq 0 ]; then
                make $make_target -j$PARALLEL_JOBS 2>&1 | tee "$TEMP_LOG"
              else
                make $make_target -j1 V=s 2>&1 | tee "$TEMP_LOG"
              fi
              local result=${PIPESTATUS[0]}
              set -e
              
              cat "$TEMP_LOG" >> "$MAIN_LOG"
              
              if [ $result -eq 0 ]; then
                echo "✅ $stage_name 成功" | tee -a "$MAIN_LOG"
                rm -f "$TEMP_LOG"
                return 0
              else
                echo "❌ $stage_name 失败" | tee -a "$MAIN_LOG"
                
                if [ $retry_count -lt $max_retries ]; then
                  echo "🔄 准备重试..." | tee -a "$MAIN_LOG"
                  retry_count=$((retry_count + 1))
                  
                  case "$stage_name" in
                    "工具链编译")
                      echo "🔄 清理工具链缓存..." | tee -a "$MAIN_LOG"
                      make toolchain/clean
                      ;;
                    "内核编译")
                      echo "🔄 清理内核构建..." | tee -a "$MAIN_LOG"
                      make target/linux/clean
                      ;;
                  esac
                else
                  echo "💥 $stage_name 彻底失败" | tee -a "$MAIN_LOG"
                  cat "$TEMP_LOG" >> "$ERROR_LOG"
                  rm -f "$TEMP_LOG"
                  return 1
                fi
              fi
              rm -f "$TEMP_LOG"
            done
          }
          
          echo "🔧 预编译宿主工具..." | tee -a "$MAIN_LOG"
          if ! compile_with_recovery "宿主工具编译" "tools/install"; then
            echo "⚠️ 宿主工具编译有警告，但继续构建" | tee -a "$MAIN_LOG"
          fi
          
          if ! compile_with_recovery "工具链编译" "toolchain/install"; then
            echo "💥 工具链编译失败，构建终止" | tee -a "$MAIN_LOG"
            exit 1
          fi
          
          if ! compile_with_recovery "内核编译" "target/linux/install"; then
            echo "💥 内核编译失败，构建终止" | tee -a "$MAIN_LOG"
            exit 1
          fi
          
          if ! compile_with_recovery "完整编译" ""; then
            echo "💥 完整编译失败" | tee -a "$MAIN_LOG"
            exit 1
          fi
          
          FIRMWARE_FOUND=false
          if find bin/targets -type f \( -name "*.bin" -o -name "*.img" -o -name "*.trx" \) 2>/dev/null | grep -q .; then
            echo "🎉 找到固件文件！" | tee -a "$MAIN_LOG"
            find bin/targets -type f \( -name "*.bin" -o -name "*.img" -o -name "*.trx" \) -exec ls -la {} \; | tee -a "$MAIN_LOG"
            FIRMWARE_FOUND=true
            FIRMWARE_COUNT=$(find bin/targets -type f \( -name "*.bin" -o -name "*.img" -o -name "*.trx" \) 2>/dev/null | wc -l)
            echo "BUILD_STATUS=success" >> $GITHUB_ENV
            echo "FIRMWARE_COUNT=$FIRMWARE_COUNT" >> $GITHUB_ENV
          else
            echo "💥 未找到固件文件" | tee -a "$MAIN_LOG"
            echo "BUILD_STATUS=failed" >> $GITHUB_ENV
            exit 1
          fi
          
          echo "FIRMWARE_FOUND=$FIRMWARE_FOUND" >> $GITHUB_ENV
          
          echo "📊 CCache统计:" | tee -a "$MAIN_LOG"
          ccache -s | tee -a "$MAIN_LOG"
          
          echo "=== 编译总结 ===" | tee -a "$MAIN_LOG"
          echo "构建状态: 成功" | tee -a "$MAIN_LOG"
          echo "固件生成: 是" | tee -a "$MAIN_LOG"
          echo "固件数量: $FIRMWARE_COUNT" | tee -a "$MAIN_LOG"

      - name: "🔍 自动错误分析与报告"
        id: error_analysis
        if: always()
        run: |
          set +e
          MAIN_LOG="${{ env.MAIN_LOG }}"
          ERROR_LOG="${{ env.ERROR_LOG }}"
          ANALYSIS_REPORT="${{ env.ANALYSIS_REPORT }}"
          DOWNLOAD_LOG="${{ env.DOWNLOAD_LOG }}"
          echo "=== 🔍 自动错误分析与报告 ===" | tee -a "$MAIN_LOG"
          
          echo "# 智能错误分析报告" > "$ANALYSIS_REPORT"
          echo "生成时间: $(date)" >> "$ANALYSIS_REPORT"
          echo "" >> "$ANALYSIS_REPORT"
          
          if [ "${{ env.BUILD_STATUS }}" = "success" ]; then
            echo "## 🎉 构建成功" >> "$ANALYSIS_REPORT"
            echo "- 状态: 成功完成" >> "$ANALYSIS_REPORT"
            echo "- 固件数量: ${{ env.FIRMWARE_COUNT || 0 }}" >> "$ANALYSIS_REPORT"
          else
            echo "## ❌ 构建失败" >> "$ANALYSIS_REPORT"
            echo "- 状态: 构建失败" >> "$ANALYSIS_REPORT"
            
            if [ -f "$ERROR_LOG" ]; then
              echo "## 🔍 错误分析" >> "$ANALYSIS_REPORT"
              
              if grep -q "missing separator" "$ERROR_LOG"; then
                echo "### ❌ 配置文件语法错误" >> "$ANALYSIS_REPORT"
                echo "**问题**: .config文件存在语法错误" >> "$ANALYSIS_REPORT"
                echo "**解决方案**: 检查配置文件格式，确保使用正确的Kconfig语法" >> "$ANALYSIS_REPORT"
              fi
              
              if grep -q "mkhash" "$ERROR_LOG"; then
                echo "### ❌ 主机工具缺失" >> "$ANALYSIS_REPORT"
                echo "**问题**: 缺少mkhash等主机工具" >> "$ANALYSIS_REPORT"
                echo "**解决方案**: 确保主机工具正确编译，检查依赖是否完整" >> "$ANALYSIS_REPORT"
              fi
              
              if grep -q "Error opening terminal" "$ERROR_LOG"; then
                echo "### ❌ 终端环境问题" >> "$ANALYSIS_REPORT"
                echo "**问题**: 终端环境配置错误" >> "$ANALYSIS_REPORT"
                echo "**解决方案**: 设置TERM=linux环境变量" >> "$ANALYSIS_REPORT"
              fi
              
              echo "### 📋 主要错误列表" >> "$ANALYSIS_REPORT"
              grep -E "(error:|Error:|ERROR:)" "$ERROR_LOG" | head -10 | while read -r error; do
                echo "- $error" >> "$ANALYSIS_REPORT"
              done
            fi
            
            echo "## 🛠️ 修复建议" >> "$ANALYSIS_REPORT"
            echo "1. 检查.config文件语法是否正确" >> "$ANALYSIS_REPORT"
            echo "2. 确保所有主机工具正确编译" >> "$ANALYSIS_REPORT"
            echo "3. 设置正确的终端环境变量: TERM=linux" >> "$ANALYSIS_REPORT"
            echo "4. 检查构建系统的依赖是否完整" >> "$ANALYSIS_REPORT"
          fi
          
          if [ -f "$DOWNLOAD_LOG" ]; then
            echo "## 🌐 下载问题分析" >> "$ANALYSIS_REPORT"
            DOWNLOAD_RETRIES=$(grep -c "下载尝试" "$DOWNLOAD_LOG" 2>/dev/null || echo 0)
            echo "### 📊 下载统计" >> "$ANALYSIS_REPORT"
            echo "- 下载重试次数: $DOWNLOAD_RETRIES" >> "$ANALYSIS_REPORT"
            echo "- 最终下载文件数: ${{ env.DOWNLOAD_FILE_COUNT || 0 }}" >> "$ANALYSIS_REPORT"
          fi
          
          echo "## 📊 系统资源报告" >> "$ANALYSIS_REPORT"
          echo "\`\`\`" >> "$ANALYSIS_REPORT"
          echo "磁盘使用情况:" >> "$ANALYSIS_REPORT"
          df -h >> "$ANALYSIS_REPORT" 2>/dev/null || echo "磁盘使用情况获取失败" >> "$ANALYSIS_REPORT"
          echo "" >> "$ANALYSIS_REPORT"
          echo "内存使用情况:" >> "$ANALYSIS_REPORT"
          free -h >> "$ANALYSIS_REPORT" 2>/dev/null || echo "内存使用情况获取失败" >> "$ANALYSIS_REPORT"
          echo "\`\`\`" >> "$ANALYSIS_REPORT"
          
          echo "✅ 错误分析报告生成完成" | tee -a "$MAIN_LOG"

      - name: "📦 极速产物收集"
        id: collect_artifacts
        if: always()
        run: |
          set +e
          MAIN_LOG="${{ env.MAIN_LOG }}"
          echo "=== 📦 极速产物收集 ===" | tee -a "$MAIN_LOG"
          
          mkdir -p /mnt/artifacts/firmware
          ARTIFACT_COUNT=0
          
          if [ -d "/mnt/source/bin/targets" ]; then
            find /mnt/source/bin/targets -type f \( -name "*.bin" -o -name "*.img" -o -name "*.trx" -o -name "*.gz" \) -exec cp {} /mnt/artifacts/firmware/ \;
            ARTIFACT_COUNT=$(ls /mnt/artifacts/firmware/ 2>/dev/null | wc -l || echo 0)
          fi
          
          cp "$MAIN_LOG" /mnt/artifacts/ 2>/dev/null || true
          cp "$ERROR_LOG" /mnt/artifacts/ 2>/dev/null || true
          cp "$WARNING_LOG" /mnt/artifacts/ 2>/dev/null || true
          cp "$ANALYSIS_REPORT" /mnt/artifacts/ 2>/dev/null || true
          cp "$DOWNLOAD_LOG" /mnt/artifacts/ 2>/dev/null || true
          cp /mnt/source/.config /mnt/artifacts/ 2>/dev/null || true
          
          echo "ARTIFACT_COUNT=$ARTIFACT_COUNT" >> $GITHUB_ENV
          echo "✅ 收集了 $ARTIFACT_COUNT 个产物" | tee -a "$MAIN_LOG"

      - name: "📤 提交新配置文件到仓库"
        id: commit_config
        if: env.CONFIG_SOURCE == 'new' && github.event.inputs.auto_commit_config == 'true'
        run: |
          set +e
          MAIN_LOG="${{ env.MAIN_LOG }}"
          echo "=== 📤 提交新配置文件到仓库 ===" | tee -a "$MAIN_LOG"
          
          CONFIG_PROFILE="${{ github.event.inputs.config_profile }}"
          CONFIG_DIR="${{ env.CONFIG_REPO_DIR }}/firmware-config/configs"
          CONFIG_FILE="$CONFIG_DIR/config_$CONFIG_PROFILE"
          
          if [ -f "$CONFIG_FILE" ]; then
            echo "📁 找到新创建的配置文件: $CONFIG_FILE" | tee -a "$MAIN_LOG"
            
            cd "${{ env.CONFIG_REPO_DIR }}"
            
            git config --local user.email "github-actions[bot]@users.noreply.github.com"
            git config --local user.name "github-actions[bot]"
            
            git add "firmware-config/configs/config_$CONFIG_PROFILE"
            
            if git diff --staged --quiet; then
              echo "ℹ️ 没有新的配置文件需要提交" | tee -a "$MAIN_LOG"
            else
              COMMIT_MSG="添加新的设备配置文件: $CONFIG_PROFILE"
              if [ -n "${{ env.TARGET_CONFIG }}" ]; then
                COMMIT_MSG="$COMMIT_MSG (目标: ${{ env.TARGET_CONFIG }})"
              fi
              git commit -m "$COMMIT_MSG"
              echo "✅ 配置文件已提交到本地仓库: $COMMIT_MSG" | tee -a "$MAIN_LOG"
              
              echo "🔄 推送到远程仓库..." | tee -a "$MAIN_LOG"
              git push
              echo "✅ 配置文件已推送到远程仓库" | tee -a "$MAIN_LOG"
            fi
          else
            echo "ℹ️ 没有创建新的配置文件" | tee -a "$MAIN_LOG"
          fi

      - name: "💾 上传构建产物"
        id: upload_artifacts
        if: always()
        uses: "actions/upload-artifact@v4"
        with:
          name: "Build-Artifacts-${{ github.run_number }}"
          path: "/mnt/artifacts"
          retention-days: 7
          compression-level: 6

      - name: "📊 智能构建报告"
        id: final_report
        if: always()
        run: |
          echo "=== 📊 智能构建报告 ==="
          echo ""
          echo "🎯 构建摘要"
          echo "=========================================="
          echo "⚡ 编译性能:"
          echo "  - 并行作业: ${{ env.PARALLEL_JOBS || 'N/A' }}"
          echo "  - 构建状态: ${{ env.BUILD_STATUS || 'unknown' }}"
          echo "  - 下载重试: ${{ env.DOWNLOAD_RETRY_COUNT || 'N/A' }}"
          echo ""
          echo "📦 构建结果:"
          echo "  - 固件数量: ${{ env.FIRMWARE_COUNT || 0 }}"
          echo "  - 产物数量: ${{ env.ARTIFACT_COUNT || 0 }}"
          echo "  - 下载文件: ${{ env.DOWNLOAD_FILE_COUNT || 0 }}"
          echo ""
          echo "🔧 配置信息:"
          echo "  - 配置来源: ${{ env.CONFIG_SOURCE || 'unknown' }}"
          echo "  - 目标配置: ${{ env.TARGET_CONFIG || 'unknown' }}"
          echo "  - 构建模式: ${{ github.event.inputs.build_mode || 'unknown' }}"
          echo ""
          echo "🔧 高级功能:"
          echo "  - 自定义IPK: ${{ env.CUSTOM_IPK_COUNT || 0 }} 个"
          echo "  - 自定义脚本: ${{ env.CUSTOM_SCRIPT_COUNT || 0 }} 个"
          echo "  - 额外软件包: ${{ env.EXTRA_PACKAGES_ADDED || 0 }} 个"
          echo ""
          if [ "${{ env.BUILD_STATUS }}" = "success" ] && [ "${{ env.FIRMWARE_COUNT }}" -gt 0 ]; then
            echo "🎉 智能构建成功！"
            echo "💡 固件已生成并上传到产物"
            if [ "${{ env.CONFIG_SOURCE }}" = "new" ] && [ "${{ github.event.inputs.auto_commit_config }}" = "true" ]; then
              echo "📝 新配置文件已自动提交到仓库"
            fi
          else
            echo "❌ 构建失败或未生成固件"
            echo "🔧 主要问题: 配置文件语法错误和主机工具缺失"
            echo "💡 解决方案: 检查.config文件格式，确保主机工具正确编译"
            if [ "${{ env.DOWNLOAD_RETRY_COUNT }}" -gt 1 ]; then
              echo "🌐 下载过程多次重试，建议检查网络连接"
            fi
          fi
          echo "=========================================="
