# OpenWrt 最小化配置
# 包含基础LuCI界面和中文支持，适合资源有限的设备

# 基础系统包 - 系统运行必需的核心组件
CONFIG_PACKAGE_base-files=y        # 基础文件系统
CONFIG_PACKAGE_busybox=y           # 嵌入式Linux工具集
CONFIG_PACKAGE_dnsmasq-full=y      # DNS和DHCP服务器（完整版）
CONFIG_PACKAGE_dropbear=y          # SSH服务器
CONFIG_PACKAGE_firewall=y          # 防火墙
CONFIG_PACKAGE_opkg=y              # 包管理器
CONFIG_PACKAGE_iptables=y          # 网络包过滤工具
CONFIG_PACKAGE_iptables-mod-extra=y # iptables额外模块

# 基础网络包 - 网络连接必需组件
CONFIG_PACKAGE_ppp=y               # PPP协议支持
CONFIG_PACKAGE_ppp-mod-pppoe=y     # PPPoE拨号支持
CONFIG_PACKAGE_kmod-ppp=y          # PPP内核模块
CONFIG_PACKAGE_kmod-pppoe=y        # PPPoE内核模块

# 基础LuCI配置 - Web管理界面
CONFIG_PACKAGE_luci=y              # LuCI核心
CONFIG_PACKAGE_luci-base=y         # LuCI基础库
CONFIG_PACKAGE_luci-mod-admin-full=y # 完整管理模块
CONFIG_PACKAGE_luci-theme-bootstrap=y # Bootstrap主题
CONFIG_PACKAGE_luci-i18n-base-zh-cn=y # 中文语言包（基础）
CONFIG_PACKAGE_luci-i18n-base-en=y # 英文语言包（基础）
CONFIG_PACKAGE_luci-app-firewall=y # 防火墙Web界面
CONFIG_PACKAGE_luci-i18n-firewall-zh-cn=y # 防火墙中文语言包
CONFIG_PACKAGE_luci-proto-ipv6=y   # IPv6协议支持
CONFIG_PACKAGE_luci-proto-ppp=y    # PPP协议Web界面
CONFIG_PACKAGE_luci-compat=y       # LuCI兼容层

# 注意：最小化配置不包含额外插件，保持系统精简
